private import javascript
private import evaluation.NosqlInjectionWorse
private import tsm.TaintSpecificationMining

module NosqlInjectionWorseTSM {
  class KnownSource extends TSM::KnownSource {
    KnownSource() { this instanceof NosqlInjectionWorse::Source }
  }

  class KnownSink extends TSM::KnownSink {
    KnownSink() { this instanceof NosqlInjectionWorse::Sink }
  }

  class NosqlInjectionWorseRepresentations extends TSM::Representations {
    NosqlInjectionWorseRepresentations() { this = "NosqlInjectionWorseRepresentations" }


    predicate filteredSinkRepr(string repr){
        repr="(parameter 0 (return (member exec *)))" or
        repr="(parameter 1 (return (member post (parameter $http *))))" or
        repr="(member count (parameter 0 (return (member json *))))" or
        repr="(member count *)" or
        repr="(parameter 0 (return (member findOne *)))" or
        repr="(parameter 0 (return (member postMessage *)))" or
        repr="(return (member get *))" or
        repr="(parameter 0 (return (member get *)))" or
        repr="(return (member post *))" or
        repr="(parameter 0 (return (member extend *)))" or
        repr="(parameter 0 (return (member get (return (root https://www.npmjs.com/package/supertest)))))" or
        repr="(return (member get (return (root https://www.npmjs.com/package/supertest))))" or
        repr="(parameter 0 (return (member get (member DOM *))))" or
        repr="(member body (member toplevel *))" or
        repr="(parameter 0 (return (member blockStatement *)))" or
        repr="(parameter 0 (return (member run (member prototype *))))" or
        repr="(member body (member body (parameter self *)))" or
        repr="(member body (member body *))" or
        repr="(parameter 0 (return (member get (parameter $http *))))" or
        repr="(member body (return (member buildRequest (member self *))))" or
        repr="(parameter 0 (return (member findOne (return (member collection *)))))" or
        repr="(parameter 0 (return (member findOneAndUpdate *)))" or
        repr="(member query (return (member URL (global))))" or
        repr="(member query (instance (member URL (global))))" or
        repr="(parameter 0 (return (member getWorkspace *)))" or
        repr="(member parentNode *)" or
        repr="(return (member findOne (member Playlist (global))))" or
        repr="(parameter 0 (return (member findOne (member Playlist (global)))))" or
        repr="(return (member count *))" or
        repr="(parameter 1 (return (member extend (root https://www.npmjs.com/package/underscore))))" or
        repr="(parameter 1 (return (member extend (root https://www.npmjs.com/package/jquery))))" or
        repr="(parameter 3 (return (member extend *)))" or
        repr="(parameter 1 (return (member findOneAndUpdate *)))" or
        repr="(parameter 0 (return (member extend (root https://www.npmjs.com/package/jquery))))" or
        repr="(parameter 0 (parameter 0 (return (member exec *))))" or
        repr="(parameter 0 (return (member findOne (member Node *))))" or
        repr="(parameter 0 (instance (member model *)))" or
        repr="(parameter 0 (return (member model *)))" or
        repr="(parameter 1 (return (member extend *)))" or
        repr="(parameter 0 (return (member extend (member tools *))))" or
        repr="(return (member extend (member tools (member CKEDITOR (global)))))" or
        repr="(parameter 1 (return (member exec *)))" or
        repr="(parameter 0 (return (member get (return (member request *)))))" or
        repr="(return (member findOneAndUpdate *))" or
        repr="(member nodeName (parameter target *))" or
        repr="(parameter 0 (return (member extend (parameter $ *))))" or
        repr="(return (member extend (member tools *)))" 
    }

    override float getReprScore(string repr, string t){
         result = getRawScore(repr, t) and
         ( t = "snk" and filteredSinkRepr(repr) or 
         t != "snk")
    }

    /**
     * The list of TSM representations for NoSQL injection along with their scores.
     * These representations were generated from a TSM model trained using training data from NoSQL
     * injection worse (`ql/javascript/evaluation/NosqlInjectionWorse.ql`), to enable [recovery-style
     * evaluation](https://github.com/github/ml-ql-adaptive-threat-modeling/tree/main/docs/evaluation).
     */
    float getRawScore(string repr, string t) {
      repr = "(member body (parameter req *))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 1 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (parameter 2 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 2 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body *)" and t = "src" and result = 1.0
      or
      repr = "(member src *)" and t = "src" and result = 1.0
      or
      repr = "(member src (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member src (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter ctrl *)" and t = "src" and result = 0.16666666666666666
      or
      repr = "(member _id (member items *))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member loginUser *)))" and t = "src" and result = 1.0
      or
      repr = "(member _id *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member test *)))" and t = "src" and result = 1.0
      or
      repr = "(member name (member window (global)))" and t = "src" and result = 1.0
      or
      repr = "(parameter locals (return (member recurse (member prototype *))))" and
      t = "src" and
      result = 0.8333333333333333
      or
      repr = "(member $$parserName *)" and t = "src" and result = 0.33333333333333337
      or
      repr = "(member body (parameter 0 (member createSecure *)))" and t = "src" and result = 1.0
      or
      repr = "(member name (parameter window *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member log *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member president (parameter 0 (return (member json *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member id (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member path *)" and t = "src" and result = 0.33574999999999994
      or
      repr = "(parameter 0 (return (member findByIdAndRemove *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter request (parameter 1 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member recurse (member prototype *))))" and
      t = "src" and
      result = 0.8333333333333335
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member id (member params *))" and t = "src" and result = 1.0
      or
      repr = "(return (member substr (parameter url *)))" and t = "src" and result = 0.5
      or
      repr = "(member body (parameter 0 (member updatePresident *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member log (member console (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member id *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member findByIdAndUpdate *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter request *))" and t = "src" and result = 1.0
      or
      repr = "(member userId (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member _id (parameter 0 (return (member update *))))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter request (member updatePresident *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name *)" and t = "src" and result = 1.0
      or
      repr = "(return (member getOption (member $options *)))" and
      t = "src" and
      result = 0.33333333333333337
      or
      repr = "(parameter 0 (member validates *))" and t = "src" and result = 0.5
      or
      repr = "(return (member $normalize *))" and t = "src" and result = 0.5
      or
      repr = "(member body (parameter req (parameter 1 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member update *)))" and t = "src" and result = 1.0
      or
      repr = "(member userId (member params (parameter request *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member log (member console (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member createSecure *)))" and t = "src" and result = 1.0
      or
      repr = "(member _id (member items (member $pull *)))" and t = "src" and result = 1.0
      or
      repr = "(member query *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (member loginUser *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter assign (return (member recurse (member prototype *))))" and
      t = "src" and
      result = 0.8333333333333335
      or
      repr = "(member name (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member indexOf *))" and t = "src" and result = 0.75
      or
      repr = "(member userId (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member date *)" and t = "src" and result = 0.125
      or
      repr = "(member name (global))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member criminal (parameter 0 (return (member json *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member criminal *))" and t = "src" and result = 1.0
      or
      repr = "(parameter url *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member log *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter timezone *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(return (member map *))" and t = "src" and result = 0.5
      or
      repr = "(parameter 1 (parameter 0 (return (member push *))))" and
      t = "src" and
      result = 0.9999999999999998
      or
      repr = "(parameter 0 (return (member findById *)))" and t = "src" and result = 1.0
      or
      repr = "(member name (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member create *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter request (parameter 1 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member findByIdAndUpdate *)))" and t = "src" and result = 1.0
      or
      repr = "(member userId *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 1 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member id (member params (parameter request *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member president *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 3 (return (member recurse (member prototype *))))" and
      t = "src" and
      result = 0.8333333333333335
      or
      repr = "(member _id (parameter 0 (return (member findById *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member _id (parameter 0 (return (member remove *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member getRecordByZonePromise *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member parseInt (global))))" and t = "src" and result = 1.0
      or
      repr = "(member group *)" and t = "src" and result = 1.0
      or
      repr = "(member algorithm *)" and t = "src" and result = 1.0
      or
      repr = "(member range (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member value *)" and t = "src" and result = 1.0
      or
      repr = "(member limit (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member port (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member source *)" and t = "src" and result = 1.0
      or
      repr = "(member zone (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member dnsType (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member zone *)" and t = "src" and result = 1.0
      or
      repr = "(member algorithm (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter err *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 0 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member inRange (root https://www.npmjs.com/package/range_check))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member ip (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member addNetworkClass *)))" and t = "src" and result = 1.0
      or
      repr = "(member limit (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member contains *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter org *)" and t = "src" and result = 0.5
      or
      repr = "(member domain (member query *))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member contains (return (root https://www.npmjs.com/package/cidr-matcher)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member ip *)" and t = "src" and result = 1.0
      or
      repr = "(member source (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member source (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member range (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member page (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member inRange *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter zone *)" and t = "src" and result = 0.27272727272727254
      or
      repr = "(parameter errorMsg *)" and t = "src" and result = 0.09848484848484872
      or
      repr = "(parameter 0 (return (member getRecordByIPPromise *)))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member addNetworkClass (instance (root https://www.npmjs.com/package/cidr-matcher)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member inRange *)))" and t = "src" and result = 1.0
      or
      repr = "(member port (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 0 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter data *)" and t = "src" and result = 1.0
      or
      repr = "(member range *)" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member inRange (root https://www.npmjs.com/package/range_check))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter results *)" and t = "src" and result = 0.375
      or
      repr = "(member dnsType (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member domain *)" and t = "src" and result = 1.0
      or
      repr = "(member algorithm (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member zone (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member value (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member page *)" and t = "src" and result = 1.0
      or
      repr = "(member dnsType *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member encodeURIComponent (global))))" and
      t = "src" and
      result = 0.75
      or
      repr = "(member body (parameter 0 (parameter 0 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member limit *)" and t = "src" and result = 1.0
      or
      repr = "(parameter d *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 0 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member addNetworkClass (return (root https://www.npmjs.com/package/cidr-matcher)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member zone (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member page (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member ip (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member p443.https.tls.certificate.parsed.extensions.subject_alt_name.dns_names *)" and
      t = "src" and
      result = 0.5537190082644624
      or
      repr = "(parameter 0 (return (member getZoneByNamePromise *)))" and t = "src" and result = 1.0
      or
      repr = "(member port *)" and t = "src" and result = 1.0
      or
      repr = "(member value (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member zone (member query *))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member contains (instance (root https://www.npmjs.com/package/cidr-matcher)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member domain (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 3 (member exports (parameter module *)))" and
      t = "src" and
      result = 0.16170825906120007
      or
      repr = "(member match *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(return (member slice *))" and t = "src" and result = 0.7275132275132276
      or
      repr = "(member prototype (member Object (global)))" and
      t = "src" and
      result = 0.33333333333333337
      or
      repr = "(member 0 (return (member split *)))" and t = "src" and result = 0.5
      or
      repr = "(parameter str *)" and t = "src" and result = 0.75
      or
      repr = "(member body (parameter req (parameter 2 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 2 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter op *)" and t = "src" and result = 0.043294164555774534
      or
      repr = "(parameter 0 (return (member escapeExpression *)))" and
      t = "src" and
      result = 0.3333333333333333
      or
      repr = "(parameter e *)" and t = "src" and result = 1.0
      or
      repr = "(return (parameter t *))" and t = "src" and result = 0.25000000000000006
      or
      repr = "(return (member valueOf *))" and t = "src" and result = 0.75
      or
      repr = "(member dump (parameter state *))" and t = "src" and result = 0.005514705882353033
      or
      repr = "(parameter compact *)" and t = "src" and result = 0.005514705882353033
      or
      repr = "(member OAuthSchemeKey *)" and t = "src" and result = 0.13636363636363635
      or
      repr = "(return (member substring *))" and t = "src" and result = 0.2498886062216643
      or
      repr = "(parameter 0 (parameter 1 (return (member registerHelper *))))" and
      t = "src" and
      result = 0.33333333333333326
      or
      repr = "(member headers *)" and t = "src" and result = 0.3656974153297682
      or
      repr = "(return (member toString (member prototype *)))" and
      t = "src" and
      result = 0.505050505050505
      or
      repr = "(parameter auth *)" and t = "src" and result = 0.04590909090909093
      or
      repr = "(parameter name *)" and t = "src" and result = 0.5
      or
      repr = "(member data (parameter e *))" and t = "src" and result = 1.0
      or
      repr = "(return (member call (member toString (member prototype *))))" and
      t = "src" and
      result = 0.445050505050505
      or
      repr = "(member contextPath (member data *))" and t = "src" and result = 0.21999999999999997
      or
      repr = "(return (member wrap *))" and t = "src" and result = 0.037600472276463355
      or
      repr = "(member hasOwnProperty (member prototype *))" and
      t = "src" and
      result = 0.5050505050505051
      or
      repr = "(return (member call *))" and t = "src" and result = 0.5
      or
      repr = "(parameter state *)" and t = "src" and result = 0.5
      or
      repr = "(member dump *)" and t = "src" and result = 0.005514705882353033
      or
      repr = "(member ids (parameter 1 (parameter 1 (return (member registerHelper *)))))" and
      t = "src" and
      result = 0.21999999999999997
      or
      repr = "(member loginId *)" and t = "src" and result = 0.25
      or
      repr = "(member url *)" and t = "src" and result = 1.0
      or
      repr = "(parameter object *)" and t = "src" and result = 0.06944444444444446
      or
      repr = "(parameter path *)" and t = "src" and result = 0.5
      or
      repr = "(return (member add *))" and t = "src" and result = 0.25
      or
      repr = "(parameter key *)" and t = "src" and result = 0.5
      or
      repr = "(parameter name (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (member exports *))" and t = "src" and result = 0.3333333333333333
      or
      repr = "(return (return (parameter require *)))" and
      t = "src" and
      result = 0.08999999999999997
      or
      repr = "(parameter string *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.75
      or
      repr = "(parameter s *)" and t = "src" and result = 0.25
      or
      repr = "(return (member lex *))" and t = "src" and result = 0.6622807017543859
      or
      repr = "(parameter options *)" and t = "src" and result = 0.4166666666666667
      or
      repr = "(parameter $ *)" and t = "src" and result = 0.2499999999999999
      or
      repr = "(member text *)" and t = "src" and result = 0.75
      or
      repr = "(parameter 1 (member insert *))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(parameter password *)" and t = "src" and result = 0.2499999999999999
      or
      repr = "(member query (instance (member constructor (member prototype *))))" and
      t = "src" and
      result = 0.202418094323832
      or
      repr = "(member query (instance (member Constructor (member typeahead (member fn *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getCursorPosition (parameter editor *)))" and
      t = "src" and
      result = 0.2857142857142858
      or
      repr = "(member type *)" and t = "src" and result = 1.0
      or
      repr = "(return (member token (member _tokenStream *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter param *)" and t = "src" and result = 0.25
      or
      repr = "(return (member trim *))" and t = "src" and result = 0.5506735670446262
      or
      repr = "(parameter moduleName *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (member init *))" and t = "src" and result = 0.8963963963963963
      or
      repr = "(member column (member start (parameter range *)))" and
      t = "src" and
      result = 0.7272727272727274
      or
      repr = "(return (member token (instance (member TokenStream (member css *)))))" and
      t = "src" and
      result = 0.56
      or
      repr = "(return (member getCurrentToken *))" and t = "src" and result = 0.12749999999999995
      or
      repr = "(parameter 1 (return (member replace *)))" and t = "src" and result = 0.5
      or
      repr = "(member regex *)" and t = "src" and result = 0.25
      or
      repr = "(return (member find *))" and t = "src" and result = 0.10540177472554517
      or
      repr = "(parameter token *)" and t = "src" and result = 0.5
      or
      repr = "(parameter parentId *)" and t = "src" and result = 0.0911903448275862
      or
      repr = "(parameter n *)" and t = "src" and result = 1.0
      or
      repr = "(member value (return (member token (member _tokenStream *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter count *)" and t = "src" and result = 0.2164502164502165
      or
      repr = "(parameter 0 (member parse *))" and t = "src" and result = 0.2499999999999999
      or
      repr = "(return (member getRange *))" and t = "src" and result = 0.75
      or
      repr = "(parameter editor *)" and t = "src" and result = 0.2164502164502165
      or
      repr = "(return (member getTextRange *))" and t = "src" and result = 0.25
      or
      repr = "(return (member stepForward *))" and t = "src" and result = 0.16500000000000004
      or
      repr = "(return (member substr *))" and t = "src" and result = 0.75
      or
      repr = "(parameter range *)" and t = "src" and result = 0.3987888279356248
      or
      repr = "(member value (return (member token *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter body *)" and t = "src" and result = 0.75
      or
      repr = "(return (member querySelector *))" and t = "src" and result = 0.43975
      or
      repr = "(parameter 0 (member CallExpression (member visitor *)))" and
      t = "src" and
      result = 0.008238144686742924
      or
      repr = "(member value (return (member getElementById *)))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (parameter 0 (return (member map *)))))" and
      t = "src" and
      result = 0.23395631013208412
      or
      repr = "(return (member trim (return (member getAttribute *))))" and
      t = "src" and
      result = 0.12163120254608001
      or
      repr = "(return (member match *))" and t = "src" and result = 0.48000000000000015
      or
      repr = "(return (member cwd *))" and t = "src" and result = 0.1842568992101701
      or
      repr = "(parameter error *)" and t = "src" and result = 1.0
      or
      repr = "(parameter srcFilename *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (parameter 1 (return (member addEventListener *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member host *)" and t = "src" and result = 1.0
      or
      repr = "(parameter type *)" and t = "src" and result = 0.25
      or
      repr = "(return (member red *))" and t = "src" and result = 0.5
      or
      repr = "(member tag (parameter data *))" and t = "src" and result = 0.5
      or
      repr = "(member deps *)" and t = "src" and result = 0.43919522326064386
      or
      repr = "(member host (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(return (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.5151515151515149
      or
      repr = "(return (member format *))" and t = "src" and result = 0.0011371534661253901
      or
      repr = "(member context (member window (global)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member expectPostMessage *)))" and
      t = "src" and
      result = 0.33356178608515075
      or
      repr = "(return (member default *))" and t = "src" and result = 0.125
      or
      repr = "(parameter prop *)" and t = "src" and result = 0.05249999999999999
      or
      repr = "(parameter keys *)" and t = "src" and result = 0.03749999999999998
      or
      repr = "(member document *)" and t = "src" and result = 0.3890619591554172
      or
      repr = "(parameter event (parameter 1 (return (member addEventListener (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member trigger_ *)" and t = "src" and result = 0.2727345102111457
      or
      repr = "(member repr *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(return (member dirname *))" and t = "src" and result = 0.7478208880078044
      or
      repr = "(member changedTouches *)" and t = "src" and result = 0.4475
      or
      repr = "(instance (member __AMP_BASE_CE_CLASS *))" and
      t = "src" and
      result = 0.30476304391979825
      or
      repr = "(parameter win *)" and t = "src" and result = 0.165
      or
      repr = "(parameter encryptedDocumentKey *)" and t = "src" and result = 0.25
      or
      repr = "(parameter bytes *)" and t = "src" and result = 0.17999999999999994
      or
      repr = "(return (member next (instance (member TextScanner *))))" and
      t = "src" and
      result = 0.41414141414141376
      or
      repr = "(return (member querySelector (member document (member win *))))" and
      t = "src" and
      result = 0.005882798573975003
      or
      repr = "(return (member keys (member Object (global))))" and t = "src" and result = 0.5
      or
      repr = "(parameter requests (parameter 0 (return (member then *))))" and
      t = "src" and
      result = 0.7579731898423485
      or
      repr = "(member ampTestRuntimeConfig *)" and t = "src" and result = 1.0
      or
      repr = "(return (member slice (parameter t *)))" and t = "src" and result = 0.8742713741779162
      or
      repr = "(member textContent *)" and t = "src" and result = 0.3053317017248222
      or
      repr = "(return (member addParamsToUrl *))" and t = "src" and result = 0.21339044652128758
      or
      repr = "(member LOW *)" and t = "src" and result = 0.10000000000000037
      or
      repr = "(parameter 0 (return (parameter t *)))" and t = "src" and result = 0.09505711318795412
      or
      repr = "(parameter 0 (return (member sort *)))" and t = "src" and result = 0.22568362755278643
      or
      repr = "(parameter t *)" and t = "src" and result = 1.0
      or
      repr = "(member validPositions *)" and t = "src" and result = 0.05800000000000005
      or
      repr = "(parameter k *)" and t = "src" and result = 0.7760207181658879
      or
      repr = "(parameter decryptedDocumentKey *)" and t = "src" and result = 0.5125655763239876
      or
      repr = "(member localWin_ *)" and t = "src" and result = 0.004786318795430788
      or
      repr = "(member TAG *)" and t = "src" and result = 0.018982661506025997
      or
      repr = "(member backgroundAudioEl_ *)" and t = "src" and result = 0.02999999999999997
      or
      repr = "(parameter 0 (parameter 0 (return (member then *))))" and
      t = "src" and
      result = 0.41999999999999993
      or
      repr = "(return (parameter 0 (return (member catch *))))" and
      t = "src" and
      result = 0.4407382550335571
      or
      repr = "(member linkmateOptions_ *)" and t = "src" and result = 0.5486327448944271
      or
      repr = "(member query (parameter req (parameter 1 (return (member use *)))))" and
      t = "src" and
      result = 0.575
      or
      repr = "(member partnerId_ *)" and t = "src" and result = 0.33999999999999997
      or
      repr = "(return (member concat *))" and t = "src" and result = 0.6283904465212877
      or
      repr = "(parameter o *)" and t = "src" and result = 0.25
      or
      repr = "(root https://www.npmjs.com/package/path)" and t = "src" and result = 0.25
      or
      repr = "(member unit *)" and t = "src" and result = 0.08333333333333331
      or
      repr = "(parameter 1 (return (member replace (return (member replace *)))))" and
      t = "src" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member replace *)))" and
      t = "src" and
      result = 0.47477064220183474
      or
      repr = "(return (member readFile *))" and t = "src" and result = 0.255
      or
      repr = "(member textContent (parameter styleEl *))" and
      t = "src" and
      result = 0.1885707450165945
      or
      repr = "(parameter 0 (parameter 0 (return (member map *))))" and t = "src" and result = 0.5
      or
      repr = "(return (member listen *))" and t = "src" and result = 0.3472654897888543
      or
      repr = "(member trust *)" and t = "src" and result = 1.0
      or
      repr = "(parameter poolMediaEl *)" and t = "src" and result = 0.09106060606060604
      or
      repr = "(member pid *)" and t = "src" and result = 0.4075
      or
      repr = "(return (member bytesToString *))" and t = "src" and result = 0.2495823814468671
      or
      repr = "(return (member create (member Object (global))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(return (member toString (member contents (parameter file *))))" and
      t = "src" and
      result = 0.6244768557852671
      or
      repr = "(return (member toUpperCase (member tagName *)))" and
      t = "src" and
      result = 0.33131313131313134
      or
      repr = "(return (member getAttribute (member element *)))" and
      t = "src" and
      result = 0.5075757575757575
      or
      repr = "(return (member split *))" and t = "src" and result = 0.394453097957771
      or
      repr = "(return (member dict *))" and t = "src" and result = 0.05249999999999999
      or
      repr = "(parameter binding *)" and t = "src" and result = 0.04863274489442726
      or
      repr = "(parameter srcDir *)" and t = "src" and result = 0.25
      or
      repr = "(return (member createElement (member document (member win (parameter env *)))))" and
      t = "src" and
      result = 0.3599999999999999
      or
      repr = "(return (member removeFragment *))" and t = "src" and result = 0.4475
      or
      repr = "(parameter 0 (return (parameter 0 (return (member then *)))))" and
      t = "src" and
      result = 0.36847734006734034
      or
      repr = "(parameter mask *)" and t = "src" and result = 0.375
      or
      repr = "(member host (member headers (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter value *)" and t = "src" and result = 0.3333333333333333
      or
      repr = "(parameter req (parameter 1 (return (member get *))))" and t = "src" and result = 1.0
      or
      repr = "(return (member enqueueMediaElementTask_ *))" and t = "src" and result = 0.35475
      or
      repr = "(return (member create *))" and t = "src" and result = 0.24701392599451064
      or
      repr = "(member performanceTrackedVideos_ *)" and t = "src" and result = 0.5
      or
      repr = "(return (member split (parameter t *)))" and
      t = "src" and
      result = 0.25499999999999995
      or
      repr = "(member signals *)" and t = "src" and result = 0.07973769470404996
      or
      repr = "(parameter 0 (return (member parseJson *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member get *))))" and
      t = "src" and
      result = 0.5151515151515149
      or
      repr = "(parameter 0 (parameter 1 (return (member addEventListener (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member Number (global)))" and t = "src" and result = 0.5
      or
      repr = "(member props *)" and t = "src" and result = 0.5
      or
      repr = "(member ampTestRuntimeConfig (member window (global)))" and
      t = "src" and
      result = 0.19353841162415364
      or
      repr = "(parameter req *)" and t = "src" and result = 0.5200000000000001
      or
      repr = "(return (member createElement (parameter document *)))" and
      t = "src" and
      result = 0.16343434343434346
      or
      repr = "(parameter atRule *)" and t = "src" and result = 0.15
      or
      repr = "(member touches *)" and t = "src" and result = 0.4475
      or
      repr = "(member win *)" and t = "src" and result = 0.3054690204222914
      or
      repr = "(member default *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getUrl *))" and t = "src" and result = 0.3250933975759227
      or
      repr = "(member data (parameter message *))" and t = "src" and result = 0.17943149039722292
      or
      repr = "(return (member isValid *))" and t = "src" and result = 0.09583333333333333
      or
      repr = "(parameter impls *)" and t = "src" and result = 0.22029874213836473
      or
      repr = "(parameter node *)" and t = "src" and result = 0.43975
      or
      repr = "(parameter 1 (return (member setHeader (parameter res *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member data *)" and t = "src" and result = 0.33333333333333337
      or
      repr = "(return (member getElementById (member document (global))))" and
      t = "src" and
      result = 0.30000000000000004
      or
      repr = "(return (member next (return (member TextScanner *))))" and t = "src" and result = 1.0
      or
      repr = "(return (member join (return (member split *))))" and
      t = "src" and
      result = 0.0838309964802952
      or
      repr = "(return (member exec *))" and t = "src" and result = 0.5
      or
      repr = "(parameter filePath *)" and t = "src" and result = 0.255
      or
      repr = "(parameter 0 (parameter 1 (return (member use *))))" and
      t = "src" and
      result = 0.04050233644859805
      or
      repr = "(member declarations *)" and t = "src" and result = 0.15
      or
      repr = "(return (parameter 0 (parameter 0 (return (member all *)))))" and
      t = "src" and
      result = 0.07973769470404996
      or
      repr = "(member base *)" and t = "src" and result = 0.1050000000000001
      or
      repr = "(parameter 0 (return (member encode *)))" and
      t = "src" and
      result = 0.6877939272088487
      or
      repr = "(parameter style *)" and t = "src" and result = 0.75
      or
      repr = "(return (member getUrl (return (member getAmpDoc *))))" and
      t = "src" and
      result = 0.46580130490055566
      or
      repr = "(return (member updateWidgetVisibilities *))" and
      t = "src" and
      result = 0.17999999999999994
      or
      repr = "(parameter trust *)" and t = "src" and result = 0.044999999999999984
      or
      repr = "(return (member slice (parameter n *)))" and t = "src" and result = 0.4958809276566283
      or
      repr = "(return (member getSingleDoc (return (member ampdocServiceFor (member Services *)))))" and
      t = "src" and
      result = 0.3890619591554172
      or
      repr = "(member line *)" and t = "src" and result = 0.23031152123006043
      or
      repr = "(member 1 (return (member match (member userAgent *))))" and
      t = "src" and
      result = 0.33333333333333337
      or
      repr = "(return (member resolve (member Promise (global))))" and
      t = "src" and
      result = 0.14525
      or
      repr = "(parameter b *)" and t = "src" and result = 0.3500000000000001
      or
      repr = "(parameter 1 (parameter 0 (return (member reduce *))))" and
      t = "src" and
      result = 0.43919522326064386
      or
      repr = "(parameter 0 (return (member filter *)))" and
      t = "src" and
      result = 0.22568362755278643
      or
      repr = "(return (member all (member Promise (global))))" and
      t = "src" and
      result = 0.2692198328384785
      or
      repr = "(return (member createElementFromItem_ (parameter ampAutocomplete *)))" and
      t = "src" and
      result = 0.04494696969696952
      or
      repr = "(parameter element *)" and t = "src" and result = 0.39266585086241124
      or
      repr = "(return (member placeAt_ *))" and t = "src" and result = 0.08999999999999997
      or
      repr = "(member pubcode *)" and t = "src" and result = 0.004863274489442571
      or
      repr = "(parameter 0 (return (member assertString (return (member dev *)))))" and
      t = "src" and
      result = 0.24242424242424246
      or
      repr = "(parameter cmd *)" and t = "src" and result = 0.2722
      or
      repr = "(parameter env *)" and t = "src" and result = 0.06481953491299282
      or
      repr = "(member PERMISSION_POPUP_URL_FRAGMENT *)" and
      t = "src" and
      result = 0.3074839910003462
      or
      repr = "(member ActionTrust *)" and t = "src" and result = 0.42
      or
      repr = "(member element *)" and t = "src" and result = 0.3246885159628012
      or
      repr = "(return (member getRealChildNodes *))" and t = "src" and result = 0.125
      or
      repr = "(member message (parameter error *))" and t = "src" and result = 0.5
      or
      repr = "(member search *)" and t = "src" and result = 1.0
      or
      repr = "(member level *)" and t = "src" and result = 0.21250000000000002
      or
      repr = "(return (parameter 1 (return (member then *))))" and
      t = "src" and
      result = 0.33499999999999996
      or
      repr = "(return (member evaluate_ *))" and t = "src" and result = 0.08999999999999997
      or
      repr = "(parameter tagName *)" and t = "src" and result = 0.8120300751879699
      or
      repr = "(parameter t (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.31959375381836275
      or
      repr = "(member defaultView *)" and t = "src" and result = 0.08999999999999997
      or
      repr = "(parameter actual *)" and t = "src" and result = 0.031145223260643895
      or
      repr = "(return (member getFilename (parameter context *)))" and t = "src" and result = 1.0
      or
      repr = "(member locale *)" and t = "src" and result = 0.014883451817394146
      or
      repr = "(parameter 0 (member update *))" and t = "src" and result = 0.7958333333333334
      or
      repr = "(return (member readFileSync (root https://www.npmjs.com/package/fs-extra)))" and
      t = "src" and
      result = 0.51
      or
      repr = "(member userAgent (member navigator_ *))" and
      t = "src" and
      result = 0.33333333333333337
      or
      repr = "(return (member get *))" and t = "src" and result = 1.0
      or
      repr = "(member win (parameter 0 (parameter 2 (return (member realWin *)))))" and
      t = "src" and
      result = 0.15384562132225654
      or
      repr = "(member argv (root https://www.npmjs.com/package/process))" and
      t = "src" and
      result = 0.37753967504746316
      or
      repr = "(member contents (parameter file (parameter 0 (return (member obj *)))))" and
      t = "src" and
      result = 0.3819570156392584
      or
      repr = "(parameter opt_data *)" and t = "src" and result = 0.7204690204222914
      or
      repr = "(return (member toLowerCase (return (member replace *))))" and
      t = "src" and
      result = 0.33235782684319837
      or
      repr = "(return (member arrayBuffer *))" and t = "src" and result = 0.46839044652128764
      or
      repr = "(parameter event (parameter 1 (return (member addEventListener *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getComputedStyle (member win *)))" and
      t = "src" and
      result = 0.3890619591554172
      or
      repr = "(member arguments (member node (parameter 0 (member CallExpression *))))" and
      t = "src" and
      result = 0.016476289373485848
      or
      repr = "(parameter scopeSelector *)" and t = "src" and result = 0.5
      or
      repr = "(return (member filter (return (member filter *))))" and t = "src" and result = 0.75
      or
      repr = "(return (member toString *))" and t = "src" and result = 0.5
      or
      repr = "(parameter msg *)" and t = "src" and result = 0.25
      or
      repr = "(return (member cloneNode *))" and t = "src" and result = 0.16287878787878798
      or
      repr = "(return (member match (member userAgent *)))" and
      t = "src" and
      result = 0.08333333333333333
      or
      repr = "(member a *)" and t = "src" and result = 0.45833333333333337
      or
      repr = "(parameter event *)" and t = "src" and result = 1.0
      or
      repr = "(return (member monthsShort *))" and t = "src" and result = 0.125
      or
      repr = "(member command *)" and t = "src" and result = 0.2278
      or
      repr = "(return (member getDataParamsFromAttributes *))" and
      t = "src" and
      result = 0.20136725510557274
      or
      repr = "(return (member getAttribute *))" and t = "src" and result = 0.5
      or
      repr = "(parameter styleEl *)" and t = "src" and result = 0.14705882352941174
      or
      repr = "(parameter script *)" and t = "src" and result = 0.4625
      or
      repr = "(parameter text *)" and t = "src" and result = 0.5151515151515149
      or
      repr = "(parameter code *)" and t = "src" and result = 0.040410043400697315
      or
      repr = "(member noContentAvailable *)" and t = "src" and result = 0.7575757575757576
      or
      repr = "(member PAUSE *)" and t = "src" and result = 0.6291853110544698
      or
      repr = "(return (member duplicate *))" and t = "src" and result = 0.5682458982346832
      or
      repr = "(parameter 1 (return (member setHeader *)))" and t = "src" and result = 1.0
      or
      repr = "(member key *)" and t = "src" and result = 0.2919105432892004
      or
      repr = "(parameter 0 (return (parameter e *)))" and t = "src" and result = 0.3712121212121214
      or
      repr = "(return (member join *))" and t = "src" and result = 0.4375
      or
      repr = "(return (member catch *))" and t = "src" and result = 0.19349818474981775
      or
      repr = "(member description (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter id *)" and t = "src" and result = 0.5
      or
      repr = "(member allocated *)" and t = "src" and result = 0.09106060606060604
      or
      repr = "(member target *)" and t = "src" and result = 0.43975
      or
      repr = "(return (member random (member Math (global))))" and
      t = "src" and
      result = 0.34852157943067086
      or
      repr = "(parameter group *)" and t = "src" and result = 0.25
      or
      repr = "(return (member push *))" and t = "src" and result = 0.07040238836967809
      or
      repr = "(parameter input *)" and t = "src" and result = 0.75
      or
      repr = "(member node (parameter path *))" and t = "src" and result = 1.0
      or
      repr = "(return (root https://www.npmjs.com/package/minimist))" and
      t = "src" and
      result = 0.12458809276566282
      or
      repr = "(parameter vars *)" and t = "src" and result = 0.06854216955804171
      or
      repr = "(parameter 0 (return (member encodeURI (global))))" and t = "src" and result = 0.5
      or
      repr = "(parameter a4a *)" and t = "src" and result = 0.3325
      or
      repr = "(parameter a *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member utf8Encode *)))" and
      t = "src" and
      result = 0.8787878787878788
      or
      repr = "(return (member then *))" and t = "src" and result = 0.33999999999999997
      or
      repr = "(member GrantReason *)" and t = "src" and result = 0.080813066805123
      or
      repr = "(return (member createElement (member document (global))))" and
      t = "src" and
      result = 0.5
      or
      repr = "(return (member ww_ *))" and t = "src" and result = 0.16666666666666669
      or
      repr = "(return (member collapse_dots *))" and t = "src" and result = 0.4
      or
      repr = "(return (member createAmpElement *))" and t = "src" and result = 0.011609553478712287
      or
      repr = "(return (member assertElement (return (member dev *))))" and
      t = "src" and
      result = 0.1724242424242426
      or
      repr = "(parameter opts *)" and t = "src" and result = 0.5
      or
      repr = "(return (member now (member Date (global))))" and
      t = "src" and
      result = 0.08499999999999996
      or
      repr = "(return (member getElementById *))" and t = "src" and result = 0.3787878787878788
      or
      repr = "(member context *)" and t = "src" and result = 0.5
      or
      repr = "(return (member filter (return (member split *))))" and
      t = "src" and
      result = 0.10236972835539981
      or
      repr = "(parameter 0 (return (member assertElement (return (member dev *)))))" and
      t = "src" and
      result = 0.011478420569329312
      or
      repr = "(parameter params *)" and t = "src" and result = 0.33666666666666667
      or
      repr = "(parameter filename *)" and t = "src" and result = 0.593859649122807
      or
      repr = "(parameter config *)" and t = "src" and result = 0.2722
      or
      repr = "(return (member waitForExtension *))" and t = "src" and result = 0.035273451021114455
      or
      repr = "(member rules *)" and t = "src" and result = 0.14999999999999997
      or
      repr = "(return (member map (parameter n *)))" and t = "src" and result = 0.08273769470405004
      or
      repr = "(member ampTestRuntimeConfig (global))" and t = "src" and result = 1.0
      or
      repr = "(return (member replace (return (member trim *))))" and t = "src" and result = 0.5
      or
      repr = "(return (parameter 0 (parameter 0 (return (member push *)))))" and
      t = "src" and
      result = 0.84
      or
      repr = "(parameter props *)" and t = "src" and result = 0.08499999999999996
      or
      repr = "(parameter message *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getAttribute (instance (member __AMP_BASE_CE_CLASS *))))" and
      t = "src" and
      result = 0.02272727272727262
      or
      repr = "(member navigator_ *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (member dirname (root https://www.npmjs.com/package/path))))" and
      t = "src" and
      result = 0.25217911199219556
      or
      repr = "(member col *)" and t = "src" and result = 0.08333333333333331
      or
      repr = "(member stack *)" and t = "src" and result = 0.4049428868120457
      or
      repr = "(parameter baseUrl *)" and t = "src" and result = 0.5
      or
      repr = "(member context (global))" and t = "src" and result = 0.015151515151514913
      or
      repr = "(member trust (instance (member ActionInvocation *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter format *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (member remove *))" and t = "src" and result = 0.75
      or
      repr = "(parameter env (parameter 2 (return (member realWin *))))" and
      t = "src" and
      result = 0.32424242424242433
      or
      repr = "(parameter global *)" and t = "src" and result = 0.39769521621807413
      or
      repr = "(member unlistenMessage_ *)" and t = "src" and result = 0.35385946694357906
      or
      repr = "(parameter file *)" and t = "src" and result = 0.262688109278678
      or
      repr = "(return (member getAdUrl *))" and t = "src" and result = 0.12839044652128767
      or
      repr = "(member extensions *)" and t = "src" and result = 0.25
      or
      repr = "(member titulo *)" and t = "src" and result = 1.0
      or
      repr = "(member autor (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member findById (return (member model *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member sort (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member sort *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member equals *)))" and t = "src" and result = 1.0
      or
      repr = "(member titulo (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member autor *)" and t = "src" and result = 1.0
      or
      repr = "(member titulo (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member equals (return (member where *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member usuarioId (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member usuarioId (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member usuarioId *)" and t = "src" and result = 1.0
      or
      repr = "(member sort (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member autor (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member node (parameter container *))" and t = "src" and result = 0.3333333333333333
      or
      repr = "(member $ *)" and t = "src" and result = 0.13070833333333332
      or
      repr = "(member dataTypes *)" and t = "src" and result = 0.2007779571338894
      or
      repr = "(parameter 0 (member buildFragment (parameter 0 (return (member extend *)))))" and
      t = "src" and
      result = 0.9469696969696969
      or
      repr = "(member attributes *)" and t = "src" and result = 0.16833333333333317
      or
      repr = "(member jQuery *)" and t = "src" and result = 0.13070833333333332
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member delete *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member 2 (return (member match *)))" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (member template (member exports (member module (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member template *))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 1 (return (member delete *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter container *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(return (member replace (parameter 0 (member template (member _ *)))))" and
      t = "src" and
      result = 0.7142857142857142
      or
      repr = "(member userID (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member userID (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member userID *)" and t = "src" and result = 1.0
      or
      repr = "(return (member weekdays *))" and t = "src" and result = 0.125
      or
      repr = "(member filter *)" and t = "src" and result = 1.0
      or
      repr = "(member appendTo *)" and t = "src" and result = 0.1875
      or
      repr = "(return (member weekdaysShort *))" and t = "src" and result = 0.125
      or
      repr = "(return (member weekdaysMin *))" and t = "src" and result = 0.5
      or
      repr = "(parameter src *)" and t = "src" and result = 0.25
      or
      repr = "(return (member toLowerCase *))" and t = "src" and result = 0.5
      or
      repr = "(parameter val *)" and t = "src" and result = 0.375
      or
      repr = "(member DATETIME_FORMATS (parameter $locale *))" and
      t = "src" and
      result = 0.3333333333333333
      or
      repr = "(return (member months *))" and t = "src" and result = 0.125
      or
      repr = "(parameter 1 (return (member findOneAndUpdate *)))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (return (member model (root https://www.npmjs.com/package/mongoose)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter single (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.4150655162460153
      or
      repr = "(member value (return (member highlightAuto *)))" and
      t = "src" and
      result = 0.8337499999999998
      or
      repr = "(member zeiten *)" and t = "src" and result = 0.47306397306397296
      or
      repr = "(member teams *)" and t = "src" and result = 0.10332578786632546
      or
      repr = "(parameter 0 (return (member ObjectId (member Types *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member targets (member data *))" and t = "src" and result = 0.22224396295561474
      or
      repr = "(parameter 2 (return (parameter 0 (return (member run *)))))" and
      t = "src" and
      result = 1.0
      or
      repr =
        "(parameter 0 (instance (return (member model (root https://www.npmjs.com/package/mongoose)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member calcSpielDateTime *))" and t = "src" and result = 0.11077868852459016
      or
      repr = "(member id (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member warn *)))" and t = "src" and result = 1.0
      or
      repr = "(member ansprechpartner *)" and t = "src" and result = 0.10332578786632546
      or
      repr = "(parameter 0 (instance (return (member model *))))" and t = "src" and result = 1.0
      or
      repr =
        "(member jugend (return (return (member model (root https://www.npmjs.com/package/mongoose)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member call (member toString *)))" and
      t = "src" and
      result = 0.4050505050505051
      or
      repr = "(member jugend *)" and t = "src" and result = 1.0
      or
      repr = "(parameter spiel *)" and t = "src" and result = 0.4741666666666666
      or
      repr = "(parameter 2 (return (member removeEntityBy *)))" and t = "src" and result = 1.0
      or
      repr = "(member spiele (parameter 1 (return (member extend *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member filter *))" and t = "src" and result = 0.435775
      or
      repr = "(member jugend (instance (return (member model *))))" and t = "src" and result = 1.0
      or
      repr = "(member hasOwnProperty *)" and t = "src" and result = 0.5050505050505051
      or
      repr = "(parameter spiele *)" and t = "src" and result = 0.28166666666666673
      or
      repr = "(member dependencies *)" and t = "src" and result = 0.12886171297214843
      or
      repr = "(parameter id (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.1415680414985405
      or
      repr = "(parameter 0 (member _id *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member _id (parameter 0 (return (member findOne *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member ObjectId *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter payload *)" and t = "src" and result = 0.1561111111111111
      or
      repr = "(member _id (parameter spiel *))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(member jugend (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member gruppen *)" and t = "src" and result = 0.10332578786632544
      or
      repr = "(member id (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (return (member model *))))" and t = "src" and result = 1.0
      or
      repr = "(member jugend (return (return (member model *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter entry (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.9877633234872636
      or
      repr = "(member _id (member body *))" and t = "src" and result = 0.27272727272727276
      or
      repr = "(parameter 2 (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(return (member attr *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member findOneAndUpdate (return (member model *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member jugend (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member ObjectId (member Types (root https://www.npmjs.com/package/mongoose)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member defaultValue *)" and t = "src" and result = 0.2129166666666667
      or
      repr = "(member user *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(parameter 1 (return (member calcSpielDateTime *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member warn (return (member get *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member env (root https://www.npmjs.com/package/process))" and
      t = "src" and
      result = 0.0959595959595965
      or
      repr = "(parameter 1 (return (member setTimeout (global))))" and
      t = "src" and
      result = 0.10000000000000002
      or
      repr =
        "(member jugend (instance (return (member model (root https://www.npmjs.com/package/mongoose)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member _id (parameter 0 (return (member findOne *))))" and t = "src" and result = 1.0
      or
      repr = "(return (member trim (parameter t *)))" and t = "src" and result = 0.38181615776081435
      or
      repr = "(member max (parameter rules (member normalizeRules *)))" and
      t = "src" and
      result = 0.4545454545454546
      or
      repr = "(return (member getAttribute (parameter t *)))" and
      t = "src" and
      result = 0.8141030534351145
      or
      repr = "(member name (parameter e *))" and t = "src" and result = 0.1960235368956743
      or
      repr = "(parameter 1 (parameter 0 (return (member add *))))" and
      t = "src" and
      result = 0.5750000000000002
      or
      repr = "(return (member getRng *))" and t = "src" and result = 0.11651613419391642
      or
      repr = "(member contextPath (member data (parameter options *)))" and
      t = "src" and
      result = 0.09959887658913198
      or
      repr = "(parameter elems (member buildFragment *))" and
      t = "src" and
      result = 0.5539732246125375
      or
      repr = "(parameter 0 (parameter 0 (member lang (member pseudos *))))" and
      t = "src" and
      result = 0.750951017811705
      or
      repr = "(member 0 (return (member tokenize (member find *))))" and
      t = "src" and
      result = 0.1351636870800972
      or
      repr = "(member 3 (return (member exec *)))" and t = "src" and result = 0.02701494910941473
      or
      repr = "(member 5 (return (member exec *)))" and t = "src" and result = 0.007222222222222241
      or
      repr = "(return (member match (parameter t *)))" and
      t = "src" and
      result = 0.41999999999999993
      or
      repr = "(return (member replace (member hash *)))" and t = "src" and result = 0.5
      or
      repr = "(return (member slice (parameter args (member fireWith *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member create *))" and t = "src" and result = 0.5555555555555556
      or
      repr = "(member 1 (parameter 0 (member CLASS *)))" and t = "src" and result = 0.25
      or
      repr = "(return (member map (parameter jQuery *)))" and t = "src" and result = 1.0
      or
      repr = "(member className *)" and t = "src" and result = 0.54
      or
      repr = "(member token *)" and t = "src" and result = 1.0
      or
      repr = "(parameter h *)" and t = "src" and result = 0.754727728411939
      or
      repr = "(member render *)" and t = "src" and result = 0.75
      or
      repr = "(parameter s (parameter 1 (return (member each *))))" and
      t = "src" and
      result = 0.78003367003367
      or
      repr = "(return (parameter stripAndCollapse *))" and
      t = "src" and
      result = 0.055976390816562624
      or
      repr = "(parameter num *)" and t = "src" and result = 0.2462962962962963
      or
      repr = "(member localStorage (global))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(parameter fragment *)" and t = "src" and result = 0.13054830287206268
      or
      repr = "(parameter responseText *)" and t = "src" and result = 0.10915032679738561
      or
      repr = "(parameter content *)" and t = "src" and result = 0.25
      or
      repr = "(parameter handler *)" and t = "src" and result = 0.275
      or
      repr = "(parameter e (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.4460267753874623
      or
      repr = "(parameter 0 (member enable *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member complete *))" and t = "src" and result = 0.545751633986928
      or
      repr = "(parameter 1 (parameter 1 (return (member each (member jQuery *)))))" and
      t = "src" and
      result = 0.06944444444444434
      or
      repr = "(return (member createElement (member ownerDocument *)))" and
      t = "src" and
      result = 0.020000000000000018
      or
      repr = "(member data (parameter 1 (parameter 1 (return (member registerHelper *)))))" and
      t = "src" and
      result = 0.38667799146368886
      or
      repr = "(member PluginManager (member tinymce (global)))" and
      t = "src" and
      result = 0.24999999999999994
      or
      repr = "(return (member String (global)))" and t = "src" and result = 0.5
      or
      repr = "(member main *)" and t = "src" and result = 0.08000000000000007
      or
      repr = "(member maxlength *)" and t = "src" and result = 0.45454545454545453
      or
      repr = "(member response (instance (member XMLHttpRequest (global))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member _default *)" and t = "src" and result = 0.013811111111110996
      or
      repr = "(parameter i *)" and t = "src" and result = 0.5
      or
      repr = "(member fragment *)" and t = "src" and result = 0.13054830287206265
      or
      repr = "(parameter name (member css *))" and t = "src" and result = 0.2500000000000002
      or
      repr = "(return (member slice (parameter tokens *)))" and
      t = "src" and
      result = 0.40808869278553367
      or
      repr = "(member format (parameter o *))" and t = "src" and result = 0.18945545682387777
      or
      repr = "(return (member join (return (member map *))))" and
      t = "src" and
      result = 0.8536052541734361
      or
      repr = "(member jsonpCallback *)" and t = "src" and result = 0.585
      or
      repr = "(parameter R *)" and t = "src" and result = 0.08416666666666667
      or
      repr = "(member href (member location (member window (global))))" and
      t = "src" and
      result = 0.5294516971279374
      or
      repr = "(member img *)" and t = "src" and result = 0.33670033670033667
      or
      repr = "(return (member indexOf (parameter url (member load (member fn *)))))" and
      t = "src" and
      result = 0.8214285714285725
      or
      repr = "(return (member getAttribute (parameter b *)))" and
      t = "src" and
      result = 0.23000000000000032
      or
      repr = "(return (member replace (return (member call *))))" and
      t = "src" and
      result = 0.3787878787878788
      or
      repr = "(return (parameter n *))" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (parameter 1 (return (member each *))))" and
      t = "src" and
      result = 0.14757575757575764
      or
      repr = "(parameter ch *)" and t = "src" and result = 0.13971370431413363
      or
      repr = "(member proxy *)" and t = "src" and result = 0.0757041458278494
      or
      repr = "(member jsonpCallback (parameter n (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "src" and
      result = 0.005647840531561608
      or
      repr = "(return (member map (member jQuery (global))))" and
      t = "src" and
      result = 0.21464646464646456
      or
      repr = "(parameter args *)" and t = "src" and result = 0.5973000000000002
      or
      repr = "(member ThemeManager (member tinymce (global)))" and t = "src" and result = 0.25
      or
      repr = "(return (member querySelector (member document (global))))" and
      t = "src" and
      result = 0.5
      or
      repr = "(parameter 0 (member removeClass (parameter 0 (return (member extend *)))))" and
      t = "src" and
      result = 0.10454545454545412
      or
      repr = "(return (member slice (parameter e *)))" and t = "src" and result = 0.2170979855810009
      or
      repr = "(return (member pop (return (member split *))))" and
      t = "src" and
      result = 0.3500000000000001
      or
      repr = "(return (member indexOf (parameter 0 (member load (member fn *)))))" and
      t = "src" and
      result = 0.777500000000001
      or
      repr = "(return (member serialize (member serializer *)))" and
      t = "src" and
      result = 0.8105445431761222
      or
      repr = "(return (member splice *))" and t = "src" and result = 0.3333333333333333
      or
      repr = "(member responseText (parameter 0 (member complete *)))" and
      t = "src" and
      result = 0.6901960784313725
      or
      repr = "(return (member indexOf (parameter a *)))" and
      t = "src" and
      result = 0.527500000000001
      or
      repr = "(return (return (member require (global))))" and
      t = "src" and
      result = 0.20633054684778818
      or
      repr = "(parameter source *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (member clean *))" and t = "src" and result = 1.0
      or
      repr = "(parameter gradient *)" and t = "src" and result = 0.29166666666666663
      or
      repr = "(parameter responseText (parameter 0 (return (member done *))))" and
      t = "src" and
      result = 0.003307585660526845
      or
      repr = "(return (member pop *))" and t = "src" and result = 0.5
      or
      repr = "(parameter 0 (parameter 0 (return (member done *))))" and
      t = "src" and
      result = 0.21830065359477122
      or
      repr = "(parameter n (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member urls *)" and t = "src" and result = 0.3787878787878788
      or
      repr = "(parameter param (parameter 1 (return (member addMethod (member validator *)))))" and
      t = "src" and
      result = 0.625
      or
      repr = "(member 0 (return (member match *)))" and t = "src" and result = 0.31165262525792053
      or
      repr = "(parameter elems (member clean (parameter 0 (return (member extend *)))))" and
      t = "src" and
      result = 0.40641870061915536
      or
      repr = "(parameter 1 (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member href *)" and t = "src" and result = 0.13054830287206265
      or
      repr = "(parameter arg (parameter 1 (return (member each (member constructor *)))))" and
      t = "src" and
      result = 0.19185555555555572
      or
      repr = "(member prefix *)" and t = "src" and result = 0.18134510178117047
      or
      repr = "(parameter 0 (member clean (parameter 0 (return (member extend *)))))" and
      t = "src" and
      result = 0.24463628809646545
      or
      repr = "(return (return (member _fnGetObjectDataFn *)))" and
      t = "src" and
      result = 0.2287092915097723
      or
      repr = "(member url (parameter handler *))" and t = "src" and result = 0.55
      or
      repr = "(parameter c *)" and t = "src" and result = 0.16122781179753506
      or
      repr = "(parameter tokens *)" and t = "src" and result = 0.5
      or
      repr = "(member minlength (parameter rules *))" and t = "src" and result = 0.45454545454545453
      or
      repr = "(return (parameter data *))" and t = "src" and result = 0.4722222222222222
      or
      repr = "(return (member getBody *))" and t = "src" and result = 0.4052722715880611
      or
      repr = "(member documentBaseURL *)" and t = "src" and result = 0.25
      or
      repr = "(member jsonpCallback (parameter 0 (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "src" and
      result = 0.085542635658915
      or
      repr = "(parameter start *)" and t = "src" and result = 0.23999999999999994
      or
      repr = "(return (member indexOf (parameter 0 (member load *))))" and
      t = "src" and
      result = 0.2324999999999991
      or
      repr = "(member caption *)" and t = "src" and result = 0.33670033670033667
      or
      repr = "(parameter 1 (return (member apply *)))" and
      t = "src" and
      result = 0.06154898218829519
      or
      repr = "(return (member createFrame (return (parameter __webpack_require__ *))))" and
      t = "src" and
      result = 0.2929378723209764
      or
      repr = "(member options *)" and t = "src" and result = 0.25
      or
      repr = "(member start *)" and t = "src" and result = 0.17414999999999992
      or
      repr = "(member desc *)" and t = "src" and result = 0.33670033670033667
      or
      repr = "(parameter v *)" and t = "src" and result = 0.6099999999999999
      or
      repr = "(return (member getPath *))" and t = "src" and result = 0.13054830287206265
      or
      repr = "(parameter window *)" and t = "src" and result = 0.2916350508905855
      or
      repr = "(parameter g *)" and t = "src" and result = 0.4867329572237328
      or
      repr = "(member response (return (member XMLHttpRequest (global))))" and
      t = "src" and
      result = 0.26515151515151486
      or
      repr = "(member noop (member jQuery (global)))" and t = "src" and result = 0.4310344827586206
      or
      repr = "(return (member g (global)))" and t = "src" and result = 0.1111111111111111
      or
      repr = "(member location *)" and t = "src" and result = 0.5
      or
      repr = "(member min (return (member normalizeRules *)))" and
      t = "src" and
      result = 0.45454545454545453
      or
      repr = "(parameter 0 (parameter 0 (return (member createPseudo (member selectors *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member match (parameter value *)))" and t = "src" and result = 0.75
      or
      repr = "(member 0 (return (member tokenize *)))" and t = "src" and result = 0.3028787878787879
      or
      repr = "(parameter el *)" and t = "src" and result = 0.5
      or
      repr = "(member body (parameter 0 (parameter 2 (return (member delete *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member username *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 2 (return (member delete *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member enable (member exports *)))" and
      t = "src" and
      result = 0.3397357873815273
      or
      repr = "(return (member max (return (member string *))))" and
      t = "src" and
      result = 0.2360233992450158
      or
      repr = "(return (member readFileSync (root https://www.npmjs.com/package/fs)))" and
      t = "src" and
      result = 0.64740814623485
      or
      repr = "(return (member lastIndexOf *))" and t = "src" and result = 0.37936664243223633
      or
      repr = "(member email *)" and t = "src" and result = 1.0
      or
      repr = "(return (member slice (return (member slice *))))" and
      t = "src" and
      result = 0.08999999999999997
      or
      repr = "(parameter error (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member 2 (parameter 0 (member PSEUDO (member preFilter (member selectors *)))))" and
      t = "src" and
      result = 0.06394669587440668
      or
      repr = "(member passphrase *)" and t = "src" and result = 0.18
      or
      repr = "(return (member charAt (parameter data *)))" and
      t = "src" and
      result = 0.00022278755667137773
      or
      repr = "(return (member base64urlDecode *))" and t = "src" and result = 0.19599415018874605
      or
      repr = "(member 2 (member argv (member process (global))))" and
      t = "src" and
      result = 0.9046422741573208
      or
      repr = "(member data (parameter packet (member encodePacket (parameter exports *))))" and
      t = "src" and
      result = 0.5133333333333322
      or
      repr = "(parameter expected *)" and t = "src" and result = 0.5963636363636363
      or
      repr = "(parameter 0 (member send *))" and t = "src" and result = 1.0
      or
      repr = "(instance (member SlowBuffer *))" and t = "src" and result = 0.3338756179107372
      or
      repr = "(parameter match (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.0718030303030302
      or
      repr = "(member question *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member relative *))" and t = "src" and result = 0.3793666424322363
      or
      repr = "(member email (parameter 0 (return (member findOne *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member parse (root https://www.npmjs.com/package/querystring)))" and
      t = "src" and
      result = 0.5000000000000001
      or
      repr = "(return (member min *))" and t = "src" and result = 0.3819883003774921
      or
      repr = "(parameter 0 (return (member encryptBlock *)))" and
      t = "src" and
      result = 0.7423535314593083
      or
      repr = "(return (member replace (return (member toString *))))" and
      t = "src" and
      result = 0.6298236756630406
      or
      repr = "(member cookie *)" and t = "src" and result = 0.75
      or
      repr = "(member tbsCertificate *)" and t = "src" and result = 0.1554422026744697
      or
      repr = "(member infos *)" and t = "src" and result = 0.007575757575757569
      or
      repr = "(return (member dirname (root https://www.npmjs.com/package/path)))" and
      t = "src" and
      result = 0.3200000000000003
      or
      repr = "(member document (global))" and t = "src" and result = 0.2548991925640199
      or
      repr = "(return (member identifier *))" and t = "src" and result = 0.25
      or
      repr = "(parameter contents *)" and t = "src" and result = 0.4017447199265381
      or
      repr = "(return (member decode *))" and t = "src" and result = 0.12494430311083216
      or
      repr = "(parameter result *)" and t = "src" and result = 0.7575757575757576
      or
      repr = "(return (member day *))" and t = "src" and result = 0.11801770725537541
      or
      repr = "(parameter key (parameter 1 (return (member forOwn *))))" and
      t = "src" and
      result = 0.19531522775815452
      or
      repr = "(parameter parts *)" and t = "src" and result = 0.31088040299622494
      or
      repr = "(member Date *)" and t = "src" and result = 0.06726964157770832
      or
      repr = "(parameter 1 (parameter 1 (return (member forOwn *))))" and t = "src" and result = 1.0
      or
      repr = "(member version (root https://www.npmjs.com/package/process))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member CLASS *))" and t = "src" and result = 0.0508239327911143
      or
      repr = "(member argument *)" and t = "src" and result = 0.15052792195309497
      or
      repr = "(parameter err (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member expires *)" and t = "src" and result = 0.022956841138659298
      or
      repr = "(parameter target *)" and t = "src" and result = 0.6299999999999999
      or
      repr = "(parameter res (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member matchesSelector *)" and t = "src" and result = 0.009999999999999953
      or
      repr = "(parameter 0 (member set (parameter 2 (return (member defineProperty *)))))" and
      t = "src" and
      result = 0.5
      or
      repr = "(parameter list *)" and t = "src" and result = 0.14076562500000003
      or
      repr = "(member style *)" and t = "src" and result = 0.2585858585858584
      or
      repr = "(return (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.5224242424242427
      or
      repr = "(parameter parent *)" and t = "src" and result = 0.125
      or
      repr = "(return (member getIndent *))" and t = "src" and result = 0.25964989898989893
      or
      repr = "(return (member update *))" and t = "src" and result = 1.0
      or
      repr = "(return (member slice (return (member replace *))))" and
      t = "src" and
      result = 0.5808333333333339
      or
      repr = "(member currentPath *)" and t = "src" and result = 0.07036092068514084
      or
      repr = "(parameter protocol *)" and t = "src" and result = 1.0
      or
      repr = "(return (member get (instance (member constructor *))))" and
      t = "src" and
      result = 0.28849592688257497
      or
      repr = "(parameter values *)" and t = "src" and result = 0.33036929791619296
      or
      repr = "(member 1 (parameter a *))" and t = "src" and result = 0.18663977819309224
      or
      repr = "(member answer (member body (parameter req *)))" and
      t = "src" and
      result = 0.2887931034482758
      or
      repr = "(return (member toArray *))" and t = "src" and result = 0.36766200000000004
      or
      repr = "(member declarations (parameter node *))" and
      t = "src" and
      result = 0.11111111111111109
      or
      repr = "(parameter s (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.3279344847679555
      or
      repr = "(parameter locals *)" and t = "src" and result = 0.22630500000000003
      or
      repr = "(return (member floor (member Math (global))))" and t = "src" and result = 1.0
      or
      repr = "(member pos (member state *))" and t = "src" and result = 0.25450086881129996
      or
      repr = "(member value (parameter node *))" and t = "src" and result = 0.8333333333333334
      or
      repr = "(parameter chunk *)" and t = "src" and result = 0.5075757575757576
      or
      repr = "(parameter 0 (parameter 0 (return (member encode *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member path *))" and t = "src" and result = 0.5
      or
      repr = "(member privateKey *)" and t = "src" and result = 0.18
      or
      repr = "(parameter 0 (parameter 0 (return (root https://www.npmjs.com/package/through2))))" and
      t = "src" and
      result = 0.015151515151515138
      or
      repr = "(member duration *)" and t = "src" and result = 0.46190000000000003
      or
      repr = "(parameter node (member Function *))" and t = "src" and result = 0.16312056737588626
      or
      repr = "(return (member flatten *))" and t = "src" and result = 0.25
      or
      repr = "(member select (member options *))" and t = "src" and result = 0.5
      or
      repr = "(return (member join (root https://www.npmjs.com/package/path)))" and
      t = "src" and
      result = 0.5
      or
      repr = "(parameter 0 (parameter 0 (parameter 0 (return (member push *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member 3 (return (member match *)))" and t = "src" and result = 0.16753984889546392
      or
      repr = "(parameter encoding *)" and t = "src" and result = 0.007575757575757569
      or
      repr = "(return (member split (parameter namespaces (member enable (member exports *)))))" and
      t = "src" and
      result = 0.4106684142025219
      or
      repr = "(parameter compressor *)" and t = "src" and result = 0.09799999999999992
      or
      repr = "(member content-type *)" and t = "src" and result = 1.0
      or
      repr = "(member w *)" and t = "src" and result = 0.20959595959595959
      or
      repr = "(parameter 0 (member onData (member prototype *)))" and
      t = "src" and
      result = 1.0000000000000002
      or
      repr = "(member ids *)" and t = "src" and result = 0.9587999999999999
      or
      repr = "(member name (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member version *)" and t = "src" and result = 0.323704073117425
      or
      repr = "(member input (member strm *))" and t = "src" and result = 0.1686030821717978
      or
      repr = "(member contentType *)" and t = "src" and result = 1.0
      or
      repr = "(parameter namespace *)" and t = "src" and result = 0.08333333333333337
      or
      repr = "(member arbiters *)" and t = "src" and result = 0.1335979634412874
      or
      repr = "(member _opt *)" and t = "src" and result = 0.026316882952172604
      or
      repr = "(member content-type (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(return (member stringify (member JSON (global))))" and
      t = "src" and
      result = 0.7309214336891666
      or
      repr = "(parameter chunk (member _transform (member prototype *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member _baseState *)" and t = "src" and result = 0.125
      or
      repr = "(parameter files *)" and t = "src" and result = 0.35
      or
      repr = "(return (member filter (return (member slice *))))" and
      t = "src" and
      result = 0.39879620340901556
      or
      repr = "(member output *)" and t = "src" and result = 0.08128787878787885
      or
      repr = "(parameter 1 (return (member replace (parameter str *))))" and
      t = "src" and
      result = 0.10577272727272732
      or
      repr = "(parameter r (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.40572567090403183
      or
      repr = "(member d *)" and t = "src" and result = 0.25000000000000006
      or
      repr = "(return (member explodeExpression *))" and t = "src" and result = 0.06940242067550997
      or
      repr = "(return (member getBindingIdentifiers *))" and
      t = "src" and
      result = 0.23435157151095734
      or
      repr = "(parameter p *)" and t = "src" and result = 0.016666666666666663
      or
      repr = "(parameter 0 (return (member concat (return (member concat *)))))" and
      t = "src" and
      result = 0.006000000000000116
      or
      repr = "(parameter location *)" and t = "src" and result = 0.33
      or
      repr = "(return (member obj (root https://www.npmjs.com/package/through2)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member parse (instance (return (member defaults *)))))" and
      t = "src" and
      result = 0.22331491218062735
      or
      repr = "(parameter 0 (return (member flatten *)))" and
      t = "src" and
      result = 0.041200000000000125
      or
      repr = "(return (parameter path *))" and t = "src" and result = 0.1216379999999999
      or
      repr = "(parameter chunk (member _transform *))" and t = "src" and result = 0.9924242424242427
      or
      repr = "(return (member replace (member yytext *)))" and
      t = "src" and
      result = 0.9873512001688759
      or
      repr = "(return (member createPseudo (member selectors (member find (member exports *)))))" and
      t = "src" and
      result = 0.08333333333333295
      or
      repr = "(parameter packet *)" and t = "src" and result = 0.5589963441287504
      or
      repr = "(member expression *)" and t = "src" and result = 0.09947207804690503
      or
      repr = "(return (member replace (return (member join *))))" and t = "src" and result = 0.5
      or
      repr = "(parameter xs *)" and t = "src" and result = 0.25999999999999995
      or
      repr = "(parameter 0 (member push (member prototype *)))" and
      t = "src" and
      result = 0.3800505050505053
      or
      repr = "(parameter reporter *)" and t = "src" and result = 0.125
      or
      repr = "(member argv *)" and t = "src" and result = 0.2317213622063155
      or
      repr = "(member sourceRoot *)" and t = "src" and result = 0.10954766655086504
      or
      repr = "(member jsonp *)" and t = "src" and result = 0.5335555960885615
      or
      repr = "(parameter schema *)" and t = "src" and result = 0.25565050037716436
      or
      repr = "(return (root https://www.npmjs.com/package/spdx-correct))" and
      t = "src" and
      result = 0.16666666666666666
      or
      repr = "(parameter keyPath *)" and t = "src" and result = 0.1639214336891666
      or
      repr = "(parameter chunk (parameter 1 (return (member on (member stdin *)))))" and
      t = "src" and
      result = 0.9924242424242427
      or
      repr = "(return (member substr (member yytext (parameter yy_ (member performAction *)))))" and
      t = "src" and
      result = 0.7374625939472115
      or
      repr = "(parameter comment *)" and t = "src" and result = 0.5
      or
      repr = "(return (member RegExp (global)))" and t = "src" and result = 0.04000000000000001
      or
      repr = "(return (member parse (member SemVer (root https://www.npmjs.com/package/semver))))" and
      t = "src" and
      result = 0.07739143830651013
      or
      repr = "(return (member lowercase *))" and t = "src" and result = 0.19099415018874605
      or
      repr = "(parameter to *)" and t = "src" and result = 0.09263178406484424
      or
      repr = "(parameter raw *)" and t = "src" and result = 0.125
      or
      repr = "(parameter context *)" and t = "src" and result = 0.33
      or
      repr = "(parameter newValue *)" and t = "src" and result = 0.5028037240214585
      or
      repr = "(return (member split (parameter 0 (member enable (member exports *)))))" and
      t = "src" and
      result = 0.4711421216637311
      or
      repr = "(member leftId *)" and t = "src" and result = 0.20833333333333331
      or
      repr = "(parameter self *)" and t = "src" and result = 0.4036462631714544
      or
      repr = "(member q *)" and t = "src" and result = 0.2095959595959596
      or
      repr = "(parameter 1 (member performAction (member lexer (member prototype *))))" and
      t = "src" and
      result = 0.718073600122819
      or
      repr = "(parameter dir *)" and t = "src" and result = 0.2518860941012325
      or
      repr = "(member _wcurpath *)" and t = "src" and result = 0.05146501718814883
      or
      repr = "(parameter 3 (member exports *))" and t = "src" and result = 0.2972711997959653
      or
      repr = "(return (root https://www.npmjs.com/package/node-uuid))" and
      t = "src" and
      result = 0.5792999999999999
      or
      repr = "(return (member decode (member Certificate *)))" and
      t = "src" and
      result = 0.12505569688916784
      or
      repr = "(parameter string (member parse *))" and t = "src" and result = 0.09222940932020372
      or
      repr = "(member version (member process (global)))" and t = "src" and result = 1.0
      or
      repr = "(member 0 (return (member exec *)))" and t = "src" and result = 0.08999999999999997
      or
      repr = "(parameter elems *)" and t = "src" and result = 0.3325
      or
      repr = "(return (member startNode *))" and t = "src" and result = 0.16999999999999998
      or
      repr = "(parameter items *)" and t = "src" and result = 0.6810902255639097
      or
      repr = "(member email (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter buffer *)" and t = "src" and result = 0.38955805032038215
      or
      repr = "(parameter file (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.05893939393939451
      or
      repr = "(parameter min *)" and t = "src" and result = 0.06726964157770832
      or
      repr = "(parameter lines *)" and t = "src" and result = 0.09000000000000008
      or
      repr = "(parameter root *)" and t = "src" and result = 0.25565050037716436
      or
      repr = "(member extension *)" and t = "src" and result = 0.12499999999999994
      or
      repr = "(member key (return (member startNode *)))" and t = "src" and result = 0.5
      or
      repr = "(parameter 0 (parameter 0 (return (member createPseudo *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member print *))" and t = "src" and result = 0.616659489962595
      or
      repr = "(member data (parameter packet *))" and t = "src" and result = 0.5700000000000003
      or
      repr = "(member name (parameter 0 (return (member findOne *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (parameter _dereq_ *))" and t = "src" and result = 0.1428916867885479
      or
      repr = "(parameter f *)" and t = "src" and result = 0.013183205618674372
      or
      repr = "(member tok *)" and t = "src" and result = 0.41666666666666663
      or
      repr = "(member opts *)" and t = "src" and result = 0.040632534346689474
      or
      repr = "(parameter block *)" and t = "src" and result = 0.3110902255639098
      or
      repr = "(parameter 3 (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.25
      or
      repr = "(return (member toJSON *))" and t = "src" and result = 0.02409393939393939
      or
      repr = "(return (member readdirSync (root https://www.npmjs.com/package/fs)))" and
      t = "src" and
      result = 0.5
      or
      repr = "(parameter aSourceMap *)" and t = "src" and result = 0.5075757575757576
      or
      repr = "(return (member replace (parameter comment *)))" and t = "src" and result = 1.0
      or
      repr = "(member content-type (member headers (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member split (parameter namespaces *)))" and
      t = "src" and
      result = 0.07603541451075041
      or
      repr = "(member state *)" and t = "src" and result = 0.12725043440564998
      or
      repr = "(member message *)" and t = "src" and result = 0.2095959595959596
      or
      repr = "(return (member clone *))" and t = "src" and result = 0.3817461867474585
      or
      repr = "(parameter res *)" and t = "src" and result = 1.0
      or
      repr = "(parameter data (member send *))" and t = "src" and result = 0.6590909090909085
      or
      repr = "(return (member keys (member decls *)))" and
      t = "src" and
      result = 0.07990731174249949
      or
      repr = "(parameter r *)" and t = "src" and result = 0.25
      or
      repr = "(return (member comment *))" and t = "src" and result = 0.5
      or
      repr = "(return (member createUTC *))" and t = "src" and result = 0.11801770725537541
      or
      repr = "(parameter stdout *)" and t = "src" and result = 0.75
      or
      repr = "(member responseText *)" and t = "src" and result = 0.4999999999999999
      or
      repr = "(return (member uppercase *))" and t = "src" and result = 0.19099415018874605
      or
      repr = "(parameter streams *)" and t = "src" and result = 0.04202190355192084
      or
      repr = "(return (member indexOf (member url *)))" and
      t = "src" and
      result = 0.7741285898486654
      or
      repr = "(return (member escape (global)))" and t = "src" and result = 0.065
      or
      repr = "(return (return (parameter _dereq_ *)))" and
      t = "src" and
      result = 0.36673906584382815
      or
      repr = "(parameter 1 (member serialize *))" and t = "src" and result = 1.0
      or
      repr = "(member env *)" and t = "src" and result = 0.1157168034054109
      or
      repr = "(member name (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter offset (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.2727272727272726
      or
      repr = "(parameter obj *)" and t = "src" and result = 0.10075203655871254
      or
      repr = "(return (parameter 0 (return (member then *))))" and
      t = "src" and
      result = 0.5771962759785418
      or
      repr = "(member email (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (member exports (parameter module *)))" and
      t = "src" and
      result = 0.6663046707808592
      or
      repr = "(member ignore (instance (member Glob *)))" and
      t = "src" and
      result = 0.40364626317145447
      or
      repr = "(member close *)" and t = "src" and result = 0.32289168678854785
      or
      repr = "(return (member reverse (return (member split *))))" and t = "src" and result = 1.0
      or
      repr = "(member 1 (return (member exec *)))" and t = "src" and result = 0.75
      or
      repr = "(parameter sub *)" and t = "src" and result = 0.25565050037716436
      or
      repr = "(parameter 0 (return (member encode (return (parameter _dereq_ *)))))" and
      t = "src" and
      result = 0.8163531870428421
      or
      repr = "(parameter username *)" and t = "src" and result = 0.46285599889216017
      or
      repr = "(parameter 0 (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member 1 (return (member split (return (member replace *)))))" and
      t = "src" and
      result = 0.9999999999999998
      or
      repr = "(parameter prefix *)" and t = "src" and result = 0.16499999999999992
      or
      repr = "(return (member slice (parameter args *)))" and
      t = "src" and
      result = 0.41000000000000014
      or
      repr = "(parameter charset *)" and t = "src" and result = 0.4166666666666667
      or
      repr = "(return (member getModuleName *))" and t = "src" and result = 0.014140271493212671
      or
      repr = "(member getElementsByClassName *)" and t = "src" and result = 0.009999999999999953
      or
      repr = "(member documentElement (member document *))" and
      t = "src" and
      result = 0.3862108978242724
      or
      repr = "(parameter 0 (member encodePacket *))" and t = "src" and result = 0.19857941344700708
      or
      repr = "(parameter 1 (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.3564600550964187
      or
      repr = "(member styles *)" and t = "src" and result = 0.5
      or
      repr = "(return (member fromJS *))" and t = "src" and result = 0.2733333333333334
      or
      repr = "(member input *)" and t = "src" and result = 0.061988300377491846
      or
      repr = "(return (member toDate *))" and t = "src" and result = 0.06940371456500488
      or
      repr = "(member url (parameter 0 (return (member ajax *))))" and
      t = "src" and
      result = 0.6628409875775101
      or
      repr = "(parameter index *)" and t = "src" and result = 0.25
      or
      repr = "(parameter charCode *)" and t = "src" and result = 0.022199999999999998
      or
      repr = "(parameter 4 (member exports (parameter e *)))" and
      t = "src" and
      result = 0.5197566167838206
      or
      repr = "(parameter 0 (parameter 2 (return (member get *))))" and t = "src" and result = 1.0
      or
      repr = "(member picture *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member picture_id (parameter 0 (return (member find *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter e (member exports *))" and t = "src" and result = 0.20523314782065816
      or
      repr = "(parameter 0 (member _id (parameter 0 (return (member remove *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member query (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member user_id *)" and t = "src" and result = 1.0
      or
      repr = "(member value (parameter e *))" and t = "src" and result = 0.18113910106849385
      or
      repr = "(parameter 1 (member exports (parameter e *)))" and
      t = "src" and
      result = 0.11987830839191013
      or
      repr = "(parameter req (parameter 2 (return (member get *))))" and
      t = "src" and
      result = 0.5151515151515149
      or
      repr = "(parameter y *)" and t = "src" and result = 0.16858534202196157
      or
      repr = "(return (parameter t (member exports *)))" and
      t = "src" and
      result = 0.195509522075764
      or
      repr = "(parameter 0 (member picture_id *))" and t = "src" and result = 1.0
      or
      repr = "(member search (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member picture_id (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member auth (parameter 0 (return (member authorizeRequest (member authActions *)))))" and
      t = "src" and
      result = 0.016891189014875352
      or
      repr = "(return (member bind *))" and t = "src" and result = 0.2662807804907046
      or
      repr = "(member authActions *)" and t = "src" and result = 0.75
      or
      repr = "(return (member join (member scopes *)))" and
      t = "src" and
      result = 0.12378237802975074
      or
      repr = "(return (member valueOf (parameter e *)))" and t = "src" and result = 0.25
      or
      repr = "(member picture (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member 1 (return (member split *)))" and t = "src" and result = 0.25
      or
      repr = "(member scopes (member auth *))" and t = "src" and result = 0.24999999999999992
      or
      repr = "(member picture_id (parameter 0 (return (member insert *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (parameter r *))" and t = "src" and result = 0.019012252022661713
      or
      repr = "(return (return (parameter n *)))" and t = "src" and result = 0.1523670822744999
      or
      repr = "(member query (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member prototype *)" and t = "src" and result = 0.25
      or
      repr = "(member picture_id *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member stringify (member JSON (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member picture_id (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member stringify *)))" and t = "src" and result = 1.0
      or
      repr = "(member headers (parameter 1 (return (member createElement *))))" and
      t = "src" and
      result = 0.8761404469464827
      or
      repr = "(member picture (member params *))" and t = "src" and result = 1.0
      or
      repr = "(return (member Object (global)))" and t = "src" and result = 0.04766839176115659
      or
      repr = "(parameter 0 (return (member Number (global))))" and t = "src" and result = 1.0
      or
      repr = "(return (member toString (member prototype (member Function (global)))))" and
      t = "src" and
      result = 0.3787878787878788
      or
      repr = "(member pos (parameter e (member exports (parameter e *))))" and
      t = "src" and
      result = 0.4375699284533705
      or
      repr = "(member picture_id (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member picture_id (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member render *))" and t = "src" and result = 0.38469062287481215
      or
      repr = "(parameter 0 (member _id (parameter 0 (return (member findOneAndUpdate *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member search (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter t (member exports (parameter t *)))" and
      t = "src" and
      result = 0.5487500000000001
      or
      repr = "(member body (member req *))" and t = "src" and result = 1.0
      or
      repr = "(return (member keys *))" and t = "src" and result = 0.5
      or
      repr = "(return (member getFile *))" and t = "src" and result = 0.004705000000000048
      or
      repr = "(return (member getRecentEntryArray *))" and
      t = "src" and
      result = 0.11800000000000013
      or
      repr = "(parameter userData *)" and t = "src" and result = 0.4
      or
      repr = "(parameter domainName *)" and t = "src" and result = 0.8275000000000001
      or
      repr = "(member lines *)" and t = "src" and result = 0.21654135338345867
      or
      repr = "(member if-match *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member stringify *))))" and
      t = "src" and
      result = 0.09850000000000006
      or
      repr = "(member error *)" and t = "src" and result = 0.01810606060606057
      or
      repr = "(parameter contentType *)" and t = "src" and result = 0.25
      or
      repr = "(return (member read *))" and t = "src" and result = 0.07931039161900029
      or
      repr = "(return (member workdir *))" and t = "src" and result = 0.6461552612334077
      or
      repr = "(parameter query *)" and t = "src" and result = 0.2863645639127732
      or
      repr = "(parameter 0 (member exports (parameter t *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (member computeHoverInfo *))" and
      t = "src" and
      result = 0.4268049555555553
      or
      repr = "(member params (member _searchHelper *))" and
      t = "src" and
      result = 0.21428571428571427
      or
      repr = "(instance (member ProjectionTextModel *))" and
      t = "src" and
      result = 0.020833333333333245
      or
      repr = "(parameter 0 (return (member slice (member input *))))" and
      t = "src" and
      result = 0.8750000000000002
      or
      repr = "(member allLineBreaks *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getProperty (member 0 (return (member getServiceReferences *)))))" and
      t = "src" and
      result = 0.81896
      or
      repr = "(member manual *)" and t = "src" and result = 0.3913427561837456
      or
      repr = "(parameter max *)" and t = "src" and result = 0.25
      or
      repr = "(return (member then (return (member then *))))" and
      t = "src" and
      result = 0.7568049555555554
      or
      repr = "(parameter opt *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(member themeClass *)" and t = "src" and result = 0.04166666666666666
      or
      repr = "(member body (parameter 0 (parameter 5 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member files (return (member node *)))" and
      t = "src" and
      result = 0.35515641806565657
      or
      repr = "(return (member createElement *))" and t = "src" and result = 0.12626262626262622
      or
      repr = "(parameter 0 (parameter 0 (return (member catch *))))" and
      t = "src" and
      result = 0.4900000000000001
      or
      repr = "(member 4 (return (member match *)))" and t = "src" and result = 0.17999999999999994
      or
      repr = "(parameter e (member construct *))" and t = "src" and result = 0.9252380952380945
      or
      repr = "(parameter branch *)" and t = "src" and result = 0.2025
      or
      repr = "(member body (parameter req (parameter 4 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member _lineHeight (instance (member TextView *)))" and t = "src" and result = 1.0
      or
      repr = "(member resource (member params *))" and t = "src" and result = 0.18811904761904769
      or
      repr = "(member originalUrl *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member replace (return (member replace *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getLocation *))" and t = "src" and result = 0.33000000000000007
      or
      repr = "(parameter 0 (return (member parse (member JSON (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter ref (parameter 0 (return (member then *))))" and
      t = "src" and
      result = 0.35215794306703463
      or
      repr = "(parameter 0 (member router *))" and t = "src" and result = 0.0025252525252524994
      or
      repr = "(return (member appendChild *))" and t = "src" and result = 0.03250000000000003
      or
      repr = "(parameter 1 (return (member getFile *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 4 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (member compare *))" and t = "src" and result = 0.1862944
      or
      repr = "(parameter 0 (return (member dirname *)))" and
      t = "src" and
      result = 0.35999999999999943
      or
      repr = "(member prev (member prev (member prev (member prev *))))" and
      t = "src" and
      result = 0.0668750000000004
      or
      repr = "(return (member getProjectDeployService *))" and
      t = "src" and
      result = 0.03449153822222231
      or
      repr = "(parameter scope *)" and t = "src" and result = 0.169054939539642
      or
      repr = "(member $1 (member RegExp (global)))" and t = "src" and result = 0.06633333333333324
      or
      repr = "(member prev (member prev (member prev (member prev (member prev *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member href (return (member createElement (member document (global)))))" and
      t = "src" and
      result = 0.6052231111111108
      or
      repr = "(member 0 (parameter 0 (parameter 0 (return (member then *)))))" and
      t = "src" and
      result = 0.10227272727272732
      or
      repr = "(member cursor *)" and t = "src" and result = 0.236371
      or
      repr = "(parameter line (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.984848484848485
      or
      repr = "(member resource *)" and t = "src" and result = 0.2136354360872268
      or
      repr = "(member Name (parameter branch *))" and t = "src" and result = 0.2878787878787878
      or
      repr = "(return (member _getEntry *))" and t = "src" and result = 0.66
      or
      repr = "(parameter err (parameter 0 (return (member catch *))))" and
      t = "src" and
      result = 0.02515151515151482
      or
      repr = "(parameter 0 (return (member makeRenderFunction *)))" and
      t = "src" and
      result = 0.02616666666666671
      or
      repr = "(parameter 1 (return (member assign *)))" and t = "src" and result = 1.0
      or
      repr = "(member typed *)" and t = "src" and result = 0.12499999999999978
      or
      repr = "(member GitRepoDir *)" and t = "src" and result = 0.019364090190495314
      or
      repr = "(member parent *)" and t = "src" and result = 0.04166666666666666
      or
      repr = "(return (member workdir (parameter repo *)))" and
      t = "src" and
      result = 0.061534216299776734
      or
      repr = "(member DiffLocation *)" and t = "src" and result = 0.16666666666666666
      or
      repr = "(return (member getRepo *))" and t = "src" and result = 0.20750000000000005
      or
      repr = "(parameter key (member remove *))" and t = "src" and result = 1.0
      or
      repr = "(member parameters (parameter data *))" and t = "src" and result = 0.39266666666666683
      or
      repr = "(member ownerDocument *)" and t = "src" and result = 0.08333333333333331
      or
      repr = "(parameter 0 (parameter 0 (return (member every *))))" and
      t = "src" and
      result = 0.07499999999999996
      or
      repr = "(parameter 0 (return (member resolve (member Promise (global)))))" and
      t = "src" and
      result = 0.7515226599326598
      or
      repr = "(member Target (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member getBaseModel *))" and t = "src" and result = 0.09273684210526312
      or
      repr = "(return (member reject (root https://www.npmjs.com/package/bluebird)))" and
      t = "src" and
      result = 0.559261744966443
      or
      repr = "(parameter metadata *)" and t = "src" and result = 0.1600293953765851
      or
      repr = "(member scope *)" and t = "src" and result = 0.020738255033557196
      or
      repr = "(parameter serviceReference *)" and t = "src" and result = 0.0637056
      or
      repr = "(member filter (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member id (return (member createElement (member document (global)))))" and
      t = "src" and
      result = 0.021173165618449154
      or
      repr = "(parameter cx *)" and t = "src" and result = 0.21337500000000004
      or
      repr = "(member Target (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter fileMetadata *)" and t = "src" and result = 0.482160435434344
      or
      repr = "(return (member getProperty (parameter serviceRef *)))" and
      t = "src" and
      result = 0.15229333333333325
      or
      repr = "(return (member join (return (member slice *))))" and
      t = "src" and
      result = 0.6881541375291376
      or
      repr = "(parameter 1 (return (parameter 0 (return (member then *)))))" and
      t = "src" and
      result = 0.3684773400673404
      or
      repr = "(parameter manifest *)" and t = "src" and result = 0.5845871212121214
      or
      repr = "(member Clone *)" and t = "src" and result = 0.8333333333333333
      or
      repr = "(return (member getText *))" and t = "src" and result = 0.25
      or
      repr = "(return (member get (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member getProperty *))" and t = "src" and result = 0.18104
      or
      repr = "(return (member getEditorContext *))" and t = "src" and result = 0.16666666666666669
      or
      repr = "(parameter 0 (parameter 1 (return (member define (global)))))" and
      t = "src" and
      result = 0.25
      or
      repr = "(member start (parameter selection *))" and t = "src" and result = 0.5566666666666666
      or
      repr = "(parameter category *)" and t = "src" and result = 0.585
      or
      repr = "(member Url *)" and t = "src" and result = 0.2575757575757574
      or
      repr = "(member optionalParameters *)" and t = "src" and result = 0.0637056
      or
      repr = "(parameter key (member remove (member prototype *)))" and
      t = "src" and
      result = 0.9800000000000011
      or
      repr = "(return (member _stripBraces *))" and t = "src" and result = 0.22
      or
      repr = "(member pathname (return (member URL (global))))" and t = "src" and result = 1.0
      or
      repr = "(parameter end *)" and t = "src" and result = 0.0625
      or
      repr = "(return (member name *))" and t = "src" and result = 0.2025
      or
      repr = "(parameter 0 (member start (member prototype *)))" and
      t = "src" and
      result = 0.25505050505050497
      or
      repr = "(parameter evnt *)" and t = "src" and result = 1.0
      or
      repr = "(parameter repo *)" and t = "src" and result = 0.12878787878787867
      or
      repr = "(member body (parameter req (parameter 5 (return (member put *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member encode *))" and t = "src" and result = 1.0
      or
      repr = "(member files *)" and t = "src" and result = 0.25
      or
      repr = "(return (member parseInt (global)))" and t = "src" and result = 0.5
      or
      repr = "(member location (global))" and t = "src" and result = 0.6396630087542086
      or
      repr = "(return (member parse (root https://www.npmjs.com/package/url)))" and
      t = "src" and
      result = 1.0000000000000002
      or
      repr = "(parameter searchLocation *)" and t = "src" and result = 0.5240000000000001
      or
      repr = "(return (member decodeURIComponent *))" and t = "src" and result = 0.17134993437733004
      or
      repr = "(return (member valueFor (member parameters (parameter 0 (member callback *)))))" and
      t = "src" and
      result = 0.6249999999999998
      or
      repr = "(parameter infer *)" and t = "src" and result = 0.35317499999999996
      or
      repr = "(return (member formatMessage *))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(parameter 1 (return (member assign (member Object (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member text (member arg *))" and t = "src" and result = 0.472742
      or
      repr = "(parameter 2 (parameter 1 (return (member define (global)))))" and
      t = "src" and
      result = 0.2566400000000002
      or
      repr = "(member New *)" and t = "src" and result = 0.02358600378893171
      or
      repr = "(parameter problem *)" and t = "src" and result = 0.25
      or
      repr = "(parameter branchName *)" and t = "src" and result = 0.18796992481203012
      or
      repr = "(member fileType *)" and t = "src" and result = 0.2362813852813851
      or
      repr = "(parameter 1 (member remove *))" and t = "src" and result = 0.5199999999999991
      or
      repr = "(member eol *)" and t = "src" and result = 0.06416000000000005
      or
      repr = "(member branchName (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter searchOpts *)" and t = "src" and result = 0.08999999999999997
      or
      repr = "(parameter 0 (return (member parse (root https://www.npmjs.com/package/url))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member parse *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member getProjectHandler *))" and t = "src" and result = 0.25
      or
      repr = "(member password (parameter userData *))" and t = "src" and result = 0.8
      or
      repr = "(parameter clazz *)" and t = "src" and result = 0.15130577777777765
      or
      repr = "(return (member shorthand *))" and t = "src" and result = 0.405
      or
      repr = "(parameter projects *)" and t = "src" and result = 0.03445670180722904
      or
      repr = "(member title *)" and t = "src" and result = 0.35
      or
      repr = "(member InlineLexer (root https://www.npmjs.com/package/marked/marked))" and
      t = "src" and
      result = 0.07272727272727245
      or
      repr = "(member userData (parameter 0 (member callback *)))" and
      t = "src" and
      result = 0.6666666666666666
      or
      repr = "(member pathname (instance (member URL (global))))" and
      t = "src" and
      result = 0.14141414141414121
      or
      repr = "(member description (member _tag *))" and t = "src" and result = 0.5891194319066148
      or
      repr = "(member wrap_line_length (parameter options *))" and
      t = "src" and
      result = 0.6174242424242423
      or
      repr = "(parameter 0 (member set *))" and t = "src" and result = 0.9021579430670338
      or
      repr = "(member Location *)" and t = "src" and result = 0.012048192771084414
      or
      repr = "(member Location (parameter 0 (parameter 0 (return (member map *)))))" and
      t = "src" and
      result = 0.4930023122794206
      or
      repr = "(return (member setAssignment *))" and t = "src" and result = 0.7369546801346808
      or
      repr = "(return (member node (root https://www.npmjs.com/package/orion/webui/littlelib)))" and
      t = "src" and
      result = 0.3030303030303031
      or
      repr = "(return (member inspect *))" and t = "src" and result = 0.17599999999999993
      or
      repr = "(return (member getUser *))" and t = "src" and result = 0.4569212121212122
      or
      repr = "(return (member getProperties *))" and t = "src" and result = 0.08066725703030274
      or
      repr = "(parameter 0 (member construct (parameter 1 (member exports (parameter t *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member 0 (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter selection *)" and t = "src" and result = 0.2783333333333333
      or
      repr = "(return (member findExprType *))" and t = "src" and result = 0.1499999999999999
      or
      repr = "(parameter selections *)" and t = "src" and result = 0.11440716565656597
      or
      repr = "(member if-match (member headers (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter value (member set *))" and t = "src" and result = 0.5902662993572084
      or
      repr = "(parameter filter *)" and t = "src" and result = 0.5
      or
      repr = "(member Name (parameter 0 (return (member stringify *))))" and
      t = "src" and
      result = 0.4214525030917319
      or
      repr = "(parameter 0 (member delete *))" and t = "src" and result = 1.0
      or
      repr = "(member memory *)" and t = "src" and result = 0.15750000000000003
      or
      repr = "(return (member getFileMetadata (parameter editorContext *)))" and
      t = "src" and
      result = 0.14911844686290188
      or
      repr = "(return (member now *))" and t = "src" and result = 1.0
      or
      repr = "(member category *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member push *)))" and t = "src" and result = 0.12106742424242423
      or
      repr = "(member originalUrl (parameter req *))" and t = "src" and result = 1.0
      or
      repr = "(member description (parameter proposal *))" and t = "src" and result = 0.925
      or
      repr = "(member textContent (return (member createElement (member document (global)))))" and
      t = "src" and
      result = 0.4585023569023567
      or
      repr = "(parameter settings *)" and t = "src" and result = 0.3986942222222224
      or
      repr = "(member ret *)" and t = "src" and result = 0.08000000000000007
      or
      repr = "(parameter title *)" and t = "src" and result = 0.18126383838383847
      or
      repr = "(parameter 0 (return (member createElement (member document (global)))))" and
      t = "src" and
      result = 0.16602457358490585
      or
      repr = "(member branchName (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 3 (parameter 2 (return (member define (global)))))" and
      t = "src" and
      result = 0.5
      or
      repr = "(return (member split (return (member replace *))))" and
      t = "src" and
      result = 0.05999999999999994
      or
      repr = "(return (member getSelections *))" and t = "src" and result = 0.12159752222222232
      or
      repr = "(member end (parameter query *))" and t = "src" and result = 0.1499999999999999
      or
      repr = "(parameter 0 (parameter 0 (return (member push *))))" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (member type *))" and t = "src" and result = 0.08557046979865765
      or
      repr = "(member Target *)" and t = "src" and result = 1.0
      or
      repr = "(parameter project *)" and t = "src" and result = 0.24672916666666667
      or
      repr = "(parameter 0 (return (member replace (return (member toString *)))))" and
      t = "src" and
      result = 0.3500000000000001
      or
      repr = "(return (member getfileRelativePath *))" and
      t = "src" and
      result = 0.12121212121212133
      or
      repr = "(member kind *)" and t = "src" and result = 0.38044028404669267
      or
      repr = "(parameter item (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.5075757575757575
      or
      repr = "(member 0 (member params *))" and t = "src" and result = 1.0
      or
      repr = "(return (member tostrS *))" and t = "src" and result = 0.16999999999999993
      or
      repr = "(member manifest *)" and t = "src" and result = 0.00500000000000006
      or
      repr = "(return (member fileServiceRootURL (member _fileClient *)))" and
      t = "src" and
      result = 0.22599999999999987
      or
      repr = "(return (member findQueryExpr *))" and t = "src" and result = 0.17500000000000004
      or
      repr = "(parameter 0 (member construct (parameter 1 (member exports *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member filter (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member fromCharCode *))" and t = "src" and result = 0.7850000000000001
      or
      repr = "(return (member _getToken (parameter 1 (member prototype *))))" and
      t = "src" and
      result = 0.1296522269676631
      or
      repr = "(member names *)" and t = "src" and result = 0.25
      or
      repr = "(parameter fileName *)" and t = "src" and result = 0.5371735555555556
      or
      repr = "(parameter data (member callback *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member update (member prototype *)))" and
      t = "src" and
      result = 0.3500000000000001
      or
      repr = "(parameter item (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.12537080465802217
      or
      repr = "(parameter 0 (member callback (parameter 0 (instance (member constructor *)))))" and
      t = "src" and
      result = 0.24242424242424274
      or
      repr = "(member activeElement *)" and t = "src" and result = 0.5479999999999999
      or
      repr = "(return (member substring (member source *)))" and t = "src" and result = 0.82368
      or
      repr = "(parameter pair *)" and t = "src" and result = 0.17714285714285727
      or
      repr = "(member parameters *)" and t = "src" and result = 0.274
      or
      repr = "(parameter c (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.47727272727272735
      or
      repr = "(return (member loadWorkspaces *))" and t = "src" and result = 0.16666666666666666
      or
      repr = "(parameter 0 (member log *))" and t = "src" and result = 0.5475
      or
      repr = "(member branchName *)" and t = "src" and result = 1.0
      or
      repr = "(return (member getProposal *))" and t = "src" and result = 0.30856313131313134
      or
      repr = "(member if-match (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(return (member start *))" and t = "src" and result = 0.3333333333333333
      or
      repr = "(return (member _createParents *))" and t = "src" and result = 0.6666666666666666
      or
      repr = "(member _blocks *)" and t = "src" and result = 0.16666666666666663
      or
      repr = "(member arg *)" and t = "src" and result = 0.08260000000000006
      or
      repr = "(parameter 0 (member callback *))" and t = "src" and result = 0.10606060606060597
      or
      repr = "(parameter 0 (return (member decodeURIComponent *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member parseFloat (global)))" and t = "src" and result = 0.75
      or
      repr = "(member _searchHelper *)" and t = "src" and result = 0.10714285714285718
      or
      repr = "(parameter launchConf *)" and t = "src" and result = 0.24672916666666667
      or
      repr = "(parameter convo *)" and t = "src" and result = 0.5
      or
      repr = "(parameter 0 (return (member indexOf *)))" and t = "src" and result = 1.0
      or
      repr = "(member matches *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (return (member indexOf (member boards *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member parse *))" and t = "src" and result = 0.375
      or
      repr = "(parameter match *)" and t = "src" and result = 0.5
      or
      repr = "(member chat (parameter data *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member exec *))))" and t = "src" and result = 0.5
      or
      repr = "(member 0 (return (member parse *)))" and t = "src" and result = 0.25
      or
      repr = "(parameter e (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member 1 (parameter match *))" and t = "src" and result = 0.2272727272727273
      or
      repr = "(member chat *)" and t = "src" and result = 1.0
      or
      repr = "(member value (return (member highlight *)))" and
      t = "src" and
      result = 0.5192307692307693
      or
      repr = "(member data (member 0 (return (member parse *))))" and
      t = "src" and
      result = 0.42666666666666686
      or
      repr = "(return (member replace (return (member replace (parameter str *)))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(parameter ar *)" and t = "src" and result = 0.05555555555555555
      or
      repr = "(parameter re *)" and t = "src" and result = 0.05555555555555558
      or
      repr = "(member sku (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member currency (member session *))" and t = "src" and result = 1.0
      or
      repr = "(parameter recurseTimes *)" and t = "src" and result = 0.05555555555555558
      or
      repr = "(member vacation (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member sku (parameter 1 (return (member render *))))" and t = "src" and result = 1.0
      or
      repr = "(member slug *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member clean (parameter exports *)))" and
      t = "src" and
      result = 0.4999999999999999
      or
      repr = "(return (member replace (parameter str (member clean (parameter exports *)))))" and
      t = "src" and
      result = 0.47058823529411736
      or
      repr = "(member authRedirect (member session (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member currency (member session (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member currency *)" and t = "src" and result = 1.0
      or
      repr = "(member sku *)" and t = "src" and result = 1.0
      or
      repr = "(member vacation *)" and t = "src" and result = 1.0
      or
      repr = "(member sku (parameter 0 (return (member findOne *))))" and t = "src" and result = 1.0
      or
      repr = "(member sku (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member sku (parameter 1 (return (member render (parameter res *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member slug (parameter 0 (return (member findOne *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member authRedirect *)" and t = "src" and result = 1.0
      or
      repr = "(member vacation (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member authRedirect (member session *))" and t = "src" and result = 1.0
      or
      repr = "(return (member replace (parameter str (member clean *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member currency (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter js *)" and t = "src" and result = 0.24000000000000005
      or
      repr = "(member redirect (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member redirect (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member redirect *)" and t = "src" and result = 1.0
      or
      repr = "(member currency (member params *))" and t = "src" and result = 1.0
      or
      repr = "(return (member param (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member description *)" and t = "src" and result = 0.3333333333333333
      or
      repr = "(return (member param (parameter 0 (member list *))))" and t = "src" and result = 1.0
      or
      repr = "(member query (instance (member Constructor (member typeahead *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter x (parameter 0 (member $in (member _id *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member param *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member match *)))" and t = "src" and result = 0.3433333333333334
      or
      repr = "(member listeners *)" and t = "src" and result = 0.2777777777777772
      or
      repr =
        "(member query (instance (member constructor (member prototype (member constructor *)))))" and
      t = "src" and
      result = 0.8708333333333335
      or
      repr = "(parameter 0 (parameter 0 (return (member use *))))" and
      t = "src" and
      result = 0.03749999999999992
      or
      repr = "(return (member param (parameter req (member view *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member param (parameter req (member create *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter collected (parameter 1 (return (member aggregate *))))" and
      t = "src" and
      result = 0.3156565656565657
      or
      repr = "(member slugs *)" and t = "src" and result = 1.0
      or
      repr = "(return (member param (parameter 0 (member edit *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member parse (member JSON *)))" and
      t = "src" and
      result = 0.02272727272727258
      or
      repr = "(return (member GetVariable *))" and t = "src" and result = 0.07769423558897239
      or
      repr = "(member headers (parameter req *))" and t = "src" and result = 0.24999999999999994
      or
      repr = "(return (member param (parameter 0 (member create *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (parameter 1 (parameter 0 (return (member exec *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (parameter artist (parameter 0 (return (member exec *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (parameter artist *))" and t = "src" and result = 1.0
      or
      repr = "(parameter x *)" and t = "src" and result = 0.4825757575757574
      or
      repr = "(return (member param (parameter req (member list *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member param (parameter req (member edit *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member room *)" and t = "src" and result = 0.1250000000000002
      or
      repr = "(member app *)" and t = "src" and result = 0.1250000000000002
      or
      repr = "(parameter 1 (parameter 1 (return (member get *))))" and
      t = "src" and
      result = 0.49000000000000005
      or
      repr = "(member _id (parameter 0 (parameter 0 (return (member map *)))))" and
      t = "src" and
      result = 0.24999999999999994
      or
      repr = "(parameter x (parameter 0 (member $in *)))" and
      t = "src" and
      result = 0.010281385281385445
      or
      repr = "(return (member param (parameter 0 (member view *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter playlists (parameter 0 (return (member exec *))))" and
      t = "src" and
      result = 0.2575757575757579
      or
      repr = "(parameter 0 (parameter 0 (member $in (member _id *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member plugins *)" and t = "src" and result = 0.5
      or
      repr = "(parameter benchmark *)" and t = "src" and result = 0.08333333333333334
      or
      repr = "(parameter request *)" and t = "src" and result = 0.08333333333333334
      or
      repr = "(parameter 0 (return (member cleanForBridge *)))" and
      t = "src" and
      result = 0.12363616164721858
      or
      repr = "(parameter entry *)" and t = "src" and result = 0.75
      or
      repr = "(return (member cleanForBridge *))" and t = "src" and result = 0.12363616164721858
      or
      repr = "(return (member split (member stack *)))" and t = "src" and result = 0.5
      or
      repr = "(return (member renderToString (member ReactDOMServer (global))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter exp *)" and t = "src" and result = 0.25
      or
      repr = "(parameter inputs (return (member recurse *)))" and
      t = "src" and
      result = 0.8333333333333333
      or
      repr = "(parameter 0 (return (member recurse *)))" and
      t = "src" and
      result = 0.8333333333333333
      or
      repr = "(parameter from *)" and t = "src" and result = 0.2
      or
      repr = "(parameter 1 (return (member recurse (member prototype *))))" and
      t = "src" and
      result = 0.8333333333333331
      or
      repr = "(return (member $id *))" and t = "src" and result = 0.33333333333333326
      or
      repr = "(member body (parameter 0 (member handleNewPost *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member handleNewComment *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member handleLoginRequest *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member handleNewComment *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (member handleSignup *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (member handleNewPost *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member handleLoginRequest *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member handleSignup *)))" and t = "src" and result = 1.0
      or
      repr = "(member resolve *)" and t = "src" and result = 0.125
      or
      repr = "(parameter 0 (member open *))" and t = "src" and result = 0.125
      or
      repr = "(parameter 0 (member removeClass *))" and t = "src" and result = 0.48
      or
      repr = "(parameter 0 (member when *))" and t = "src" and result = 0.75
      or
      repr = "(parameter 1 (return (member extend (parameter $ *))))" and
      t = "src" and
      result = 0.09180035650623895
      or
      repr = "(parameter 1 (member compile *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member keydown *))" and t = "src" and result = 1.0
      or
      repr = "(return (member tip (instance (member Constructor (member tooltip *)))))" and
      t = "src" and
      result = 0.15114873035066514
      or
      repr = "(parameter placement *)" and t = "src" and result = 0.0976251596252537
      or
      repr = "(member defaults *)" and t = "src" and result = 0.2869875222816398
      or
      repr = "(member body (parameter 0 (parameter 2 (return (member use *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member 0 (return (member match (return (member replace *)))))" and
      t = "src" and
      result = 0.41523740747427335
      or
      repr = "(member noop *)" and t = "src" and result = 0.25
      or
      repr = "(member fn (parameter $ *))" and t = "src" and result = 0.5
      or
      repr = "(member placement *)" and t = "src" and result = 0.049879081015719445
      or
      repr = "(member body (parameter request (parameter 2 (return (member use *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member startSymbol *))" and t = "src" and result = 1.0
      or
      repr = "(member heading *)" and t = "src" and result = 0.3333333333333333
      or
      repr = "(return (member match (return (member replace *))))" and
      t = "src" and
      result = 0.20761870373713665
      or
      repr = "(member 0 (return (member apply *)))" and t = "src" and result = 0.25
      or
      repr = "(parameter div *)" and t = "src" and result = 0.25
      or
      repr = "(parameter jQuery (parameter 1 (return (member define (global)))))" and
      t = "src" and
      result = 0.6950236308034469
      or
      repr = "(member body (parameter req (parameter 2 (return (member use *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter heading *)" and t = "src" and result = 0.33333333333333326
      or
      repr = "(member current *)" and t = "src" and result = 0.1007575757575758
      or
      repr = "(parameter 1 (return (member extend *)))" and
      t = "src" and
      result = 0.3787878787878788
      or
      repr = "(parameter def *)" and t = "src" and result = 0.06657458563535912
      or
      repr = "(return (member parsePrefix *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(parameter viewDef *)" and t = "src" and result = 0.06657458563535912
      or
      repr = "(return (member factory *))" and t = "src" and result = 0.06657458563535912
      or
      repr = "(parameter 0 (return (parameter 2 (return (member d *)))))" and
      t = "src" and
      result = 0.25
      or
      repr = "(return (member _codegenSourceModule *))" and
      t = "src" and
      result = 0.37593984962406013
      or
      repr = "(parameter nativeNode (return (parameter 2 (return (member d *)))))" and
      t = "src" and
      result = 0.35606060606060597
      or
      repr = "(instance (return (parameter 2 (return (member d *)))))" and
      t = "src" and
      result = 0.10408022130013832
      or
      repr = "(parameter expr *)" and t = "src" and result = 0.25
      or
      repr = "(return (member parseEquality *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(return (member parseMultiplicative *))" and
      t = "src" and
      result = 0.020979020979020935
      or
      repr = "(return (member parseRelational *))" and t = "src" and result = 0.020979020979021268
      or
      repr = "(member currentStyle *)" and t = "src" and result = 0.08416666666666667
      or
      repr = "(return (member parseLogicalAnd *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(return (member parseConditional *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(parameter 0 (instance (member RegExp (global))))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member RegExp (global))))" and t = "src" and result = 0.4175
      or
      repr = "(parameter a (parameter 0 (return (member createPseudo (member selectors *)))))" and
      t = "src" and
      result = 0.5617469879518074
      or
      repr = "(parameter viewDef$$1 *)" and t = "src" and result = 0.06657458563535912
      or
      repr = "(return (member parsePipe *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(return (member parseAdditive *))" and t = "src" and result = 0.020979020979020935
      or
      repr = "(parameter stmt *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 0 (return (member create (member Object (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu *)" and t = "src" and result = 1.0
      or
      repr = "(member vno (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet *)" and t = "src" and result = 1.0
      or
      repr = "(member outlet (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member savecase4Version *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member docs *)" and t = "src" and result = 0.1742424242424242
      or
      repr = "(parameter _f *)" and t = "src" and result = 0.16666666666666666
      or
      repr = "(parameter 0 (member formatter (parameter 2 (member defaults *))))" and
      t = "src" and
      result = 0.7142857142857143
      or
      repr = "(member type (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member loc (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member ?? *)" and t = "src" and result = 1.0
      or
      repr = "(member sku (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter req (member getCalcResult *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter 0 (member getGrid *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (member savecase4Version *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member left *)" and t = "src" and result = 0.11363636363636358
      or
      repr = "(return (member eval (global)))" and t = "src" and result = 0.15909090909090912
      or
      repr = "(member outlet (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member loc *)" and t = "src" and result = 1.0
      or
      repr = "(member period (member query (parameter req (member getGrid *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (member saveNonKaVersion *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member loc (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member sku (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member checkVersion *)))" and t = "src" and result = 1.0
      or
      repr = "(member bu (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member groupSeparator (return (member numberbox *)))" and
      t = "src" and
      result = 0.5411255411255411
      or
      repr = "(member ?? (parameter 0 (return (member find *))))" and t = "src" and result = 1.0
      or
      repr = "(member period (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member page (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member loc (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member getCalcResult *)))" and t = "src" and result = 1.0
      or
      repr = "(member name (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member data (member jQuery (global))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member rows (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member vno (member query (parameter 0 (member getGrid *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member outlet (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member getMonth (return (member Date (global)))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member loc (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member bu (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member textbox (return (parameter $ *))))" and
      t = "src" and
      result = 0.5050505050505051
      or
      repr = "(member field *)" and t = "src" and result = 0.13983050847457626
      or
      repr = "(member period (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter docs *)" and t = "src" and result = 0.18796992481203006
      or
      repr = "(member sku (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member outlet (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (member getCalcResult *)))" and t = "src" and result = 1.0
      or
      repr = "(member bu (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member startLeft *)" and t = "src" and result = 0.11363636363636366
      or
      repr = "(member loc (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member numberbox (return (member jQuery (global)))))" and
      t = "src" and
      result = 0.5411255411255412
      or
      repr = "(member outlet (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member sku (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member getDataForExcel *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member rows (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member page (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member getFullYear (return (member Date (global)))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member vno (member query (parameter req (member getGrid *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member outlet (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter 0 (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member loc (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member period (member query (parameter 0 (member getCalcResult *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member loc (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member passwordbox (return (member jQuery (global)))))" and
      t = "src" and
      result = 0.5050505050505051
      or
      repr = "(member loc (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member loc (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member rows *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member getDataForExcel *)))" and t = "src" and result = 1.0
      or
      repr = "(member startTop *)" and t = "src" and result = 0.11363636363636366
      or
      repr = "(parameter fields (parameter 1 (return (member paramNoDb *))))" and
      t = "src" and
      result = 0.42424242424242425
      or
      repr = "(member type (member query (parameter 0 (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member name (member query (parameter req (member getDataHistory *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member rows (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member rows (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member period (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member name (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member vno (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member saveNonKaVersion *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter fields *)" and t = "src" and result = 0.3333333333333333
      or
      repr = "(member period (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member type (member query (parameter req (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter 0 (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter 0 (member checkVersion *)))" and t = "src" and result = 1.0
      or
      repr = "(member vno (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member vno *)" and t = "src" and result = 1.0
      or
      repr = "(member loc (member query (parameter 0 (member getData *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member unescape (global)))" and t = "src" and result = 0.16666666666666669
      or
      repr = "(member period *)" and t = "src" and result = 1.0
      or
      repr = "(member name (member query (parameter req (member getDataForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member bu (member query (parameter req (member getDataHistoryForExcel *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter $scope *)" and t = "src" and result = 0.1847014925373135
      or
      repr = "(member $tip (instance (member Constructor *)))" and
      t = "src" and
      result = 0.35185001193459825
      or
      repr = "(member params *)" and t = "src" and result = 0.20833333333333334
      or
      repr = "(parameter seed *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getData *))" and t = "src" and result = 0.75
      or
      repr = "(parameter 7 (return (member push *)))" and t = "src" and result = 0.25
      or
      repr = "(return (member renderFgSegEls *))" and t = "src" and result = 0.08333333333333333
      or
      repr = "(member 2 (return (member exec *)))" and t = "src" and result = 0.023749999999999993
      or
      repr = "(parameter 0 (parameter 1 (return (member add (member hooks *)))))" and
      t = "src" and
      result = 0.3675757575757576
      or
      repr = "(return (member attr (return (parameter $ *))))" and t = "src" and result = 1.0
      or
      repr = "(return (member getTime *))" and t = "src" and result = 1.0
      or
      repr = "(return (member getFirst *))" and t = "src" and result = 0.23249999999999998
      or
      repr = "(return (member ceil *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member load *))" and t = "src" and result = 0.5
      or
      repr = "(member langCode *)" and t = "src" and result = 0.33
      or
      repr = "(return (member getParent (return (member getParent *))))" and
      t = "src" and
      result = 0.03547729379054704
      or
      repr = "(return (return (member get (member options *))))" and t = "src" and result = 0.25
      or
      repr = "(member data (parameter 0 (parameter 1 (return (member on *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member serializeParam *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member title *))" and t = "src" and result = 0.011599999999999944
      or
      repr = "(return (member slice (parameter buffer *)))" and
      t = "src" and
      result = 0.38512500000000005
      or
      repr = "(return (member replace (return (member attr *))))" and t = "src" and result = 1.0
      or
      repr = "(instance (member Constructor (member tooltip (member fn (parameter $ *)))))" and
      t = "src" and
      result = 0.14620370370370364
      or
      repr = "(return (member load *))" and t = "src" and result = 0.3866120218579234
      or
      repr = "(parameter eventName *)" and t = "src" and result = 0.004759615384615334
      or
      repr = "(parameter span *)" and t = "src" and result = 1.0
      or
      repr = "(return (member getOpt (member editor (global))))" and
      t = "src" and
      result = 0.19125000000000003
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member use *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member fn (member jQuery (global)))" and t = "src" and result = 0.4679495898863648
      or
      repr = "(member theme *)" and t = "src" and result = 0.38787754511019745
      or
      repr = "(return (member yboundary (instance (member Grid (member Morris *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member langCode (parameter a *))" and t = "src" and result = 0.050000000000000155
      or
      repr = "(member _y *)" and t = "src" and result = 0.11823149528067554
      or
      repr = "(parameter 1 (member render *))" and t = "src" and result = 0.74
      or
      repr = "(return (member parent (member element *)))" and
      t = "src" and
      result = 0.2403008265821036
      or
      repr = "(return (member child *))" and t = "src" and result = 0.05952380952380951
      or
      repr = "(parameter elem *)" and t = "src" and result = 0.375
      or
      repr = "(member data (parameter b (parameter 1 (return (member on *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member successNum (return (member getStats (member uploader *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getStats (return (member create *))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member body (parameter req (parameter 1 (return (member use *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member textEle *)" and t = "src" and result = 0.25
      or
      repr = "(member dt (member s *))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(return (member getOption *))" and t = "src" and result = 0.16666666666666669
      or
      repr = "(member charCode *)" and t = "src" and result = 0.1582136363636364
      or
      repr = "(return (member indexOf (parameter url *)))" and t = "src" and result = 0.5
      or
      repr = "(member fixDomain (member tools *))" and t = "src" and result = 1.0
      or
      repr = "(return (member _get *))" and t = "src" and result = 0.3333333333333333
      or
      repr = "(member url (parameter 0 (return (member ajax (member constructor *)))))" and
      t = "src" and
      result = 0.5457142857142854
      or
      repr = "(return (member templateUrl *))" and t = "src" and result = 0.02999999999999997
      or
      repr = "(member fontFamily (member style *))" and t = "src" and result = 0.5
      or
      repr = "(member ch (member to *))" and t = "src" and result = 0.23031152123006043
      or
      repr = "(parameter segs *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(return (member getElementsByTagName (member document (global))))" and
      t = "src" and
      result = 0.11238477443609032
      or
      repr = "(member fixDomain (member tools (member CKEDITOR (global))))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member selection *)" and t = "src" and result = 0.16666666666666669
      or
      repr = "(member uploadFailNum (return (member getStats *)))" and
      t = "src" and
      result = 0.7575757575757576
      or
      repr = "(member selector *)" and t = "src" and result = 0.7174999999999998
      or
      repr = "(member lng *)" and t = "src" and result = 0.25
      or
      repr = "(member s *)" and t = "src" and result = 0.7744786701620592
      or
      repr = "(member ch (member from *))" and t = "src" and result = 0.23031152123006043
      or
      repr = "(return (member get (member options *)))" and t = "src" and result = 0.25
      or
      repr = "(return (member get (member _config *)))" and
      t = "src" and
      result = 0.32999999999999996
      or
      repr = "(parameter b (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(parameter defaultValue *)" and t = "src" and result = 0.25
      or
      repr = "(member display *)" and t = "src" and result = 0.17277891750384
      or
      repr = "(member classes *)" and t = "src" and result = 0.041666666666666664
      or
      repr = "(member icons *)" and t = "src" and result = 0.6207070707070708
      or
      repr = "(member keyCode (parameter e *))" and t = "src" and result = 0.3713
      or
      repr = "(member tools *)" and t = "src" and result = 0.5
      or
      repr = "(member data (parameter b *))" and t = "src" and result = 0.125
      or
      repr = "(member segs *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(member data (parameter a (parameter 1 (return (member on *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member className (parameter 0 (member addClass *)))" and
      t = "src" and
      result = 0.665151515151515
      or
      repr = "(member widgetHeaderClass *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(return (member data (parameter b *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member previousSibling (member startContainer *))" and t = "src" and result = 0.5
      or
      repr = "(return (member getAttribute (parameter el *)))" and t = "src" and result = 0.25
      or
      repr = "(parameter flags *)" and t = "src" and result = 0.2210721405787195
      or
      repr = "(return (member getNodeTitle *))" and t = "src" and result = 1.0
      or
      repr = "(member overlay *)" and t = "src" and result = 0.05952380952380955
      or
      repr = "(member successNum (return (member getStats *)))" and
      t = "src" and
      result = 0.2500000000000002
      or
      repr = "(return (member split (parameter value *)))" and t = "src" and result = 0.75
      or
      repr = "(parameter tpl *)" and t = "src" and result = 0.36499999999999994
      or
      repr = "(member nodeName (parameter element *))" and
      t = "src" and
      result = 0.22769230769230764
      or
      repr = "(return (parameter $ *))" and t = "src" and result = 0.5
      or
      repr = "(member modal *)" and t = "src" and result = 0.05952380952380955
      or
      repr = "(return (member attr (parameter $this *)))" and
      t = "src" and
      result = 0.23692355889724315
      or
      repr = "(return (member getAttribute (return (member getItem *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getNext *))" and t = "src" and result = 0.07226135310472634
      or
      repr = "(return (member closest *))" and t = "src" and result = 0.48626084840199674
      or
      repr = "(member server *)" and t = "src" and result = 0.7878787878787878
      or
      repr = "(return (member yboundary *))" and t = "src" and result = 0.13636363636363613
      or
      repr = "(parameter search *)" and t = "src" and result = 0.25
      or
      repr = "(parameter 1 (member removeAttr *))" and t = "src" and result = 0.5
      or
      repr = "(parameter c (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getSetting *)))" and t = "src" and result = 0.625
      or
      repr = "(parameter 0 (parameter 1 (return (member forEach *))))" and
      t = "src" and
      result = 0.25
      or
      repr = "(member breakpoints *)" and t = "src" and result = 0.23373267559314062
      or
      repr = "(return (member clazz *))" and t = "src" and result = 0.05952380952380955
      or
      repr = "(return (member random *))" and t = "src" and result = 0.670532060027285
      or
      repr = "(parameter 0 (parameter 2 (return (member iter *))))" and
      t = "src" and
      result = 0.5876932501669669
      or
      repr = "(return (member unique *))" and t = "src" and result = 0.125
      or
      repr = "(member duration (member options *))" and t = "src" and result = 0.31786863863577103
      or
      repr = "(member direction *)" and t = "src" and result = 1.0
      or
      repr = "(return (member stylize *))" and t = "src" and result = 0.13888888888888892
      or
      repr = "(member doc *)" and t = "src" and result = 0.19499999999999995
      or
      repr = "(member value (return (member $G (global))))" and t = "src" and result = 0.5
      or
      repr = "(member scayt (member plugins (member CKEDITOR (global))))" and
      t = "src" and
      result = 0.9999999999999999
      or
      repr = "(member userAgent (member navigator (global)))" and
      t = "src" and
      result = 0.28195488721804524
      or
      repr = "(parameter 1 (member setSize *))" and t = "src" and result = 0.25
      or
      repr = "(member label *)" and t = "src" and result = 0.6853535353535354
      or
      repr = "(parameter cells *)" and t = "src" and result = 0.24
      or
      repr = "(parameter 2 (return (member extend (parameter $ *))))" and
      t = "src" and
      result = 0.4694775194935346
      or
      repr = "(member selector (parameter 0 (return (member extend *))))" and
      t = "src" and
      result = 0.42643939393939423
      or
      repr = "(member legend *)" and t = "src" and result = 0.5066335213881337
      or
      repr = "(return (member getOuterHtml *))" and t = "src" and result = 0.5
      or
      repr = "(member regexp *)" and t = "src" and result = 0.75
      or
      repr = "(member toolbarGroups *)" and t = "src" and result = 0.5
      or
      repr = "(return (member map (root https://www.npmjs.com/package/jquery)))" and
      t = "src" and
      result = 0.15937786960514203
      or
      repr = "(return (member $G (global)))" and t = "src" and result = 0.25
      or
      repr = "(member style (return (member extend *)))" and
      t = "src" and
      result = 0.22999999999999998
      or
      repr = "(parameter b (parameter 1 (return (member on (parameter a *)))))" and
      t = "src" and
      result = 0.09375000000000022
      or
      repr = "(member nodeName *)" and t = "src" and result = 0.7050000000000001
      or
      repr = "(member y *)" and t = "src" and result = 0.12333333333333335
      or
      repr = "(return (member findParent (member domUtils *)))" and
      t = "src" and
      result = 0.33333333333333326
      or
      repr = "(return (member substring (member text *)))" and
      t = "src" and
      result = 0.708677685950413
      or
      repr = "(parameter e (parameter 1 (return (member each *))))" and
      t = "src" and
      result = 0.4599999999999997
      or
      repr = "(parameter flag *)" and t = "src" and result = 0.6539278594212805
      or
      repr = "(member plugins (member navigator (global)))" and t = "src" and result = 0.5
      or
      repr = "(member dom *)" and t = "src" and result = 0.33333333333333337
      or
      repr = "(return (member abs (member Math (global))))" and
      t = "src" and
      result = 0.08000000000000007
      or
      repr = "(member s (instance (member TableTools (global))))" and
      t = "src" and
      result = 0.23915178096576029
      or
      repr = "(member widgetContentClass (member view *))" and
      t = "src" and
      result = 0.16666666666666666
      or
      repr = "(return (member getTime (instance (member Date (global)))))" and
      t = "src" and
      result = 0.45454545454545475
      or
      repr = "(parameter a (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member run *))" and t = "src" and result = 0.31786863863577103
      or
      repr = "(member 1 (return (member match *)))" and t = "src" and result = 0.25
      or
      repr = "(return (member closest (return (root https://www.npmjs.com/package/jquery))))" and
      t = "src" and
      result = 0.3016179394767911
      or
      repr = "(member oLanguage *)" and t = "src" and result = 0.24999999999999994
      or
      repr = "(member plugins (member CKEDITOR (global)))" and
      t = "src" and
      result = 0.7999999999999999
      or
      repr = "(member fullToolbarEditor (instance (member ToolbarTextModifier *)))" and
      t = "src" and
      result = 0.20661157024793356
      or
      repr = "(parameter 0 (return (member call *)))" and t = "src" and result = 0.3557544949736307
      or
      repr = "(member slide *)" and t = "src" and result = 0.25
      or
      repr = "(member highlightedCode *)" and t = "src" and result = 0.18214936247723132
      or
      repr = "(member view *)" and t = "src" and result = 0.08333333333333333
      or
      repr = "(member x-forwarded-for (member headers (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member x-forwarded-for *)" and t = "src" and result = 1.0
      or
      repr = "(member x-forwarded-for (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(parameter link *)" and t = "src" and result = 0.14787571481442047
      or
      repr = "(return (member sort (return (member map *))))" and
      t = "src" and
      result = 0.445919887743057
      or
      repr = "(return (member installFromGallery *))" and t = "src" and result = 0.11438144642614057
      or
      repr = "(member workspace *)" and t = "src" and result = 0.020000000000000295
      or
      repr = "(parameter suggestion *)" and t = "src" and result = 0.28148510175988106
      or
      repr = "(parameter option *)" and t = "src" and result = 0.15602684610878792
      or
      repr = "(return (member toString (return (member random *))))" and
      t = "src" and
      result = 0.3395869995669271
      or
      repr = "(parameter rule *)" and t = "src" and result = 0.2272727272727273
      or
      repr =
        "(parameter 1 (parameter 1 (return (member exec (root https://www.npmjs.com/package/child_process)))))" and
      t = "src" and
      result = 0.338109879079284
      or
      repr = "(return (member run *))" and t = "src" and result = 0.11118474798076722
      or
      repr = "(member cwd *)" and t = "src" and result = 0.07666666666666672
      or
      repr = "(member sources (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "src" and
      result = 0.13642564802182816
      or
      repr = "(return (member split (member value *)))" and
      t = "src" and
      result = 0.21630723152188405
      or
      repr = "(parameter relativePath *)" and t = "src" and result = 0.25
      or
      repr = "(parameter buff *)" and t = "src" and result = 1.0
      or
      repr = "(return (member substr (return (member toString *))))" and
      t = "src" and
      result = 0.329467939972715
      or
      repr = "(member folder (parameter options *))" and t = "src" and result = 0.4701092371807791
      or
      repr = "(member stderr *)" and t = "src" and result = 0.3582089552238807
      or
      repr = "(member _placeholderGroups *)" and t = "src" and result = 0.03636456391277321
      or
      repr = "(return (member replace (parameter path *)))" and
      t = "src" and
      result = 0.676219758158568
      or
      repr = "(member _editorGroupService *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (parameter 0 (return (member resolve *)))))" and
      t = "src" and
      result = 0.05959454375148188
      or
      repr =
        "(parameter file (parameter 0 (return (member through (root https://www.npmjs.com/package/event-stream)))))" and
      t = "src" and
      result = 0.1706773535600753
      or
      repr = "(member content *)" and t = "src" and result = 0.40975806526854486
      or
      repr = "(return (member queryGallery *))" and t = "src" and result = 0.11333333333333333
      or
      repr = "(parameter paths *)" and t = "src" and result = 0.10200489745944269
      or
      repr = "(parameter value (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.2281812835692156
      or
      repr = "(parameter oldUri *)" and t = "src" and result = 0.24500000000000013
      or
      repr = "(parameter candidate *)" and t = "src" and result = 0.08000000000000007
      or
      repr = "(parameter 0 (parameter 0 (return (member reduce *))))" and
      t = "src" and
      result = 0.12878787878787876
      or
      repr = "(return (member concat (return (member concat *))))" and
      t = "src" and
      result = 0.06821282401091408
      or
      repr = "(parameter initData *)" and t = "src" and result = 0.15857728342057684
      or
      repr =
        "(parameter 0 (return (member stripCodicons (root https://www.npmjs.com/package/vs/base/common/codicons))))" and
      t = "src" and
      result = 0.46689154111913944
      or
      repr = "(return (member text *))" and t = "src" and result = 0.2989899564450015
      or
      repr = "(parameter values (parameter 0 (return (member then *))))" and
      t = "src" and
      result = 0.10237831917008669
      or
      repr = "(parameter sessionId *)" and t = "src" and result = 0.5177391839540182
      or
      repr = "(member windowsPath *)" and t = "src" and result = 0.48000000000000015
      or
      repr = "(parameter elements *)" and t = "src" and result = 0.20599938781756977
      or
      repr = "(member file *)" and t = "src" and result = 0.060606060606060067
      or
      repr = "(return (member reduce *))" and t = "src" and result = 0.12878787878787876
      or
      repr = "(member id (parameter group *))" and t = "src" and result = 1.0
      or
      repr = "(parameter d (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.296636616911396
      or
      repr = "(parameter OS *)" and t = "src" and result = 0.07210991641923925
      or
      repr = "(parameter styles *)" and t = "src" and result = 0.13533170172482223
      or
      repr = "(return (member from (member Buffer (global))))" and
      t = "src" and
      result = 0.39334618513550124
      or
      repr = "(parameter fileMatch *)" and t = "src" and result = 0.048123021911304534
      or
      repr = "(member _processManager *)" and t = "src" and result = 0.14787571481442047
      or
      repr = "(member innerText *)" and t = "src" and result = 0.22934747980767253
      or
      repr = "(member None (member CancellationToken *))" and
      t = "src" and
      result = 0.288439665676957
      or
      repr = "(parameter 0 (return (member get (member options (member configuration *)))))" and
      t = "src" and
      result = 0.06363656975839402
      or
      repr = "(parameter channelName *)" and t = "src" and result = 0.23137462004124476
      or
      repr = "(return (member trim (return (member replace *))))" and
      t = "src" and
      result = 0.4772727272727275
      or
      repr = "(parameter dep *)" and t = "src" and result = 0.14692473528223915
      or
      repr =
        "(member IEditorService (root https://www.npmjs.com/package/vs/workbench/services/editor/common/editorService))" and
      t = "src" and
      result = 0.31352912782554726
      or
      repr = "(member fileService *)" and t = "src" and result = 0.1442198328384785
      or
      repr = "(parameter arg *)" and t = "src" and result = 0.24873157655526776
      or
      repr =
        "(member detail (instance (member CompletionItem (root https://www.npmjs.com/package/vscode))))" and
      t = "src" and
      result = 0.3182854537163705
      or
      repr = "(member relative *)" and t = "src" and result = 0.7213407255242956
      or
      repr = "(member windowId (member electronService *))" and
      t = "src" and
      result = 0.4055708601163152
      or
      repr = "(member documentation *)" and t = "src" and result = 0.28148510175988095
      or
      repr = "(return (member rtrim *))" and t = "src" and result = 0.25
      or
      repr = "(member Schemas (root https://www.npmjs.com/package/vs/base/common/network))" and
      t = "src" and
      result = 0.48000000000000015
      or
      repr = "(member startLineNumber *)" and t = "src" and result = 0.12878787878787876
      or
      repr = "(parameter line *)" and t = "src" and result = 0.125
      or
      repr = "(return (member replace (parameter file *)))" and t = "src" and result = 1.0
      or
      repr = "(member scheme *)" and t = "src" and result = 0.11011630533329994
      or
      repr = "(member commandId *)" and t = "src" and result = 0.1584000000000001
      or
      repr = "(parameter item (parameter 0 (return (member reduce *))))" and
      t = "src" and
      result = 0.9099999999999999
      or
      repr = "(return (member basename *))" and t = "src" and result = 0.33999999999999986
      or
      repr = "(parameter e (parameter 1 (return (member addEventListener *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member getTelemetryInfo *))" and t = "src" and result = 0.41479951025405565
      or
      repr = "(parameter description *)" and t = "src" and result = 0.24999999999999997
      or
      repr = "(return (member toString (return (member createRandomFile *))))" and
      t = "src" and
      result = 0.32905493953964216
      or
      repr = "(member contents (parameter file (parameter 0 (return (member through *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member pattern (parameter query *))" and t = "src" and result = 0.5727291278255464
      or
      repr = "(parameter pack *)" and t = "src" and result = 0.6778558265599839
      or
      repr = "(parameter _path *)" and t = "src" and result = 0.06464646464646417
      or
      repr = "(member refs *)" and t = "src" and result = 0.03014074680116119
      or
      repr = "(member ctx *)" and t = "src" and result = 0.16389999999999988
      or
      repr = "(return (member toLowerCase (parameter value *)))" and
      t = "src" and
      result = 0.08000000000000007
      or
      repr = "(member 0 (return (member filter *)))" and t = "src" and result = 0.12192672238717378
      or
      repr = "(member productService *)" and t = "src" and result = 0.11467373990383628
      or
      repr = "(parameter 0 (parameter 0 (return (member map (parameter elements *)))))" and
      t = "src" and
      result = 0.8602434495943966
      or
      repr = "(return (member toString (return (member from *))))" and
      t = "src" and
      result = 0.3854728499874035
      or
      repr = "(return (member trim (parameter name *)))" and
      t = "src" and
      result = 0.08999999999999997
      or
      repr = "(parameter handle (parameter 0 (return (member map *))))" and
      t = "src" and
      result = 0.31866956289458853
      or
      repr = "(return (member map (return (member getSelections *))))" and
      t = "src" and
      result = 0.40128062527262226
      or
      repr = "(parameter e (parameter 1 (return (member addEventListener (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member concat *))))" and
      t = "src" and
      result = 0.11704005612847135
      or
      repr = "(member name (parameter 0 (parameter 0 (return (member map *)))))" and
      t = "src" and
      result = 0.6799999999999997
      or
      repr = "(member value (parameter 0 (parameter 0 (return (member map *)))))" and
      t = "src" and
      result = 0.13454174434890676
      or
      repr =
        "(parameter resource (member handler (parameter 0 (return (member registerCommand *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member previousCacheState *)" and t = "src" and result = 0.15
      or
      repr = "(member nameLong (member productService *))" and
      t = "src" and
      result = 0.22934747980767256
      or
      repr = "(parameter colorType *)" and t = "src" and result = 0.10998232658802715
      or
      repr = "(member 4 (return (member exec *)))" and t = "src" and result = 0.24999999999999997
      or
      repr = "(parameter taskId *)" and t = "src" and result = 0.0707131887999865
      or
      repr = "(member folder *)" and t = "src" and result = 0.10261989064476729
      or
      repr = "(member _activeItems *)" and t = "src" and result = 0.07121181202907557
      or
      repr = "(parameter item *)" and t = "src" and result = 0.08999999999999997
      or
      repr = "(member properties *)" and t = "src" and result = 0.11467373990383628
      or
      repr = "(return (member localize (root https://www.npmjs.com/package/vs/nls)))" and
      t = "src" and
      result = 0.08261008076930987
      or
      repr = "(parameter uri *)" and t = "src" and result = 0.16999999999999998
      or
      repr = "(parameter folderQuery *)" and t = "src" and result = 0.10141918250316279
      or
      repr = "(return (member getAction *))" and t = "src" and result = 0.3416180663326936
      or
      repr = "(parameter groupId *)" and t = "src" and result = 0.18834737474207947
      or
      repr = "(member isWindows *)" and t = "src" and result = 0.0641566719506205
      or
      repr = "(parameter descriptor *)" and t = "src" and result = 0.07920000000000005
      or
      repr = "(parameter cacheKey *)" and t = "src" and result = 0.42
      or
      repr = "(member _filter *)" and t = "src" and result = 0.4220585169226492
      or
      repr = "(return (member createServer *))" and t = "src" and result = 0.169054939539642
      or
      repr = "(member path (parameter 0 (return (member with (parameter uri *)))))" and
      t = "src" and
      result = 0.09812244803668423
      or
      repr = "(member pathService *)" and t = "src" and result = 0.08392674403284867
      or
      repr = "(member personalInfo *)" and t = "src" and result = 0.17028775641013638
      or
      repr = "(member key (parameter 0 (return (member push *))))" and
      t = "src" and
      result = 0.18571428467367068
      or
      repr = "(member url (parameter req *))" and t = "src" and result = 1.0
      or
      repr = "(parameter openConfig *)" and t = "src" and result = 0.06724935915669758
      or
      repr = "(member loadingPhase *)" and t = "src" and result = 0.15
      or
      repr = "(member environmentService *)" and t = "src" and result = 0.02557410386779488
      or
      repr = "(parameter 1 (member handler (parameter 0 (return (member registerCommand *)))))" and
      t = "src" and
      result = 0.2857142857142855
      or
      repr = "(parameter session *)" and t = "src" and result = 0.10267676767676773
      or
      repr = "(parameter extensionId *)" and t = "src" and result = 0.018491887659540185
      or
      repr = "(member filesToOpenOrCreate *)" and t = "src" and result = 0.3354541744348907
      or
      repr = "(member resolvedUserHome *)" and t = "src" and result = 0.3075987756351388
      or
      repr = "(parameter kind *)" and t = "src" and result = 0.28148510175988095
      or
      repr = "(return (member toLowerCase (return (member trim *))))" and
      t = "src" and
      result = 0.07500000000000007
      or
      repr = "(return (member describe *))" and t = "src" and result = 0.3000000000000004
      or
      repr = "(return (return (member promisify *)))" and t = "src" and result = 0.5473694959615347
      or
      repr = "(return (member split (return (member getText *))))" and
      t = "src" and
      result = 0.027260805102944957
      or
      repr = "(member isTrusted *)" and t = "src" and result = 0.31827025999202313
      or
      repr = "(return (member getId *))" and t = "src" and result = 0.18678506255038174
      or
      repr = "(parameter events *)" and t = "src" and result = 0.10516095575521422
      or
      repr = "(parameter problemMatchers *)" and t = "src" and result = 0.14074255087994053
      or
      repr = "(member electronService *)" and t = "src" and result = 0.2500000000000001
      or
      repr = "(member run *)" and t = "src" and result = 0.16999999999999993
      or
      repr = "(parameter test (parameter 0 (return (member forEach *))))" and
      t = "src" and
      result = 0.4807721225982985
      or
      repr = "(parameter moduleId *)" and t = "src" and result = 0.17621975815856805
      or
      repr = "(parameter kb *)" and t = "src" and result = 0.42789008358076075
      or
      repr = "(member bundles *)" and t = "src" and result = 0.4370297964802379
      or
      repr = "(member parent (member parent (member parent (member parent *))))" and
      t = "src" and
      result = 0.581870151082074
      or
      repr = "(member name (parameter rule *))" and t = "src" and result = 0.4545454545454546
      or
      repr = "(parameter insertText *)" and t = "src" and result = 0.3125
      or
      repr = "(member filesToDiff *)" and t = "src" and result = 0.004796466408411831
      or
      repr = "(member StorageScope *)" and t = "src" and result = 0.2863645639127732
      or
      repr = "(return (member homedir *))" and t = "src" and result = 0.40633215309732573
      or
      repr =
        "(member CancellationToken (root https://www.npmjs.com/package/vs/base/common/cancellation))" and
      t = "src" and
      result = 0.1442198328384785
      or
      repr = "(parameter viewIndex *)" and t = "src" and result = 0.17727087217445348
      or
      repr = "(member detail *)" and t = "src" and result = 0.3875968992248062
      or
      repr = "(return (member getExtensionElement *))" and
      t = "src" and
      result = 0.09003163848156759
      or
      repr = "(member _selectedItems *)" and t = "src" and result = 0.10881523961358114
      or
      repr = "(member env (root https://www.npmjs.com/package/vs/base/common/process))" and
      t = "src" and
      result = 0.9600000000000003
      or
      repr = "(member webviewResourceRoot *)" and t = "src" and result = 0.17600244872972134
      or
      repr =
        "(member kind (return (member CompletionItem (root https://www.npmjs.com/package/vscode))))" and
      t = "src" and
      result = 0.42439725118129573
      or
      repr = "(parameter test *)" and t = "src" and result = 0.12148510175988089
      or
      repr = "(parameter data (parameter 1 (return (member on (member stdout *)))))" and
      t = "src" and
      result = 0.5919096683365316
      or
      repr = "(return (member createRandomFile *))" and t = "src" and result = 0.33000000000000007
      or
      repr = "(parameter position *)" and t = "src" and result = 0.014233162192710674
      or
      repr = "(parameter arg1 *)" and t = "src" and result = 0.49
      or
      repr = "(parameter log *)" and t = "src" and result = 0.25
      or
      repr = "(member languageName *)" and t = "src" and result = 0.08409162151748077
      or
      repr = "(member _model *)" and t = "src" and result = 0.1519858349314884
      or
      repr = "(parameter 0 (parameter 0 (return (member basename *))))" and
      t = "src" and
      result = 0.31999999999999973
      or
      repr = "(return (member getValue *))" and t = "src" and result = 0.3155915137653381
      or
      repr = "(member detail (parameter item *))" and t = "src" and result = 0.28967397294964725
      or
      repr =
        "(return (member generateUuid (root https://www.npmjs.com/package/vs/base/common/uuid)))" and
      t = "src" and
      result = 0.5616526252579206
      or
      repr = "(member configPath *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getText (member document *)))" and
      t = "src" and
      result = 0.3817410301276
      or
      repr = "(member userHome *)" and t = "src" and result = 0.29575142962884093
      or
      repr = "(parameter label *)" and t = "src" and result = 0.5090000000000001
      or
      repr = "(return (member toString (parameter uri *)))" and
      t = "src" and
      result = 0.474513195190735
      or
      repr = "(return (member createModel *))" and t = "src" and result = 0.17497438618818284
      or
      repr = "(member window (root https://www.npmjs.com/package/vscode))" and
      t = "src" and
      result = 0.3630521846819761
      or
      repr = "(parameter 0 (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter resource *)" and t = "src" and result = 0.2863645639127732
      or
      repr = "(member tunnel *)" and t = "src" and result = 0.09983698647482364
      or
      repr = "(return (member normalize *))" and t = "src" and result = 0.5
      or
      repr = "(return (member substr (return (member getLineContent *))))" and
      t = "src" and
      result = 0.05105296911415935
      or
      repr = "(return (member pick (member quickInputService *)))" and
      t = "src" and
      result = 0.6274398371551864
      or
      repr = "(member authority (return (member parse *)))" and
      t = "src" and
      result = 0.3999999999999999
      or
      repr = "(parameter origin *)" and t = "src" and result = 0.0852004897459443
      or
      repr = "(member 0 (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "src" and
      result = 0.6363636363636364
      or
      repr = "(parameter 0 (return (member from (member Buffer (global)))))" and
      t = "src" and
      result = 0.48334856434865764
      or
      repr = "(member displayCategory *)" and t = "src" and result = 0.04022633454527502
      or
      repr = "(parameter pattern *)" and t = "src" and result = 0.04019815228919332
      or
      repr = "(return (member map (return (member filter *))))" and
      t = "src" and
      result = 0.16144005612847162
      or
      repr = "(parameter positions *)" and t = "src" and result = 0.11637403021641479
      or
      repr = "(return (member parse (member Uri *)))" and t = "src" and result = 0.6060606060606061
      or
      repr = "(member TaskGroup (root https://www.npmjs.com/package/vscode))" and
      t = "src" and
      result = 0.3875968992248062
      or
      repr = "(parameter keybinding *)" and t = "src" and result = 0.022727272727272596
      or
      repr = "(return (member get (return (member getConfiguration (member workspace *)))))" and
      t = "src" and
      result = 0.056028303842508384
      or
      repr = "(member fsPath (parameter resource *))" and t = "src" and result = 0.3545417443489072
      or
      repr =
        "(member EditorOption (root https://www.npmjs.com/package/vs/editor/common/config/editorOptions))" and
      t = "src" and
      result = 0.21127341159786825
      or
      repr =
        "(parameter 0 (return (member rtrim (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "src" and
      result = 0.12878787878787873
      or
      repr = "(parameter 1 (return (member editorGroupToViewColumn *)))" and
      t = "src" and
      result = 0.2500000000000002
      or
      repr =
        "(member documentation (instance (member CompletionItem (root https://www.npmjs.com/package/vscode))))" and
      t = "src" and
      result = 0.4243972511812959
      or
      repr = "(member fsPath (member configPath *))" and t = "src" and result = 0.5631525201923274
      or
      repr = "(member referer *)" and t = "src" and result = 1.0
      or
      repr = "(member X-Forwarded-For (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(member referer (member headers (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member username (parameter 0 (return (member findOne (member Account *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member username (parameter 0 (return (member findOne *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member username (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member REMOTE_ADDR *)" and t = "src" and result = 1.0
      or
      repr = "(member username (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member referer (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(member REMOTE_ADDR (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(member X-Forwarded-For *)" and t = "src" and result = 1.0
      or
      repr = "(return (member data (return (member jQuery (global)))))" and
      t = "src" and
      result = 0.1725757575757575
      or
      repr = "(member orphan *)" and t = "src" and result = 0.25
      or
      repr = "(parameter specifier *)" and t = "src" and result = 0.0625
      or
      repr = "(member category (member session *))" and t = "src" and result = 1.0
      or
      repr = "(return (member prop *))" and t = "src" and result = 0.33333333333333337
      or
      repr = "(return (member val (return (member jQuery *))))" and
      t = "src" and
      result = 0.6666666666666665
      or
      repr = "(member slug (member params *))" and t = "src" and result = 1.0
      or
      repr = "(return (member getStep *))" and t = "src" and result = 0.25
      or
      repr = "(return (member data *))" and t = "src" and result = 0.3443526170798898
      or
      repr = "(member slug (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 1 (return (member get *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member markers *)" and t = "src" and result = 0.25
      or
      repr = "(member category (member session (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 1 (return (member get *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter step *)" and t = "src" and result = 0.25
      or
      repr = "(member cookies (parameter req (parameter 1 (return (member get *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member appSrc *)" and t = "src" and result = 0.035714285714285726
      or
      repr = "(member appIndexJs *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member dotenv *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member cookies (parameter 0 (parameter 1 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member cookies (parameter req *))" and t = "src" and result = 1.0
      or
      repr = "(member cookies (parameter req (parameter 1 (return (member post *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member yarnLockFile *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member cookies (parameter 0 (parameter 1 (return (member get *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member appPackageJson *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member cookies *)" and t = "src" and result = 1.0
      or
      repr = "(member appPublic *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member appBuild *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member appNodeModules *)" and t = "src" and result = 0.03571428571428571
      or
      repr = "(member _id (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member _id (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member count (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member _id (parameter 0 (return (member findOneAndUpdate *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member count *)" and t = "src" and result = 1.0
      or
      repr = "(member count (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member test *))" and t = "src" and result = 0.022222222222222476
      or
      repr = "(member _id (parameter 0 (return (member findOneAndRemove *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member target (parameter 0 (member keydown *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter latex *)" and t = "src" and result = 0.5
      or
      repr = "(parameter data (member exports *))" and t = "src" and result = 1.0
      or
      repr = "(member first *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member query (member prototype *)))" and t = "src" and result = 1.0
      or
      repr = "(member category (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (member cookie *))" and t = "src" and result = 0.8125
      or
      repr = "(member attributes (parameter a *))" and t = "src" and result = 0.25
      or
      repr = "(parameter units *)" and t = "src" and result = 0.06436170212765963
      or
      repr = "(member tools (member CKEDITOR (global)))" and t = "src" and result = 0.5
      or
      repr = "(member P *)" and t = "src" and result = 0.08000000000000002
      or
      repr = "(return (member split (parameter name *)))" and
      t = "src" and
      result = 0.08264813768488682
      or
      repr = "(parameter 0 (member exports *))" and t = "src" and result = 1.0
      or
      repr = "(member 1 (parameter b *))" and t = "src" and result = 0.030000000000000082
      or
      repr = "(instance (member uNode (member UM (global))))" and
      t = "src" and
      result = 0.5303030303030303
      or
      repr = "(member referrer (member document (global)))" and t = "src" and result = 0.75
      or
      repr = "(member iDisplayStart *)" and t = "src" and result = 1.0
      or
      repr = "(return (member replace (member className *)))" and
      t = "src" and
      result = 0.1659472525575172
      or
      repr = "(member password *)" and t = "src" and result = 0.37878787878787873
      or
      repr = "(parameter params (member query (member prototype *)))" and
      t = "src" and
      result = 0.2500000000000005
      or
      repr = "(member fillChar *)" and t = "src" and result = 0.2500000000000002
      or
      repr = "(instance (member Corner (member reposition (member prototype *))))" and
      t = "src" and
      result = 0.9281914893617023
      or
      repr = "(return (member data (member selectedElement *)))" and
      t = "src" and
      result = 0.13974654705563802
      or
      repr = "(member height (return (member extend *)))" and
      t = "src" and
      result = 0.43103448275862066
      or
      repr = "(member product_id *)" and t = "src" and result = 1.0
      or
      repr = "(parameter wysihtml5 *)" and t = "src" and result = 0.075
      or
      repr = "(parameter 0 (return (member each *)))" and t = "src" and result = 1.0
      or
      repr = "(member product_id (parameter 0 (return (member find *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member stringify *))" and t = "src" and result = 0.49431818181818155
      or
      repr = "(parameter d (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.49167492171128524
      or
      repr = "(member utils *)" and t = "src" and result = 0.5
      or
      repr = "(member attrs (instance (member uNode (member UM (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member options (instance (member Inflate *)))" and
      t = "src" and
      result = 0.1925505050505052
      or
      repr = "(member avail_out *)" and t = "src" and result = 0.12458333333333332
      or
      repr = "(parameter b (member exports (parameter b *)))" and
      t = "src" and
      result = 0.47500000000000014
      or
      repr = "(parameter className *)" and t = "src" and result = 0.75
      or
      repr = "(member items *)" and t = "src" and result = 0.25
      or
      repr = "(parameter b (member exports *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member format *))" and t = "src" and result = 0.25
      or
      repr = "(return (member attr (return (member $ (global)))))" and t = "src" and result = 0.25
      or
      repr = "(parameter a (member get *))" and t = "src" and result = 0.7613636363636362
      or
      repr = "(member 0 (member matches *))" and t = "src" and result = 0.0625
      or
      repr = "(parameter 0 (return (member html *)))" and t = "src" and result = 0.25
      or
      repr = "(return (member convertToList *))" and t = "src" and result = 0.4265625000000002
      or
      repr = "(return (member option *))" and t = "src" and result = 0.5
      or
      repr = "(member first (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter a (member setData *))" and t = "src" and result = 0.4128787878787878
      or
      repr = "(member icon *)" and t = "src" and result = 0.8691503924917832
      or
      repr = "(member strm (instance (member Inflate *)))" and
      t = "src" and
      result = 0.3775252525252525
      or
      repr = "(parameter 0 (return (member each (root https://www.npmjs.com/package/lodash))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member toHtml *))" and t = "src" and result = 0.125
      or
      repr = "(parameter 0 (member exports (parameter b *)))" and
      t = "src" and
      result = 0.2875000000000001
      or
      repr = "(return (member getAttr *))" and t = "src" and result = 0.47660156996713304
      or
      repr = "(member firstTitle *)" and t = "src" and result = 1.0
      or
      repr = "(member product_id (parameter 0 (return (member find (member products *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member category (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member css *)" and t = "src" and result = 0.25
      or
      repr = "(return (member getAttribute (parameter oldNode *)))" and
      t = "src" and
      result = 0.09153005464480868
      or
      repr = "(member first (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member widgetContentClass *)" and t = "src" and result = 0.16666666666666666
      or
      repr = "(parameter 1 (member c *))" and t = "src" and result = 1.0
      or
      repr = "(member iDisplayStart (member query (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (member use *))" and t = "src" and result = 0.625
      or
      repr = "(member iDisplayStart (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member marker *)" and t = "src" and result = 0.08000000000000002
      or
      repr = "(return (member trim (member utils (global))))" and t = "src" and result = 0.75
      or
      repr = "(parameter 0 (member ATTR *))" and t = "src" and result = 0.4545454545454546
      or
      repr = "(return (member createDocumentFragment *))" and t = "src" and result = 0.075
      or
      repr = "(return (member unhtml (member utils (global))))" and
      t = "src" and
      result = 0.26169921501643345
      or
      repr = "(member $element *)" and t = "src" and result = 0.45454545454545453
      or
      repr = "(member firstTitle (member prev_category (parameter 1 (return (member render *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member config (parameter 0 (member exec *)))" and t = "src" and result = 1.0
      or
      repr = "(return (return (member get *)))" and t = "src" and result = 0.30000000000000004
      or
      repr = "(member lat *)" and t = "src" and result = 0.08000000000000002
      or
      repr = "(parameter data (member exports (parameter module *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member init (parameter 1 (return (member add *)))))" and
      t = "src" and
      result = 0.24999999999999994
      or
      repr = "(member firstTitle (member prev_category *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member push *))" and t = "src" and result = 0.7575757575757576
      or
      repr = "(member keywords *)" and t = "src" and result = 1.0
      or
      repr = "(return (member attr (return (member jQuery (global)))))" and
      t = "src" and
      result = 0.27272727272727226
      or
      repr = "(member body (parameter req (member getSwixDetails *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (parameter req (member registerNode *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member getSwixDetails *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (member tokenSwapRawTransaction *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter element (parameter 0 (return (member each *))))" and
      t = "src" and
      result = 0.3246753246753247
      or
      repr = "(member keywords (parameter 0 (member sourceDecorator *)))" and
      t = "src" and
      result = 0.041666666666666824
      or
      repr = "(parameter 0 (member sourceDecorator (member PR *)))" and
      t = "src" and
      result = 0.8863636363636362
      or
      repr = "(member body (parameter 0 (member payVpnUsage *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (member payVpnUsage *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member decodeURIComponent (global)))" and
      t = "src" and
      result = 0.21428571428571425
      or
      repr = "(member body (parameter 0 (member registerNode *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (member tokenSwapRawTransaction *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member replace (member content *)))" and t = "src" and result = 0.73
      or
      repr = "(parameter n (parameter 1 (return (member replace (parameter e *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member src (parameter e *))" and t = "src" and result = 0.19117647058823545
      or
      repr = "(return (member indexOf (member src *)))" and
      t = "src" and
      result = 0.4848484848484849
      or
      repr = "(parameter 0 (member exports (parameter e *)))" and
      t = "src" and
      result = 0.4250000000000001
      or
      repr = "(return (member slice (member src (parameter e (member exports *)))))" and
      t = "src" and
      result = 0.8000000000000002
      or
      repr = "(member author *)" and t = "src" and result = 0.41666666666666663
      or
      repr = "(parameter ticket *)" and t = "src" and result = 0.20833333333333331
      or
      repr = "(member allowedProjects (member props *))" and t = "src" and result = 0.5
      or
      repr = "(parameter email *)" and t = "src" and result = 0.3266666666666666
      or
      repr = "(member value (member props *))" and t = "src" and result = 1.0
      or
      repr = "(parameter b (parameter 1 (return (member replace *))))" and
      t = "src" and
      result = 0.12878787878787873
      or
      repr = "(member skin (member CKEDITOR (global)))" and
      t = "src" and
      result = 0.12070707070707076
      or
      repr = "(return (member _findActive *))" and t = "src" and result = 0.11746987951807235
      or
      repr = "(member x *)" and t = "src" and result = 0.16666666666666663
      or
      repr = "(parameter 1 (return (member emit (return (member to *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member _fnGetCellData *))" and t = "src" and result = 0.20833333333333334
      or
      repr = "(return (member reverse *))" and t = "src" and result = 0.25
      or
      repr = "(parameter oldNode *)" and t = "src" and result = 0.125
      or
      repr = "(return (member ceil (member Math (global))))" and
      t = "src" and
      result = 0.9999999999999999
      or
      repr = "(parameter rows *)" and t = "src" and result = 0.1609056068285718
      or
      repr = "(return (member map (parameter $ *)))" and t = "src" and result = 0.853605254173436
      or
      repr = "(member fixDomain *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (member get *))" and t = "src" and result = 0.2626262626262626
      or
      repr = "(member nextSibling *)" and t = "src" and result = 0.375
      or
      repr = "(parameter that *)" and t = "src" and result = 0.5184548978153629
      or
      repr = "(parameter 1 (member trigger *))" and t = "src" and result = 0.29166666666666663
      or
      repr = "(parameter 0 (parameter 1 (return (member on (parameter b *)))))" and
      t = "src" and
      result = 0.04996227247812929
      or
      repr = "(member lang (parameter 0 (parameter 1 (return (member add *)))))" and
      t = "src" and
      result = 0.2823333333333334
      or
      repr = "(instance (member Constructor *))" and t = "src" and result = 0.009171393421393401
      or
      repr = "(parameter 1 (return (member emit *)))" and t = "src" and result = 1.0
      or
      repr = "(return (member join (return (member reverse *))))" and t = "src" and result = 0.73
      or
      repr = "(return (member getTime (return (member Date (global)))))" and
      t = "src" and
      result = 0.6060606060606061
      or
      repr =
        "(parameter 1 (parameter 5 (return (member pbkdf2 (root https://www.npmjs.com/package/crypto)))))" and
      t = "src" and
      result = 0.2500000000000002
      or
      repr = "(member offset (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member offset *)" and t = "src" and result = 1.0
      or
      repr = "(member offset (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member toString (instance (member Buffer (global)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 5 (return (member pbkdf2 *))))" and t = "src" and result = 1.0
      or
      repr = "(member request_id (parameter 0 (return (member findOne *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member removeParticipantByRequestId *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member $search *)" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/moment)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member debug *)))" and t = "src" and result = 1.0
      or
      repr = "(member id_project *)" and t = "src" and result = 1.0
      or
      repr = "(member request_id (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member request_id (parameter 0 (return (member findOneAndUpdate *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member status (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member recipient *)" and t = "src" and result = 1.0
      or
      repr = "(member pendinginvitationid (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member resetpswrequestid (member params (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member body (member req (parameter 1 (return (member emit *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member pendinginvitationid (member params (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member token (parameter 0 (return (member json (parameter res *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member pendinginvitationid *)" and t = "src" and result = 1.0
      or
      repr = "(member status (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter req (parameter 2 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member status *)" and t = "src" and result = 1.0
      or
      repr = "(member start_date (member query *))" and t = "src" and result = 1.0
      or
      repr = "(return (member signup *))" and t = "src" and result = 0.02999999999999986
      or
      repr = "(member token (parameter 0 (return (member json *))))" and t = "src" and result = 1.0
      or
      repr = "(member recipient (parameter 0 (return (member find *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member start_date *)" and t = "src" and result = 1.0
      or
      repr = "(member body (parameter 0 (parameter 2 (return (member patch *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member end_date (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member closeRequestByRequestId *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member resetpswrequestid *)" and t = "src" and result = 1.0
      or
      repr = "(member id_project (parameter 0 (return (member find *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member projectid (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member request_id (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member authorization (member headers *))" and t = "src" and result = 1.0
      or
      repr = "(member resetpswrequestid (member params *))" and t = "src" and result = 1.0
      or
      repr = "(member projectid (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 2 (return (member create *)))" and t = "src" and result = 1.0
      or
      repr = "(member authorization *)" and t = "src" and result = 1.0
      or
      repr = "(member $search (member $text *))" and t = "src" and result = 1.0
      or
      repr = "(member authorization (member headers (parameter req *)))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member requestid (member params *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member debug *))))" and t = "src" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member sign (root https://www.npmjs.com/package/jsonwebtoken))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member authorization (member headers (member req *)))" and t = "src" and result = 1.0
      or
      repr = "(member end_date *)" and t = "src" and result = 1.0
      or
      repr = "(member requestid *)" and t = "src" and result = 1.0
      or
      repr = "(member end_date (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member sign *)))" and t = "src" and result = 1.0
      or
      repr = "(member request_id *)" and t = "src" and result = 1.0
      or
      repr = "(member direction (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member log *))))" and t = "src" and result = 1.0
      or
      repr = "(member full_text *)" and t = "src" and result = 1.0
      or
      repr = "(member requestid (member params (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member direction (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (return (member debug *)))" and t = "src" and result = 1.0
      or
      repr = "(member projectid *)" and t = "src" and result = 1.0
      or
      repr = "(member full_text (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member start_date (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member full_text (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(member activity (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter data (parameter 1 (return (member on (parameter socket *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member String (global))))" and t = "src" and result = 1.0
      or
      repr = "(member activity *)" and t = "src" and result = 1.0
      or
      repr = "(member activity (return (member toObject (parameter kpi *))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(member userId (member query (parameter req *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member merge *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 1 (return (member merge (root https://www.npmjs.com/package/lodash))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(return (member search *))" and t = "src" and result = 1.0
      or
      repr = "(member userId (member query *))" and t = "src" and result = 1.0
      or
      repr = "(member activity (member query *))" and t = "src" and result = 1.0
      or
      repr = "(parameter data (parameter 1 (return (member on *))))" and t = "src" and result = 1.0
      or
      repr = "(member activity (return (member toObject *)))" and t = "src" and result = 1.0
      or
      repr = "(member list *)" and t = "src" and result = 1.0
      or
      repr = "(return (member search (parameter $location *)))" and t = "src" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member on (parameter socket *)))))" and
      t = "src" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find *)))" and t = "snk" and result = 1.0
      or
      repr = "(member _id (parameter article (parameter 0 (return (member then *)))))" and
      t = "snk" and
      result = 0.9848484848484851
      or
      repr = "(parameter 1 (return (member findOneAndUpdate *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member send (parameter res *))))" and t = "snk" and result = 0.5
      or
      repr = "(return (member findOne *))" and t = "snk" and result = 0.7233333333333334
      or
      repr = "(member from_id *)" and t = "snk" and result = 0.515151515151515
      or
      repr = "(member _id *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member put (parameter $http *))))" and
      t = "snk" and
      result = 0.20833333333333337
      or
      repr = "(return (member call (member slice *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member $watch *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(return (member substr (return (member replace *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member splice *)))" and t = "snk" and result = 0.5
      or
      repr = "(return (member put *))" and t = "snk" and result = 0.25757575757575757
      or
      repr = "(parameter 0 (return (member push (member savedGifs *))))" and
      t = "snk" and
      result = 0.20833333333333337
      or
      repr = "(member path *)" and t = "snk" and result = 1.0
      or
      repr = "(return (parameter $templateRequest *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member exec *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member isArray (return (parameter __webpack_require__ *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member apply *)))" and t = "snk" and result = 0.5307400932400932
      or
      repr = "(member _params (instance (member UrlMatcher (parameter exports *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member data *)))" and t = "snk" and result = 0.75
      or
      repr = "(member name (parameter 1 (return (member put (parameter $http *)))))" and
      t = "snk" and
      result = 0.25252525252525226
      or
      repr = "(member _params (instance (member UrlMatcher *)))" and
      t = "snk" and
      result = 0.27272727272727226
      or
      repr = "(member 0 (return (member match *)))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(member url *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member recurse *))" and t = "snk" and result = 0.1301932156817214
      or
      repr = "(parameter 0 (return (member annotate *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member savedGifs *)" and t = "snk" and result = 0.10416666666666666
      or
      repr = "(member _keys *)" and t = "snk" and result = 0.5
      or
      repr = "(member document (global))" and t = "snk" and result = 0.6666666666666666
      or
      repr = "(member noop (member angular *))" and t = "snk" and result = 0.4999999999999999
      or
      repr = "(member query *)" and t = "snk" and result = 1.0
      or
      repr = "(return (member replace (return (member replace *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member parse *)))" and t = "snk" and result = 0.7254098360655737
      or
      repr = "(member _params *)" and t = "snk" and result = 1.0
      or
      repr = "(return (member leave (parameter $animate *)))" and t = "snk" and result = 0.5
      or
      repr = "(member date *)" and t = "snk" and result = 0.2764
      or
      repr = "(parameter 1 (return (return (member recurse (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (member tokens *))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member concat *)))" and t = "snk" and result = 0.5
      or
      repr = "(member gifs (member currentUser *))" and t = "snk" and result = 0.21148989898989906
      or
      repr = "(parameter 0 (return (member isString *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member post *)))" and t = "snk" and result = 0.2083333333333334
      or
      repr = "(member console *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(parameter 1 (member value *))" and t = "snk" and result = 0.7626262626262625
      or
      repr = "(parameter 0 (return (member appendChild *)))" and
      t = "snk" and
      result = 0.5042929292929292
      or
      repr = "(parameter 0 (return (member removeChild *)))" and t = "snk" and result = 0.75
      or
      repr = "(member controller *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (instance (member Error (global))))" and
      t = "snk" and
      result = 0.8245614035087719
      or
      repr = "(member location (member window (global)))" and
      t = "snk" and
      result = 0.6666666666666667
      or
      repr = "(member instance *)" and t = "snk" and result = 0.4999999999999999
      or
      repr = "(parameter 0 (return (member find (member zgrab80Model *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getElementById (member document (global)))))" and
      t = "snk" and
      result = 0.7499999999999999
      or
      repr = "(parameter 0 (return (member create_anchor (global))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member find (member VirustotalModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member zgrabPortModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member DeadDnsModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member create_list_entry (global))))" and
      t = "snk" and
      result = 0.12500000000000006
      or
      repr = "(parameter 0 (return (member find (return (member model *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member find (member AllDnsModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member CertTransModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member IPAddrModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member censysModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member zgrab2_80_model *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member 1 (return (member split *)))" and t = "snk" and result = 0.20000000000000012
      or
      repr = "(parameter 0 (return (member open *)))" and t = "snk" and result = 0.38636363636363635
      or
      repr = "(parameter 0 (return (member find (member ipv6AddrModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member json (return (member status *)))))" and
      t = "snk" and
      result = 0.44000000000000017
      or
      repr = "(parameter 0 (return (member find (member zgrab443Model *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member zgrab2PortModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member json *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member find (member WhoisModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member toString *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member create_table_entry (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member create_table_entry (global))))" and
      t = "snk" and
      result = 0.125
      or
      repr = "(parameter 0 (return (member find (member RdnsModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member find (member zgrab2_443_model *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isNaN (global))))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member find (member HostModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member make_get_request (global))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member open (global))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member find (member AllIPsModel *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member open (member window (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member make_get_request (global))))" and
      t = "snk" and
      result = 0.225563909774436
      or
      repr = "(parameter 1 (return (member exports *)))" and
      t = "snk" and
      result = 0.2626262626262624
      or
      repr = "(parameter 0 (return (member isObject *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (parameter 1 (return (member registerHelper *)))))" and
      t = "snk" and
      result = 0.36
      or
      repr = "(return (member bind *))" and t = "snk" and result = 0.125
      or
      repr = "(parameter 0 (return (parameter 1 (return (member registerHelper *)))))" and
      t = "snk" and
      result = 0.41666666666666663
      or
      repr = "(parameter 0 (return (member RegExp (global))))" and
      t = "snk" and
      result = 0.8766947494841589
      or
      repr = "(member value (return (member highlightAuto *)))" and
      t = "snk" and
      result = 0.5494505494505494
      or
      repr = "(return (member pop *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member stringify (member JSON (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member indexOf *)))" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (return (member isUndefined *)))" and
      t = "snk" and
      result = 0.06602143728304724
      or
      repr = "(parameter 0 (instance (member default *)))" and
      t = "snk" and
      result = 0.866112650046168
      or
      repr = "(parameter 0 (return (member call *)))" and t = "snk" and result = 0.29000000000000004
      or
      repr = "(parameter 0 (return (member push (return (member slice *)))))" and
      t = "snk" and
      result = 0.8245614035087718
      or
      repr = "(parameter 0 (return (member isArray *)))" and
      t = "snk" and
      result = 0.49750000000000005
      or
      repr = "(return (member substr *))" and t = "snk" and result = 0.7927927927927926
      or
      repr = "(member indent *)" and t = "snk" and result = 0.89125128998968
      or
      repr = "(parameter 1 (return (member exports (parameter module *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(instance (member RegExp (global)))" and t = "snk" and result = 1.0
      or
      repr = "(member externalDocs *)" and t = "snk" and result = 0.13204287456609448
      or
      repr = "(parameter 1 (return (member replace *)))" and
      t = "snk" and
      result = 0.6475604836828638
      or
      repr = "(member language *)" and t = "snk" and result = 0.18131868131868129
      or
      repr = "(member 1 (return (member exec *)))" and t = "snk" and result = 0.36500000000000005
      or
      repr = "(parameter 0 (instance (member RegExp (global))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member execute *)))" and
      t = "snk" and
      result = 0.15909090909090906
      or
      repr = "(parameter 0 (return (member escape (member _ (global)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member value *)" and t = "snk" and result = 1.0
      or
      repr = "(member source (instance (member compiler (member prototype (member default *)))))" and
      t = "snk" and
      result = 0.5466297322253001
      or
      repr = "(parameter 0 (return (member accept *)))" and
      t = "snk" and
      result = 0.49448529411764697
      or
      repr = "(return (member split (return (member replace *))))" and t = "snk" and result = 1.0
      or
      repr = "(member * *)" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (return (member apply *)))" and
      t = "snk" and
      result = 0.31818181818181823
      or
      repr = "(member source (instance (member compiler (member prototype (member compiler *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isUndefined (member _ (global)))))" and
      t = "snk" and
      result = 0.4339785627169528
      or
      repr = "(parameter 0 (return (member html *)))" and t = "snk" and result = 0.3712121212121212
      or
      repr = "(parameter 0 (return (member findOne *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member replace *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member call (member exec *))))" and
      t = "snk" and
      result = 0.21680180180180186
      or
      repr = "(parameter 0 (return (member parseFloat (global))))" and t = "snk" and result = 0.5
      or
      repr = "(member text *)" and t = "snk" and result = 0.576271186440678
      or
      repr = "(parameter 0 (return (member Date (global))))" and t = "snk" and result = 1.0
      or
      repr = "(member documentsPriv *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member push (parameter stringBuilder *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member push *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member fire *)))" and t = "snk" and result = 0.7200000000000002
      or
      repr = "(parameter 0 (return (member indexOf *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member importScripts (global))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (instance (member constructor (instance (parameter NativeDate *)))))" and
      t = "snk" and
      result = 0.9999999999999976
      or
      repr = "(parameter 0 (parameter 0 (return (member push *))))" and
      t = "snk" and
      result = 0.8637823780297509
      or
      repr = "(parameter 0 (return (member add *)))" and t = "snk" and result = 0.6297239948119325
      or
      repr = "(parameter 0 (instance (member constructor (return (parameter NativeDate *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member parseFloat (global)))" and t = "snk" and result = 0.5
      or
      repr = "(return (member split (parameter parentId *)))" and
      t = "snk" and
      result = 0.10360360360360366
      or
      repr = "(parameter 1 (return (member moveCursorTo (parameter editor *))))" and
      t = "snk" and
      result = 0.6428571428571429
      or
      repr = "(parameter 1 (return (member replace (parameter doc *))))" and
      t = "snk" and
      result = 0.9133929010978192
      or
      repr = "(parameter 0 (return (member equals *)))" and
      t = "snk" and
      result = 0.7857142857142856
      or
      repr = "(parameter 0 (instance (member Date (global))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member toUpperCase *))" and t = "snk" and result = 0.07748965517241377
      or
      repr = "(return (member toLowerCase *))" and t = "snk" and result = 0.9683333333333332
      or
      repr = "(member start *)" and t = "snk" and result = 0.25450450450450457
      or
      repr = "(parameter 0 (instance (member constructor (return (member constructor *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member insert *)))" and t = "snk" and result = 0.5
      or
      repr = "(member vars *)" and t = "snk" and result = 0.4277604707511246
      or
      repr = "(member remove (return (member test *)))" and t = "snk" and result = 0.573020277192136
      or
      repr = "(return (member match *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member $ *)))" and t = "snk" and result = 0.6468661359178496
      or
      repr = "(member filters *)" and t = "snk" and result = 0.3760895559960978
      or
      repr = "(parameter 0 (return (member isBlocked *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member log (member console (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member metadata *)" and t = "snk" and result = 0.08333333333333331
      or
      repr = "(parameter 0 (return (member deepMerge *)))" and
      t = "snk" and
      result = 0.022199999999999998
      or
      repr = "(parameter 1 (return (member createFrameFor *)))" and
      t = "snk" and
      result = 0.33499999999999996
      or
      repr = "(return (member expr (parameter t *)))" and t = "snk" and result = 0.5091
      or
      repr = "(parameter 0 (return (member dict *)))" and t = "snk" and result = 0.2666666666666667
      or
      repr = "(member mediaPerformanceMetricsService_ *)" and
      t = "snk" and
      result = 0.08499999999999996
      or
      repr = "(parameter 0 (return (member postMessage *)))" and
      t = "snk" and
      result = 0.4166666666666667
      or
      repr = "(parameter 1 (return (member warn *)))" and t = "snk" and result = 0.605
      or
      repr = "(parameter 0 (return (member format *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member evaluate *)))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(parameter 1 (parameter 3 (return (member createElement *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member mod *)))" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (member endsWith *)))" and
      t = "snk" and
      result = 0.055469020422291404
      or
      repr = "(parameter 0 (return (member field *)))" and
      t = "snk" and
      result = 0.21910038940809962
      or
      repr = "(parameter 0 (return (member createElement *)))" and
      t = "snk" and
      result = 0.08131313131313134
      or
      repr = "(parameter 0 (return (member redispatch *)))" and
      t = "snk" and
      result = 0.40363274489442713
      or
      repr = "(member deps *)" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(parameter 1 (return (parameter next *)))" and
      t = "snk" and
      result = 0.015019037729318119
      or
      repr = "(parameter 2 (return (member createElement (member default *))))" and
      t = "snk" and
      result = 0.7772941708505138
      or
      repr = "(parameter 0 (return (member sendBeacon *)))" and
      t = "snk" and
      result = 0.11833333333333329
      or
      repr = "(parameter 11 (return (member push *)))" and t = "snk" and result = 0.0625
      or
      repr = "(parameter 1 (return (member withExactArgs *)))" and
      t = "snk" and
      result = 0.021990503634475633
      or
      repr = "(return (member idFilter *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (parameter 0 (return (member pipe *))))" and
      t = "snk" and
      result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (parameter r *)))" and t = "snk" and result = 0.2715447283553998
      or
      repr = "(member selectedElements_ *)" and t = "snk" and result = 0.19758882961232271
      or
      repr = "(parameter 0 (return (member set (return (parameter t *)))))" and
      t = "snk" and
      result = 0.05673333333333341
      or
      repr = "(member repr *)" and t = "snk" and result = 0.16666666666666666
      or
      repr = "(return (member fetchJson (return (member xhrFor *))))" and
      t = "snk" and
      result = 0.21651877025708796
      or
      repr = "(parameter 0 (return (member push (member matches *))))" and
      t = "snk" and
      result = 0.17786015922464526
      or
      repr = "(parameter 1 (return (member join (root https://www.npmjs.com/package/path))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member checked *)" and t = "snk" and result = 0.12452830188679248
      or
      repr = "(member rewrittenUrl_ *)" and t = "snk" and result = 0.30091150340371514
      or
      repr = "(member to *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member Function (global))))" and
      t = "snk" and
      result = 0.5666666666666668
      or
      repr = "(member params *)" and t = "snk" and result = 0.7025
      or
      repr = "(parameter 0 (return (member push (member mod *))))" and
      t = "snk" and
      result = 0.3333333333333333
      or
      repr = "(parameter 0 (return (member contains *)))" and
      t = "snk" and
      result = 0.33712121212121204
      or
      repr = "(member i *)" and t = "snk" and result = 0.05499999999999999
      or
      repr = "(parameter 1 (return (member signal *)))" and t = "snk" and result = 0.375
      or
      repr = "(member get *)" and t = "snk" and result = 0.4999999999999999
      or
      repr = "(parameter 0 (return (member hasAttribute (parameter element *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member encode *)))" and
      t = "snk" and
      result = 0.9305246105918998
      or
      repr = "(return (member keys (member Object (global))))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(parameter 0 (return (member reportError *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (parameter n *)))" and t = "snk" and result = 0.5800000000000001
      or
      repr = "(return (member map (return (member array *))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member zeros *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (parameter 2 (return (member createCustomEvent *))))" and
      t = "snk" and
      result = 0.7026552440290755
      or
      repr = "(return (member slice (parameter t *)))" and t = "snk" and result = 0.327812391831084
      or
      repr = "(member textContent *)" and t = "snk" and result = 0.02986268736348255
      or
      repr = "(parameter 0 (return (member fetchJson (return (member xhrServiceForTesting *)))))" and
      t = "snk" and
      result = 0.14910260677751455
      or
      repr = "(parameter 0 (return (member timeout *)))" and
      t = "snk" and
      result = 0.2863676758359707
      or
      repr = "(member src (return (member createElement (member document *))))" and
      t = "snk" and
      result = 0.18643821391484983
      or
      repr = "(parameter 0 (return (member child *)))" and
      t = "snk" and
      result = 0.25000000000000006
      or
      repr = "(parameter 0 (return (member jsonEqual (member to *))))" and
      t = "snk" and
      result = 0.08000000000000007
      or
      repr = "(parameter 1 (return (member hasOwn *)))" and
      t = "snk" and
      result = 0.29505711318795436
      or
      repr = "(parameter 0 (return (parameter t *)))" and t = "snk" and result = 0.4999999999999999
      or
      repr = "(parameter 0 (return (member init *)))" and t = "snk" and result = 0.8300000000000001
      or
      repr = "(parameter 0 (return (member play (return (member MediaPool *)))))" and
      t = "snk" and
      result = 0.34343434343434326
      or
      repr = "(parameter 0 (return (member src *)))" and t = "snk" and result = 0.37719298245614036
      or
      repr = "(return (member map *))" and t = "snk" and result = 0.5833333333333334
      or
      repr = "(return (member indexOf *))" and t = "snk" and result = 0.08384124999999987
      or
      repr = "(parameter 1 (return (member sendRequest *)))" and t = "snk" and result = 0.6975
      or
      repr = "(parameter 1 (return (member dispatchCustomEvent *)))" and
      t = "snk" and
      result = 0.265
      or
      repr = "(parameter 0 (return (member addParamsToUrl *)))" and t = "snk" and result = 0.5
      or
      repr = "(member fontSize *)" and t = "snk" and result = 0.21666666666666662
      or
      repr = "(parameter 0 (return (member utf8Decode *)))" and
      t = "snk" and
      result = 0.08000000000000008
      or
      repr = "(parameter 0 (return (member execOrDie *)))" and
      t = "snk" and
      result = 0.28611976462443767
      or
      repr = "(member accessor *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member then (return (member buildUrl *))))" and
      t = "snk" and
      result = 0.16000000000000003
      or
      repr = "(parameter 0 (return (member isValid (return (parameter t *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (parameter 0 (return (member then *))))" and
      t = "snk" and
      result = 0.8204999999999998
      or
      repr = "(parameter 0 (return (member getOwnPropertyDescriptor *)))" and
      t = "snk" and
      result = 0.045057113187954384
      or
      repr = "(return (member btoa (global)))" and t = "snk" and result = 0.32
      or
      repr = "(parameter 0 (parameter 1 (return (member sendMessage *))))" and
      t = "snk" and
      result = 0.23333333333333328
      or
      repr = "(member ds *)" and t = "snk" and result = 0.6439393939393939
      or
      repr = "(parameter 4 (return (member call *)))" and t = "snk" and result = 0.2545765489788855
      or
      repr = "(return (member concat *))" and t = "snk" and result = 0.3293
      or
      repr = "(member type *)" and t = "snk" and result = 0.16666666666666666
      or
      repr = "(parameter 1 (return (member replace (return (member replace *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member href (return (member closestAncestorElementBySelector *)))" and
      t = "snk" and
      result = 0.12878065313137782
      or
      repr = "(member prelude *)" and t = "snk" and result = 0.19999999999999998
      or
      repr = "(member rem (return (member create *)))" and t = "snk" and result = 0.3050682261208577
      or
      repr = "(member textContent (parameter styleEl *))" and
      t = "snk" and
      result = 0.4995490773423997
      or
      repr = "(parameter 0 (return (member send *)))" and t = "snk" and result = 0.5
      or
      repr = "(return (member listen *))" and t = "snk" and result = 0.25
      or
      repr = "(return (member sha384 *))" and t = "snk" and result = 0.08999999999999997
      or
      repr = "(return (member applySsrOrCsrTemplate *))" and
      t = "snk" and
      result = 0.09917618553132569
      or
      repr = "(return (member userAssert *))" and t = "snk" and result = 0.1319660782277605
      or
      repr = "(parameter 0 (return (member Number (global))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 2 (return (member createError *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member fix *)))" and t = "snk" and result = 0.43695522200195114
      or
      repr = "(parameter 0 (return (member unwrap *)))" and
      t = "snk" and
      result = 0.5950571131879543
      or
      repr = "(parameter 1 (return (member fireEvent *)))" and
      t = "snk" and
      result = 0.18333333333333324
      or
      repr = "(return (member split *))" and t = "snk" and result = 0.74
      or
      repr = "(member info *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member appendChild (return (member createElement *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member then (return (member all *))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member setAttribute (return (member child *)))))" and
      t = "snk" and
      result = 0.25000000000000006
      or
      repr = "(parameter 0 (instance (member Blob (global))))" and t = "snk" and result = 0.375
      or
      repr = "(parameter 1 (return (member loadScript *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (parameter 0 (return (member forEach *)))))" and
      t = "snk" and
      result = 0.062140178608515395
      or
      repr = "(parameter 0 (return (member indexOf (member value *))))" and
      t = "snk" and
      result = 0.7407984308295836
      or
      repr = "(parameter 0 (return (member data (member _graph *))))" and
      t = "snk" and
      result = 0.42500000000000004
      or
      repr = "(return (member exports *))" and t = "snk" and result = 0.8555246105918999
      or
      repr = "(parameter 0 (return (member getParentWindowFrameElement *)))" and
      t = "snk" and
      result = 0.15
      or
      repr = "(parameter 0 (return (member appendChild (return (member getElementById *)))))" and
      t = "snk" and
      result = 0.7293365965503555
      or
      repr = "(parameter 0 (return (member decodeURIComponent (global))))" and
      t = "snk" and
      result = 0.27081571326216675
      or
      repr = "(parameter 0 (parameter 0 (return (member replaceWith *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member matches *)))" and
      t = "snk" and
      result = 0.34712121212121183
      or
      repr = "(return (member enqueueMediaElementTask_ *))" and t = "snk" and result = 0.35475
      or
      repr = "(parameter 1 (return (member timeoutPromise (return (member timerFor *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (parameter e *)))" and t = "snk" and result = 0.39375
      or
      repr = "(parameter 1 (parameter 1 (parameter 3 (return (member createElement *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member msg *)" and t = "snk" and result = 0.75
      or
      repr = "(return (member RegExp (global)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member map (parameter t *))))" and
      t = "snk" and
      result = 0.6682623052959502
      or
      repr = "(member matches *)" and t = "snk" and result = 0.08893007961232263
      or
      repr = "(parameter 2 (return (return (parameter t *))))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(member signals *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member stringify *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member assertString *)))" and
      t = "snk" and
      result = 0.06439655172413793
      or
      repr = "(parameter 0 (return (member writeScript *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member _valueSet (member inputmask *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member Number (global)))" and t = "snk" and result = 0.625
      or
      repr = "(parameter 1 (return (member openWindowDialog *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member computedStyle *)))" and
      t = "snk" and
      result = 0.1386177192485603
      or
      repr = "(parameter 0 (return (member append *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member insertBefore *)))" and
      t = "snk" and
      result = 0.4445309795777086
      or
      repr = "(member __ *)" and t = "snk" and result = 0.04001269920387679
      or
      repr = "(parameter 1 (return (member slice (parameter t *))))" and
      t = "snk" and
      result = 0.875
      or
      repr = "(member 0 (return (member exec *)))" and t = "snk" and result = 0.5
      or
      repr = "(member entries_ *)" and t = "snk" and result = 0.07294911734164072
      or
      repr = "(member win *)" and t = "snk" and result = 0.20136725510557285
      or
      repr = "(member index (return (member getDetail *)))" and
      t = "snk" and
      result = 0.3999999999999999
      or
      repr = "(parameter 1 (return (member writeFileSync *)))" and
      t = "snk" and
      result = 0.14740814623485
      or
      repr = "(parameter 0 (return (return (parameter t *))))" and
      t = "snk" and
      result = 0.006780893042575287
      or
      repr = "(return (member catch (return (member then *))))" and t = "snk" and result = 0.84
      or
      repr = "(parameter 0 (return (member getAmpDoc *)))" and
      t = "snk" and
      result = 0.08712121212121202
      or
      repr = "(parameter 2 (return (member set (return (parameter t *)))))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(member 0 (return (member split *)))" and t = "snk" and result = 0.5833333333333334
      or
      repr = "(member cid *)" and t = "snk" and result = 0.11733149878850782
      or
      repr = "(return (member set (return (member set *))))" and
      t = "snk" and
      result = 0.7491761855313257
      or
      repr = "(member data *)" and t = "snk" and result = 0.5
      or
      repr = "(member value (member target *))" and t = "snk" and result = 0.12735849056603776
      or
      repr = "(member stream *)" and t = "snk" and result = 0.042911244847226304
      or
      repr = "(parameter 1 (return (member point (parameter t *))))" and
      t = "snk" and
      result = 0.09638554216867465
      or
      repr = "(parameter 2 (return (member info (return (member dev *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member push *)" and t = "snk" and result = 0.25
      or
      repr = "(member allowedFor *)" and t = "snk" and result = 0.03827622014537896
      or
      repr = "(member value (member props *))" and t = "snk" and result = 0.2592015691704164
      or
      repr = "(return (member field (return (parameter t *))))" and
      t = "snk" and
      result = 0.21910038940809962
      or
      repr = "(member tagName *)" and t = "snk" and result = 0.39140977443609026
      or
      repr = "(parameter 0 (return (member encode *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member setStyles *)))" and
      t = "snk" and
      result = 0.19719999999999996
      or
      repr = "(member define *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(return (member map (return (member keys *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member push *))))" and
      t = "snk" and
      result = 0.3787878787878787
      or
      repr = "(return (member slice (return (member split *))))" and t = "snk" and result = 0.25
      or
      repr = "(return (member fetchJson (return (member xhrServiceForTesting *))))" and
      t = "snk" and
      result = 0.14910260677751455
      or
      repr = "(parameter 0 (return (member hasOwn *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member replaceUrls *)))" and
      t = "snk" and
      result = 0.4230228452751817
      or
      repr = "(parameter 0 (return (member copyPosTo *)))" and
      t = "snk" and
      result = 0.19999999999999998
      or
      repr = "(member iframe_ *)" and t = "snk" and result = 0.05746105919003086
      or
      repr = "(member 0 (member items *))" and t = "snk" and result = 0.12500000000000003
      or
      repr = "(member _data *)" and t = "snk" and result = 0.2891251608187135
      or
      repr = "(parameter 0 (return (member value *)))" and t = "snk" and result = 0.5716229491173416
      or
      repr = "(member to (return (member expect (global))))" and
      t = "snk" and
      result = 0.5296812360363765
      or
      repr = "(parameter 0 (return (member filter *)))" and
      t = "snk" and
      result = 0.20136725510557285
      or
      repr = "(member m (parameter n *))" and t = "snk" and result = 0.2970529123005759
      or
      repr = "(parameter 0 (parameter 0 (return (member toArray *))))" and
      t = "snk" and
      result = 0.7956532615878411
      or
      repr = "(parameter 0 (return (member getValueForExpr *)))" and
      t = "snk" and
      result = 0.230546902042229
      or
      repr = "(return (member trim (return (member replace *))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member then (return (member start_ *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member fireEvent *)))" and
      t = "snk" and
      result = 0.18333333333333324
      or
      repr = "(parameter 0 (return (return (member withStyles *))))" and
      t = "snk" and
      result = 0.2928126802815276
      or
      repr = "(parameter 0 (return (member loadScript *)))" and
      t = "snk" and
      result = 0.461093804084458
      or
      repr = "(parameter 0 (return (member bytesToString *)))" and
      t = "snk" and
      result = 0.39666666666666656
      or
      repr = "(member href *)" and t = "snk" and result = 0.8620689655172412
      or
      repr = "(member next *)" and t = "snk" and result = 0.06250000000000001
      or
      repr = "(member element *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member rootNodeFor *)))" and
      t = "snk" and
      result = 0.14999999999999997
      or
      repr = "(parameter 0 (parameter 0 (return (member pipe *))))" and
      t = "snk" and
      result = 0.16666666666666666
      or
      repr = "(parameter 0 (return (member isEnabled *)))" and
      t = "snk" and
      result = 0.437121212121212
      or
      repr = "(parameter 1 (return (parameter 0 (return (member map *)))))" and
      t = "snk" and
      result = 0.4772727272727274
      or
      repr = "(parameter 0 (return (member bind *)))" and t = "snk" and result = 0.3700000000000001
      or
      repr = "(parameter 1 (return (member open *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member assign *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member timedExecOrDie *)))" and t = "snk" and result = 0.5222
      or
      repr = "(parameter 0 (return (parameter resolve *)))" and t = "snk" and result = 0.65
      or
      repr = "(parameter 0 (return (member withArgs *)))" and
      t = "snk" and
      result = 0.6014061959155417
      or
      repr = "(member iframeReady_ *)" and t = "snk" and result = 0.31716095534787114
      or
      repr = "(parameter 1 (return (root https://www.npmjs.com/package/gulp-file)))" and
      t = "snk" and
      result = 0.3433333333333333
      or
      repr = "(return (member withArgs (return (member expects *))))" and
      t = "snk" and
      result = 0.1282511249567323
      or
      repr = "(member y *)" and t = "snk" and result = 0.3787878787878788
      or
      repr = "(parameter 0 (return (member indexOf (member prototype *))))" and
      t = "snk" and
      result = 0.8628827448944276
      or
      repr = "(parameter 2 (return (member trigger (member action_ *))))" and
      t = "snk" and
      result = 0.9409380408445829
      or
      repr = "(parameter 0 (return (member parse (member JSON (global)))))" and
      t = "snk" and
      result = 0.505
      or
      repr = "(parameter 0 (return (member register (instance (member MediaPool *)))))" and
      t = "snk" and
      result = 0.34343434343434326
      or
      repr = "(return (member digest *))" and t = "snk" and result = 0.2625655763239876
      or
      repr = "(return (member get *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (return (parameter t *))))" and t = "snk" and result = 0.125
      or
      repr = "(parameter 0 (return (member fetchJson *)))" and
      t = "snk" and
      result = 0.7986327448944271
      or
      repr = "(return (member map (return (member filter *))))" and
      t = "snk" and
      result = 0.22397928183411214
      or
      repr = "(parameter 0 (return (member parseInt (global))))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member appendChild (parameter container *))))" and
      t = "snk" and
      result = 0.031685704395984726
      or
      repr = "(parameter 0 (return (member removeEventListener *)))" and
      t = "snk" and
      result = 0.2080894567107996
      or
      repr = "(parameter 0 (return (member setImportantStyles *)))" and
      t = "snk" and
      result = 0.40494288681204577
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/request)))" and
      t = "snk" and
      result = 0.0356299065420561
      or
      repr = "(member arguments (member node (parameter 0 (member CallExpression *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member render *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member logOnSameLine *)))" and t = "snk" and result = 0.5
      or
      repr = "(member rem (member _input (instance (member exports (parameter e *)))))" and
      t = "snk" and
      result = 0.42888115264797494
      or
      repr = "(parameter 0 (return (return (parameter _dereq_ *))))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member dependencyLib *)))" and
      t = "snk" and
      result = 0.14001710193838707
      or
      repr = "(parameter 2 (return (member error (return (member user *)))))" and
      t = "snk" and
      result = 0.6742424242424241
      or
      repr =
        "(member promiseArray_ (instance (member RealTimeConfigManager (member AMP (global)))))" and
      t = "snk" and
      result = 0.04491409631098096
      or
      repr = "(parameter 0 (return (member appendChild (member body *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member applySsrOrCsrTemplate *)))" and
      t = "snk" and
      result = 0.09917618553132569
      or
      repr = "(member matches (return (member pop *)))" and
      t = "snk" and
      result = 0.08893007961232263
      or
      repr = "(parameter 0 (return (member readFileSync (root https://www.npmjs.com/package/fs))))" and
      t = "snk" and
      result = 0.6395636607338177
      or
      repr = "(parameter 1 (return (member debug *)))" and
      t = "snk" and
      result = 0.07499999999999996
      or
      repr = "(return (parameter a *))" and t = "snk" and result = 0.12148741271363522
      or
      repr = "(return (member sha384Base64 *))" and t = "snk" and result = 0.21250000000000002
      or
      repr = "(member deep (member to (return (member expect (global)))))" and
      t = "snk" and
      result = 0.9126814065990906
      or
      repr = "(parameter 0 (return (member clear *)))" and
      t = "snk" and
      result = 0.25000000000000006
      or
      repr = "(parameter 1 (return (member dispatch (member storeService_ *))))" and
      t = "snk" and
      result = 0.275
      or
      repr = "(return (member then (return (member then *))))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (parameter 0 (return (member filter *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member replaceWith (parameter path *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/fancy-log)))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member idFilter *)))" and t = "snk" and result = 0.25
      or
      repr = "(member content *)" and t = "snk" and result = 0.5700000000000001
      or
      repr = "(parameter 2 (return (member triggerAnalyticsEvent *)))" and
      t = "snk" and
      result = 0.105546902042229
      or
      repr = "(return (member trim *))" and t = "snk" and result = 0.7616992150164335
      or
      repr = "(parameter 0 (return (member setTime *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member atob (global))))" and
      t = "snk" and
      result = 0.7166666666666666
      or
      repr = "(parameter 0 (return (member removeChild (member body *))))" and
      t = "snk" and
      result = 0.05262626262626266
      or
      repr = "(return (member sort *))" and t = "snk" and result = 1.0
      or
      repr = "(member key *)" and t = "snk" and result = 0.5419105432892004
      or
      repr = "(parameter 1 (return (root https://www.npmjs.com/package/fancy-log)))" and
      t = "snk" and
      result = 0.6549428868120457
      or
      repr = "(member canonicalUrl (member context *))" and
      t = "snk" and
      result = 0.5358532364139852
      or
      repr = "(parameter 0 (return (parameter a *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (parameter e *)))" and t = "snk" and result = 0.718384748703393
      or
      repr = "(return (member join *))" and t = "snk" and result = 0.75
      or
      repr = "(return (member catch *))" and t = "snk" and result = 0.06496680197614793
      or
      repr = "(parameter 0 (return (member setState *)))" and
      t = "snk" and
      result = 0.5075757575757576
      or
      repr = "(parameter 0 (return (member Object (global))))" and
      t = "snk" and
      result = 0.5661011970404983
      or
      repr = "(return (member fetch (global)))" and t = "snk" and result = 0.75
      or
      repr = "(member controller_ *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(member runnerPromise_ *)" and t = "snk" and result = 0.058905378787878715
      or
      repr = "(parameter 0 (return (member concat (return (member map *)))))" and
      t = "snk" and
      result = 0.05868656074766351
      or
      repr = "(parameter 0 (return (member Error (global))))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member isArray (member Array (global)))))" and
      t = "snk" and
      result = 0.8064507697909074
      or
      repr = "(parameter 0 (return (member addListener *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member call *)))" and t = "snk" and result = 0.43269230769230765
      or
      repr = "(member style *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member fine *)))" and t = "snk" and result = 0.605
      or
      repr = "(return (member maybeValidateAmpCreative *))" and
      t = "snk" and
      result = 0.040000000000000036
      or
      repr = "(parameter 2 (return (member warn (return (member user *)))))" and
      t = "snk" and
      result = 0.19835237106265138
      or
      repr = "(parameter 2 (return (member splice *)))" and t = "snk" and result = 0.666986896750797
      or
      repr = "(parameter 1 (return (member setStyles *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (parameter 1 (return (member timeoutPromise (member timer_ *)))))" and
      t = "snk" and
      result = 0.20136725510557285
      or
      repr = "(return (member then *))" and t = "snk" and result = 0.875
      or
      repr = "(member message *)" and t = "snk" and result = 0.4166666666666667
      or
      repr = "(parameter 0 (return (member listen *)))" and
      t = "snk" and
      result = 0.29863274489442715
      or
      repr = "(parameter 0 (return (member abs *)))" and t = "snk" and result = 0.13157894736842105
      or
      repr = "(parameter 0 (return (member openWindowDialog *)))" and t = "snk" and result = 0.25
      or
      repr = "(return (member collapse_dots *))" and t = "snk" and result = 0.75
      or
      repr = "(return (member src (root https://www.npmjs.com/package/gulp)))" and
      t = "snk" and
      result = 0.37719298245614036
      or
      repr = "(member w *)" and t = "snk" and result = 0.03947715472481833
      or
      repr = "(parameter 1 (return (member create *)))" and
      t = "snk" and
      result = 0.7216666666666667
      or
      repr = "(member S *)" and t = "snk" and result = 0.06666666666666676
      or
      repr = "(member pathCache *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member apply (member push *))))" and
      t = "snk" and
      result = 0.5775
      or
      repr = "(parameter 0 (return (member startMeasuring *)))" and t = "snk" and result = 0.25
      or
      repr = "(member activityPorts_ *)" and t = "snk" and result = 0.057192957613518315
      or
      repr = "(parameter 2 (return (member createCustomEvent *)))" and
      t = "snk" and
      result = 0.47046902042229144
      or
      repr = "(parameter 0 (return (member sha384Base64 *)))" and
      t = "snk" and
      result = 0.21250000000000002
      or
      repr = "(parameter 2 (return (member userAssert *)))" and t = "snk" and result = 0.3025
      or
      repr = "(member document_ *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (member assertElement (return (member dev *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member report (parameter context *))))" and
      t = "snk" and
      result = 0.5750000000000002
      or
      repr = "(parameter 2 (return (member call *)))" and t = "snk" and result = 0.272191860724319
      or
      repr = "(member arcs *)" and t = "snk" and result = 0.8491735741932763
      or
      repr = "(return (member then (return (member layoutCallback (parameter ampAutocomplete *)))))" and
      t = "snk" and
      result = 0.1446969696969695
      or
      repr = "(parameter 1 (return (member writeScript *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member reduce *)))" and
      t = "snk" and
      result = 0.3497189445839711
      or
      repr = "(member classList (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "snk" and
      result = 0.05326760989961929
      or
      repr = "(return (member slice *))" and t = "snk" and result = 0.4716981132075471
      or
      repr = "(parameter 0 (return (member tryDecodeUriComponent *)))" and
      t = "snk" and
      result = 0.245
      or
      repr = "(member arguments (member node *))" and t = "snk" and result = 0.016476289373485848
      or
      repr = "(parameter 0 (return (member Component *)))" and
      t = "snk" and
      result = 0.4599873007961232
      or
      repr = "(return (member replace (return (member trim *))))" and
      t = "snk" and
      result = 0.09749083592938734
      or
      repr = "(parameter 0 (return (member includes *)))" and
      t = "snk" and
      result = 0.9683333333333332
      or
      repr = "(parameter 1 (return (member assign *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member createError *)))" and t = "snk" and result = 0.15
      or
      repr =
        "(parameter 1 (return (member writeFileSync (root https://www.npmjs.com/package/fs-extra))))" and
      t = "snk" and
      result = 0.375
      or
      repr = "(parameter 0 (return (return (member $ *))))" and
      t = "snk" and
      result = 0.4652623052959499
      or
      repr = "(parameter 0 (return (member write *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member debug (return (parameter t *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member excludedDomains *)" and t = "snk" and result = 0.15540238836967807
      or
      repr = "(member buffer *)" and t = "snk" and result = 0.040000000000000036
      or
      repr = "(parameter 1 (return (member put *)))" and t = "snk" and result = 0.4
      or
      repr = "(parameter 1 (return (member writeFile *)))" and t = "snk" and result = 0.505
      or
      repr = "(parameter 0 (return (member removeElement *)))" and
      t = "snk" and
      result = 0.16666666666666669
      or
      repr = "(return (member fetchJson *))" and t = "snk" and result = 0.7986327448944271
      or
      repr = "(member classList *)" and t = "snk" and result = 0.4
      or
      repr = "(return (member fetchDocument *))" and t = "snk" and result = 0.010995251817237872
      or
      repr = "(return (member withExactArgs (return (member expects *))))" and
      t = "snk" and
      result = 0.9780094963655244
      or
      repr = "(return (member map (return (member split *))))" and
      t = "snk" and
      result = 0.5376420251889203
      or
      repr = "(parameter 0 (return (member default *)))" and t = "snk" and result = 0.1875
      or
      repr = "(parameter 0 (return (member write (member document *))))" and
      t = "snk" and
      result = 0.010218169633091245
      or
      repr = "(parameter 0 (return (member getPrototypeOf (member Object (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member format (member prototype *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member querySelector *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 3 (return (member push *)))" and t = "snk" and result = 0.0625
      or
      repr =
        "(parameter 0 (return (member readFile (member promises (root https://www.npmjs.com/package/fs)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member removeAttribute *)))" and
      t = "snk" and
      result = 0.04191054328920041
      or
      repr = "(parameter 0 (return (member set *)))" and t = "snk" and result = 0.27659999999999996
      or
      repr = "(parameter 0 (return (member addEventListener *)))" and
      t = "snk" and
      result = 0.2080894567107996
      or
      repr = "(return (member then (return (member timeoutPromise *))))" and
      t = "snk" and
      result = 0.0889299106957423
      or
      repr = "(parameter 0 (return (member all (member Promise (global)))))" and
      t = "snk" and
      result = 0.6442198328384785
      or
      repr = "(parameter 0 (return (member startsWith *)))" and
      t = "snk" and
      result = 0.17621975815856805
      or
      repr = "(member login *)" and t = "snk" and result = 0.125
      or
      repr = "(return (member post (member http *)))" and t = "snk" and result = 0.25757575757575757
      or
      repr = "(member temporaryToken *)" and t = "snk" and result = 0.125
      or
      repr = "(member token *)" and t = "snk" and result = 0.25757575757575746
      or
      repr = "(parameter 1 (return (member put (member http *))))" and
      t = "snk" and
      result = 0.25757575757575757
      or
      repr = "(parameter 0 (member className *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (member data *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member getAttribute (parameter elem *))))" and
      t = "snk" and
      result = 0.27
      or
      repr = "(parameter 0 (return (member Function (global))))" and
      t = "snk" and
      result = 0.16999999999999993
      or
      repr = "(parameter 0 (return (parameter $parse *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member test *)))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member removeChild (member lastChild *))))" and
      t = "snk" and
      result = 0.03125000000000026
      or
      repr = "(parameter 0 (return (member text *)))" and t = "snk" and result = 0.4166666666666667
      or
      repr = "(parameter 2 (return (member Function (global))))" and
      t = "snk" and
      result = 0.8571428571428571
      or
      repr = "(member top (return (member positionElements *)))" and
      t = "snk" and
      result = 0.2537614678899085
      or
      repr = "(member regex *)" and t = "snk" and result = 0.5126262626262625
      or
      repr = "(member platz (return (member calcSpielDateTime *)))" and
      t = "snk" and
      result = 0.27322404371584696
      or
      repr =
        "(parameter 0 (return (member post (return (root https://www.npmjs.com/package/supertest)))))" and
      t = "snk" and
      result = 0.1799999999999995
      or
      repr = "(parameter 0 (return (member selectAll (member svg *))))" and
      t = "snk" and
      result = 0.3851611111111112
      or
      repr = "(member teams *)" and t = "snk" and result = 0.1633031514653017
      or
      repr = "(parameter 0 (return (member each (member helpers (parameter e *)))))" and
      t = "snk" and
      result = 0.4592898683807771
      or
      repr = "(parameter 1 (return (member extend (member _ (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member mapToIds *)))" and
      t = "snk" and
      result = 0.24999999999999997
      or
      repr = "(member platz (parameter spiel *))" and t = "snk" and result = 0.8983275376718001
      or
      repr = "(parameter 3 (return (member value *)))" and
      t = "snk" and
      result = 0.41506551624601506
      or
      repr = "(member date (return (member calcSpielDateTime *)))" and
      t = "snk" and
      result = 0.41397582381188947
      or
      repr = "(parameter 1 (return (member requestPUT *)))" and
      t = "snk" and
      result = 0.5316666666666667
      or
      repr = "(member ansprechpartner *)" and t = "snk" and result = 0.16330315146530178
      or
      repr = "(member be *)" and t = "snk" and result = 1.0
      or
      repr = "(member be (member to (return (member expect *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member sortBy (member _ (global)))))" and
      t = "snk" and
      result = 0.08412586590636753
      or
      repr = "(member values (parameter t (parameter 0 (return (member forEach *)))))" and
      t = "snk" and
      result = 0.5555555555555556
      or
      repr = "(member spiele (parameter 1 (return (member extend *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member spielplan *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (parameter 0 (return (member run *)))))" and
      t = "snk" and
      result = 0.20305555555555554
      or
      repr = "(parameter 0 (member spiele *))" and t = "snk" and result = 0.20305555555555554
      or
      repr = "(member uhrzeit (parameter spiel *))" and t = "snk" and result = 0.03957608875641627
      or
      repr =
        "(member be (member to (return (member expect (root https://www.npmjs.com/package/chai)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member forEach (member _ (global)))))" and
      t = "snk" and
      result = 0.09941457136711736
      or
      repr = "(member gruppen *)" and t = "snk" and result = 0.012916666666666687
      or
      repr = "(return (member select (member main *)))" and t = "snk" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member get (return (root https://www.npmjs.com/package/supertest)))))" and
      t = "snk" and
      result = 0.8200000000000003
      or
      repr = "(return (member sortBy (member _ (global))))" and
      t = "snk" and
      result = 0.16330315146530172
      or
      repr = "(parameter 0 (return (member setGlobalState *)))" and
      t = "snk" and
      result = 0.054297699158696866
      or
      repr = "(member nummer *)" and t = "snk" and result = 0.2732240437158471
      or
      repr = "(parameter 0 (return (member close *)))" and t = "snk" and result = 0.2653700466200466
      or
      repr = "(return (member selectAll (member svg *)))" and
      t = "snk" and
      result = 0.10577499999999995
      or
      repr = "(parameter 0 (return (member select (member main *))))" and
      t = "snk" and
      result = 0.16000000000000003
      or
      repr = "(member veranstaltung *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member mapToIds *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member extend (member _ (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member user *)" and t = "snk" and result = 0.013802818269055328
      or
      repr = "(member uhrzeit *)" and t = "snk" and result = 0.41397582381188947
      or
      repr = "(parameter 0 (return (member replace (return (member replace *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member get (return (root https://www.npmjs.com/package/supertest))))" and
      t = "snk" and
      result = 0.8200000000000003
      or
      repr = "(return (member selectAll *))" and t = "snk" and result = 0.5800000000000001
      or
      repr = "(parameter 2 (return (member spliceWithArray *)))" and
      t = "snk" and
      result = 0.054297699158696866
      or
      repr = "(parameter 0 (return (member selectAll *)))" and
      t = "snk" and
      result = 0.5800000000000001
      or
      repr = "(parameter 1 (return (member render (parameter res *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member username *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member json (parameter res *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member isPlainObject (member $ *))))" and
      t = "snk" and
      result = 0.7222222222222222
      or
      repr = "(member base *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member encode (member DOM *))))" and
      t = "snk" and
      result = 0.2520833333333345
      or
      repr = "(parameter 2 (return (member attr (member constructor *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (member url (parameter s *)))" and
      t = "snk" and
      result = 0.15396238128796272
      or
      repr = "(return (member append (instance (member $ (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member init *)))" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (return (member append (return (parameter $ *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member get (member DOM *))))" and t = "snk" and result = 0.25
      or
      repr = "(return (member append *))" and t = "snk" and result = 0.5
      or
      repr = "(return (member attr (return (member css *))))" and
      t = "snk" and
      result = 0.07399103139013452
      or
      repr = "(member 2 (return (member exec *)))" and t = "snk" and result = 0.2583333333333332
      or
      repr = "(return (member append (instance (member init (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member deps (member requirejs (global)))" and
      t = "snk" and
      result = 0.6313131313131313
      or
      repr = "(member className *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member dispatch *)))" and t = "snk" and result = 0.125
      or
      repr = "(return (member append (instance (member jQuery *))))" and
      t = "snk" and
      result = 0.7156862745098033
      or
      repr = "(member render *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member String (global))))" and
      t = "snk" and
      result = 0.17109022556390974
      or
      repr = "(return (member trim (member jQuery (global))))" and
      t = "snk" and
      result = 0.6136111111111112
      or
      repr = "(return (member append (return (member jQuery *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member removeChild (return (member appendChild *)))))" and
      t = "snk" and
      result = 0.7187499999999999
      or
      repr = "(parameter 0 (return (member removeClass (member DOM *))))" and
      t = "snk" and
      result = 0.08239369479548841
      or
      repr = "(parameter 1 (return (member replace (member data *))))" and
      t = "snk" and
      result = 0.07522366522366553
      or
      repr = "(parameter 0 (return (member isFinite (global))))" and
      t = "snk" and
      result = 0.531689733877234
      or
      repr = "(parameter 3 (return (member add *)))" and t = "snk" and result = 0.12972772841193903
      or
      repr = "(parameter 2 (return (member attr (return (member noConflict *)))))" and
      t = "snk" and
      result = 0.272727272727273
      or
      repr = "(parameter 0 (return (member createTextNode *)))" and
      t = "snk" and
      result = 0.4399999999999995
      or
      repr = "(member buyType *)" and t = "snk" and result = 0.028055555555555556
      or
      repr = "(parameter 1 (return (member slice (return (member split *)))))" and
      t = "snk" and
      result = 0.8899589180327869
      or
      repr = "(parameter 0 (return (member hasClass (member DOM *))))" and
      t = "snk" and
      result = 0.24242424242424254
      or
      repr = "(parameter 0 (return (member dataFilter *)))" and
      t = "snk" and
      result = 0.5094554568238783
      or
      repr = "(return (member String (global)))" and t = "snk" and result = 0.125
      or
      repr = "(member main *)" and t = "snk" and result = 1.0
      or
      repr = "(member documentBaseURL (member tinymce (global)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member apply *))))" and
      t = "snk" and
      result = 0.32574074074074066
      or
      repr = "(member 1 (return (member match *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member dispatch *)))" and
      t = "snk" and
      result = 0.6552722715880611
      or
      repr = "(parameter 0 (parameter 0 (return (member append *))))" and t = "snk" and result = 1.0
      or
      repr = "(member alpha *)" and t = "snk" and result = 0.504727728411939
      or
      repr = "(return (member append (instance (return (member noConflict *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member opacity *)" and t = "snk" and result = 0.0018518518518516602
      or
      repr = "(parameter 0 (return (member push (member items *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member clearTimeout (global))))" and
      t = "snk" and
      result = 0.6950100561284716
      or
      repr = "(parameter 0 (instance (member Function (global))))" and
      t = "snk" and
      result = 0.6999999999999997
      or
      repr = "(parameter 2 (return (member setStyle (member DOM *))))" and
      t = "snk" and
      result = 0.27999999999999997
      or
      repr = "(member firstChild *)" and t = "snk" and result = 0.8074829758143086
      or
      repr = "(parameter 0 (return (member join *)))" and t = "snk" and result = 0.5323232323232323
      or
      repr = "(member 4 (return (member exec *)))" and t = "snk" and result = 0.1111111111111111
      or
      repr = "(member contentType *)" and t = "snk" and result = 0.3498062015503876
      or
      repr = "(parameter 0 (return (member test (member jsExtRegExp *))))" and
      t = "snk" and
      result = 0.4166666666666667
      or
      repr = "(parameter 0 (return (member getAttribute (parameter e *))))" and
      t = "snk" and
      result = 0.6699999999999996
      or
      repr = "(parameter 0 (return (member setAttrib *)))" and t = "snk" and result = 0.125
      or
      repr = "(parameter 0 (return (member push (parameter a *))))" and
      t = "snk" and
      result = 0.03924814814814817
      or
      repr = "(member $http *)" and t = "snk" and result = 0.2383333333333333
      or
      repr = "(return (member attr *))" and t = "snk" and result = 0.09722222222222224
      or
      repr = "(member records *)" and t = "snk" and result = 0.08000000000000002
      or
      repr = "(member value (return (parameter 0 (return (member map *)))))" and
      t = "snk" and
      result = 0.7352941176470588
      or
      repr = "(return (member toLowerCase (return (member replace *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (member matches (parameter 0 (return (member push *)))))" and
      t = "snk" and
      result = 0.36891130721446647
      or
      repr = "(member items (parameter 0 (member _protect *)))" and
      t = "snk" and
      result = 0.27363864205969535
      or
      repr = "(member hex (return (member getRGB (parameter R *))))" and
      t = "snk" and
      result = 0.4354818337368292
      or
      repr = "(member state *)" and t = "snk" and result = 0.16257575757575765
      or
      repr = "(member items (parameter o *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member appendChild *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member split (return (member join *))))" and
      t = "snk" and
      result = 0.3231776558712495
      or
      repr = "(parameter 0 (return (member clean (member jQuery *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (member url *))" and t = "snk" and result = 0.6652236652236655
      or
      repr = "(parameter 0 (parameter 0 (return (member isNaN (global)))))" and
      t = "snk" and
      result = 0.625
      or
      repr = "(parameter 0 (return (member parseJSON (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 0.500000000000002
      or
      repr = "(parameter 0 (return (member isNumeric (member $ (global)))))" and
      t = "snk" and
      result = 0.5555555555555555
      or
      repr = "(parameter 0 (return (member compareDocumentPosition *)))" and
      t = "snk" and
      result = 0.2238892623756652
      or
      repr = "(return (member splice *))" and t = "snk" and result = 0.004727728411939025
      or
      repr = "(return (member append (instance (member $ *))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member substring (return (member toLowerCase *))))" and
      t = "snk" and
      result = 0.00945545682387805
      or
      repr = "(parameter 1 (return (member setAttribute (member node *))))" and
      t = "snk" and
      result = 0.34499999999999975
      or
      repr = "(parameter 0 (member content *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member isArray (parameter $ *))))" and
      t = "snk" and
      result = 0.24555555555555553
      or
      repr = "(parameter 0 (return (member parseJSON *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member removeChild (return (member buildFragment *)))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 1 (return (member setAttribute (parameter t *))))" and
      t = "snk" and
      result = 0.5019020356234096
      or
      repr = "(member constructor *)" and t = "snk" and result = 0.16000000000000003
      or
      repr = "(member items (parameter o (member _protect *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member push (member children (member block *)))))" and
      t = "snk" and
      result = 0.02500000000000016
      or
      repr = "(member staticRenderFns *)" and t = "snk" and result = 0.75
      or
      repr = "(return (member access *))" and t = "snk" and result = 0.17499999999999993
      or
      repr = "(parameter 0 (member pos *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (return (parameter require *))))" and
      t = "snk" and
      result = 0.22421524663677128
      or
      repr = "(parameter 0 (return (member push (member children *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member get *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (member data *))" and t = "snk" and result = 0.59
      or
      repr = "(member $store *)" and t = "snk" and result = 0.2616666666666667
      or
      repr = "(parameter 1 (return (member setAttribute (member node (parameter o *)))))" and
      t = "snk" and
      result = 0.3151064015572887
      or
      repr = "(member items *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (member content (parameter o *)))" and t = "snk" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member on (return (root https://www.npmjs.com/package/jquery)))))" and
      t = "snk" and
      result = 0.14798206278026904
      or
      repr = "(parameter 0 (return (member clean (member jQuery (global)))))" and
      t = "snk" and
      result = 0.47058823529411736
      or
      repr = "(return (member append (return (member $ *))))" and t = "snk" and result = 1.0
      or
      repr = "(member input *)" and t = "snk" and result = 0.05759398148148148
      or
      repr = "(parameter 0 (return (member clearTimeout *)))" and
      t = "snk" and
      result = 0.004727728411939025
      or
      repr = "(parameter 0 (return (member createTextNode (member document (global)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member addClass *)))" and t = "snk" and result = 0.375
      or
      repr = "(return (root https://www.npmjs.com/package/jquery))" and
      t = "snk" and
      result = 0.14798206278026904
      or
      repr = "(parameter 1 (return (member attr *)))" and t = "snk" and result = 0.07399103139013452
      or
      repr = "(parameter 1 (return (return (return (parameter n *)))))" and
      t = "snk" and
      result = 0.12499999999999997
      or
      repr = "(return (member min (member Math (global))))" and t = "snk" and result = 1.0
      or
      repr = "(member labels *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member default (return (member default *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member _fnStringToCss (parameter 1 (return (member extend *)))))" and
      t = "snk" and
      result = 0.057142857142857155
      or
      repr = "(parameter 2 (return (member attr (member jQuery *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member setAttribute *)))" and t = "snk" and result = 0.75
      or
      repr = "(member source *)" and t = "snk" and result = 0.6666666666666665
      or
      repr = "(member documentBaseURL *)" and t = "snk" and result = 0.66
      or
      repr = "(parameter 0 (return (member append (return (member empty *)))))" and
      t = "snk" and
      result = 0.4898478371501273
      or
      repr = "(member parentNode *)" and t = "snk" and result = 0.33999999999999997
      or
      repr = "(return (member append (instance (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 0.7630718954248342
      or
      repr = "(return (member append (instance (member init (member fn *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setStyles (member DOM *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member TypeError (global))))" and t = "snk" and result = 0.5
      or
      repr = "(member seller *)" and t = "snk" and result = 0.38068181818181823
      or
      repr = "(member now *)" and t = "snk" and result = 0.536
      or
      repr = "(parameter 0 (return (member write (member document (global)))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(parameter 1 (member data (parameter s *)))" and
      t = "snk" and
      result = 0.15396238128796272
      or
      repr = "(member firstChild (parameter e *))" and t = "snk" and result = 0.2518796992481203
      or
      repr = "(parameter 0 (return (member push (parameter ignored *))))" and
      t = "snk" and
      result = 0.22960000000000036
      or
      repr = "(parameter 0 (return (member removeClass *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member eq *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 2 (return (member set *)))" and t = "snk" and result = 0.4175
      or
      repr = "(return (member addClass *))" and t = "snk" and result = 0.07496977025392988
      or
      repr = "(return (member eq *))" and t = "snk" and result = 1.0
      or
      repr = "(return (member removeClass *))" and t = "snk" and result = 0.07496977025392985
      or
      repr = "(member 0 (return (member tokenize *)))" and
      t = "snk" and
      result = 0.17924242424242418
      or
      repr = "(parameter 0 (return (parameter stripAndCollapse *)))" and
      t = "snk" and
      result = 0.125
      or
      repr = "(parameter 0 (return (member create *)))" and t = "snk" and result = 0.404375
      or
      repr = "(parameter 0 (return (member keys (member Object (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member deleteOne *))" and t = "snk" and result = 0.25
      or
      repr = "(member emit *)" and t = "snk" and result = 0.25
      or
      repr =
        "(parameter 0 (return (member push (return (root https://www.npmjs.com/package/browser-pack)))))" and
      t = "snk" and
      result = 0.4556
      or
      repr = "(parameter 1 (return (parameter cb *)))" and t = "snk" and result = 0.6613967701702177
      or
      repr = "(parameter 0 (return (member jsonEqual *)))" and
      t = "snk" and
      result = 0.42318181818181816
      or
      repr = "(parameter 2 (return (member pbkdf2Sync *)))" and
      t = "snk" and
      result = 0.14713380789613972
      or
      repr =
        "(member stdin (return (member spawn (root https://www.npmjs.com/package/child_process))))" and
      t = "snk" and
      result = 0.15151515151515096
      or
      repr = "(parameter 0 (return (parameter supportsBinary *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (member name (parameter 0 (return (member push *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member initializer *)" and t = "snk" and result = 0.3799999999999999
      or
      repr = "(parameter 1 (return (member writeFileSync (root https://www.npmjs.com/package/fs))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter callback *)))" and
      t = "snk" and
      result = 0.47630500000000003
      or
      repr = "(parameter 0 (return (member visit (root https://www.npmjs.com/package/recast))))" and
      t = "snk" and
      result = 0.33333333333333326
      or
      repr = "(parameter 0 (return (member sign *)))" and t = "snk" and result = 0.10999999999999999
      or
      repr = "(parameter 0 (return (member push (instance (member value *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member Array (global)))" and t = "snk" and result = 0.29000000000000004
      or
      repr = "(return (member stringify *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member sequenceExpression *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member format (root https://www.npmjs.com/package/util))))" and
      t = "snk" and
      result = 0.1117717336287573
      or
      repr = "(parameter 0 (return (parameter callback *)))" and t = "snk" and result = 0.5
      or
      repr = "(member not *)" and t = "snk" and result = 0.14994061514007545
      or
      repr = "(return (member parse (member JSON (global))))" and
      t = "snk" and
      result = 0.05577695494058277
      or
      repr = "(member stack (parameter error *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member localeCompare *)))" and
      t = "snk" and
      result = 0.279881230280151
      or
      repr =
        "(parameter 0 (return (member add (return (root https://www.npmjs.com/package/regenerate)))))" and
      t = "snk" and
      result = 0.020770676691729276
      or
      repr = "(return (member substr (member yytext (parameter 1 (member performAction *)))))" and
      t = "snk" and
      result = 0.7886756000844378
      or
      repr = "(parameter 1 (return (member deepEqual (global))))" and
      t = "snk" and
      result = 0.08999999999999997
      or
      repr = "(return (member replace (member yytext (parameter yy_ (member performAction *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member captureStackTrace (member Error (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member _cipher (parameter self *))" and t = "snk" and result = 0.5151515151515151
      or
      repr = "(parameter 0 (return (member stylize (parameter ctx *))))" and
      t = "snk" and
      result = 0.48000000000000015
      or
      repr = "(parameter 0 (instance (root https://www.npmjs.com/package/blob)))" and
      t = "snk" and
      result = 0.6361948579116126
      or
      repr = "(parameter 0 (return (member update *)))" and
      t = "snk" and
      result = 0.3429734848484849
      or
      repr = "(return (member fromArray (member ArraySet *)))" and
      t = "snk" and
      result = 0.36227811797535064
      or
      repr = "(parameter 1 (return (member findOne *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (parameter 1 (member encodePacket *))))" and
      t = "snk" and
      result = 0.5484959268825735
      or
      repr = "(return (member format *))" and t = "snk" and result = 1.0
      or
      repr = "(member hashes *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member onData *)))" and t = "snk" and result = 1.0
      or
      repr = "(member body (member toplevel *))" and t = "snk" and result = 0.2235666666666667
      or
      repr = "(return (member Function (global)))" and t = "snk" and result = 0.48673295722373267
      or
      repr = "(return (member update *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (return (parameter $compile *))))" and
      t = "snk" and
      result = 0.14097863450468426
      or
      repr = "(parameter 0 (return (member blockStatement *)))" and t = "snk" and result = 0.5
      or
      repr = "(member codePoint *)" and t = "snk" and result = 0.04031954887218053
      or
      repr = "(member currentPath *)" and t = "snk" and result = 0.06016132836625619
      or
      repr = "(parameter 2 (return (member defineProperty *)))" and
      t = "snk" and
      result = 0.7599999999999998
      or
      repr = "(parameter 0 (member _update *))" and t = "snk" and result = 0.08440000000000002
      or
      repr = "(parameter 0 (return (member normalize (root https://www.npmjs.com/package/path))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member call *))" and t = "snk" and result = 0.6666666666666666
      or
      repr = "(parameter 0 (member body *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member normalize *)))" and
      t = "snk" and
      result = 0.3062576484817269
      or
      repr = "(parameter 0 (return (member finishNode *)))" and
      t = "snk" and
      result = 0.16666666666666663
      or
      repr = "(parameter 0 (return (member getArg (return (parameter e *)))))" and
      t = "snk" and
      result = 0.14040097433724616
      or
      repr = "(parameter 0 (member name *))" and t = "snk" and result = 0.12214700994073152
      or
      repr = "(parameter 0 (return (member captureStackTrace *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member toComputedKey *)))" and
      t = "snk" and
      result = 0.023163992869875416
      or
      repr = "(member value (parameter node *))" and t = "snk" and result = 0.33333333333333337
      or
      repr =
        "(parameter 0 (parameter 1 (return (member writeFileSync (root https://www.npmjs.com/package/fs)))))" and
      t = "snk" and
      result = 0.18585151515151488
      or
      repr = "(parameter 2 (return (member _update *)))" and
      t = "snk" and
      result = 0.3197790251601911
      or
      repr = "(parameter 1 (return (member scrollTo *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member equal *)))" and
      t = "snk" and
      result = 0.14784205693296595
      or
      repr = "(parameter 0 (member path *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member indexOf (return (member relative *)))))" and
      t = "snk" and
      result = 0.8220935066713804
      or
      repr = "(parameter 0 (return (member fromComment *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member push (member body (member body *)))))" and
      t = "snk" and
      result = 0.23428816713851278
      or
      repr = "(member ts *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (member file *))" and t = "snk" and result = 0.07465124855249612
      or
      repr = "(member duration *)" and t = "snk" and result = 0.2544354996417583
      or
      repr = "(return (member Buffer (global)))" and t = "snk" and result = 0.5444
      or
      repr = "(parameter 1 (return (member get *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member update *)))" and t = "snk" and result = 1.0
      or
      repr = "(member reStart (return (member pop *)))" and
      t = "snk" and
      result = 0.7498886062216643
      or
      repr = "(return (member substr (member yytext *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member deepEqual (parameter t *))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member extname (root https://www.npmjs.com/package/path))))" and
      t = "snk" and
      result = 0.2469866666666668
      or
      repr = "(parameter 0 (return (member concat (member Buffer (global)))))" and
      t = "snk" and
      result = 0.5151515151515151
      or
      repr = "(member default *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 3 (return (member splice *)))" and t = "snk" and result = 1.0
      or
      repr = "(member preferredFileExtension *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(member stack *)" and t = "snk" and result = 0.6077760051880675
      or
      repr = "(member right *)" and t = "snk" and result = 0.025527921953094963
      or
      repr = "(parameter 1 (return (member spawn *)))" and t = "snk" and result = 0.7599999999999998
      or
      repr = "(return (member concat (member Buffer (global))))" and
      t = "snk" and
      result = 0.48166565968103864
      or
      repr = "(parameter 0 (parameter 0 (return (member push (member properties *)))))" and
      t = "snk" and
      result = 0.33838383838383845
      or
      repr = "(parameter 0 (return (parameter encoder *)))" and
      t = "snk" and
      result = 0.08299999999999999
      or
      repr = "(member 0 (return (member split (return (member replace *)))))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(member host *)" and t = "snk" and result = 0.12857142857142853
      or
      repr = "(member depMaps *)" and t = "snk" and result = 0.3885257992403689
      or
      repr = "(parameter 0 (return (member concat (return (parameter _dereq_ *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr =
        "(parameter 0 (return (member Minimatch (root https://www.npmjs.com/package/minimatch))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (instance (member constructor *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member arraySet *)))" and
      t = "snk" and
      result = 0.10251739896025497
      or
      repr = "(parameter 1 (return (member replace (parameter str *))))" and
      t = "snk" and
      result = 0.5271455204318168
      or
      repr = "(return (member toString (return (member encode *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member call (member onData *))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member append (instance (member jQuery (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member variableDeclaration *)))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(parameter 1 (return (root https://www.npmjs.com/package/buffer-xor)))" and
      t = "snk" and
      result = 0.312116969463316
      or
      repr = "(return (member append (return (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member validate *)))" and
      t = "snk" and
      result = 0.44099415018874605
      or
      repr = "(parameter 2 (return (return (parameter _dereq_ *))))" and
      t = "snk" and
      result = 0.09623124172620962
      or
      repr = "(return (member join (return (member map *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (instance (root https://www.npmjs.com/package/ordered-read-streams)))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member scrollTo *)))" and t = "snk" and result = 1.0
      or
      repr = "(member ext *)" and t = "snk" and result = 0.24000000000000005
      or
      repr = "(parameter 0 (return (member memberExpression *)))" and
      t = "snk" and
      result = 0.10294117647058836
      or
      repr = "(member indexers *)" and t = "snk" and result = 0.16999999999999998
      or
      repr = "(member entries *)" and t = "snk" and result = 0.19226964157770834
      or
      repr = "(parameter 0 (return (member push (parameter stackB *))))" and
      t = "snk" and
      result = 0.7665218683123437
      or
      repr = "(return (member replace (member yytext *)))" and
      t = "snk" and
      result = 0.9936756000844381
      or
      repr = "(parameter 0 (return (member isBuffer *)))" and
      t = "snk" and
      result = 0.08299999999999999
      or
      repr = "(parameter 0 (return (member copy *)))" and t = "snk" and result = 0.13174618674745853
      or
      repr = "(parameter 0 (member sourcesContent *))" and
      t = "snk" and
      result = 0.07465124855249619
      or
      repr = "(member names (member exports *))" and t = "snk" and result = 0.4460820694004551
      or
      repr = "(member expression *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member encodePacket *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/escape-html)))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member RegExp *)))" and
      t = "snk" and
      result = 0.7665218683123437
      or
      repr = "(parameter 0 (return (member check *)))" and
      t = "snk" and
      result = 0.27083333333333337
      or
      repr = "(parameter 0 (return (member push (member chunks *))))" and
      t = "snk" and
      result = 0.484306417351445
      or
      repr = "(parameter 0 (parameter 0 (return (member push (member names *)))))" and
      t = "snk" and
      result = 0.11915295417089139
      or
      repr = "(parameter 0 (return (member toExpression *)))" and t = "snk" and result = 0.65
      or
      repr = "(parameter 1 (return (member call (member onData (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member writeFileSync *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member src *)" and t = "snk" and result = 1.0
      or
      repr = "(member curLineStart *)" and t = "snk" and result = 0.16666666666666666
      or
      repr = "(member to (return (member expect *)))" and t = "snk" and result = 0.27272727272727226
      or
      repr = "(parameter 1 (return (member findAndModify *)))" and
      t = "snk" and
      result = 0.2911091039141665
      or
      repr = "(parameter 0 (return (member error (member console (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member run *)))" and t = "snk" and result = 0.3500000000000001
      or
      repr = "(parameter 0 (return (member bind (member emit *))))" and
      t = "snk" and
      result = 0.3875757575757574
      or
      repr = "(parameter 1 (return (member template *)))" and
      t = "snk" and
      result = 0.02941176470588236
      or
      repr = "(member should (return (member String (global))))" and
      t = "snk" and
      result = 0.2998812302801509
      or
      repr = "(parameter 0 (return (member all *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member Minimatch *)))" and
      t = "snk" and
      result = 0.17760435579543996
      or
      repr = "(parameter 0 (return (member generate *)))" and
      t = "snk" and
      result = 0.061090225563909806
      or
      repr = "(parameter 0 (return (member indexOf (return (member toLowerCase *)))))" and
      t = "snk" and
      result = 0.5333333333333333
      or
      repr = "(parameter 0 (return (member md5 *)))" and t = "snk" and result = 0.46150407311742503
      or
      repr = "(parameter 0 (return (member check (member BlockStatement *))))" and
      t = "snk" and
      result = 0.23421717171717157
      or
      repr = "(member _wcurpath *)" and t = "snk" and result = 0.11836196290862472
      or
      repr = "(member body (member body (parameter self *)))" and
      t = "snk" and
      result = 0.9273666666666665
      or
      repr = "(parameter 0 (return (parameter 0 (return (member then *)))))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(parameter 1 (return (return (member toFunction *))))" and
      t = "snk" and
      result = 0.25565050037716436
      or
      repr = "(member gs *)" and t = "snk" and result = 0.08175805032038214
      or
      repr = "(parameter 0 (return (member splice (member MAP (global)))))" and
      t = "snk" and
      result = 0.52
      or
      repr = "(parameter 0 (return (parameter callback (member encodePacket *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member onData (member prototype *))))" and
      t = "snk" and
      result = 0.24050713773940569
      or
      repr = "(return (member memberExpression *))" and t = "snk" and result = 0.10294117647058816
      or
      repr = "(return (root https://www.npmjs.com/package/escape-html))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(member skips *)" and t = "snk" and result = 0.1840088536276877
      or
      repr = "(member hashes (instance (member compiler (member prototype *))))" and
      t = "snk" and
      result = 0.347599999999999
      or
      repr = "(member to (return (member expect (root https://www.npmjs.com/package/code))))" and
      t = "snk" and
      result = 0.6090731823901391
      or
      repr = "(return (member append (return (return (member noConflict *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member overflow *)" and t = "snk" and result = 0.04058874471357174
      or
      repr = "(member callProperties *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member decode (return (member define *)))))" and
      t = "snk" and
      result = 0.7363636363636363
      or
      repr = "(return (member splice (return (member split *))))" and
      t = "snk" and
      result = 0.27272727272727315
      or
      repr = "(parameter 0 (return (parameter f *)))" and t = "snk" and result = 0.23624534597221736
      or
      repr = "(member $in *)" and t = "snk" and result = 0.05823333333333328
      or
      repr = "(member body (member body *))" and t = "snk" and result = 0.33333333333333326
      or
      repr = "(parameter 0 (return (member push (parameter args *))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member path (return (member urlParse *)))" and
      t = "snk" and
      result = 0.28620115515961153
      or
      repr = "(member tok *)" and t = "snk" and result = 0.16666666666666663
      or
      repr = "(member encoding *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member variableDeclaration *))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(parameter 2 (return (member traverse *)))" and
      t = "snk" and
      result = 0.4848484848484844
      or
      repr = "(member chunks *)" and t = "snk" and result = 0.567550505050505
      or
      repr = "(parameter 0 (parameter 0 (return (member ok *))))" and
      t = "snk" and
      result = 0.3333333333333333
      or
      repr = "(return (member parseInt (global)))" and t = "snk" and result = 0.5168223441287504
      or
      repr = "(parameter 0 (return (member toIdentifier *)))" and
      t = "snk" and
      result = 0.08000000000000002
      or
      repr = "(parameter 1 (return (member equal (parameter st *))))" and
      t = "snk" and
      result = 0.10215794306703405
      or
      repr = "(parameter 0 (return (member fromArray (member ArraySet *))))" and
      t = "snk" and
      result = 0.14491124719014026
      or
      repr = "(member 0 (return (member slice *)))" and t = "snk" and result = 0.11568796698127726
      or
      repr = "(return (member append (instance (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member notEnumerableProp *)))" and
      t = "snk" and
      result = 0.3333333333333333
      or
      repr = "(parameter 1 (return (member defineProperties (member Object (global)))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(parameter 0 (return (member stylize *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member createSign *)))" and
      t = "snk" and
      result = 0.10000000000000003
      or
      repr = "(return (member format (root https://www.npmjs.com/package/util)))" and
      t = "snk" and
      result = 0.5271359933529614
      or
      repr = "(member m *)" and t = "snk" and result = 0.14694618674745868
      or
      repr = "(parameter 0 (return (member getArg *)))" and
      t = "snk" and
      result = 0.004510272852894383
      or
      repr = "(parameter 1 (return (member callExpression *)))" and
      t = "snk" and
      result = 0.10294117647058819
      or
      repr = "(member responseText *)" and t = "snk" and result = 0.7965328077575268
      or
      repr = "(parameter 0 (return (parameter filter *)))" and
      t = "snk" and
      result = 0.08299999999999999
      or
      repr = "(member properties *)" and t = "snk" and result = 0.37144651551081287
      or
      repr = "(parameter 1 (return (member emit *)))" and t = "snk" and result = 0.42
      or
      repr = "(member position (instance (member default *)))" and
      t = "snk" and
      result = 0.2698173706764612
      or
      repr = "(parameter 0 (return (member push (member body *))))" and
      t = "snk" and
      result = 0.25056031770997167
      or
      repr = "(parameter 2 (return (member calculateMac *)))" and
      t = "snk" and
      result = 0.29499999999999993
      or
      repr = "(parameter 0 (return (member decode *)))" and
      t = "snk" and
      result = 0.39000000000000007
      or
      repr = "(parameter 1 (return (member reduce (return (member keys *)))))" and
      t = "snk" and
      result = 0.2599999999999998
      or
      repr = "(parameter 0 (return (member from *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member end *)))" and t = "snk" and result = 0.538495926882575
      or
      repr = "(member conv (instance (member value (member constructor *))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member concat (return (member update *)))))" and
      t = "snk" and
      result = 0.6395580503203822
      or
      repr = "(parameter 0 (return (member trim (member jQuery (parameter a *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member stdin (return (member spawn *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member debug *)))" and
      t = "snk" and
      result = 0.46910382385347615
      or
      repr = "(parameter 0 (return (member encryptBlock (member _cipher (parameter self *)))))" and
      t = "snk" and
      result = 0.6977902516019108
      or
      repr = "(member properties (return (member startNode *)))" and
      t = "snk" and
      result = 0.08333333333333337
      or
      repr = "(return (member trim (member jQuery (parameter a *))))" and
      t = "snk" and
      result = 0.16341117206437764
      or
      repr = "(parameter 1 (return (parameter opts *)))" and
      t = "snk" and
      result = 0.5886032298297823
      or
      repr = "(parameter 0 (return (parameter check *)))" and t = "snk" and result = 0.5
      or
      repr = "(member except *)" and t = "snk" and result = 0.04263353126028165
      or
      repr = "(return (member call (member toString (member prototype *))))" and
      t = "snk" and
      result = 0.2575757575757575
      or
      repr = "(member 1 (return (member split (return (member replace *)))))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(parameter 0 (return (member existsSync (root https://www.npmjs.com/package/fs))))" and
      t = "snk" and
      result = 0.5094100000000001
      or
      repr = "(parameter 0 (return (member ReferenceError (global))))" and
      t = "snk" and
      result = 0.09699185376514985
      or
      repr = "(return (member concat (member body *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 2 (return (return (return (parameter _dereq_ *)))))" and
      t = "snk" and
      result = 0.3124721515554161
      or
      repr = "(parameter 0 (return (member isInteger *)))" and t = "snk" and result = 0.165
      or
      repr = "(parameter 1 (return (member ok (root https://www.npmjs.com/package/assert))))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(member hoistDeclarators *)" and t = "snk" and result = 0.3716666666666667
      or
      repr = "(parameter 0 (return (parameter utf8encode *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member info (member logger (parameter state *)))))" and
      t = "snk" and
      result = 0.46910382385347615
      or
      repr = "(parameter 1 (return (member assert *)))" and
      t = "snk" and
      result = 0.25757575757575757
      or
      repr = "(parameter 0 (return (member push (return (member exports *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member emit (parameter _this *))))" and
      t = "snk" and
      result = 0.09515151515151521
      or
      repr = "(member closeRe *)" and t = "snk" and result = 1.0
      or
      repr = "(return (member append (return (member $ (global)))))" and t = "snk" and result = 1.0
      or
      repr = "(member firstChild (return (member appendChild *)))" and
      t = "snk" and
      result = 0.8066666666666678
      or
      repr = "(parameter 0 (return (member trim (member $ (parameter a *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member find *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member write (member stream *))))" and
      t = "snk" and
      result = 0.3220897485547848
      or
      repr = "(return (member __empty *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member getOwnPropertyNames *)))" and
      t = "snk" and
      result = 0.16024338321617937
      or
      repr = "(parameter 0 (return (member get (parameter $http *))))" and
      t = "snk" and
      result = 0.3333333333333333
      or
      repr = "(return (return (parameter r *)))" and t = "snk" and result = 0.4662176219702492
      or
      repr = "(member _dispatchListeners *)" and t = "snk" and result = 0.028888654182155554
      or
      repr = "(parameter 0 (return (member authorizeRequest *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 2 (return (member defineProperty (member Object (global)))))" and
      t = "snk" and
      result = 0.15466321647768683
      or
      repr = "(parameter 1 (return (member slice *)))" and t = "snk" and result = 0.2668911890148754
      or
      repr = "(parameter 0 (return (member hasOwnProperty *)))" and t = "snk" and result = 0.5
      or
      repr = "(member file *)" and t = "snk" and result = 0.33333333333333337
      or
      repr = "(member body (return (member buildRequest (member self *))))" and
      t = "snk" and
      result = 0.8825993889394866
      or
      repr = "(parameter 1 (return (member slice (parameter e *))))" and
      t = "snk" and
      result = 0.8931088109851247
      or
      repr = "(member compiledImplicit *)" and t = "snk" and result = 0.08333333333333333
      or
      repr = "(member specSelectors *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member getPrototypeOf *)))" and
      t = "snk" and
      result = 0.4662176219702492
      or
      repr = "(parameter 0 (return (member unshift *)))" and
      t = "snk" and
      result = 0.36666666666666653
      or
      repr = "(parameter 0 (return (member error *)))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(member specActions *)" and t = "snk" and result = 1.0
      or
      repr = "(member tokens *)" and t = "snk" and result = 0.5
      or
      repr = "(member specActions (return (member getSystem *)))" and
      t = "snk" and
      result = 0.9411764705882347
      or
      repr = "(parameter 0 (return (member findOne (return (member collection *)))))" and
      t = "snk" and
      result = 0.6334455945074378
      or
      repr = "(parameter 1 (return (member call (member hasOwnProperty (member prototype *)))))" and
      t = "snk" and
      result = 0.03715021325288203
      or
      repr = "(parameter 0 (return (member createElement (member default *))))" and
      t = "snk" and
      result = 0.3012048192771085
      or
      repr = "(parameter 1 (return (member exports (parameter e *))))" and
      t = "snk" and
      result = 0.5147827527027585
      or
      repr = "(member tokens (parameter 0 (member exports (parameter e *))))" and
      t = "snk" and
      result = 0.8652803262155842
      or
      repr = "(return (member default *))" and t = "snk" and result = 0.08434448886180856
      or
      repr = "(parameter 0 (return (member charCodeAt (member src *))))" and
      t = "snk" and
      result = 0.32573669179047304
      or
      repr = "(parameter 0 (return (member push (member tokens (parameter e *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (parameter t *))" and t = "snk" and result = 0.2668911890148754
      or
      repr = "(parameter 0 (return (member push (parameter e (member exports *)))))" and
      t = "snk" and
      result = 0.42997243316342443
      or
      repr = "(member osversion (return (member _detect (return (member _detect *)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 3 (return (member createError *)))" and
      t = "snk" and
      result = 0.6022521585353167
      or
      repr = "(parameter 0 (instance (member TypeError (global))))" and
      t = "snk" and
      result = 0.09533678352231317
      or
      repr = "(parameter 0 (instance (return (parameter n *))))" and t = "snk" and result = 0.5
      or
      repr = "(member _dispatchInstances *)" and t = "snk" and result = 0.3246753246753246
      or
      repr = "(parameter 0 (return (member defineProperty (member Object (global)))))" and
      t = "snk" and
      result = 0.6666666666666669
      or
      repr = "(return (member push *))" and t = "snk" and result = 0.8637823780297509
      or
      repr = "(parameter 1 (return (member createElement *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member accumulateTwoPhaseDispatches (return (parameter n *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member defaultValue *)" and t = "snk" and result = 0.5
      or
      repr = "(member specActions (return (member exports (parameter e *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member auth *)" and t = "snk" and result = 0.035306634616814235
      or
      repr = "(parameter 3 (return (member call *)))" and t = "snk" and result = 0.2668911890148754
      or
      repr = "(member title *)" and t = "snk" and result = 0.5
      or
      repr = "(return (return (parameter n *)))" and t = "snk" and result = 0.4325931402416371
      or
      repr = "(parameter 0 (return (member findOneAndUpdate (return (member collection *)))))" and
      t = "snk" and
      result = 0.36589791911251085
      or
      repr = "(member start_mark *)" and t = "snk" and result = 0.08000000000000007
      or
      repr = "(parameter 1 (return (parameter i *)))" and t = "snk" and result = 0.25
      or
      repr = "(member prototype *)" and t = "snk" and result = 0.3876683917611564
      or
      repr = "(parameter 2 (member exports *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (instance (return (member extend (member Util *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (parameter n *))" and t = "snk" and result = 0.5452149990340439
      or
      repr = "(return (member decodeURIComponent (global)))" and
      t = "snk" and
      result = 0.18355785568154206
      or
      repr = "(parameter 1 (return (member createElement (member default *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member call (member hasOwnProperty *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member unmountComponent (return (parameter n *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member concat (return (member concat *)))))" and
      t = "snk" and
      result = 0.42500000000000004
      or
      repr = "(parameter 0 (return (member render (parameter 0 (member default *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member join (return (member concat *))))" and
      t = "snk" and
      result = 0.34023314782065817
      or
      repr = "(parameter 0 (return (member stylize (parameter e *))))" and
      t = "snk" and
      result = 0.4662176219702492
      or
      repr = "(member fullPath (return (parameter 0 (return (member map *)))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 1 (return (member call (parameter e *))))" and t = "snk" and result = 1.0
      or
      repr = "(member path (parameter t *))" and t = "snk" and result = 1.0
      or
      repr = "(return (member apply *))" and t = "snk" and result = 0.7321127946127948
      or
      repr = "(parameter 0 (return (member find (return (member collection *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member default *)))" and
      t = "snk" and
      result = 0.4171722444309043
      or
      repr = "(parameter 0 (return (parameter 2 (return (member default *)))))" and
      t = "snk" and
      result = 0.08012169160808968
      or
      repr = "(member process *)" and t = "snk" and result = 0.5909090909090909
      or
      repr = "(parameter 0 (instance (member constructor *)))" and t = "snk" and result = 1.0
      or
      repr = "(return (member call (parameter t *)))" and t = "snk" and result = 0.20690219053113132
      or
      repr = "(parameter 0 (return (member f (return (parameter n *)))))" and
      t = "snk" and
      result = 0.19970795939211086
      or
      repr = "(member props *)" and t = "snk" and result = 0.29999999999999993
      or
      repr = "(member 1 (parameter e *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member remove (return (member collection *)))))" and
      t = "snk" and
      result = 0.05439078653410734
      or
      repr = "(parameter 0 (return (member insert *)))" and
      t = "snk" and
      result = 0.6334455945074378
      or
      repr = "(parameter 0 (instance (return (member extend *))))" and
      t = "snk" and
      result = 0.8161525129660718
      or
      repr = "(parameter 0 (return (member slice (member src (parameter e *)))))" and
      t = "snk" and
      result = 0.32573669179047304
      or
      repr = "(parameter 1 (return (member default *)))" and
      t = "snk" and
      result = 0.08000000000000002
      or
      repr = "(parameter 1 (return (member batchedUpdates *)))" and t = "snk" and result = 0.25
      or
      repr = "(member version (return (member _detect *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(return (member getOwnPropertyNames (member Object (global))))" and
      t = "snk" and
      result = 0.16024338321617937
      or
      repr = "(parameter 2 (member exports (parameter e *)))" and
      t = "snk" and
      result = 0.9831088109851246
      or
      repr = "(parameter 0 (return (member reduce *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member render *)))" and t = "snk" and result = 0.5
      or
      repr = "(return (member construct_object *))" and t = "snk" and result = 0.8731300101819196
      or
      repr = "(member domain *)" and t = "snk" and result = 0.40870342987918146
      or
      repr = "(parameter 1 (return (member substring *)))" and
      t = "snk" and
      result = 0.3006735670446262
      or
      repr = "(member body (parameter 0 (return (member authorizeRequest (member authActions *)))))" and
      t = "snk" and
      result = 0.4999999999999999
      or
      repr = "(member implicit *)" and t = "snk" and result = 0.08333333333333333
      or
      repr = "(member patches *)" and t = "snk" and result = 0.5162807804907046
      or
      repr = "(member compiledExplicit *)" and t = "snk" and result = 0.08333333333333337
      or
      repr = "(member path (return (member getFile *)))" and
      t = "snk" and
      result = 0.5218333333333335
      or
      repr = "(return (member keys *))" and t = "snk" and result = 0.2303030303030298
      or
      repr = "(parameter 0 (return (member done *)))" and t = "snk" and result = 0.5761999999999999
      or
      repr = "(return (member getFile *))" and t = "snk" and result = 0.0047050000000000555
      or
      repr = "(member selections *)" and t = "snk" and result = 0.7000000000000001
      or
      repr = "(parameter 0 (return (member encodeSlug *)))" and
      t = "snk" and
      result = 0.42775606666666666
      or
      repr = "(member fetch *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member pipe *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (instance (member TextView *)))" and
      t = "snk" and
      result = 0.7156862745098036
      or
      repr = "(parameter 0 (return (member access *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member indexOf (return (member getProperty *)))))" and
      t = "snk" and
      result = 0.19734711111111103
      or
      repr = "(parameter 0 (return (member all (root https://www.npmjs.com/package/util/util))))" and
      t = "snk" and
      result = 0.15439635217422348
      or
      repr = "(member host (return (member exports *)))" and
      t = "snk" and
      result = 0.42139622641509417
      or
      repr = "(member output *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member delete (return (return (member setupOrionServer *)))))" and
      t = "snk" and
      result = 0.8577760051880675
      or
      repr = "(member name (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "snk" and
      result = 0.19999999999999934
      or
      repr = "(return (member substring (return (member replace *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member basename *))" and t = "snk" and result = 0.5050505050505051
      or
      repr = "(return (member substring *))" and t = "snk" and result = 0.625
      or
      repr = "(parameter 2 (return (member writeResponse *)))" and t = "snk" and result = 0.2791
      or
      repr = "(member fileNamePatterns (parameter searchParams *))" and
      t = "snk" and
      result = 0.3030303030303029
      or
      repr = "(member manual *)" and t = "snk" and result = 0.14134275618374556
      or
      repr = "(parameter 0 (return (member info *)))" and t = "snk" and result = 0.8333333333333334
      or
      repr =
        "(parameter 0 (return (member resolve (return (root https://www.npmjs.com/package/orion/Deferred)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member concat (return (member slice *)))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(member caseSensitive *)" and t = "snk" and result = 0.09999999999999998
      or
      repr =
        "(return (member then (return (member resolve (root https://www.npmjs.com/package/bluebird)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (parameter xhr *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member each *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member checkRights (member user (parameter req *)))))" and
      t = "snk" and
      result = 0.7394141414141412
      or
      repr = "(member _userinfo *)" and t = "snk" and result = 0.31666666666666665
      or
      repr = "(parameter 0 (return (member getProposal *)))" and
      t = "snk" and
      result = 0.16249999999999998
      or
      repr = "(parameter 1 (return (member replaceAnnotations *)))" and
      t = "snk" and
      result = 0.43126383838383847
      or
      repr = "(member ast *)" and t = "snk" and result = 0.41161616161616166
      or
      repr = "(return (member showWhile *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member propagate *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member substring (return (member substring *))))" and
      t = "snk" and
      result = 0.06149999999999992
      or
      repr = "(member Children *)" and t = "snk" and result = 0.21282644444444443
      or
      repr = "(parameter 1 (return (member connect *)))" and
      t = "snk" and
      result = 0.12666666666666668
      or
      repr = "(parameter 1 (return (parameter 1 (return (member getUser *)))))" and
      t = "snk" and
      result = 0.20000000000000007
      or
      repr = "(member occurrences *)" and t = "snk" and result = 0.25
      or
      repr = "(member response (parameter result *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member join *))))" and t = "snk" and result = 0.905
      or
      repr = "(member RemoteLocation *)" and t = "snk" and result = 0.9636500000000001
      or
      repr = "(parameter 0 (return (member resolve *)))" and
      t = "snk" and
      result = 0.20000000000000007
      or
      repr = "(member FriendlyPath *)" and t = "snk" and result = 0.061499999999999944
      or
      repr = "(member rows *)" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (member start *))" and t = "snk" and result = 0.3030303030303029
      or
      repr = "(member Mappings (member siteConfiguration *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member setProgressResult (return (member getService *)))))" and
      t = "snk" and
      result = 0.5200000000000001
      or
      repr = "(return (member formatMessage (root https://www.npmjs.com/package/orion/i18nUtil)))" and
      t = "snk" and
      result = 0.3030303030303031
      or
      repr = "(parameter 1 (return (member checkRights *)))" and
      t = "snk" and
      result = 0.4015151515151515
      or
      repr = "(member _lineHeight *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member done (return (member Task *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (member Children *))))" and
      t = "snk" and
      result = 0.11582362117822553
      or
      repr = "(return (member all *))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member IsCallee (parameter infer *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member forEach *)))" and
      t = "snk" and
      result = 0.1413427561837456
      or
      repr = "(return (member resolveModule *))" and t = "snk" and result = 0.17500000000000004
      or
      repr = "(return (member outputPos (parameter tern *)))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(return (member stringify (member JSON (global))))" and
      t = "snk" and
      result = 0.4620000000000001
      or
      repr = "(return (member then (return (member progress *))))" and
      t = "snk" and
      result = 0.08333333333333331
      or
      repr = "(parameter 0 (return (member trim *)))" and t = "snk" and result = 0.7293474798076726
      or
      repr = "(parameter 0 (return (member push (member occurrences *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member writeError *)))" and t = "snk" and result = 0.645
      or
      repr = "(parameter 2 (return (member report (parameter context *))))" and
      t = "snk" and
      result = 0.8500000000000001
      or
      repr = "(parameter 2 (return (member setText *)))" and
      t = "snk" and
      result = 0.13661202185792348
      or
      repr = "(member diffs (return (member patch_obj (member diff_match_patch *))))" and
      t = "snk" and
      result = 0.28571428571428514
      or
      repr = "(return (member then (return (member getRepo *))))" and
      t = "snk" and
      result = 0.6314765100671142
      or
      repr =
        "(return (member getProperty (parameter 0 (member _getProjectDeployService (member prototype *)))))" and
      t = "snk" and
      result = 0.409459281045751
      or
      repr = "(member diffs (return (member patch_obj (member diff_match_patch (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member connect *))" and t = "snk" and result = 0.13050505050505026
      or
      repr = "(member _project *)" and t = "snk" and result = 0.25333333333333335
      or
      repr = "(parameter 0 (return (member showWhile *)))" and
      t = "snk" and
      result = 0.5281528888888888
      or
      repr = "(member id *)" and t = "snk" and result = 0.35899999999999993
      or
      repr = "(parameter 0 (return (member map (root https://www.npmjs.com/package/bluebird))))" and
      t = "snk" and
      result = 0.47905226764287157
      or
      repr = "(parameter 2 (return (member join *)))" and t = "snk" and result = 0.33333333333333337
      or
      repr = "(parameter 3 (return (member writeResponse *)))" and t = "snk" and result = 0.4525
      or
      repr = "(parameter 2 (return (member writeError *)))" and t = "snk" and result = 0.375
      or
      repr = "(member query (instance (member exports *)))" and
      t = "snk" and
      result = 0.053117968498874804
      or
      repr = "(member query (return (member URL (global))))" and t = "snk" and result = 1.0
      or
      repr = "(member classList (return (member createElement (member document (global)))))" and
      t = "snk" and
      result = 0.8160781144781142
      or
      repr = "(parameter 0 (return (member exports *)))" and
      t = "snk" and
      result = 0.6745901639344257
      or
      repr = "(parameter 0 (return (member basename (root https://www.npmjs.com/package/path))))" and
      t = "snk" and
      result = 0.3333333333333333
      or
      repr = "(member workspaceId *)" and t = "snk" and result = 0.6146363636363636
      or
      repr = "(member CommitLocation *)" and t = "snk" and result = 0.5151515151515149
      or
      repr = "(parameter 1 (return (member put (return (parameter LRU *)))))" and
      t = "snk" and
      result = 0.2427674224459353
      or
      repr = "(parameter 1 (return (member ok (member assert (parameter chai *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member name (member entity *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (return (member bind *))))" and t = "snk" and result = 0.5
      or
      repr = "(return (member cfRequest *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member ok (member assert *))))" and t = "snk" and result = 1.0
      or
      repr = "(member regEx *)" and t = "snk" and result = 0.09999999999999998
      or
      repr = "(parameter 0 (return (member confirm (return (member getService *)))))" and
      t = "snk" and
      result = 0.30000000000000004
      or
      repr = "(parameter 0 (return (parameter edit *)))" and
      t = "snk" and
      result = 0.07500000000000004
      or
      repr = "(parameter 0 (return (member push (member problems *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member done (instance (member Task *)))))" and
      t = "snk" and
      result = 0.1813757575757574
      or
      repr = "(parameter 0 (return (member substring (parameter sig *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member fetchChildren (member fileClient *))))" and
      t = "snk" and
      result = 0.16000000000000003
      or
      repr = "(instance (member Buffer (global)))" and t = "snk" and result = 1.0
      or
      repr = "(member _aststack *)" and t = "snk" and result = 0.2058080808080808
      or
      repr = "(parameter 1 (instance (member IsCallee (parameter infer *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member saveSetting *)))" and t = "snk" and result = 1.0
      or
      repr = "(member Message (return (member parse (member JSON (global)))))" and
      t = "snk" and
      result = 0.30000000000000027
      or
      repr = "(parameter 1 (return (member saveSetting (parameter util *))))" and
      t = "snk" and
      result = 0.9303030303030304
      or
      repr = "(parameter 5 (return (member createLink (parameter navigatorRenderer *))))" and
      t = "snk" and
      result = 0.4184343434343435
      or
      repr = "(parameter 2 (return (member log (member console (global)))))" and
      t = "snk" and
      result = 0.4605984848484849
      or
      repr = "(parameter 0 (return (member keys *)))" and t = "snk" and result = 0.3275340909090916
      or
      repr = "(parameter 0 (return (member reject *)))" and
      t = "snk" and
      result = 0.3030303030303031
      or
      repr = "(parameter 3 (return (member cfRequest *)))" and
      t = "snk" and
      result = 0.41999999999999993
      or
      repr =
        "(parameter 1 (return (member ok (member assert (root https://www.npmjs.com/package/chai/chai)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (member hunks *))))" and
      t = "snk" and
      result = 0.8550000000000004
      or
      repr = "(parameter 0 (return (member resolve (parameter deferred *))))" and
      t = "snk" and
      result = 0.21911122222222212
      or
      repr = "(member scriptResolver *)" and t = "snk" and result = 0.4999999999999999
      or
      repr = "(parameter 2 (return (member logEvent *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member handler *)))" and
      t = "snk" and
      result = 0.10000000000000003
      or
      repr = "(parameter 1 (return (member createFile *)))" and
      t = "snk" and
      result = 0.28717355555555557
      or
      repr = "(return (member createFile *))" and t = "snk" and result = 0.21282644444444443
      or
      repr = "(parameter 0 (instance (member TextView (parameter mTextView *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member query (instance (member exports (parameter t *))))" and
      t = "snk" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member all (root https://www.npmjs.com/package/orion/Deferred))))" and
      t = "snk" and
      result = 0.26262626262626243
      or
      repr = "(parameter 0 (return (return (member makeRenderFunction *))))" and
      t = "snk" and
      result = 0.029000000000000026
      or
      repr = "(member Message (return (member parse *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member setText *)))" and
      t = "snk" and
      result = 0.5926767676767677
      or
      repr = "(parameter 0 (return (member withStatsAndETag *)))" and
      t = "snk" and
      result = 0.22147651006711405
      or
      repr =
        "(return (member showWhile (return (member getService (parameter serviceRegistry *)))))" and
      t = "snk" and
      result = 0.041666666666666935
      or
      repr = "(parameter 1 (return (member setText *)))" and
      t = "snk" and
      result = 0.13661202185792348
      or
      repr = "(member occurrences (return (member pop (member blocks *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member getFile *))))" and
      t = "snk" and
      result = 0.6666666666666667
      or
      repr = "(parameter 0 (return (member config *)))" and t = "snk" and result = 0.4
      or
      repr = "(parameter 0 (return (member encodeStringLocation *)))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(member id (return (member parse (member JSON (global)))))" and
      t = "snk" and
      result = 0.14099999999999946
      or
      repr = "(parameter 0 (return (member appendChild (return (member insertCell *)))))" and
      t = "snk" and
      result = 0.27616666666666667
      or
      repr = "(member _socket (instance (member DebugSocket *)))" and
      t = "snk" and
      result = 0.25333333333333335
      or
      repr = "(parameter 2 (parameter 0 (return (member progress *))))" and
      t = "snk" and
      result = 0.37499999999999994
      or
      repr = "(parameter 0 (return (member ajax (member $ (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member apply (member splice *))))" and
      t = "snk" and
      result = 0.6575757575757575
      or
      repr = "(parameter 0 (return (member reject (member Promise (global)))))" and
      t = "snk" and
      result = 0.1969696969696969
      or
      repr = "(member doc (parameter c (parameter 3 (return (parameter gather *)))))" and
      t = "snk" and
      result = 0.9853333333333332
      or
      repr = "(parameter 0 (return (member showWhile (return (member getService *)))))" and
      t = "snk" and
      result = 0.10316024242424258
      or
      repr = "(member 0 (return (member map *)))" and t = "snk" and result = 0.2885681821774796
      or
      repr = "(parameter 0 (return (member add (member classList *))))" and
      t = "snk" and
      result = 0.6676644496325123
      or
      repr = "(member oldLines *)" and t = "snk" and result = 0.1449999999999998
      or
      repr = "(member ecma (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "snk" and
      result = 0.9853333333333332
      or
      repr = "(parameter 0 (parameter 0 (return (member unshift *))))" and
      t = "snk" and
      result = 0.18333333333333326
      or
      repr = "(parameter 0 (return (member indexOf (member name *))))" and
      t = "snk" and
      result = 0.972742
      or
      repr = "(parameter 1 (return (member put (member cache *))))" and
      t = "snk" and
      result = 0.17434711111111123
      or
      repr = "(parameter 1 (instance (member IsCallee *)))" and
      t = "snk" and
      result = 0.7500000000000001
      or
      repr = "(parameter 0 (return (member all (parameter Deferred *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member query (instance (member URL (global))))" and t = "snk" and result = 1.0
      or
      repr = "(member proposal (parameter proposal *))" and
      t = "snk" and
      result = 0.25787373737373737
      or
      repr = "(parameter 1 (return (root https://www.npmjs.com/package/orion/xhr)))" and
      t = "snk" and
      result = 0.5
      or
      repr =
        "(parameter 0 (return (member findTemplatesForKind (root https://www.npmjs.com/package/javascript/finder))))" and
      t = "snk" and
      result = 0.41802020202020196
      or
      repr =
        "(parameter 0 (return (member reject (return (root https://www.npmjs.com/package/orion/Deferred)))))" and
      t = "snk" and
      result = 0.42196969696969683
      or
      repr = "(parameter 0 (return (member mixin (parameter util *))))" and
      t = "snk" and
      result = 0.1823907616432784
      or
      repr = "(member services *)" and t = "snk" and result = 0.10000000000000003
      or
      repr = "(parameter 0 (return (member delete *)))" and
      t = "snk" and
      result = 0.42888800259403376
      or
      repr = "(member show *)" and t = "snk" and result = 0.08974999999999998
      or
      repr = "(parameter 0 (return (member each (root https://www.npmjs.com/package/async))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member showWhile *)))" and
      t = "snk" and
      result = 0.4791666666666665
      or
      repr = "(parameter 0 (return (member toURLPath *)))" and
      t = "snk" and
      result = 0.4066666666666667
      or
      repr = "(member offset (member position *))" and t = "snk" and result = 0.273224043715847
      or
      repr = "(parameter 1 (return (member set (member query *))))" and t = "snk" and result = 1.0
      or
      repr = "(member selectedGroupIndex (member linkedModeModel *))" and
      t = "snk" and
      result = 0.27322404371584696
      or
      repr = "(member settingsCategories *)" and t = "snk" and result = 0.06468166666666669
      or
      repr = "(return (member replace (return (member toLowerCase *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member replace (member Location *)))" and
      t = "snk" and
      result = 0.4800000000000001
      or
      repr = "(member applications *)" and t = "snk" and result = 0.1845871212121214
      or
      repr = "(parameter 0 (return (member propagate *)))" and
      t = "snk" and
      result = 0.3400000000000001
      or
      repr = "(member workspaceDir (return (member getFile *)))" and
      t = "snk" and
      result = 0.33882000000000034
      or
      repr = "(return (member split (return (member substring *))))" and
      t = "snk" and
      result = 0.6133333333333335
      or
      repr = "(member url (parameter template *))" and t = "snk" and result = 0.48533333333333323
      or
      repr = "(parameter 0 (return (member Arr (parameter exports *))))" and
      t = "snk" and
      result = 0.17281879194630997
      or
      repr = "(parameter 0 (return (member freeRepo *)))" and
      t = "snk" and
      result = 0.3419127516778524
      or
      repr =
        "(parameter 0 (return (member resolve (instance (root https://www.npmjs.com/package/orion/Deferred)))))" and
      t = "snk" and
      result = 0.235867777777778
      or
      repr = "(parameter 0 (return (member existsSync *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (parameter 1 (return (member equal *))))" and
      t = "snk" and
      result = 0.1955456823877876
      or
      repr = "(parameter 0 (return (member search *)))" and
      t = "snk" and
      result = 0.32499999999999996
      or
      repr = "(member response (parameter result (parameter 0 (return (member then *)))))" and
      t = "snk" and
      result = 0.5071907957814004
      or
      repr = "(member Location (return (parameter 0 (return (member map *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member showWhile *))))" and
      t = "snk" and
      result = 0.4040404040404042
      or
      repr = "(parameter 1 (return (member setAttribute (return (member createElement *)))))" and
      t = "snk" and
      result = 0.07901155555555528
      or
      repr = "(member search *)" and t = "snk" and result = 0.2500000000000001
      or
      repr = "(parameter 0 (return (member getWorkspace *)))" and t = "snk" and result = 0.1875
      or
      repr = "(parameter 0 (parameter 0 (return (member open (global)))))" and
      t = "snk" and
      result = 0.6666666666666666
      or
      repr = "(member Source *)" and t = "snk" and result = 0.5
      or
      repr = "(return (member then (return (member resolve *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member open *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter setText *)))" and
      t = "snk" and
      result = 0.5283333333333333
      or
      repr = "(parameter 0 (return (member resolve (return (member defer *)))))" and
      t = "snk" and
      result = 0.14233333333333326
      or
      repr = "(parameter 0 (return (member querySelector (member document (global)))))" and
      t = "snk" and
      result = 0.04799999999999993
      or
      repr = "(member mediaElement *)" and t = "snk" and result = 1.0
      or
      repr = "(member userid (parameter e *))" and t = "snk" and result = 0.33333333333333337
      or
      repr = "(return (member attr (return (member $ (global)))))" and t = "snk" and result = 1.0
      or
      repr = "(return (member ajax (member $ (global))))" and t = "snk" and result = 0.5
      or
      repr = "(member extra *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(member mediaElement (parameter e (member onstreamended (parameter connection *))))" and
      t = "snk" and
      result = 0.6666666666666661
      or
      repr = "(member thumb *)" and t = "snk" and result = 0.12030075187969927
      or
      repr = "(return (member serializeSdp *))" and t = "snk" and result = 0.07000000000000006
      or
      repr = "(member name *)" and t = "snk" and result = 0.3487673244068593
      or
      repr = "(parameter 0 (return (member log *)))" and t = "snk" and result = 1.0
      or
      repr = "(member 1 (parameter match *))" and t = "snk" and result = 0.3181818181818182
      or
      repr = "(parameter 0 (return (member getElementById *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member union (root https://www.npmjs.com/package/underscore))))" and
      t = "snk" and
      result = 0.2857142857142857
      or
      repr = "(member host (member headers *))" and t = "snk" and result = 0.28571428571428575
      or
      repr = "(member slug (member _artist *))" and t = "snk" and result = 0.5
      or
      repr = "(member _id (member _artist *))" and t = "snk" and result = 0.5
      or
      repr = "(return (member get (root https://www.npmjs.com/package/restler)))" and
      t = "snk" and
      result = 0.5499999999999998
      or
      repr = "(parameter 0 (return (member redirect *)))" and
      t = "snk" and
      result = 0.21428571428571436
      or
      repr = "(return (member findOne (member Playlist (global))))" and
      t = "snk" and
      result = 0.1633333333333331
      or
      repr = "(parameter 0 (return (member get (root https://www.npmjs.com/package/restler))))" and
      t = "snk" and
      result = 0.55
      or
      repr = "(parameter 0 (return (member find (member Person (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member findOne (member Playlist (global)))))" and
      t = "snk" and
      result = 0.8866666666666665
      or
      repr = "(parameter 0 (return (member find (member Playlist (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member encodeURIComponent (global))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(parameter 0 (return (member find (member Play (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member count *))" and t = "snk" and result = 0.2500000000000001
      or
      repr = "(parameter 0 (return (member count *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member find (member Track (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member playlist *)" and t = "snk" and result = 0.25
      or
      repr = "(return (member union *))" and t = "snk" and result = 0.43290043290043284
      or
      repr = "(member id (parameter 0 (parameter 1 (return (member on *)))))" and
      t = "snk" and
      result = 0.2857142857142857
      or
      repr = "(parameter 0 (return (member find (member Chat (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member current *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member set *)))" and t = "snk" and result = 0.5
      or
      repr = "(member search (member location *))" and t = "snk" and result = 0.5000000000000004
      or
      repr = "(return (member expect (global)))" and t = "snk" and result = 0.25
      or
      repr = "(member nodeType *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 1 (return (member log *)))" and t = "snk" and result = 0.49999999999999994
      or
      repr = "(parameter 0 (return (member cleanForBridge *)))" and
      t = "snk" and
      result = 0.17171937178673233
      or
      repr = "(parameter 0 (return (parameter fn *)))" and t = "snk" and result = 0.5
      or
      repr = "(member search (member location (member window (global))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member toErrorDev *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (parameter 0 (return (member from *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member statSync *)))" and
      t = "snk" and
      result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (member createRoot (member ReactDOM (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member cleanForBridge *))" and t = "snk" and result = 0.17171937178673233
      or
      repr = "(member _dispatchInstances (parameter event *))" and
      t = "snk" and
      result = 0.3817504655493483
      or
      repr = "(parameter 0 (return (member it (global))))" and t = "snk" and result = 0.25
      or
      repr =
        "(return (member unstable_createEventHandle (root https://www.npmjs.com/package/react-dom)))" and
      t = "snk" and
      result = 0.4999999999999999
      or
      repr = "(member _dispatchListeners (parameter event *))" and
      t = "snk" and
      result = 0.38175046554934816
      or
      repr = "(parameter 0 (instance (member Set (global))))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(return (member leave *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member setAttribute (parameter el *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member document (parameter window *))" and t = "snk" and result = 0.5111538461538462
      or
      repr = "(parameter 0 (return (member val *)))" and t = "snk" and result = 0.5833333333333333
      or
      repr = "(parameter 0 (return (member $watch (parameter scope *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member replace (member name *)))" and
      t = "snk" and
      result = 0.7424242424242427
      or
      repr = "(parameter 0 (return (member append (return (member find *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member location *)" and t = "snk" and result = 0.015151515151515206
      or
      repr = "(member $$path *)" and t = "snk" and result = 0.25
      or
      repr = "(member exp *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member redirect (parameter res *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getPostByPermalink (return (member PostsDAO *)))))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(parameter 0 (return (member getPostByPermalink (instance (member PostsDAO *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getPostByPermalink *)))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(parameter 0 (return (member trim (member constructor (member fn *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (parameter $interpolate *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member merge (member jQuery *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member merge (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member css *)))" and t = "snk" and result = 0.3749999999999999
      or
      repr = "(parameter 1 (return (member $on *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member inArray *)))" and
      t = "snk" and
      result = 0.08250000000000002
      or
      repr = "(parameter 0 (return (member warn *)))" and t = "snk" and result = 0.25
      or
      repr = "(member 0 (return (member match (return (member replace *)))))" and
      t = "snk" and
      result = 0.5280736595053257
      or
      repr = "(parameter 1 (return (member contains (parameter jQuery *))))" and
      t = "snk" and
      result = 0.16500000000000004
      or
      repr = "(parameter 1 (return (member merge *)))" and
      t = "snk" and
      result = 0.25000000000000006
      or
      repr = "(parameter 2 (return (return (member $$minErr *))))" and
      t = "snk" and
      result = 0.1320184148763315
      or
      repr = "(parameter 0 (return (member max *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member trim (return (member noConflict *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member appendChild (return (member createDocumentFragment *)))))" and
      t = "snk" and
      result = 0.4311999999999999
      or
      repr = "(parameter 0 (return (member trim (member jQuery *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 0.9162844036697261
      or
      repr = "(parameter 1 (return (member merge (member $ (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member heading *)" and t = "snk" and result = 0.5833333333333333
      or
      repr = "(parameter 0 (return (member merge *)))" and
      t = "snk" and
      result = 0.16500000000000004
      or
      repr = "(member old *)" and t = "snk" and result = 0.5102040816326531
      or
      repr = "(parameter 0 (return (member cancel (parameter $timeout *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member trim (member $ *))))" and t = "snk" and result = 1.0
      or
      repr = "(member namespace *)" and t = "snk" and result = 0.20230263157894743
      or
      repr = "(parameter 1 (return (member merge (return (member noConflict *)))))" and
      t = "snk" and
      result = 0.9207673060884095
      or
      repr = "(parameter 0 (return (member $watch (parameter 0 (member pre *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member elem *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member interpolate (global))))" and t = "snk" and result = 0.125
      or
      repr = "(return (member map (return (member map *))))" and
      t = "snk" and
      result = 0.02410901467505233
      or
      repr = "(return (member build *))" and t = "snk" and result = 0.1475
      or
      repr = "(parameter 3 (return (member parseAccessMemberOrMethodCall *)))" and
      t = "snk" and
      result = 0.10839160839160839
      or
      repr = "(return (member _codegenSourceModule *))" and
      t = "snk" and
      result = 0.06555627791282716
      or
      repr = "(return (member map (return (member get *))))" and
      t = "snk" and
      result = 0.16666666666666663
      or
      repr = "(member nativeNode *)" and t = "snk" and result = 0.3
      or
      repr = "(member name (parameter el *))" and t = "snk" and result = 0.753012048192771
      or
      repr = "(parameter 1 (return (member parseAccessMemberOrMethodCall (member prototype *))))" and
      t = "snk" and
      result = 0.10839160839160839
      or
      repr = "(parameter 1 (return (member insertBefore *)))" and
      t = "snk" and
      result = 0.33333333333333337
      or
      repr = "(parameter 2 (return (member parseCallChain *)))" and
      t = "snk" and
      result = 0.10839160839160839
      or
      repr = "(parameter 2 (return (member defineMetadata (member Reflect (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member placeholders *)" and t = "snk" and result = 0.25
      or
      repr = "(member _dom *)" and t = "snk" and result = 0.14750000000000002
      or
      repr = "(parameter 1 (return (member parseCallChain *)))" and
      t = "snk" and
      result = 0.10839160839160839
      or
      repr = "(parameter 2 (return (member defineMetadata (member Reflect *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member parsePipe *))" and t = "snk" and result = 0.10839160839160836
      or
      repr = "(parameter 0 (return (parameter $ *)))" and t = "snk" and result = 0.3787878787878788
      or
      repr = "(parameter 0 (return (member build *)))" and
      t = "snk" and
      result = 0.14750000000000002
      or
      repr = "(parameter 1 (return (member menu *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member _outerWidth (return (member find *)))))" and
      t = "snk" and
      result = 0.09999999999999953
      or
      repr =
        "(parameter 0 (return (member extend (root https://www.npmjs.com/package/underscore))))" and
      t = "snk" and
      result = 0.24293785310734484
      or
      repr = "(parameter 0 (return (member html (return (member appendTo *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (parameter cb *)))" and t = "snk" and result = 0.25
      or
      repr = "(return (member $ (global)))" and t = "snk" and result = 0.25
      or
      repr = "(member left *)" and t = "snk" and result = 0.09090909090909088
      or
      repr = "(parameter 4 (instance (member Date (global))))" and
      t = "snk" and
      result = 0.40000000000000047
      or
      repr = "(parameter 1 (return (member where *)))" and
      t = "snk" and
      result = 0.16666666666666663
      or
      repr = "(parameter 0 (return (member extend *)))" and t = "snk" and result = 0.67277891750384
      or
      repr = "(member validator *)" and t = "snk" and result = 0.125
      or
      repr = "(parameter 5 (return (member Date (global))))" and
      t = "snk" and
      result = 0.40000000000000047
      or
      repr = "(parameter 0 (return (member count (return (member model *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member add (return (member Schema *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member onBeforeValidate *)" and t = "snk" and result = 0.125
      or
      repr = "(member field *)" and t = "snk" and result = 0.07486647525547221
      or
      repr = "(parameter 0 (return (member eval (global))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (parameter cb (member getDataForExcel *))))" and
      t = "snk" and
      result = 0.5075757575757575
      or
      repr = "(member startLeft *)" and t = "snk" and result = 0.09090909090909088
      or
      repr = "(parameter 1 (return (member textbox (return (parameter $ *)))))" and
      t = "snk" and
      result = 0.8333333333333334
      or
      repr = "(return (member limit *))" and t = "snk" and result = 0.3600000000000002
      or
      repr =
        "(parameter 1 (return (member extend (root https://www.npmjs.com/package/underscore))))" and
      t = "snk" and
      result = 0.3833333333333333
      or
      repr = "(parameter 0 (return (member limit *)))" and
      t = "snk" and
      result = 0.36000000000000015
      or
      repr = "(member startTop *)" and t = "snk" and result = 0.09090909090909088
      or
      repr = "(return (member _outerWidth (return (member find *))))" and
      t = "snk" and
      result = 0.40000000000000047
      or
      repr = "(parameter 3 (return (member Date (global))))" and
      t = "snk" and
      result = 0.40000000000000047
      or
      repr = "(member onValidate *)" and t = "snk" and result = 0.125
      or
      repr = "(parameter 2 (return (member _trigger *)))" and
      t = "snk" and
      result = 0.4310902255639098
      or
      repr = "(parameter 0 (return (member trim (member tools *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member createFromHtml (member element *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setStartAt (return (member clone *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member match (return (member toLowerCase *)))))" and
      t = "snk" and
      result = 0.7500000000000002
      or
      repr = "(parameter 6 (return (member call *)))" and t = "snk" and result = 0.31249999999999994
      or
      repr = "(parameter 0 (return (member replace (parameter text *))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 1 (return (member extend (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (return (member extend *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member addMenuItem *)))" and t = "snk" and result = 0.6
      or
      repr = "(member console (global))" and t = "snk" and result = 0.6000000000000001
      or
      repr = "(parameter 1 (return (member data (member $element *))))" and
      t = "snk" and
      result = 0.9384893713251924
      or
      repr = "(parameter 0 (return (member toValue *)))" and t = "snk" and result = 1.0
      or
      repr = "(return (member output (return (member addTemplate *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member insertNode (return (member cloneRange *)))))" and
      t = "snk" and
      result = 0.6824829758143086
      or
      repr = "(parameter 1 (return (member inArray *)))" and
      t = "snk" and
      result = 0.7719988344988344
      or
      repr = "(parameter 0 (return (member appendTo *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member show *)))" and t = "snk" and result = 0.25
      or
      repr = "(member version *)" and t = "snk" and result = 0.07666666666666672
      or
      repr =
        "(parameter 0 (return (member replaceChild (member parentNode (parameter element *)))))" and
      t = "snk" and
      result = 0.7272727272727274
      or
      repr = "(member data (parameter 0 (parameter 1 (return (member on *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member addMenuItem *)))" and t = "snk" and result = 0.15
      or
      repr = "(parameter 0 (parameter 0 (return (member fromCharCode *))))" and
      t = "snk" and
      result = 0.14560606060606057
      or
      repr = "(member release *)" and t = "snk" and result = 0.07500000000000004
      or
      repr = "(parameter 1 (return (member replaceRange *)))" and
      t = "snk" and
      result = 0.1813186813186813
      or
      repr = "(member 1 (return (member match (return (member replace *)))))" and
      t = "snk" and
      result = 0.5949999999999999
      or
      repr = "(parameter 0 (return (member translate *)))" and
      t = "snk" and
      result = 0.32907790691989935
      or
      repr = "(parameter 0 (return (member isArray (member angular *))))" and
      t = "snk" and
      result = 0.2925
      or
      repr = "(parameter 0 (parameter 0 (return (member createFromHtml *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member elements *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member pow *)))" and t = "snk" and result = 1.0
      or
      repr = "(member createElement *)" and t = "snk" and result = 0.15822784810126583
      or
      repr = "(return (member load *))" and t = "snk" and result = 0.13661202185792348
      or
      repr = "(member stats (member queue *))" and t = "snk" and result = 0.017500000000000016
      or
      repr = "(parameter 1 (return (member data (parameter $ *))))" and
      t = "snk" and
      result = 0.08991134179104068
      or
      repr = "(member line (return (member getCursor (parameter cm *))))" and
      t = "snk" and
      result = 0.5494505494505494
      or
      repr = "(member startContainer (parameter a *))" and t = "snk" and result = 0.7956807692307692
      or
      repr = "(member index (return (member match *)))" and
      t = "snk" and
      result = 0.2779195488721804
      or
      repr = "(return (member eq (return (member jQuery (global)))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member build *)))" and t = "snk" and result = 0.75
      or
      repr = "(member plugin *)" and t = "snk" and result = 1.0
      or
      repr =
        "(member string (instance (member StringStream (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member fire *)))" and t = "snk" and result = 0.65625
      or
      repr = "(parameter 1 (return (member RegExp (global))))" and
      t = "snk" and
      result = 0.33620689655172403
      or
      repr = "(member location (global))" and t = "snk" and result = 0.6000000000000001
      or
      repr = "(parameter 2 (return (member replaceRange *)))" and
      t = "snk" and
      result = 0.18131868131868134
      or
      repr = "(parameter 2 (return (member rect (member renderer *))))" and
      t = "snk" and
      result = 0.1363636363636361
      or
      repr = "(member attributes *)" and t = "snk" and result = 0.515151515151515
      or
      repr = "(member 0 (return (member split (parameter a *))))" and
      t = "snk" and
      result = 0.1893939393939394
      or
      repr = "(parameter 0 (return (member getMDom (member tools (member _z *)))))" and
      t = "snk" and
      result = 0.04545454545454516
      or
      repr = "(return (member setTimeout (global)))" and t = "snk" and result = 0.0362216304367467
      or
      repr = "(member data (parameter a *))" and t = "snk" and result = 0.7100000000000004
      or
      repr = "(return (member map (return (member find *))))" and
      t = "snk" and
      result = 0.031369548872180575
      or
      repr = "(parameter 1 (return (member bind (return (member bind *)))))" and
      t = "snk" and
      result = 0.008580517227133766
      or
      repr = "(return (member child *))" and t = "snk" and result = 0.09722222222222224
      or
      repr = "(member URL *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member transformToArrayFormat (member data (member _z *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (parameter arr *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member apply (member push *))))" and t = "snk" and result = 0.25
      or
      repr = "(member level *)" and t = "snk" and result = 0.3922459899749373
      or
      repr = "(member modes *)" and t = "snk" and result = 0.018749999999999933
      or
      repr = "(return (member $ (member tools *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member setText (return (member getElement *)))))" and
      t = "snk" and
      result = 0.4342308897243108
      or
      repr = "(member helper *)" and t = "snk" and result = 0.11200375851538644
      or
      repr = "(member gutters (parameter a *))" and t = "snk" and result = 0.3451215300662474
      or
      repr = "(return (member append (return (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 0.5050505050505053
      or
      repr = "(parameter 0 (return (member each (member HighchartsAdapter *))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member ltrim *)))" and t = "snk" and result = 0.4848484848484851
      or
      repr = "(member player *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member moveToElementEditStart *)))" and
      t = "snk" and
      result = 0.125
      or
      repr = "(parameter 0 (return (member extend (member tools *))))" and
      t = "snk" and
      result = 0.3846717908119658
      or
      repr = "(member player (return (member w (member Ud *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (instance (member element *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member css *)))" and t = "snk" and result = 0.08830012474660873
      or
      repr = "(parameter 0 (return (member addCommand *)))" and t = "snk" and result = 0.15
      or
      repr = "(member player (return (member w (member Td (global)))))" and
      t = "snk" and
      result = 0.9411764705882347
      or
      repr = "(member address (member email *))" and t = "snk" and result = 0.5000000000000001
      or
      repr = "(member active *)" and t = "snk" and result = 0.4838636363636363
      or
      repr = "(member path (return (member call *)))" and t = "snk" and result = 0.20588235294117666
      or
      repr = "(member relativePath *)" and t = "snk" and result = 0.11333333333333329
      or
      repr = "(parameter 0 (return (member html (return (parameter t *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member parse (member Date (global)))))" and
      t = "snk" and
      result = 0.4883333333333334
      or
      repr = "(member step (member options *))" and t = "snk" and result = 0.19073727727154205
      or
      repr = "(parameter 0 (return (member wrap *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member request *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member transformToArrayFormat (member data *))))" and
      t = "snk" and
      result = 0.554610788404712
      or
      repr = "(return (member wrap *))" and t = "snk" and result = 0.3787878787878788
      or
      repr = "(return (member call (member toString *)))" and
      t = "snk" and
      result = 0.5075757575757576
      or
      repr = "(member fontFamily (member style *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member addFeature *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member $emit *)))" and t = "snk" and result = 0.619402985074627
      or
      repr = "(member uploader *)" and t = "snk" and result = 0.12875000000000003
      or
      repr = "(parameter 2 (return (parameter iterator (member each *))))" and
      t = "snk" and
      result = 0.732142857142857
      or
      repr = "(member document *)" and t = "snk" and result = 0.08884615384615391
      or
      repr = "(member fa *)" and t = "snk" and result = 0.375
      or
      repr = "(return (member replace (parameter text *)))" and t = "snk" and result = 0.5
      or
      repr = "(member dataTransfer *)" and t = "snk" and result = 0.7400000000000002
      or
      repr = "(parameter 1 (return (member setCustomData *)))" and t = "snk" and result = 0.75
      or
      repr = "(return (member split (parameter b *)))" and t = "snk" and result = 0.73
      or
      repr = "(parameter 0 (instance (member text *)))" and t = "snk" and result = 0.25
      or
      repr = "(member className (parameter a *))" and t = "snk" and result = 0.06390977443609014
      or
      repr = "(return (member replace (member href *)))" and
      t = "snk" and
      result = 0.34343434343434326
      or
      repr = "(parameter 0 (return (member clearTimeout (parameter window *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member each (parameter $ *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member removeChild (member parentNode *))))" and
      t = "snk" and
      result = 0.3274999999999999
      or
      repr = "(member firstChild (return (member createElement (member document *))))" and
      t = "snk" and
      result = 0.19189006664581254
      or
      repr = "(parameter 0 (return (member fn *)))" and t = "snk" and result = 0.2372717418546366
      or
      repr = "(parameter 0 (return (parameter 1 (member each *))))" and
      t = "snk" and
      result = 0.5494505494505492
      or
      repr = "(member class (member attributes *))" and t = "snk" and result = 1.0
      or
      repr = "(member 2 (return (member match (parameter a *))))" and t = "snk" and result = 1.0
      or
      repr = "(member preview *)" and t = "snk" and result = 0.3333333333333333
      or
      repr = "(parameter 2 (return (member style (member jQuery *))))" and
      t = "snk" and
      result = 0.272727272727273
      or
      repr = "(return (member replace (return (member encodeURIComponent (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member classList (parameter element *))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member output (return (member addTemplate *)))))" and
      t = "snk" and
      result = 0.8707070707070708
      or
      repr = "(parameter 1 (return (member loadFile *)))" and t = "snk" and result = 0.5
      or
      repr = "(member old (return (member extend (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member path (return (member call (member applyOptions *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setHtml (return (member getElement *)))))" and
      t = "snk" and
      result = 0.2575757575757575
      or
      repr =
        "(parameter 0 (return (member html (return (root https://www.npmjs.com/package/jquery)))))" and
      t = "snk" and
      result = 0.7575757575757573
      or
      repr = "(parameter 0 (parameter 0 (return (member load *))))" and t = "snk" and result = 1.0
      or
      repr = "(member offsetWidth *)" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(member frag *)" and t = "snk" and result = 0.31645569620253156
      or
      repr = "(parameter 0 (return (member forEach (member angular (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (instance (member element (member dom *))))" and
      t = "snk" and
      result = 0.2500000000000002
      or
      repr = "(member string *)" and t = "snk" and result = 0.17999999999999994
      or
      repr = "(parameter 0 (return (member getUrl *)))" and
      t = "snk" and
      result = 0.8707070707070708
      or
      repr = "(parameter 2 (return (member style (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member sel (member doc (parameter a *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(return (member extend (member tools (member CKEDITOR (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (parameter members (member extend *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member checkBoundaryOfElement *)))" and
      t = "snk" and
      result = 0.18499999999999983
      or
      repr = "(member $ (parameter a *))" and t = "snk" and result = 0.25
      or
      repr = "(return (member invoke (parameter $injector *)))" and t = "snk" and result = 0.7
      or
      repr = "(return (member jQuery (global)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member isInsidePlot (member chart *))))" and
      t = "snk" and
      result = 0.4189944134078212
      or
      repr = "(parameter 0 (return (member getChild *)))" and
      t = "snk" and
      result = 0.4166666666666667
      or
      repr = "(parameter 0 (instance (member element (member htmlParser *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member data-cke-saved-href (member set (return (member getLinkAttributes *))))" and
      t = "snk" and
      result = 0.6250000000000002
      or
      repr = "(parameter 0 (return (member html (return (parameter $ *)))))" and
      t = "snk" and
      result = 0.7369235588972431
      or
      repr = "(member href (return (member createElement (member document (global)))))" and
      t = "snk" and
      result = 0.06060606060606015
      or
      repr = "(return (parameter $ *))" and t = "snk" and result = 0.5095192307692308
      or
      repr = "(parameter 3 (return (member arc *)))" and t = "snk" and result = 0.25
      or
      repr = "(member 2 (return (member match *)))" and t = "snk" and result = 0.27471643142064656
      or
      repr = "(parameter 1 (return (member setChkClass (member view (member _z *)))))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(parameter 1 (return (member option *)))" and t = "snk" and result = 0.51
      or
      repr = "(member el *)" and t = "snk" and result = 0.16666666666666663
      or
      repr = "(return (member $ (member tools (member _z *))))" and
      t = "snk" and
      result = 0.33110802005012535
      or
      repr = "(member innerHTML (return (member getDom *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (return (member extend (member Class *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member curHoverNode (return (member getRoot *)))" and
      t = "snk" and
      result = 0.8928571428571428
      or
      repr = "(parameter 1 (return (member data (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member append (return (root https://www.npmjs.com/package/jquery)))))" and
      t = "snk" and
      result = 0.5050505050505049
      or
      repr = "(parameter 0 (return (member clearInterval *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member each (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 0.30633401686033235
      or
      repr = "(member href (member attributes *))" and t = "snk" and result = 0.15342100247760623
      or
      repr = "(return (member clazz *))" and t = "snk" and result = 0.0972222222222222
      or
      repr = "(member _resultId *)" and t = "snk" and result = 0.44499999999999995
      or
      repr = "(parameter 0 (return (member setStartAfter *)))" and
      t = "snk" and
      result = 0.43939393939393945
      or
      repr = "(member component *)" and t = "snk" and result = 0.27322404371584696
      or
      repr = "(parameter 0 (return (member append (instance (member documentFragment *)))))" and
      t = "snk" and
      result = 0.315
      or
      repr = "(member view (member display (instance (member constructor (member prototype *)))))" and
      t = "snk" and
      result = 0.4082471089470482
      or
      repr = "(member duration (member options *))" and t = "snk" and result = 0.19073727727154208
      or
      repr = "(parameter 0 (return (member insertNode *)))" and t = "snk" and result = 0.5
      or
      repr = "(return (member stylize *))" and t = "snk" and result = 0.09722222222222224
      or
      repr = "(member paddingRight *)" and t = "snk" and result = 0.18365472910927455
      or
      repr = "(member cc *)" and t = "snk" and result = 0.4101966372180452
      or
      repr = "(parameter 0 (return (member extend (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member trigger (member treeObj *))))" and
      t = "snk" and
      result = 0.6699999999999999
      or
      repr = "(parameter 0 (return (member ajax (member jQuery (global)))))" and
      t = "snk" and
      result = 0.13636363636363616
      or
      repr = "(parameter 0 (return (member Pos *)))" and t = "snk" and result = 0.20250000000000012
      or
      repr = "(member loadChildren *)" and t = "snk" and result = 0.27322404371584696
      or
      repr = "(parameter 1 (return (member setAttribute (instance (member element *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member output (member _ *))" and t = "snk" and result = 0.770333333333333
      or
      repr = "(parameter 1 (return (member iter *)))" and t = "snk" and result = 0.5644230769230769
      or
      repr = "(parameter 1 (return (member setStyle *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member attachListener *)))" and t = "snk" and result = 0.455
      or
      repr = "(parameter 1 (return (member fire (parameter b *))))" and
      t = "snk" and
      result = 0.8749999999999996
      or
      repr = "(parameter 2 (return (member rect (member renderer (member chart *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member jQuery (global))))" and
      t = "snk" and
      result = 0.5347431077694237
      or
      repr = "(member refresh *)" and t = "snk" and result = 0.3333333333333333
      or
      repr = "(member string (instance (member StringStream (member constructor *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member getElementById *))" and t = "snk" and result = 0.275
      or
      repr = "(member old (return (member extend (return (member noConflict *)))))" and
      t = "snk" and
      result = 0.29166666666666674
      or
      repr = "(parameter 0 (return (member html (return (member find *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member regexp *)" and t = "snk" and result = 1.0
      or
      repr = "(member class (member attributes (parameter a *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 2 (return (member style (member constructor (member fn *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member parentNode (parameter element *))" and
      t = "snk" and
      result = 0.29999999999999993
      or
      repr = "(member dataTransfer (member data *))" and t = "snk" and result = 0.48727272727272675
      or
      repr = "(parameter 0 (return (member getData *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (parameter 0 (return (member round *))))" and
      t = "snk" and
      result = 0.16952498235267532
      or
      repr = "(parameter 0 (return (member bind (return (member bind *)))))" and
      t = "snk" and
      result = 0.008580517227133766
      or
      repr = "(parameter 0 (return (member call (member indexOf *))))" and
      t = "snk" and
      result = 0.9821442811574388
      or
      repr = "(member noop *)" and t = "snk" and result = 0.09687390460416778
      or
      repr = "(parameter 0 (return (member setStartBefore (instance (member range *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (parameter $ *)))" and t = "snk" and result = 0.2021804511278195
      or
      repr = "(return (member extend (member tools *)))" and
      t = "snk" and
      result = 0.48484848484848486
      or
      repr = "(member frontier *)" and t = "snk" and result = 0.2500000000000002
      or
      repr = "(return (member cssLength *))" and t = "snk" and result = 0.16000000000000014
      or
      repr = "(return (member eq (return (member $ (global)))))" and
      t = "snk" and
      result = 0.2500000000000002
      or
      repr = "(return (member translatePath *))" and t = "snk" and result = 0.49860288927257457
      or
      repr = "(member createDocumentFragment *)" and t = "snk" and result = 0.31645569620253156
      or
      repr =
        "(parameter 1 (return (member replaceChild (member parentNode (parameter element *)))))" and
      t = "snk" and
      result = 0.7272727272727274
      or
      repr = "(member className (parameter element *))" and
      t = "snk" and
      result = 0.06390977443609014
      or
      repr = "(member parentNode (return (member createElement (member document *))))" and
      t = "snk" and
      result = 0.4087631679581181
      or
      repr = "(return (member getPlotLinePath *))" and t = "snk" and result = 0.033754545454545475
      or
      repr = "(parameter 2 (return (member push *)))" and t = "snk" and result = 0.44499999999999995
      or
      repr = "(member helper (member current *))" and t = "snk" and result = 0.5437977681265159
      or
      repr = "(member data-cke-saved-href (member set *))" and t = "snk" and result = 1.0
      or
      repr = "(return (parameter 0 (parameter 2 (return (member define *)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member root *)" and t = "snk" and result = 0.13461538461538472
      or
      repr = "(member nodeName *)" and t = "snk" and result = 0.08333333333333333
      or
      repr = "(parameter 1 (return (member isInsidePlot (instance (member Series *)))))" and
      t = "snk" and
      result = 0.4189944134078212
      or
      repr = "(parameter 0 (return (member on *)))" and t = "snk" and result = 0.25
      or
      repr = "(member delay (member options *))" and t = "snk" and result = 0.026001567354950805
      or
      repr = "(member old (return (member speed (member jQuery (parameter window *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member build *)))" and t = "snk" and result = 0.125
      or
      repr = "(member distance *)" and t = "snk" and result = 0.5
      or
      repr = "(member prefix *)" and t = "snk" and result = 0.08180527062404
      or
      repr = "(return (member abs (member Math (global))))" and
      t = "snk" and
      result = 0.07417778836987612
      or
      repr = "(parameter 0 (return (member trigger (member element *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member addClass (return (member removeClass *)))))" and
      t = "snk" and
      result = 0.7213855421686747
      or
      repr = "(member init *)" and t = "snk" and result = 0.12833333333333358
      or
      repr = "(parameter 0 (return (member load (member scriptLoader *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member arc *)))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(member action *)" and t = "snk" and result = 0.4800000000000001
      or
      repr = "(parameter 1 (return (member setAttribute (return (member element *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member indexOf (member tools *))))" and
      t = "snk" and
      result = 0.6725368586300917
      or
      repr = "(parameter 0 (return (return (member bookmark *))))" and t = "snk" and result = 1.0
      or
      repr = "(member editor *)" and t = "snk" and result = 0.4611392472658921
      or
      repr = "(parameter 0 (return (member setHtml (instance (member element *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member join (parameter a *)))" and t = "snk" and result = 0.2878787878787876
      or
      repr = "(parameter 0 (return (member createFromHtml (member element (member dom *)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member isString (member angular *))))" and
      t = "snk" and
      result = 0.7925
      or
      repr = "(member mainContainer *)" and t = "snk" and result = 0.36083107788303215
      or
      repr = "(parameter 0 (return (member setHtml (return (member createElement *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member endContainer *)" and t = "snk" and result = 0.5
      or
      repr = "(member highlightedCode *)" and t = "snk" and result = 0.27322404371584696
      or
      repr = "(parameter 0 (return (member equals (member startContainer *))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(member frontier (member doc *))" and t = "snk" and result = 1.0
      or
      repr = "(return (member createFromHtml *))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(parameter 1 (return (member bind *)))" and t = "snk" and result = 0.0362216304367467
      or
      repr = "(parameter 0 (return (return (member createReporter *))))" and
      t = "snk" and
      result = 0.23838193366730637
      or
      repr = "(parameter 4 (parameter 0 (return (member _register *))))" and
      t = "snk" and
      result = 0.05295994387152858
      or
      repr = "(parameter 0 (return (member error (member notificationService *))))" and
      t = "snk" and
      result = 0.6666666666666666
      or
      repr = "(member className (return (member createElement (member document (global)))))" and
      t = "snk" and
      result = 0.1999999999999999
      or
      repr =
        "(parameter 0 (return (member parse (member URI (root https://www.npmjs.com/package/vs/base/common/uri)))))" and
      t = "snk" and
      result = 0.29091651130218565
      or
      repr = "(parameter 0 (return (member require *)))" and
      t = "snk" and
      result = 0.39879699248120304
      or
      repr = "(parameter 0 (return (member encodeURI (global))))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member get (member cache *))))" and
      t = "snk" and
      result = 0.30000000000000004
      or
      repr = "(member _verticalScrollbar *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member lastIndexOf *)))" and
      t = "snk" and
      result = 0.30272912782554656
      or
      repr =
        "(parameter 1 (return (member spawn (root https://www.npmjs.com/package/child_process))))" and
      t = "snk" and
      result = 0.061652625257920535
      or
      repr = "(member fire *)" and t = "snk" and result = 0.19999999999999998
      or
      repr = "(parameter 1 (return (member executeEdits *)))" and
      t = "snk" and
      result = 0.21369495961534507
      or
      repr = "(parameter 1 (return (member publicLogError *)))" and
      t = "snk" and
      result = 0.10815762600961637
      or
      repr = "(parameter 0 (return (member executeTask (member tasks *))))" and
      t = "snk" and
      result = 0.34484378670425164
      or
      repr = "(member initialTarget *)" and t = "snk" and result = 0.6666666666666666
      or
      repr = "(parameter 0 (return (member writeFile (member fileService *))))" and
      t = "snk" and
      result = 0.5343255639006879
      or
      repr = "(parameter 1 (parameter 0 (return (member file (member URI *)))))" and
      t = "snk" and
      result = 0.9049999999999998
      or
      repr = "(member stderr *)" and t = "snk" and result = 0.0727135001683313
      or
      repr = "(parameter 1 (return (member writeHead *)))" and
      t = "snk" and
      result = 0.419054939539642
      or
      repr = "(parameter 0 (return (member executeTask *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member renameFile *)))" and
      t = "snk" and
      result = 0.7575757575757577
      or
      repr = "(return (member create *))" and t = "snk" and result = 0.33999999999999997
      or
      repr = "(member _notificationService *)" and t = "snk" and result = 0.16666666666666666
      or
      repr = "(parameter 0 (return (member applyFontInfoSlow *)))" and
      t = "snk" and
      result = 0.515151515151515
      or
      repr = "(parameter 0 (return (member dispatchEvent *)))" and
      t = "snk" and
      result = 0.33333333333333326
      or
      repr =
        "(instance (member LinkedList (root https://www.npmjs.com/package/vs/base/common/linkedList)))" and
      t = "snk" and
      result = 0.6666666666666667
      or
      repr = "(member errors *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(member _textAreaInput *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member set (member channels *))))" and
      t = "snk" and
      result = 0.3227291278255464
      or
      repr = "(parameter 0 (return (member with *)))" and t = "snk" and result = 0.5000000000000001
      or
      repr = "(member _onProcessReady *)" and t = "snk" and result = 0.01008015006821289
      or
      repr = "(parameter 0 (return (member with (return (member parse *)))))" and
      t = "snk" and
      result = 0.3520048974594426
      or
      repr = "(return (member getCursorStates *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 2 (member placeHolder *))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member pushAll *)))" and
      t = "snk" and
      result = 0.01764480575420524
      or
      repr = "(parameter 0 (return (parameter 1 (return (member exec *)))))" and
      t = "snk" and
      result = 0.838109879079284
      or
      repr = "(parameter 0 (member size *))" and t = "snk" and result = 0.2424242424242423
      or
      repr = "(return (member from *))" and t = "snk" and result = 0.7466345371612465
      or
      repr = "(parameter 2 (return (member deepEqual *)))" and
      t = "snk" and
      result = 0.6442198328384785
      or
      repr = "(parameter 1 (return (member publicLog2 (member telemetryService *))))" and
      t = "snk" and
      result = 0.16000000000000003
      or
      repr = "(member filterText *)" and t = "snk" and result = 0.0625
      or
      repr = "(parameter 1 (return (member executeEdits (parameter editor *))))" and
      t = "snk" and
      result = 0.11008528222608682
      or
      repr = "(member lines *)" and t = "snk" and result = 0.07357720568971869
      or
      repr =
        "(parameter 1 (return (member join (root https://www.npmjs.com/package/vs/base/common/path))))" and
      t = "snk" and
      result = 0.052630504038465725
      or
      repr = "(member mouseColumn *)" and t = "snk" and result = 0.15625
      or
      repr = "(parameter 0 (return (member setDragImage *)))" and t = "snk" and result = 0.3
      or
      repr = "(member installed *)" and t = "snk" and result = 0.1377720965393143
      or
      repr =
        "(parameter 0 (return (member file (member URI (root https://www.npmjs.com/package/vs/base/common/uri)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member showQuickPick (member window *))))" and
      t = "snk" and
      result = 0.27272727272727243
      or
      repr = "(member innerText *)" and t = "snk" and result = 0.75
      or
      repr = "(parameter 0 (return (member parse (member URI *))))" and t = "snk" and result = 1.0
      or
      repr = "(member target *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member trace (parameter logService *))))" and
      t = "snk" and
      result = 0.061652625257920535
      or
      repr =
        "(parameter 0 (return (member addStandardDisposableListener (root https://www.npmjs.com/package/vs/base/browser/dom))))" and
      t = "snk" and
      result = 0.984848484848485
      or
      repr = "(member _horizontalScrollbar *)" and t = "snk" and result = 0.5
      or
      repr =
        "(parameter 0 (return (member from (member URI (root https://www.npmjs.com/package/vs/base/common/uri)))))" and
      t = "snk" and
      result = 0.6895806550352002
      or
      repr =
        "(return (member replace (member EditOperation (root https://www.npmjs.com/package/vs/editor/common/core/editOperation))))" and
      t = "snk" and
      result = 0.22003054778578746
      or
      repr = "(parameter 0 (return (member writeText *)))" and
      t = "snk" and
      result = 0.4600000000000003
      or
      repr = "(parameter 0 (return (member with (parameter uri *))))" and
      t = "snk" and
      result = 0.7040097949188853
      or
      repr = "(member stdout (return (member spawn *)))" and
      t = "snk" and
      result = 0.09341306857260695
      or
      repr = "(member mime *)" and t = "snk" and result = 0.23920000000000008
      or
      repr = "(parameter 0 (return (member extname *)))" and
      t = "snk" and
      result = 0.15122442019099594
      or
      repr = "(parameter 0 (return (member delete (member cache *))))" and
      t = "snk" and
      result = 0.2636354360872268
      or
      repr = "(return (member pipe *))" and t = "snk" and result = 0.0411534691600993
      or
      repr = "(parameter 0 (return (member toResource *)))" and t = "snk" and result = 0.375
      or
      repr = "(parameter 1 (return (member joinPath *)))" and
      t = "snk" and
      result = 0.7280744360993127
      or
      repr =
        "(parameter 1 (return (member executeCommand (member commands (root https://www.npmjs.com/package/vscode)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 1 (return (member showQuickPick *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member add (instance (member DisposableStore *)))))" and
      t = "snk" and
      result = 0.7279670540168073
      or
      repr = "(parameter 2 (return (member assign *)))" and
      t = "snk" and
      result = 0.4600000000000003
      or
      repr = "(parameter 0 (return (member applyEdits *)))" and
      t = "snk" and
      result = 0.3522452583626364
      or
      repr = "(parameter 0 (parameter 0 (return (member exists *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member fire (member _onChange *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member pattern (parameter query *))" and t = "snk" and result = 0.13333333333333336
      or
      repr = "(parameter 0 (return (member define (global))))" and t = "snk" and result = 0.41
      or
      repr = "(member range (member parent (member parent (member parent *))))" and
      t = "snk" and
      result = 0.9902000816659859
      or
      repr = "(member dependencies *)" and t = "snk" and result = 0.1859633611935869
      or
      repr = "(member _onProcessOverrideDimensions *)" and
      t = "snk" and
      result = 0.21026603001364252
      or
      repr = "(parameter 0 (return (member request (root https://www.npmjs.com/package/https))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(parameter 0 (return (member setCollapsed *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member intersect (member Range *))))" and
      t = "snk" and
      result = 0.18005815733088504
      or
      repr = "(parameter 0 (return (member fromString *)))" and t = "snk" and result = 0.75
      or
      repr = "(member identifier *)" and t = "snk" and result = 0.14545825565109283
      or
      repr = "(return (member file (member Uri *)))" and t = "snk" and result = 0.6473801093552327
      or
      repr = "(parameter 0 (parameter 0 (return (member add *))))" and t = "snk" and result = 0.5
      or
      repr = "(member previousCacheState *)" and t = "snk" and result = 0.19999999999999998
      or
      repr = "(parameter 0 (return (member openEditor (member editorService *))))" and
      t = "snk" and
      result = 0.9800000000000001
      or
      repr = "(member targetRange *)" and t = "snk" and result = 0.07915470605730915
      or
      repr = "(member master *)" and t = "snk" and result = 0.15625
      or
      repr = "(return (member localize (root https://www.npmjs.com/package/vs/nls)))" and
      t = "snk" and
      result = 0.5105495820961963
      or
      repr = "(parameter 0 (return (member call (member hasOwnProperty *))))" and
      t = "snk" and
      result = 0.8434343434343436
      or
      repr = "(parameter 1 (return (member join *)))" and t = "snk" and result = 0.7223694959615344
      or
      repr = "(parameter 0 (return (member onHide *)))" and t = "snk" and result = 1.0
      or
      repr = "(member _folderMatches *)" and t = "snk" and result = 0.10052754303752451
      or
      repr = "(parameter 1 (return (member prompt (member notificationService *))))" and
      t = "snk" and
      result = 0.6442198328384785
      or
      repr = "(parameter 2 (return (member setCursorStates *)))" and
      t = "snk" and
      result = 0.9545454545454548
      or
      repr = "(parameter 0 (return (member series *)))" and
      t = "snk" and
      result = 0.08706386733461247
      or
      repr = "(member virtualDelegate *)" and t = "snk" and result = 0.07024869911233506
      or
      repr = "(member emitter *)" and t = "snk" and result = 0.2
      or
      repr =
        "(parameter 0 (return (member firstIndex (root https://www.npmjs.com/package/vs/base/common/arrays))))" and
      t = "snk" and
      result = 0.945919887743057
      or
      repr = "(member _filter *)" and t = "snk" and result = 0.12072068941552563
      or
      repr = "(parameter 0 (return (parameter 1 (return (member then *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member insertText *)" and t = "snk" and result = 0.0625
      or
      repr = "(member cursorState *)" and t = "snk" and result = 0.04545454545454519
      or
      repr = "(parameter 0 (parameter 0 (return (member Range *))))" and
      t = "snk" and
      result = 0.1696366799108739
      or
      repr = "(member _process *)" and t = "snk" and result = 0.21026603001364252
      or
      repr =
        "(parameter 2 (return (member assign (root https://www.npmjs.com/package/vs/base/common/objects))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member rangeMap *)" and t = "snk" and result = 0.16199877563513954
      or
      repr = "(parameter 0 (return (member xhr *)))" and t = "snk" and result = 0.4278558265599839
      or
      repr = "(parameter 0 (return (member containsPosition *)))" and t = "snk" and result = 0.3125
      or
      repr = "(member toDispose *)" and t = "snk" and result = 0.25
      or
      repr =
        "(parameter 0 (return (member rewriteWorkspaceFileForNewLocation (root https://www.npmjs.com/package/vs/platform/workspaces/common/workspaces))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(return (member trim (return (member getValue *))))" and
      t = "snk" and
      result = 0.4889787844133454
      or
      repr = "(parameter 1 (return (member isEqual (member extUri (member uriIdentityService *)))))" and
      t = "snk" and
      result = 0.12749922639726585
      or
      repr = "(parameter 1 (return (member setTimeout (global))))" and
      t = "snk" and
      result = 0.3320229076186434
      or
      repr = "(parameter 0 (return (member splice (return (member get *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member file (member URI *))))" and
      t = "snk" and
      result = 0.2326198906447685
      or
      repr = "(parameter 1 (return (member notEqual (root https://www.npmjs.com/package/assert))))" and
      t = "snk" and
      result = 0.06165262525792059
      or
      repr = "(parameter 0 (return (member file *)))" and t = "snk" and result = 0.6473801093552327
      or
      repr = "(parameter 0 (parameter 0 (return (member set *))))" and
      t = "snk" and
      result = 0.06559151376533812
      or
      repr = "(parameter 0 (return (member has *)))" and t = "snk" and result = 0.33000000000000007
      or
      repr = "(parameter 1 (return (member replace (parameter editBuilder *))))" and
      t = "snk" and
      result = 0.22913426580129514
      or
      repr = "(parameter 3 (return (member create (member TextDocument *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member resource *)" and t = "snk" and result = 0.40119591977009095
      or
      repr = "(member menuCache *)" and t = "snk" and result = 0.2590000000000001
      or
      repr = "(parameter 0 (return (member appendChild (return (member $ *)))))" and
      t = "snk" and
      result = 0.24733659655035578
      or
      repr = "(parameter 0 (parameter 0 (return (member open (member _openerService *)))))" and
      t = "snk" and
      result = 0.14749396447961097
      or
      repr = "(parameter 0 (return (member pushTo (parameter repository *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member allPaths *)" and t = "snk" and result = 0.09909369173831983
      or
      repr = "(member name (parameter rule *))" and t = "snk" and result = 0.5977282002764096
      or
      repr = "(member className (parameter e *))" and t = "snk" and result = 0.24999999999999986
      or
      repr = "(member message (return (member Error (global))))" and
      t = "snk" and
      result = 0.1712732374676696
      or
      repr = "(parameter 0 (return (member set (member cache *))))" and
      t = "snk" and
      result = 0.29999999999999993
      or
      repr = "(parameter 0 (return (member define *)))" and
      t = "snk" and
      result = 0.4249829645728127
      or
      repr =
        "(parameter 0 (return (member startsWith (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member detail *)" and t = "snk" and result = 0.15625
      or
      repr = "(member _modelDisposables *)" and t = "snk" and result = 0.4652896115084104
      or
      repr = "(return (member with *))" and t = "snk" and result = 0.34999999999999987
      or
      repr = "(parameter 1 (return (member resolve (root https://www.npmjs.com/package/path))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member disposables *)" and t = "snk" and result = 0.23112912782554662
      or
      repr = "(parameter 0 (parameter 0 (return (member push (parameter result *)))))" and
      t = "snk" and
      result = 0.3227291278255465
      or
      repr = "(parameter 2 (parameter 0 (return (member error *))))" and
      t = "snk" and
      result = 0.5050505050505051
      or
      repr = "(member inputEventListener *)" and t = "snk" and result = 0.2
      or
      repr = "(parameter 0 (return (member end (parameter res *))))" and t = "snk" and result = 1.0
      or
      repr = "(member recentlyClosedEditors *)" and t = "snk" and result = 0.34375
      or
      repr = "(parameter 1 (return (member isEqual *)))" and
      t = "snk" and
      result = 0.3459856220875826
      or
      repr = "(parameter 1 (return (member applyEdits (member TextDocument *))))" and
      t = "snk" and
      result = 0.14775474163736357
      or
      repr = "(parameter 0 (return (member insert (return (member get *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (parameter 0 (return (member push *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member all (member Promise (global))))" and
      t = "snk" and
      result = 0.08999999999999997
      or
      repr = "(member stack (parameter err *))" and t = "snk" and result = 0.5242430426085155
      or
      repr = "(return (member relative *))" and t = "snk" and result = 0.019999999999999907
      or
      repr = "(parameter 0 (member query *))" and t = "snk" and result = 0.19999999999999998
      or
      repr = "(member timestamp *)" and t = "snk" and result = 0.08062179827352245
      or
      repr = "(member extensionPack *)" and t = "snk" and result = 0.1859633611935869
      or
      repr = "(parameter 1 (return (member renameFile (instance (member WorkspaceEdit *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member replace (parameter builder *))))" and
      t = "snk" and
      result = 0.3324395163171364
      or
      repr = "(parameter 0 (return (member showSaveDialog *)))" and
      t = "snk" and
      result = 0.6187999999999998
      or
      repr = "(return (member format (root https://www.npmjs.com/package/vs/base/common/strings)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member _deliveryQueue *)" and t = "snk" and result = 0.16666666666666669
      or
      repr = "(parameter 0 (return (parameter 1 (return (member withProgress *)))))" and
      t = "snk" and
      result = 0.6731549726611918
      or
      repr = "(parameter 1 (parameter 0 (parameter 0 (return (member push *)))))" and
      t = "snk" and
      result = 0.06607016971542257
      or
      repr = "(parameter 0 (return (member showOpenDialog *)))" and
      t = "snk" and
      result = 0.16184502733880815
      or
      repr = "(parameter 0 (return (member resolve (member Promise (global)))))" and
      t = "snk" and
      result = 0.7400000000000001
      or
      repr = "(parameter 0 (return (member SnippetString *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member getStartLineNumber (member regions *))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member callstack *)" and t = "snk" and result = 0.10371098258123046
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/vinyl)))" and
      t = "snk" and
      result = 0.8733461851355013
      or
      repr = "(member label *)" and t = "snk" and result = 0.0625
      or
      repr =
        "(parameter 0 (return (return (member defaults (root https://www.npmjs.com/package/request)))))" and
      t = "snk" and
      result = 0.9600000000000002
      or
      repr = "(return (member ajax *))" and t = "snk" and result = 0.4276038461538463
      or
      repr = "(member map *)" and t = "snk" and result = 0.15384615384615385
      or
      repr = "(member target (parameter e *))" and t = "snk" and result = 0.010101010101010166
      or
      repr = "(return (member insertBefore *))" and t = "snk" and result = 0.25
      or
      repr = "(member requestAnimationFrame (global))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(member cancelAnimationFrame *)" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member trigger *)))" and
      t = "snk" and
      result = 0.3333333333333334
      or
      repr = "(member appSrc *)" and t = "snk" and result = 0.03571428571428571
      or
      repr = "(member appIndexJs *)" and t = "snk" and result = 0.03571428571428571
      or
      repr = "(member dotenv *)" and t = "snk" and result = 0.03571428571428571
      or
      repr = "(member yarnLockFile *)" and t = "snk" and result = 0.0357142857142857
      or
      repr = "(member appPackageJson *)" and t = "snk" and result = 0.03571428571428571
      or
      repr = "(member appPublic *)" and t = "snk" and result = 0.03571428571428571
      or
      repr = "(member appBuild *)" and t = "snk" and result = 0.0357142857142857
      or
      repr = "(member appNodeModules *)" and t = "snk" and result = 0.03571428571428577
      or
      repr = "(member skills *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member findSkillByName *)))" and t = "snk" and result = 0.125
      or
      repr = "(return (member then (return (member populate *))))" and t = "snk" and result = 0.5
      or
      repr = "(member SSN *)" and t = "snk" and result = 0.25
      or
      repr = "(member 0 (parameter 0 (parameter 0 (return (member then *)))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member find (member ModelMarker *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member query (parameter req *))" and t = "snk" and result = 1.0
      or
      repr = "(member dataTypes *)" and t = "snk" and result = 0.21749999999999997
      or
      repr = "(parameter 2 (return (member data *)))" and t = "snk" and result = 0.25
      or
      repr = "(member query (parameter 0 (parameter 1 (return (member get *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member extend *)))" and
      t = "snk" and
      result = 0.33999999999999997
      or
      repr = "(parameter 0 (return (member ajax *)))" and t = "snk" and result = 0.7425000000000002
      or
      repr = "(return (member eq (return (member find *))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member insertAfter *)))" and
      t = "snk" and
      result = 0.05133470225872694
      or
      repr = "(member query (parameter req (parameter 2 (return (member get *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member _addClass *)))" and
      t = "snk" and
      result = 0.05133470225872694
      or
      repr = "(member tablist *)" and t = "snk" and result = 0.051334702258726606
      or
      repr = "(member tabs *)" and t = "snk" and result = 0.05133470225872694
      or
      repr = "(member query (parameter req (parameter 1 (return (member get *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member limit (return (member sort *)))))" and
      t = "snk" and
      result = 0.14999999999999986
      or
      repr = "(return (member on *))" and t = "snk" and result = 0.05133470225872694
      or
      repr = "(member anchors *)" and t = "snk" and result = 0.05133470225872694
      or
      repr = "(return (member limit (return (member sort *))))" and
      t = "snk" and
      result = 0.16999999999999998
      or
      repr = "(member query (parameter 0 (parameter 2 (return (member get *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member jQ *)" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (return (member style *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member replaceChild (member parentNode *))))" and
      t = "snk" and
      result = 0.27384375000000005
      or
      repr = "(parameter 1 (return (member apply (member handle (member event *)))))" and
      t = "snk" and
      result = 0.8600035442140703
      or
      repr = "(member focusIndex *)" and t = "snk" and result = 0.11812500000000026
      or
      repr = "(parameter 0 (return (member trim (parameter $ *))))" and
      t = "snk" and
      result = 0.0028704622955065817
      or
      repr = "(member ajaxOptions *)" and t = "snk" and result = 0.05303030303030291
      or
      repr = "(member 1 (return (member match (member selectedElement (member _ *)))))" and
      t = "snk" and
      result = 0.44505448329448327
      or
      repr = "(member tbodyEl *)" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member setHtml *)))" and
      t = "snk" and
      result = 0.4353535353535354
      or
      repr = "(member from *)" and t = "snk" and result = 0.5050505050505051
      or
      repr = "(member cols *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member populate *)))" and t = "snk" and result = 0.25
      or
      repr = "(member firstCategory *)" and t = "snk" and result = 0.16666666666666663
      or
      repr = "(parameter 0 (return (member find (member products *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member validator *)))" and
      t = "snk" and
      result = 0.5311057692307692
      or
      repr = "(parameter 2 (return (member arraySet (return (parameter a *)))))" and
      t = "snk" and
      result = 0.3745833333333333
      or
      repr = "(parameter 0 (return (member parseJSON (member jQuery (global)))))" and
      t = "snk" and
      result = 0.27
      or
      repr = "(return (member trim (member $ (member window (global)))))" and
      t = "snk" and
      result = 0.5403540142941227
      or
      repr = "(parameter 0 (return (member addEventListener (parameter a *))))" and
      t = "snk" and
      result = 0.6875
      or
      repr =
        "(member data-cke-saved-href (member attributes (parameter 0 (return (member style *)))))" and
      t = "snk" and
      result = 0.7770270270270275
      or
      repr = "(member query (instance (member Socket (member exports (parameter b *)))))" and
      t = "snk" and
      result = 0.43217614914818403
      or
      repr = "(return (member extend (member $ (global))))" and
      t = "snk" and
      result = 0.24691287878787904
      or
      repr = "(parameter 0 (return (member setHtml (return (member getById *)))))" and
      t = "snk" and
      result = 0.9500000000000002
      or
      repr = "(parameter 0 (return (member drawCircle (member target *))))" and
      t = "snk" and
      result = 0.3
      or
      repr = "(return (member compile *))" and t = "snk" and result = 0.06724375000000027
      or
      repr = "(parameter 0 (return (member appendTo (return (member css *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 0 (return (parameter c *)))" and t = "snk" and result = 0.07758620689655159
      or
      repr = "(parameter 0 (member set *))" and t = "snk" and result = 0.5
      or
      repr = "(member belong_category *)" and t = "snk" and result = 0.996987951807229
      or
      repr = "(member parentNode (return (member createElement *)))" and
      t = "snk" and
      result = 0.024999999999999994
      or
      repr = "(member _request *)" and t = "snk" and result = 0.2
      or
      repr = "(member cookie *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (instance (return (member extend (member Class *)))))" and
      t = "snk" and
      result = 0.6177777777777779
      or
      repr = "(member size *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member toHtml *)))" and
      t = "snk" and
      result = 0.009999999999999981
      or
      repr = "(parameter 0 (return (member setAttributes *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member isFunction *)))" and
      t = "snk" and
      result = 0.3787878787878788
      or
      repr = "(parameter 0 (return (member isNumeric *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member $ (global))))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 1 (return (member setItem *)))" and
      t = "snk" and
      result = 0.20833333333333331
      or
      repr = "(member de_firstCategory *)" and t = "snk" and result = 0.16666666666666663
      or
      repr = "(parameter 0 (return (member getElementsByTagName *)))" and
      t = "snk" and
      result = 0.3808496075082167
      or
      repr = "(parameter 0 (parameter 0 (return (member load (member scriptLoader *)))))" and
      t = "snk" and
      result = 0.4999999999999999
      or
      repr = "(parameter 0 (return (member fromHtml *)))" and t = "snk" and result = 1.0
      or
      repr = "(return (member replace (return (member replace (member description *)))))" and
      t = "snk" and
      result = 0.75
      or
      repr = "(parameter 0 (return (member find (member users *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member isFunction (parameter $ *))))" and
      t = "snk" and
      result = 0.02121212121212122
      or
      repr = "(return (member append (return (member jQuery (global)))))" and
      t = "snk" and
      result = 0.852727272727272
      or
      repr = "(parameter 0 (member from *))" and t = "snk" and result = 0.5050505050505049
      or
      repr = "(parameter 1 (return (member replaceChild *)))" and
      t = "snk" and
      result = 0.2738437500000001
      or
      repr = "(member lastShapeId *)" and t = "snk" and result = 0.15
      or
      repr = "(parameter 0 (return (member extend (parameter $ *))))" and
      t = "snk" and
      result = 0.14015151515151494
      or
      repr = "(parameter 0 (return (member each (member utils (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member fire (parameter a *))))" and
      t = "snk" and
      result = 0.34374999999999994
      or
      repr = "(return (member _fnBoundData *))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member join (return (member split *)))))" and
      t = "snk" and
      result = 0.375
      or
      repr = "(member options *)" and t = "snk" and result = 0.2
      or
      repr = "(parameter 1 (return (member parseTmpl *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member ltrim (member tools *))))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member find (member banners *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member cookie (member document (global)))" and
      t = "snk" and
      result = 0.4886363636363635
      or
      repr = "(parameter 0 (return (member trim (member utils (global)))))" and
      t = "snk" and
      result = 0.7616992150164335
      or
      repr = "(return (member trim (member jQuery (member window (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member send (return (member use *)))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 0 (return (member fromHtml (member fragment (member htmlParser *)))))" and
      t = "snk" and
      result = 0.18750000000000044
      or
      repr = "(parameter 0 (member to *))" and t = "snk" and result = 0.5050505050505049
      or
      repr = "(parameter 0 (return (member each (member $ (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(parameter 1 (return (member write *)))" and t = "snk" and result = 0.75
      or
      repr = "(parameter 1 (return (member apply (member handle *))))" and
      t = "snk" and
      result = 0.4051479709374447
      or
      repr = "(parameter 0 (parameter 0 (return (member exec *))))" and
      t = "snk" and
      result = 0.30666945140629315
      or
      repr = "(parameter 1 (return (member drawCircle (member target *))))" and
      t = "snk" and
      result = 0.29999999999999993
      or
      repr = "(parameter 2 (return (member style (member exports *))))" and
      t = "snk" and
      result = 0.7737130072656389
      or
      repr = "(parameter 1 (return (member moveToPoint *)))" and t = "snk" and result = 0.125
      or
      repr = "(parameter 0 (return (member push (parameter c *))))" and
      t = "snk" and
      result = 0.25757575757575746
      or
      repr = "(parameter 0 (return (member attachEvent (parameter a *))))" and
      t = "snk" and
      result = 0.6875
      or
      repr = "(parameter 2 (return (member update *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member update (member default *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member symbol *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 2 (return (member splice (parameter nav *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member exchange (member tokens *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member symbol (return (member getToken (member tokens *))))" and
      t = "snk" and
      result = 0.5151515151515149
      or
      repr = "(member to_symbol *)" and t = "snk" and result = 0.4848484848484851
      or
      repr = "(parameter 0 (return (member findOne (member Node *))))" and
      t = "snk" and
      result = 0.6174242424242428
      or
      repr = "(member from_symbol *)" and t = "snk" and result = 0.4848484848484851
      or
      repr = "(parameter 1 (return (member exchange *)))" and t = "snk" and result = 1.0
      or
      repr = "(return (member replace (member content *)))" and t = "snk" and result = 0.98
      or
      repr = "(parameter 1 (return (member makeSpan *)))" and
      t = "snk" and
      result = 0.08223684210526315
      or
      repr = "(member content (return (member push (parameter 0 (member exports *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member src (parameter 0 (member exports *)))" and
      t = "snk" and
      result = 0.41176470588235264
      or
      repr = "(member content (return (member push (parameter e (member exports *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member relevance *)" and t = "snk" and result = 0.25
      or
      repr = "(member src (parameter e (member exports *)))" and t = "snk" and result = 1.0
      or
      repr = "(return (member exports (parameter e *)))" and t = "snk" and result = 0.25
      or
      repr = "(member src (parameter e (member exports (parameter e *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 3 (return (member render *))))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 2 (return (member createElement (return (parameter require *)))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member model *)))" and t = "snk" and result = 0.5
      or
      repr = "(parameter 0 (return (member getTicketCount *)))" and
      t = "snk" and
      result = 0.22999999999999998
      or
      repr = "(parameter 2 (return (member sendMail *)))" and
      t = "snk" and
      result = 0.4166666666666667
      or
      repr = "(parameter 2 (return (member render (parameter 0 (member exports *)))))" and
      t = "snk" and
      result = 0.24242424242424243
      or
      repr = "(parameter 0 (return (member find (member model *))))" and t = "snk" and result = 1.0
      or
      repr = "(member password *)" and t = "snk" and result = 0.053333333333333344
      or
      repr = "(member focusArea *)" and t = "snk" and result = 0.0762124711316397
      or
      repr = "(member skillNames *)" and t = "snk" and result = 0.0762124711316397
      or
      repr = "(member description *)" and t = "snk" and result = 0.0762124711316397
      or
      repr = "(member trainings *)" and t = "snk" and result = 0.04041570438799086
      or
      repr = "(parameter 1 (return (member replaceChild (member parentNode *))))" and
      t = "snk" and
      result = 0.30000000000000004
      or
      repr = "(member mode (member doc (parameter a *)))" and
      t = "snk" and
      result = 0.2500000000000002
      or
      repr = "(return (member append (return (parameter $ *))))" and
      t = "snk" and
      result = 0.5050505050505049
      or
      repr = "(parameter 0 (return (member pow (member Math (global)))))" and
      t = "snk" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member addMenuItem (parameter a (member buildSuggestionMenuItems *)))))" and
      t = "snk" and
      result = 0.5999999999999999
      or
      repr = "(parameter 0 (return (member removeClass (return (member children *)))))" and
      t = "snk" and
      result = 0.3654618473895583
      or
      repr = "(parameter 0 (return (member load *)))" and t = "snk" and result = 0.375
      or
      repr = "(member range *)" and t = "snk" and result = 0.2980172413793104
      or
      repr =
        "(member helper (member current (member ddmanager (member ui (root https://www.npmjs.com/package/jquery)))))" and
      t = "snk" and
      result = 0.42025430600074243
      or
      repr = "(member name (member attributes *))" and t = "snk" and result = 0.2201981928345199
      or
      repr = "(parameter 0 (return (member createFromHtml (member element *))))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(return (member createFromHtml (member element *)))" and
      t = "snk" and
      result = 0.7575757575757576
      or
      repr = "(member root (parameter a *))" and t = "snk" and result = 0.01256913021618883
      or
      repr = "(member mode *)" and t = "snk" and result = 1.0
      or
      repr = "(member startNode *)" and t = "snk" and result = 0.015151515151514828
      or
      repr = "(member frontier (member doc (parameter a *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member setEndAfter *)))" and
      t = "snk" and
      result = 0.028301886792452935
      or
      repr = "(member 0 (parameter a *))" and t = "snk" and result = 0.33499999999999996
      or
      repr = "(parameter 0 (return (member attachListener (return (member editable *)))))" and
      t = "snk" and
      result = 0.25
      or
      repr = "(parameter 1 (return (member fire (parameter 0 (member init *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member override *))" and t = "snk" and result = 0.043387703889585946
      or
      repr = "(parameter 0 (return (member replaceChild *)))" and t = "snk" and result = 0.3
      or
      repr = "(member data (parameter a (parameter 1 (return (member on *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (parameter $ (parameter 2 (return (member define *)))))" and
      t = "snk" and
      result = 0.29011040638947616
      or
      repr = "(parameter 0 (return (member selectPage *)))" and
      t = "snk" and
      result = 0.11686633779657035
      or
      repr = "(parameter 0 (return (member getById *)))" and t = "snk" and result = 0.5
      or
      repr = "(member string (instance (member StringStream *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member match *)))" and t = "snk" and result = 0.3696999999999999
      or
      repr = "(member listeners *)" and t = "snk" and result = 0.5853430134680135
      or
      repr = "(parameter 0 (return (member getById (member document *))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member endNode *)" and t = "snk" and result = 0.015151515151514828
      or
      repr = "(parameter 0 (return (member setStartBefore *)))" and
      t = "snk" and
      result = 0.47169811320754707
      or
      repr = "(member $ *)" and t = "snk" and result = 0.25
      or
      repr = "(member localVars *)" and t = "snk" and result = 0.0625
      or
      repr = "(parameter 0 (return (member fnFormatNumber *)))" and t = "snk" and result = 0.25
      or
      repr =
        "(parameter 0 (return (member isPlainObject (root https://www.npmjs.com/package/jquery))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setStartAt *)))" and
      t = "snk" and
      result = 0.40963178294573643
      or
      repr = "(member class *)" and t = "snk" and result = 0.2585858585858585
      or
      repr = "(parameter 0 (return (member isPlainObject *)))" and
      t = "snk" and
      result = 0.5655167643804007
      or
      repr = "(parameter 1 (return (member fire (parameter a (member init *)))))" and
      t = "snk" and
      result = 0.6460124500822173
      or
      repr = "(member data-cke-saved-href *)" and t = "snk" and result = 1.0
      or
      repr = "(parameter 1 (return (member exec *)))" and t = "snk" and result = 0.25
      or
      repr = "(member elem (member currentActive *))" and t = "snk" and result = 0.7575757575757576
      or
      repr = "(member sel (member doc *))" and t = "snk" and result = 0.7575757575757573
      or
      repr = "(return (member replace (member className *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member index *)))" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (member isFunction (member jQuery (parameter a *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member uid *)" and t = "snk" and result = 0.25
      or
      repr = "(parameter 0 (return (parameter 1 (return (member findOne *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member complete *)" and t = "snk" and result = 0.599624060150376
      or
      repr = "(parameter 1 (return (member distinct (return (member collection *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member distinct *)))" and t = "snk" and result = 1.0
      or
      repr = "(parameter 0 (return (member isFunction (return (member noConflict *)))))" and
      t = "snk" and
      result = 0.7276144907723855
      or
      repr = "(parameter 1 (return (member updateOne (return (member collection *)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member to (return (member expect (root https://www.npmjs.com/package/chai))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(return (member sort (return (member find *))))" and
      t = "snk" and
      result = 0.3999999999999999
      or
      repr = "(return (member set *))" and t = "snk" and result = 0.1487878787878789
      or
      repr = "(parameter 1 (return (member info *)))" and t = "snk" and result = 0.375
      or
      repr = "(parameter 0 (return (member get (return (member request *)))))" and
      t = "snk" and
      result = 0.1200000000000001
      or
      repr = "(member data (parameter 1 (parameter 1 (return (member findOne *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(member data (parameter label (parameter 1 (return (member findOne *)))))" and
      t = "snk" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member add (member items *))))" and t = "snk" and result = 1.0
      or
      repr = "(return (root https://www.npmjs.com/package/moment))" and t = "snk" and result = 0.125
      or
      repr = "(return (member compact *))" and t = "snk" and result = 0.06212121212121251
      or
      repr = "(member context *)" and t = "snk" and result = 0.12499999999999997
      or
      repr = "(member nextTasks *)" and t = "snk" and result = 0.1499999999999999
      or
      repr = "(member KPI *)" and t = "snk" and result = 0.2500000000000001
      or
      repr = "(parameter 0 (return (member filter (member _ (global)))))" and
      t = "snk" and
      result = 0.5
      or
      repr = "(member repeatOn *)" and t = "snk" and result = 0.125
      or
      repr = "(member filterClosed *)" and t = "snk" and result = 0.04486803519061567
      or
      repr = "(member metrics (parameter 0 (parameter 1 (return (member each *)))))" and
      t = "snk" and
      result = 0.2584307359307358
      or
      repr = "(member metrics *)" and t = "snk" and result = 0.12035714285714286
      or
      repr = "(member index *)" and t = "san" and result = 1.0
      or
      repr = "(return (member trim *))" and t = "san" and result = 1.0
      or
      repr = "(member title (parameter 0 (return (member findOne *))))" and
      t = "san" and
      result = 0.5
      or
      repr = "(return (member findOne *))" and t = "san" and result = 0.75
      or
      repr = "(return (member findOneAndUpdate *))" and t = "san" and result = 0.75
      or
      repr = "(member innerHTML *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter 0 (member $watchGroup (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Number (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member $watchGroup (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter numStr *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pending (member $$state *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member expressions *))" and t = "san" and result = 1.0
      or
      repr = "(member template *)" and t = "san" and result = 1.0
      or
      repr = "(return (member encode *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter ctrl *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member element (member angular (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member $$addBindingInfo (parameter $compile *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member html (parameter $element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter $parse *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member templateDirective *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (parameter $injector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member $$asyncQueue *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter nodes *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member $$addBindingInfo *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter _value *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pending *))" and t = "san" and result = 1.0
      or
      repr = "(return (member annotate (member $injector (member services *))))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member length (return (member keys (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member nonTlbTranscludeDirective (parameter 5 (member transclude *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member min *)))" and t = "san" and result = 1.0
      or
      repr = "(member ngBindHtml *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter $rootElement *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter value *)))" and t = "san" and result = 1.0
      or
      repr = "(member ngBindHtml (parameter tAttrs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter source *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (member to *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (parameter $interpolate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member expressions (parameter 1 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member element (member angular *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substr *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter value *))" and t = "san" and result = 1.0
      or
      repr = "(member port *)" and t = "san" and result = 1.0
      or
      repr = "(return (member $$encodeUriQuery (parameter 1 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member newIsolateScopeDirective *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter o *))" and t = "san" and result = 1.0
      or
      repr = "(return (member $$encodeUriQuery *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member hash *)" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member getElementById (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member get (parameter $http *)))" and
      t = "san" and
      result = 0.5833333333333334
      or
      repr =
        "(member length (member $$asyncQueue (return (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member element *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member element (member angular (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getTemplate *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member expect *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member element *)))" and t = "san" and result = 1.0
      or
      repr = "(member protocol *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member name (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _lastIndex *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member name (member window (global)))))" and
      t = "san" and
      result = 0.7878787878787883
      or
      repr =
        "(member length (parameter watchExpressions (member $watchGroup (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateDirective *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member match *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isDefined (return (parameter __webpack_require__ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member $watchGroup *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member extend (return (parameter __webpack_require__ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member $$asyncQueue (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter 0 (return (member reduce *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member inputs *))" and t = "san" and result = 1.0
      or
      repr = "(return (member recurse *))" and t = "san" and result = 0.18796992481203006
      or
      repr = "(member length (return (member concat (return (member concat *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member replace (parameter numStr *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter obj *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member d *))" and t = "san" and result = 1.0
      or
      repr = "(member encoded *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member pending (member $$state (parameter promise *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member cache *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member html *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member nonTlbTranscludeDirective *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member element (member angular *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member _location *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member min (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member removeWatches *)" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeURIComponent (global)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter watchExpressions (member $watchGroup (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member ngBindHtml (parameter 1 (member compile *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member newScopeDirective *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member attr *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member $$encodeUriSegment *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member decodeURIComponent (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member splice (member d *))))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member getElementById *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (parameter url *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter watchExpressions *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member min (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member $$encodeUriSegment (parameter 1 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateUrl *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member name (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member expect (root https://www.npmjs.com/package/chai))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tail *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isDefined *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member create *))" and t = "san" and result = 0.9433333333333334
      or
      repr = "(member length (parameter directives *))" and t = "san" and result = 1.0
      or
      repr = "(return (member put (parameter $http *)))" and
      t = "san" and
      result = 0.7083333333333334
      or
      repr = "(member length (return (member peekMultichar *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member peekMultichar (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member min *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member replace *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member element (member angular (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member concat *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member attr (parameter element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member scope *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member $$asyncQueue (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter s *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member $$asyncQueue (return (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member inputs (parameter parsedExpression *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter path *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member ngBindHtml (parameter tAttrs (member compile *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getResolvable *))" and t = "san" and result = 0.25
      or
      repr = "(member length (parameter watchExpressions (member $watchGroup *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member element (member angular (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getRecordByDomainPromise *))" and
      t = "san" and
      result = 0.48000000000000004
      or
      repr = "(member pathname *)" and t = "san" and result = 1.0
      or
      repr = "(return (member split (member id *)))" and t = "san" and result = 0.25
      or
      repr = "(member count (parameter 0 (return (member json *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member count (parameter 0 (return (member json *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getZoneByNamePromise *))" and
      t = "san" and
      result = 0.4700000000000001
      or
      repr = "(return (member getUserIdPromise *))" and t = "san" and result = 0.25
      or
      repr = "(return (member html *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getSSLByCommonNamePromise *))" and
      t = "san" and
      result = 0.48000000000000004
      or
      repr = "(return (member getHttpHeaderPromise *))" and
      t = "san" and
      result = 0.4799999999999999
      or
      repr = "(return (member getSSLAlgorithmPromise *))" and t = "san" and result = 0.758
      or
      repr = "(return (member getSSLAlgorithmByZonePromise *))" and
      t = "san" and
      result = 0.20200000000000007
      or
      repr = "(member pathname (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member join *))" and t = "san" and result = 0.3787878787878788
      or
      repr = "(return (member getRecordsByZonePromise *))" and
      t = "san" and
      result = 0.4700000000000001
      or
      repr = "(return (member getRecordByZonePromise *))" and
      t = "san" and
      result = 0.4700000000000001
      or
      repr = "(return (member replace (parameter zone *)))" and
      t = "san" and
      result = 0.04545454545454508
      or
      repr = "(member length (member count *))" and t = "san" and result = 1.0
      or
      repr = "(member count *)" and t = "san" and result = 1.0
      or
      repr = "(return (member toString *))" and t = "san" and result = 0.25
      or
      repr = "(return (member parse *))" and t = "san" and result = 0.75
      or
      repr = "(return (member getUnknownHttpHeaderPromise *))" and
      t = "san" and
      result = 0.48000000000000015
      or
      repr = "(member pathname (member location (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getRecordByIPPromise *))" and
      t = "san" and
      result = 0.4700000000000001
      or
      repr = "(member pathname (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter data *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member split *))" and t = "san" and result = 0.75
      or
      repr = "(member length (parameter data (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parseInt (global)))" and t = "san" and result = 0.75
      or
      repr = "(return (member replace (return (member replace *))))" and t = "san" and result = 1.0
      or
      repr = "(member hash (instance (member compiler *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member resolve (parameter 1 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter data (member resolve (parameter 1 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter data (member resolve *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter array (member exports *)))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member encodeQueryCollection (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeQueryCollection (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeQueryKey (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member compiler (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter state *))" and t = "san" and result = 1.0
      or
      repr = "(return (member sanitize *))" and t = "san" and result = 1.0
      or
      repr = "(member HTML *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member Buffer (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member encodedParentId *)" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeQueryParam *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member exec *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member simpleRef *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter 0 (member output *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter elems *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (return (member substring *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unshift *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (instance (member default (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member default *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeExpression *))" and t = "san" and result = 1.0
      or
      repr = "(return (member exports *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter t *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter buf *))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeQueryParam (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Uint8Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member concat *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (parameter require *))))" and t = "san" and result = 1.0
      or
      repr = "(member host (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member templates (member Handlebars (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeQueryCollection *))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter 1 (member performAction *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (parameter require *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (parameter arr (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yytext (parameter 1 (member performAction (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter e *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Uint8Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (return (parameter require *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Buffer (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member templates *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring *)))" and t = "san" and result = 1.0
      or
      repr = "(member charReceived *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter e *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor (parameter 0 (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member exec *))" and t = "san" and result = 0.744485294117647
      or
      repr = "(member protocol (member location (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter array *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (instance (member compiler (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yytext *)" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeQueryParam (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeQueryKey *))" and t = "san" and result = 1.0
      or
      repr = "(member host (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter buffer *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Buffer (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member construct *)))" and t = "san" and result = 1.0
      or
      repr = "(member blockParams *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter e (member performAction *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter e (member output *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter object *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member yytext (parameter e (member performAction (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member token *)" and t = "san" and result = 1.0
      or
      repr =
        "(member yytext (parameter 1 (member performAction (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter string *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member charReceived (instance (member StringDecoder (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice *)))" and t = "san" and result = 1.0
      or
      repr = "(member charReceived (instance (member StringDecoder *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter e (member performAction (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member lastIndex *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member compiler (member prototype (member default *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member test *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member resolve *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter array (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (member location *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member from (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member splice *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor (return (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member from *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (parameter arr *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member depth *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(member host *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeQueryKey (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member copy *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member exports (parameter module *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member substring *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member yytext (parameter yy_ (member performAction (member lexer (member Parser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member value (return (member stepBackward *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unpackEncodedString (member DFA *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isNumber (member lang *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (parameter 0 (member $clipPosition (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member lines *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member value (return (member getCurrentToken *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tagName *))" and t = "san" and result = 1.0
      or
      repr = "(member column (parameter 0 (return (member findMatchingBracket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (return (member $findClosingBracket *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter text *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLine (member session (parameter editor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 2 (return (member splice (member $lines *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member lines *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member params (instance (member Code (root https://www.npmjs.com/package/exports)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HtmlHighlightRules (return (root https://www.npmjs.com/package/require)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unpackEncodedStringToUnsignedChars (member DFA *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member objects *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLine *)))" and t = "san" and result = 1.0
      or
      repr = "(member column (parameter 0 (member $clipPosition *)))" and t = "san" and result = 1.0
      or
      repr = "(member column (member start *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter lines (member insertLines (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isNumber *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member column (return (member $findClosingBracket (parameter 0 (member openingBracketBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (instance (member Range *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encryptBlock *))" and t = "san" and result = 1.0
      or
      repr =
        "(member column (return (member $findClosingBracket (parameter session (member openingBracketBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter array (member copyArray (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member match *))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter yy_ (member performAction *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter array (member copyArray *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member items *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params *))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member unpackEncodedStringToUnsignedChars (member DFA (member runtime (member antlr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unpackEncodedString (member DFA (member runtime *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getBytes *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isArray *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLine (member session *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member concat (return (member concat *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tagName (return (member _getFirstTagInLine *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tokens (return (member getLineTokens *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (member end *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member glyphs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter str (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getLeftNthChar *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isArray (member lang (member antlr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member expressions (instance (member Block (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (return (member stepBackward (parameter iterator *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member properties *))" and t = "san" and result = 1.0
      or
      repr = "(member end *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member column *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member Uint8Array (global))))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 3 (instance (member Range (return (root https://www.npmjs.com/package/require)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (member lines (member data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 0 (return (member map *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getLine (parameter session (member indentationBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member col *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member insertInLine *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (instance (member Code (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (member range (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(member column (return (member $findClosingBracket (parameter session *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member copyArray (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getNextLineIndent *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member unpackEncodedStringToUnsignedChars *))" and t = "san" and result = 1.0
      or
      repr = "(member column (member end (return (member Range *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member jQuery *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member parts *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter fonts *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member Range (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HtmlHighlightRules (return (parameter require *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getRightNthChar *))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeNumber (parameter self *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member charstrings *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (parameter 0 (return (member remove *))))" and t = "san" and result = 1.0
      or
      repr = "(member column (return (member $clipPositionToDocument *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter lines *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member expressions (instance (member Block (root https://www.npmjs.com/package/exports)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member Uint8Array (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (return (member getLine *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member insertInLine (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member copyArray *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isNumber (member lang (member antlr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member insertLines (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (parameter 0 (return (member findMatchingBracket (parameter doc *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter input *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member stringRepeat *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member slice (parameter lines (member insertLines *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member column (return (member $clipPositionToDocument (instance (member Anchor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member column (return (member $clipPositionToDocument *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (member lines *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLine (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member PlaceHolder *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (member range *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getLine (parameter 0 (member openingBracketBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member bits *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member match (return (member _getFirstTagInLine *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (return (member stepForward (parameter iterator *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member jQuery (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member stringRepeat (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member set *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member Range *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isArray (member lang *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 3 (return (member Range (return (root https://www.npmjs.com/package/require)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getBytes *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member Range *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLine (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data *))" and t = "san" and result = 1.0
      or
      repr = "(member yytext (parameter yy_ (member performAction (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member lines (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (instance (member Range (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 1 (return (member exec (member tagRe *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 2 (return (member splice *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member value (return (member getCurrentToken (parameter iterator *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member PlaceHolder (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member moveCursorTo *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member unpackEncodedStringToUnsignedChars (member DFA (member runtime *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yytext (parameter 1 (member performAction (member lexer (member Parser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (instance (member PlaceHolder (root https://www.npmjs.com/package/exports))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (member end (parameter range *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (member end (instance (member Range *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (return (member stepForward *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLine (parameter 0 (member indentationBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter str *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member stringRepeat (return (root https://www.npmjs.com/package/require)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tokens *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (instance (member Range (return (root https://www.npmjs.com/package/require)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter lines *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter text (member insertInLine *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLine (member doc *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member column (parameter position (member $clipPosition (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 0 (member copyArray (root https://www.npmjs.com/package/exports))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter 1 (member insertLines *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter array (member copyArray (root https://www.npmjs.com/package/exports))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getLine (parameter session (member openingBracketBlock *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (instance (member Range (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member expressions (instance (member Block *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member access *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member ranges *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member Range *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 1 (return (member exec *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLine (parameter session *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (parameter position *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter lines (member insertLines *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member insertLines *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member operators *))" and t = "san" and result = 1.0
      or
      repr = "(return (member unpackEncodedString *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member tagName (return (member _readTagForward *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member column (parameter position (member $clipPosition *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member params (instance (member Code *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member Range (return (root https://www.npmjs.com/package/require)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children *))" and t = "san" and result = 1.0
      or
      repr = "(member column (return (member $clipPosition *)))" and t = "san" and result = 1.0
      or
      repr = "(member column *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member Range (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yytext (parameter yy_ *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter text (member insertInLine (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member bufferLength *)" and t = "san" and result = 1.0
      or
      repr = "(member HtmlHighlightRules *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member tagName (return (member _readTagBackward *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getFoldWidget *))" and t = "san" and result = 0.5
      or
      repr = "(member column (return (member $clipPosition (instance (member Document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeNumber *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 1 (return (member match *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member unpackEncodedString (member DFA (member runtime (member antlr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member buffer *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member tokens (return (member getLineTokens (member $tokenizer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member l *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member samples *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeURI (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member call *))" and t = "san" and result = 0.5
      or
      repr = "(member length (member toolbars (parameter obj *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member video *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member childNodes *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setSelectionRange *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member match *))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (return (member y *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member str (member util *)))" and t = "san" and result = 0.49122807017543857
      or
      repr = "(member innerHTML (member head (member document *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member body *))" and t = "san" and result = 1.0
      or
      repr = "(return (member utf8Encode *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member validateString *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (parameter prefixedString *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member BaseTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys (return (parameter t *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member video (member allocated (return (member MediaPool *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getAllWindows (member controller *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member expr (parameter t *)))" and t = "san" and result = 0.5
      or
      repr = "(member length (return (member _valueGet *)))" and t = "san" and result = 1.0
      or
      repr = "(member findAndRenderTemplate (member templates_ *))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member replace *)))" and t = "san" and result = 1.0
      or
      repr = "(member description *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member default *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member join (return (member reverse *))))" and
      t = "san" and
      result = 0.5133333333333332
      or
      repr = "(parameter 0 (return (member fetch *)))" and t = "san" and result = 1.0
      or
      repr = "(member __html (member dangerouslySetInnerHTML *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter b *))" and t = "san" and result = 1.0
      or
      repr = "(return (member reevaluate (member prototype (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member localStorage (return (member FakeWindow *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member media *)" and t = "san" and result = 1.0
      or
      repr = "(member tag (parameter data *))" and t = "san" and result = 0.29562761572107327
      or
      repr = "(member id *)" and t = "san" and result = 1.0
      or
      repr = "(member custom-element *)" and t = "san" and result = 1.0
      or
      repr = "(return (member decodeURIComponent (global)))" and t = "san" and result = 0.3325
      or
      repr = "(member templates_ *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member max *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member requestsChangeSize_ (member resources_ *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pendingBuildResources_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member closestAncestorElementBySelector *))" and
      t = "san" and
      result = 0.026871212121212018
      or
      repr = "(member length (member recognizers_ (return (member get *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member createElement (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByClassName (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member render *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member default *))" and t = "san" and result = 0.33434448886180856
      or
      repr = "(return (member idFilter *))" and t = "san" and result = 0.25
      or
      repr = "(member length (member requestsChangeSize_ (instance (member ResourcesImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member then *)" and t = "san" and result = 1.0
      or
      repr = "(member alternation *)" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member render (instance (member AmpMustache *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HTMLElement (parameter win *))" and t = "san" and result = 1.0
      or
      repr = "(return (member array *))" and t = "san" and result = 0.6594753894081
      or
      repr = "(return (member expect (global)))" and t = "san" and result = 0.22850812235725843
      or
      repr = "(member innerHTML (member head (member document (member win *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter a *))" and t = "san" and result = 1.0
      or
      repr = "(return (member config (parameter t *)))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (parameter 0 (return (member callsFake *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member contain (member to *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (root https://www.npmjs.com/package/fancy-log))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member params *)" and t = "san" and result = 0.29249999999999987
      or
      repr = "(member length (member messageQueue_ *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member requestsChangeSize_ (member resources_ (instance (member MutatorImpl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member max (member Math (global))))" and t = "san" and result = 0.625
      or
      repr = "(parameter 0 (return (member getData *)))" and t = "san" and result = 1.0
      or
      repr = "(member port (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _valueGet (member inputmask *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templates_ (parameter ampForm (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member validateString (return (member require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member keys (member Object (global))))" and
      t = "san" and
      result = 0.09011422637590877
      or
      repr = "(member length (member children (return (member createElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member warn *)))" and t = "san" and result = 1.0
      or
      repr = "(member query_ *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member requestsChangeSize_ *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member history_ (instance (member FocusHistory *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member applySsrOrCsrTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member resolve (member Promise (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (parameter t *)))" and t = "san" and result = 0.5635008231654552
      or
      repr = "(return (member addParamsToUrl *))" and t = "san" and result = 0.585
      or
      repr = "(member length (member inputs_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member bind *))" and t = "san" and result = 0.375
      or
      repr = "(member length (return (member toArray *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member param (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member h *))" and t = "san" and result = 0.23606784354447902
      or
      repr = "(parameter 0 (return (member unescape (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member param (instance (member exports (parameter e *)))))" and
      t = "san" and
      result = 0.2500000000000002
      or
      repr = "(member hostname (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member localStorage (member win *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member add (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member indexOf *))" and t = "san" and result = 0.25
      or
      repr = "(member length (member rem (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member render *)))" and t = "san" and result = 1.0
      or
      repr = "(member ssrTemplateHelper_ (parameter ampForm *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (member element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member createElement (member document_ *)))" and
      t = "san" and
      result = 0.6388023537556248
      or
      repr = "(parameter 0 (return (member contain (member not (member to *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member cyan *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member sizes_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member String (global)))" and t = "san" and result = 0.75
      or
      repr = "(parameter 1 (return (member slice (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member ds *)" and t = "san" and result = 0.5303030303030304
      or
      repr = "(return (member concat *))" and t = "san" and result = 0.7241666666666666
      or
      repr = "(member __html *)" and t = "san" and result = 1.0
      or
      repr = "(member result *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member toolbars *))" and t = "san" and result = 1.0
      or
      repr = "(member region_ *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (return (member querySelector *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member default *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member render (return (member AmpMustache *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html *)" and t = "san" and result = 1.0
      or
      repr = "(member srcdoc *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member attributes *))" and t = "san" and result = 1.0
      or
      repr = "(member maskset (instance (return (member factory *))))" and
      t = "san" and
      result = 0.9848484848484849
      or
      repr = "(return (member listen *))" and t = "san" and result = 0.5
      or
      repr = "(return (member signalRef *))" and t = "san" and result = 0.7499999999999999
      or
      repr = "(member hostname *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member resolve *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member create (member Object (global))))" and
      t = "san" and
      result = 0.7676767676767677
      or
      repr = "(member mod *)" and t = "san" and result = 1.0
      or
      repr =
        "(member innerHTML (return (member createElementFromItem_ (parameter ampAutocomplete *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getSelectedElementsForTesting (member implementation_ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member parse *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member then (return (member all *))))" and
      t = "san" and
      result = 0.12839044652128773
      or
      repr = "(parameter 2 (return (member fine *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member inputs_ (member implementation_ *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replaceUrls *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member timeStrToMillis *))" and t = "san" and result = 0.08988168287233672
      or
      repr = "(return (member dict *))" and t = "san" and result = 0.355546902042229
      or
      repr = "(parameter 1 (return (member max *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member values (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member max *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member selection (member d3 *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlURL *)" and t = "san" and result = 1.0
      or
      repr = "(member _svg *)" and t = "san" and result = 0.9418326567859278
      or
      repr = "(member length (member items (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member devAssert *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member history_ (return (member FocusHistory *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member start *))" and t = "san" and result = 0.20797014800130076
      or
      repr = "(return (member child *))" and t = "san" and result = 0.1945309795777086
      or
      repr = "(member length (member axisItems *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member have *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member body (member document *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member transform (return (member create *))))" and
      t = "san" and
      result = 0.9207017543859649
      or
      repr = "(parameter 0 (return (member encodeURIComponent (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toArray *))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (member slice (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map *)))" and t = "san" and result = 1.0
      or
      repr = "(member hash (member location (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML *)" and t = "san" and result = 1.0
      or
      repr = "(member HtmlFormat (return (member require (member goog (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member String (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member assign *))))" and t = "san" and result = 1.0
      or
      repr = "(member min *)" and t = "san" and result = 1.0
      or
      repr = "(member ampHtmlFileContents *)" and t = "san" and result = 1.0
      or
      repr = "(return (member getAttribute (parameter element *)))" and
      t = "san" and
      result = 0.07260470751124948
      or
      repr = "(member length (instance (member selection (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pendingBuildResources_ (instance (member ResourcesImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member values *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member stringify (member JSON (global))))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(return (member call (parameter t *)))" and
      t = "san" and
      result = 0.044500000000000095
      or
      repr = "(parameter 0 (return (member userAssert *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member Number (global)))" and t = "san" and result = 0.5543907865341073
      or
      repr = "(member length (member pendingBuildResources_ (return (member ResourcesImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member mock (member sandbox *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fn *))" and t = "san" and result = 0.32162294911734157
      or
      repr = "(member channel *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member arrayBuffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member messageQueue_ (instance (member ViewerImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member returnValue *)" and t = "san" and result = 0.16666666666666669
      or
      repr = "(member length (member video (member allocated *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member createElementNS *))" and t = "san" and result = 0.3054690204222914
      or
      repr = "(member originalHash (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member items (parameter 1 (member draw (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member items (parameter e (member draw (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setSelectionRange *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member log (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByClassName *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member replace (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlRefs *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member video (member allocated (instance (member MediaPool *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member toString *))))" and t = "san" and result = 1.0
      or
      repr = "(member ampURL *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member childElementsByAttr *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member maskset (instance (return (member factory (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member keys (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member keys *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member log *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member transition (member d3 *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member UTC (member Date (global))))" and
      t = "san" and
      result = 0.8602500000000001
      or
      repr = "(member length (instance (member selection *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlFor *)" and t = "san" and result = 1.0
      or
      repr = "(return (member charCodeAt *))" and t = "san" and result = 0.3293162393162393
      or
      repr = "(member ancestorOrigins (member location *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getDetail *))" and t = "san" and result = 0.25
      or
      repr = "(return (member getByTagSpecId *))" and t = "san" and result = 0.16666666666666666
      or
      repr = "(return (member cyan *))" and t = "san" and result = 0.3765524402907582
      or
      repr = "(member length (instance (member transition *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member sizes_ (return (member parseSizeList *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member scale (parameter e *))" and t = "san" and result = 0.7613636363636362
      or
      repr = "(member length (member requestsChangeSize_ (return (member ResourcesImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member join (return (member map *))))" and
      t = "san" and
      result = 0.4666666666666668
      or
      repr = "(parameter 2 (return (member fine (return (member dev *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member body (member document (member contentWindow *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member max (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member ssrTemplateHelper_ *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (member location (parameter win *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (return (member x *))))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (member location (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member cyan (root https://www.npmjs.com/package/ansi-colors))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templates_ (parameter 0 (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (member locator *)))" and
      t = "san" and
      result = 0.38339044652128756
      or
      repr = "(member length (return (member replace (parameter value *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member max (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member head *))" and t = "san" and result = 1.0
      or
      repr = "(member ancestorOrigins *)" and t = "san" and result = 1.0
      or
      repr = "(member template_ *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter n *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member recognizers_ (return (member get (member Gestures *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member reevaluate *))" and t = "san" and result = 1.0
      or
      repr = "(return (member a *))" and t = "san" and result = 0.26986604239823125
      or
      repr = "(return (member evaluateBindings *))" and t = "san" and result = 0.16666666666666669
      or
      repr =
        "(member applySsrOrCsrTemplate (member ssrTemplateHelper_ (return (member AmpList *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member toolbars_ (member implementation_ *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member createElement (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member querySelectorAll *)))" and t = "san" and result = 1.0
      or
      repr = "(member value *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 4 (return (member log *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member join *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member events_ *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member mock *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member expect (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member messageQueue_ (return (member ViewerImpl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member transition (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member reevaluate (member prototype (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (root https://www.npmjs.com/package/@ampproject/remapping))" and
      t = "san" and
      result = 0.14416666666666664
      or
      repr = "(member length (return (member getElementsByTagName (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByClassName (member document (member win *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member url *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member split (parameter url *)))" and
      t = "san" and
      result = 0.18000000000000005
      or
      repr = "(return (member createEventObject *))" and t = "san" and result = 0.11666666666666647
      or
      repr = "(parameter 1 (return (member mod *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member encodeURIComponent (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member propTypes *)" and t = "san" and result = 0.18923491404176762
      or
      repr = "(member fragment_ *)" and t = "san" and result = 1.0
      or
      repr = "(return (member deleteModifier *))" and t = "san" and result = 0.1604411526479751
      or
      repr = "(member length (parameter 0 (return (member y *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member source *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member c *))" and t = "san" and result = 1.0
      or
      repr = "(return (member round (member Math (global))))" and t = "san" and result = 0.5
      or
      repr = "(return (member parseFloat (global)))" and t = "san" and result = 0.5
      or
      repr = "(return (member freeze *))" and t = "san" and result = 1.0
      or
      repr = "(return (member reevaluate (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member stub (member sandbox *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member head (member document (member win (member ampdoc *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getData *))" and t = "san" and result = 0.5525
      or
      repr = "(member length (member items (parameter e (member draw *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member location (member win (parameter env *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member length *))" and t = "san" and result = 1.0
      or
      repr = "(return (member count *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member parseQueryString *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member apply *))))" and t = "san" and result = 1.0
      or
      repr = "(member ampHtmlFile *)" and t = "san" and result = 1.0
      or
      repr = "(member property *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member events_ (return (member performanceFor (member Services *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member originalHash *)" and t = "san" and result = 1.0
      or
      repr = "(return (member get *))" and t = "san" and result = 0.5727291278255464
      or
      repr = "(member length (member add *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter file *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setAttribute (member element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member range *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member relative *))" and t = "san" and result = 0.19011422637590847
      or
      repr = "(return (member getParentWindowFrameElement *))" and
      t = "san" and
      result = 0.19999999999999998
      or
      repr = "(return (member scale (member exports *)))" and
      t = "san" and
      result = 0.47461499732527734
      or
      repr = "(return (member map (return (member filter *))))" and
      t = "san" and
      result = 0.6650655162460151
      or
      repr = "(member total *)" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member createElement (member document (member win *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member stub (member sandbox (parameter env *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member callCount (member have (member to *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (return (member _valueGet *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member rem *)" and t = "san" and result = 1.0
      or
      repr = "(return (member comparator (return (parameter t *))))" and t = "san" and result = 0.5
      or
      repr = "(parameter 1 (return (member slice (return (member toString *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member apply *))" and t = "san" and result = 0.3293139730639729
      or
      repr = "(member length (member stackOnPop_ *))" and t = "san" and result = 1.0
      or
      repr =
        "(member ssrTemplateHelper_ (parameter ampForm (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member last *))" and t = "san" and result = 0.25
      or
      repr = "(member hash (member location (member self (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member reverse *))" and t = "san" and result = 0.558
      or
      repr = "(return (member def *))" and t = "san" and result = 0.3125
      or
      repr = "(parameter 0 (return (member slice (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member stub *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member events_ (return (member performanceFor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member payload *)" and t = "san" and result = 1.0
      or
      repr = "(member scheme_ *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (member location (member win *)))" and t = "san" and result = 1.0
      or
      repr = "(member TEMPLATE *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member length (member have (member to *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member include *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getAttribute *))" and t = "san" and result = 0.31500000000000006
      or
      repr = "(return (member predicate *))" and t = "san" and result = 0.75
      or
      repr = "(member innerHTML (return (member createElementFromItem_ *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member assertElement *))" and t = "san" and result = 0.17424242424242403
      or
      repr = "(member pathname (parameter url *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (member document (member win *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member transform (member prototype (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member items (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member duplicate *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member include (member to *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member domain *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member dependency *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member map (return (member field *))))" and
      t = "san" and
      result = 0.6333333333333335
      or
      repr = "(return (member from *))" and t = "san" and result = 0.4375
      or
      repr = "(return (parameter _dereq_ *))" and t = "san" and result = 0.3395588473520249
      or
      repr = "(return (member replace (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member parseJson *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HtmlFormat (return (member require *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member filter *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (member document (member win *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member reevaluate (instance (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member applySsrOrCsrTemplate (member ssrTemplateHelper_ (instance (member AmpList *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setAttribute *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member ToPrimitive *))" and t = "san" and result = 0.1907894736842105
      or
      repr = "(return (member reevaluate (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member recognizers_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member str (member util (member encode *))))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member createElement *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member log *)))" and t = "san" and result = 1.0
      or
      repr = "(member findAndRenderTemplate (member templates_ (parameter ampForm *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member mod *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member detail *)" and t = "san" and result = 0.37452830188679237
      or
      repr = "(return (member parseJson *))" and t = "san" and result = 0.711093804084458
      or
      repr = "(member length (return (member getSelectedElementsForTesting *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member args (member firstCall *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter t *))" and t = "san" and result = 0.5
      or
      repr = "(member length (member history_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member toLocaleLowerCase *))" and t = "san" and result = 0.40166585086241136
      or
      repr = "(member templates_ (parameter ampForm *))" and t = "san" and result = 1.0
      or
      repr = "(member AMP.History *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member items (member mark *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (parameter n *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member atob (global))))" and t = "san" and result = 1.0
      or
      repr = "(member applySsrOrCsrTemplate (member ssrTemplateHelper_ *))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member s *)" and t = "san" and result = 0.37393215645552097
      or
      repr = "(return (member createElement (member document (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member rem *))" and t = "san" and result = 1.0
      or
      repr = "(member add *)" and t = "san" and result = 1.0
      or
      repr = "(member htmlFormat *)" and t = "san" and result = 1.0
      or
      repr = "(member ssrTemplateHelper_ (parameter 0 (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member pop *))" and t = "san" and result = 0.5
      or
      repr =
        "(member length (member requestsChangeSize_ (member resources_ (return (member MutatorImpl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member localStorage (instance (member FakeWindow *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member assertElement (return (member dev *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member max *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extend *))" and t = "san" and result = 0.75
      or
      repr = "(member length (member localStorage *))" and t = "san" and result = 1.0
      or
      repr = "(member pathCache *)" and t = "san" and result = 0.5
      or
      repr = "(return (member replace (return (member slice *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member stackOnPop_ (return (member History *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member transform *))" and t = "san" and result = 0.2500000000000001
      or
      repr = "(member HTMLElement *)" and t = "san" and result = 1.0
      or
      repr = "(member defaultProps *)" and t = "san" and result = 0.18923491404176762
      or
      repr = "(return (member day *))" and t = "san" and result = 0.08310418830044988
      or
      repr = "(member minifiedCreative *)" and t = "san" and result = 1.0
      or
      repr = "(return (return (parameter t *)))" and t = "san" and result = 0.25
      or
      repr = "(parameter 0 (return (member whenCalled *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member warn (return (member dev *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member stackOnPop_ (instance (member History *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member toolbars_ *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member args *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (return (member toString *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member expect (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 2 (return (member log *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member toolbars_ (member implementation_ (parameter sidebarElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member contain (member not *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member mock (member sandbox (parameter env *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parse (return (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member ToNumber *))" and t = "san" and result = 0.08881578947368424
      or
      repr = "(return (member Array (global)))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (member length (member have *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member replace (parameter url *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member have (member to *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member legendItems *))" and t = "san" and result = 1.0
      or
      repr = "(return (member devAssert *))" and t = "san" and result = 0.19999999999999998
      or
      repr = "(member length (return (member getAllWindows (member controller (parameter env *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member items (parameter 1 (member draw *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member valueOf *))" and t = "san" and result = 0.5
      or
      repr = "(return (member sendMessageAwaitResponse *))" and
      t = "san" and
      result = 0.10380235375562474
      or
      repr = "(return (parameter n *))" and t = "san" and result = 0.8333333333333333
      or
      repr = "(member length (member pages_ *))" and t = "san" and result = 1.0
      or
      repr = "(member findAndRenderTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member adResponseType *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member coordinates *))" and t = "san" and result = 1.0
      or
      repr = "(member HtmlFormat *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member length *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member callCount *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getNextPageId *))" and t = "san" and result = 0.08712121212121204
      or
      repr = "(return (member createElement *))" and t = "san" and result = 0.75
      or
      repr = "(member length (return (member filter (member events_ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member indexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getAllWindows *)))" and t = "san" and result = 1.0
      or
      repr = "(member TEMPLATE (member AdResponseType *))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 0 (return (member then *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member slice (parameter e *)))" and t = "san" and result = 0.7575757575757576
      or
      repr = "(parameter 0 (return (member callCount (member have *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member credentials_ *)" and t = "san" and result = 1.0
      or
      repr = "(return (member scale *))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (member contain *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member ingest (member Tuple (return (parameter t *)))))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member length (member slides_ *))" and t = "san" and result = 1.0
      or
      repr = "(member password *)" and t = "san" and result = 0.3787878787878787
      or
      repr = "(parameter 0 (return (member update *)))" and t = "san" and result = 1.0
      or
      repr = "(member template_id *)" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member nativeElement *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member delete *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member get *)))" and t = "san" and result = 1.0
      or
      repr = "(member invoiceTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member temporaryToken *)" and t = "san" and result = 0.125
      or
      repr =
        "(member length (member childNodes (member firstChild (member lastChild (member lastChild *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member selector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member access (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member set (member select *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateSettings (member exports (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member target (member pseudos *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member index *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (member _ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter text (member highlightText (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member indexOf (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member undefined (global)))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member b *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter f *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 2 (member ATTR *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member addClass *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member childNodes (member firstChild (return (member appendChild *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member TAG (member find (member selectors *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateSettings (member _ *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter url (member load *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (parameter 0 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member each *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (member noConflict (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member tokenize (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member tokenize (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member access (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member ATTR (member filter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html2canvas (member undefined (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member indexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member length (instance (member RegExp (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member RegExp (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member url (return (member ajaxSetup *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member init (member fn *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member lastIndexOf (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member init (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member checksheetdata (parameter $scope *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _wrapped (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member tweens (member anim *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member access (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (return (member shift *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member collect (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (member firstChild *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member set (member select (member valHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member noConflict (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter 0 (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (parameter 1 (member remove *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 1 (return (member html *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member merge *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member concat (member props *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tweens *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member buildFragment *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member compile (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member url *)))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member tokenize *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member lastIndexOf (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter g (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member map (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member TAG (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member sortedIndex (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member indexOf (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member checksheetdata *))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _wrapped *))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member find (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member each *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member timers (return (member noConflict (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter g *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (member nodeValue (parameter node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member sortedIndex (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tweens (return (member promise *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter text (member highlightText *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute (member document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter results (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (parameter 0 (return (member push *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter 0 (member get *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member each (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member map (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (return (member noConflict (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member sortedIndex (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers *))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member find (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member compile *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member inArray *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member trim (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html2canvas *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 2 (member ATTR (member filter (member selectors *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member target (member pseudos (member selectors *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member trim *))))" and t = "san" and result = 1.0
      or
      repr = "(member templateSettings (member _ (member exports (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member forEach (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member innerHTML *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter seed *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (parameter 1 (member add *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member collect (member _ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member set *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member trim (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member tokenize (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member error *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateSettings *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter src *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (return (member noConflict (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member access (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member load (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member childNodes (member lastChild *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter 0 (member clone *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member access (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member collect (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member access (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter 0 (member set (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member forEach *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter url *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member textContent *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member timers (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member compile (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member prevAll (return (member first *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member target *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member jQuery *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member access *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute (member ownerDocument *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member $ *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member childNodes (member lastChild (return (member appendChild *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html2canvas (global))" and t = "san" and result = 1.0
      or
      repr = "(member html2canvas (parameter window *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member RegExp (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member childNodes (member lastChild (member lastChild (member lastChild *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter b *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member load *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (return (member camelCase *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member init *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter a (member load *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member load (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member url (parameter 0 (return (member ajax *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member tokenize (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (return (member noConflict (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member prevAll *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member access (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member index (parameter 0 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter a (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member noConflict (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member trim (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member collect (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member map (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member tokenize (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter elem *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member parentNode *))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter selector (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member access (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member apply *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateSettings (member exports *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member trim (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member grep *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member forEach (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 1 (return (member access *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member forEach (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (return (member noConflict (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member timers (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member each (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member load (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member shift (return (member exec *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter 0 (member set *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member collect *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member text *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member collect (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member PSEUDO (member filter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member compile (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member target (member pseudos (member selectors (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member trim (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member $ (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member sortedIndex *)))" and t = "san" and result = 1.0
      or
      repr = "(member templateSettings (member exports (member module (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (member url *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member collect (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter 0 (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter a *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member access *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter dest *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter results *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter c *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter 0 (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member tokenize *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member find *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter obj (member each *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member length (member length *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member childNodes (return (member appendChild *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member trim *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 2 (member ATTR (member filter *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member timers (member constructor (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member merge *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member map *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter tokens *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member access (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member tokenize (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member call *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member timers (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter f (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member find (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member ATTR *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member tokenize (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member lastIndexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member init (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (instance (member init *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member highlightText (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member trim (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member highlightText *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (parameter types *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html2canvas (member window (global)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter 0 (member load *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter 0 (member get (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member domManip *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter seed (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 0 (member access (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member tokenize (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member length (return (member RegExp (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member removeClass *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone *)" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member access (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member length (member length (member length (member length *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member tokenize (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member TAG *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member access (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member $ (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 1 (return (member css *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member compile (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member loadImage (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member appendChild *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member shift (member matches *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member length (member length (member length *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member props *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member indexOf (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member error (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter 0 (member tweener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 1 (return (member access *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member options *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member each (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member constructor (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member lastIndexOf (member _ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (parameter a *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member access (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member access (member $ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (instance (member init *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member collect *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member find (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member call (member slice *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member url *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter a (member map *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member childNodes (member firstChild (member lastChild *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member load (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member tokenize *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member find (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (member matches *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member shift *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member timers (return (member noConflict (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member init (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (member PSEUDO *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter selector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (member lastChild (member lastChild *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member childNodes (member lastChild (return (member buildFragment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (member nodeValue *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member loadImage *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member access (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter a (member each *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member length *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member initialize (return (member strength *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member $watch *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _groups (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member n (instance (member Integrator *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member $eval *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member rows (member table *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter selectedArray *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter series *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member RegExp (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member RegExp (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _groups (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member moveTo *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isFunction *)))" and t = "san" and result = 1.0
      or
      repr = "(member n *)" and t = "san" and result = 1.0
      or
      repr = "(return (member templateUrl *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member pow *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member rows *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter _ *))" and t = "san" and result = 1.0
      or
      repr = "(member templateUrl (parameter attrs *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member pow (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _groups *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isFunction (member angular (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member initialize *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member changedTouches (member event *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _groups (instance (member selection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member attr *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (parameter response *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter src *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateUrl (parameter 1 (member templateUrl *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member changedTouches *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member $eval (parameter scope (member link *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _groups (instance (member transition *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateUrl (parameter attrs (member templateUrl *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter src (member output *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isDefined (member angular (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter _ (member initialize (return (member strength *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member $eval (parameter scope *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter _ (member initialize *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter polygon *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member $eval (parameter 0 (member link *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter options (parameter 1 (return (member registerHelper *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member article *)" and t = "san" and result = 0.3181818181818187
      or
      repr = "(member length (parameter 1 (parameter 0 (return (member exec *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter 1 (parameter 1 (return (member registerHelper *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member spliceWithArray (member $mobx *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member teams *)" and t = "san" and result = 0.005004774947426887
      or
      repr = "(member length (member values (member $mobx *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member expect (member chai (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member h *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member info (return (member get *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter e (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member toString (member jugend (member IDs *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member dangerouslySetInnerHTML *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member spiele *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member info *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member calcSpielDateTime *))" and t = "san" and result = 0.38661202185792354
      or
      repr = "(member title *)" and t = "san" and result = 1.0
      or
      repr = "(return (member push *))" and t = "san" and result = 1.0
      or
      repr = "(member h (parameter a *))" and t = "san" and result = 1.0
      or
      repr = "(member _namespaceURI *)" and t = "san" and result = 1.0
      or
      repr = "(member start *)" and t = "san" and result = 1.0
      or
      repr = "(return (member getTargetSelectorSuffix *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (parameter n *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member teams *))" and t = "san" and result = 1.0
      or
      repr = "(member templateUrl (return (member get (return (member get *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(member spiele *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter x *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter apiProject *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member datasets *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter options *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member exec *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter e (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member jugend *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member observing *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member children *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter data (return (member x *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member datasets (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter points *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (return (parameter n *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member observing (instance (member Reaction *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member gruppen *)" and t = "san" and result = 0.1950332531541453
      or
      repr = "(parameter 0 (return (return (parameter n *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter d *))" and t = "san" and result = 1.0
      or
      repr = "(member unusedTeams *)" and t = "san" and result = 0.7241666666666666
      or
      repr = "(return (parameter 0 (return (member reduce *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (parameter n *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member map *))" and t = "san" and result = 0.75
      or
      repr = "(parameter 0 (return (member spliceWithArray *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (return (member getMeta *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member find *))" and t = "san" and result = 1.0
      or
      repr = "(return (member expect *))" and t = "san" and result = 1.0
      or
      repr = "(return (member expect (root https://www.npmjs.com/package/chai)))" and
      t = "san" and
      result = 0.17999999999999983
      or
      repr = "(member length (member observing (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member templateUrl (return (member get *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member values *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member observing (instance (member Reaction (parameter t *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member get (return (member get *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter data (return (member y *))))" and t = "san" and result = 1.0
      or
      repr = "(member _model *)" and t = "san" and result = 0.4870833333333333
      or
      repr = "(member length (return (member id *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member grep (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByName *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member left *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter name (member attr *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member parse (member JSON (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _pathToAbsolute (return (member ninja *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member grep (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 0 (member path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 14 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize (member support (member jQuery (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member context *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter target (member apply *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member htmlSerialize (member support (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member register *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member coords *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member $ (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setEnd (return (member createRange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member points *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member path2curve (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (parameter 1 (member activate *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member jQuery (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member minWidth (member style (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member html5Clone (member support (member constructor (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (instance (member Event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter selector (member tokenize (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 1 (return (member map (parameter jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member minWidth (member style *)))" and t = "san" and result = 1.0
      or
      repr = "(member pointsize (member datapoints *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter elems (member buildFragment *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member minlength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _path2curve (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arg *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getContent *))" and t = "san" and result = 1.0
      or
      repr =
        "(member html5Clone (member support (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter str (member htmlEncode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member $ (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _pathToAbsolute (member raphael (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter target *))" and t = "san" and result = 1.0
      or
      repr = "(member content (parameter o (parameter 0 (return (member add *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (return (member Event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (parameter container (member activate *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (return (member camelCase *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (member trigger (member options *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member 0 (return (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (return (member Event (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member Buffer (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (parameter o (member _postProcess *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (parameter event (member trigger *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (member DOM *)))" and t = "san" and result = 1.0
      or
      repr = "(member mData *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member setup (member submit (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member path2curve (member raphael *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filter (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member append (instance (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter text2 *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _fnSortFlatten *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member $ (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter expr *)))" and t = "san" and result = 1.0
      or
      repr = "(member data (parameter s *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter context *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (return (member get *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (member result (member triggered *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member $ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member replace (return (member attr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member display *))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member Sizzle (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member maxWidth (member style *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _pathToAbsolute *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member left (member style (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 0 (member _path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByName (member document (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter first *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member $tip (instance (member Constructor (member tooltip *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member x *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member columns (member s *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member offsets (instance (member Constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member width (member style (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (parameter 1 (parameter 0 (return (member add *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member left (member style *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member grep *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member htmlSerialize (member support (member constructor (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member htmlEncode (member str *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter path2 (parameter 0 (member path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member processHTML (parameter 1 (return (member create *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member firstChild *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member access (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _pathToAbsolute (member raphael *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter path2 (parameter 0 (member _path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter selector (member tokenize (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter str (member htmlEncode (member str *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member attr (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member access *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (parameter 0 (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member values *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member options (parameter elem (member set (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member triggered *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member append *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter url (member load (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member maxlength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member constructor *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fnDisplayEnd *))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (instance (member Line *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _iDisplayLength (parameter settings *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (parameter elem *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member width (member style *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member append (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member post (member $http *)))" and
      t = "san" and
      result = 0.48138888888888887
      or
      repr = "(member length (parameter 0 (member attr *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member minlength *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member processHTML *))" and t = "san" and result = 1.0
      or
      repr = "(member responseText *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member coords (member attrHandle (member expr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member f *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter v *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member options (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member Sizzle (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (return (member proxy *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (return (member noConflict (member Sizzle *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member aoData (parameter settings *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member html *)))" and t = "san" and result = 1.0
      or
      repr = "(member data *)" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member jQuery (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter c *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter elems (member grep *)))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member extend (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member grep (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter undefined *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member default *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member errorLabelContainer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member path2curve (return (member ninja *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (parameter s (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member left (member style (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member text *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member access (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter selector (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member test *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member String (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (parameter 0 (member tweener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member timers (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (parameter o *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member grep (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member jQuery (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member triggered (member event (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member onreadystatechange *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (instance (member Event (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type (parameter event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member triggered (member event (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (return (member toLowerCase *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter selector *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _path2curve (member raphael (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member exports (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member trigger *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member offsets (instance (member Constructor (member scrollspy *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter n *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member aiDisplay *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member create *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (parameter 0 (member trigger (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (return (member Event (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member append (instance (member init *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member startTime (parameter 0 (member anim *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member width *)" and t = "san" and result = 0.11999999999999994
      or
      repr = "(member _length *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setEnd *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member access (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (parameter props *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member constructor (member fn (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member text (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter h (member processHTML *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (member value *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (parameter n (member trigger *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (member className *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter 0 (member trigger *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter fn *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter name (parameter 1 (return (member access *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter expr (member matchesSelector (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member shift (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member content (parameter o *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member grep (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member menu *)" and t = "san" and result = 0.9052722715880611
      or
      repr = "(member length (parameter url (member load (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (member jQuery (parameter A *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter list *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member find (member $element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member ID (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member call *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize (member support (member $ (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member onreadystatechange (return (member xhr (parameter s *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseTransformString (member raphael *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filter (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member init (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member append (instance (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (parameter 0 (member _postProcess *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter check (member ATTR (member filter (member selectors *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member triggerHandler *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member anCells *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseJSON *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member result (member triggered (member event (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member html5Clone (member support (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member $ (parameter A *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter e *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member tabIndex *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member proxy *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member events *))" and t = "san" and result = 1.0
      or
      repr = "(member template (member options (instance (member Constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member htmlSerialize (member support (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (member tabIndex (member propHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member breakpoints *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter first (member merge *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member getElementById *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member breakpoints (member c *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (parameter n (member trigger *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (member $ (member elem *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member style (member attrHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member ykeys *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize (member support (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member set (return (member set *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member columns (member s (instance (member Responsive *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member trigger (member blur (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member minlength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (return (member handle *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _columnsVisiblity (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member $ *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter path2 (parameter 0 (member _path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member parseTransformString (member raphael (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter elem *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member setup *)))" and t = "san" and result = 1.0
      or
      repr = "(member url (parameter requestSettings *))" and t = "san" and result = 1.0
      or
      repr = "(member data (return (member extend (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter attrs *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member triggered (member event (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter path2 (parameter 0 (member path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter match (member compile (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter e *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member set (member button (member valHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter dest *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (parameter elem (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (parameter container *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member $ (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member query (return (member on *))))" and
      t = "san" and
      result = 0.03730798546638244
      or
      repr = "(member data (return (member extend (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member call (member slice (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 0 (return (member map *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member nodeValue *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member path2curve (member raphael (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member id (member attrHandle (member expr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (return (member addListTxt *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter match (member compile (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member errorList *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member _l (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter buttons *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member not *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter name *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter name (member attr (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member name (member attrHandle (member expr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type (member triggered *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (member style *))))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize (member support (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member $tip *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member maxlength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (parameter undefined *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member htmlSerialize (member support (member constructor (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member grep (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member _path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member selector (parameter selector *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member handle *)))" and t = "san" and result = 1.0
      or
      repr = "(member data (parameter 0 (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByName (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member startTime (parameter 0 (return (member promise *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member prefilters *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (instance (member Event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter val *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member undefined (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member $parent (instance (member Constructor (member collapse *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter expr (member matchesSelector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member jQuery (member elem *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member jQuery *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member _fnAddData (parameter 1 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter path2 (parameter 0 (member path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter second *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member innerHTML (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member String (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (return (member attr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pointsize *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member timers (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 0 (member path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter path2 *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member $ (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member $parent *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member setup (member change *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter event (member trigger *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member by *))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize (member support (parameter 0 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member String (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member splice (parameter seed *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member htmlSerialize (member support (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getElementById (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter check (member ATTR *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter selector (member tokenize (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (return (member Event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member y *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isNaN (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member path2curve (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member isLocal *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member grep (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member $parent (instance (member Constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter d *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 1 (return (member register *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member _l *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member values (instance (member Donut *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (return (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter selector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter name (parameter 1 (return (member css *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member grep (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member _pathToAbsolute (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter unmatched *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _columnsVisiblity *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member set (member value *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter name (member removeData *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member map (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter key (member remove *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (instance (member Line (member Morris *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member rangelength *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member button (member valHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member name *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member _l (member prototype (parameter t *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 10 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member set (member value (member attrHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filter (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member iframeHTML *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member Event (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setContent (member selection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member append (return (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (instance (member Event (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member set (member button *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter n (member trigger (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member processHTML *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter e (member load *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 1 (return (member map *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member rangelength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member triggered (member event (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (parameter types (member remove *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (parameter s *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (parameter 0 (return (member ajax *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _path2curve (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter elem (member get *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member trigger (member click (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _path2curve (member raphael (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter text1 *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys (member _ *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter 0 (member toUrl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter selector (member tokenize *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member data (parameter s *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (member value (member attrHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter elems (member access *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member attrsList (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member add *))" and t = "san" and result = 0.42
      or
      repr = "(member htmlSerialize (member support (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member pushStack *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member exports (member module (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter elem (member set *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member values (instance (member Donut (member Morris *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member ykeys (member options *)))" and t = "san" and result = 1.0
      or
      repr = "(member elementName *)" and t = "san" and result = 0.03326623251435287
      or
      repr = "(return (member fnRecordsTotal *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member maxWidth *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (parameter 0 (member _path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member append (return (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member path2curve (member raphael (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member setup (member change (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member concat (parameter name *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseTransformString *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member add (member Event (member dom *))))" and
      t = "san" and
      result = 0.5800000000000001
      or
      repr = "(member length (return (member Event (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter b (member toUrl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member url (parameter s *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member _fnAddData *))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member jQuery *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (member result (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter props *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 1 (member matchesSelector (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member format *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter h *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member next *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member onreadystatechange *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter props (member tweener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member labelContainer *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member moveEnd (return (member createRange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter n *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member triggered (member event (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter matchers *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member _path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter elem (member clone *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member columns *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event (return (member noConflict *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (instance (member Event (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member version *)" and t = "san" and result = 0.7293771043771043
      or
      repr = "(member length (member attrsList *))" and t = "san" and result = 1.0
      or
      repr = "(return (member tokenize (member Sizzle *)))" and t = "san" and result = 1.0
      or
      repr = "(member outerHTML (return (member cloneNode *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setContent (member selection (member editor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter event *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlSerialize *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member id (member attrHandle *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pos (return (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member assertions *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member trigger (member click *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arg (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter setMatchers *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member append (return (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member maxlength *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member queue (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter value *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member yytext (parameter yy_ (member performAction (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member jQuery (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member $ (member elem *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member points (parameter datapoints *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (parameter props (member tweener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getContent (parameter 1 (return (member create *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member startTime *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter h *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _path2curve *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member values (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (instance (member Event (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getClientRects (parameter 0 (member visible *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member handle (member change *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (instance (member Bar (member Morris *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (parameter requestSettings *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (return (member set *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member setup (member submit *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter value (member removeClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member textContent (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (root https://www.npmjs.com/package/jquery))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (return (member noConflict (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter group (member compile (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getClientRects (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member size *))" and t = "san" and result = 1.0
      or
      repr = "(member data (return (member extend (member jQuery (parameter A *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter second (member merge *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (parameter event (member trigger (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseTransformString (member raphael (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member triggered *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter elems (member map *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter group *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member _pathToAbsolute (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter contexts *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (parameter t *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getCleanUrl *))" and t = "san" and result = 1.0
      or
      repr = "(return (member get (member DOM *)))" and t = "san" and result = 0.7222222222222222
      or
      repr = "(parameter 1 (parameter 0 (return (return (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member startTime *))" and t = "san" and result = 1.0
      or
      repr =
        "(member html5Clone (member support (member constructor (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member assertions (member current *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member onreadystatechange (return (member xhr *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member minWidth *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member bx *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member handle (return (member _data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pos (parameter nc (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(member errorContext *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member assertions (member current (member config *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member aoColumns (parameter settings *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (member url *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member removeData *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member getContent *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter o (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member result *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member htmlEncode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (member fn (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _path2curve (member raphael *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter 0 (member removeClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member hash (member location (parameter window *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member maxlength *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member $ (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member inArray *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter responseText *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (parameter 0 (member path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member splice (parameter results *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member attr *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter group (member compile (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (member trigger *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (return (member Event (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member aoData *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (return (member parseXML *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter 1 (member remove *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseTransformString (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member path2curve *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (parameter name *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member width *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fnRecordsDisplay *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter match (member compile *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (member triggered *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _path2curve (return (member ninja *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter check *))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member extend (member jQuery (member elem *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member options (instance (member Constructor (member tooltip *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member $ (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member name (member attrHandle *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member jQuery (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member _encode *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member ID (member find (member selectors *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter group (member compile (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setContent *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member maxWidth (member style (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter group (member compile (member find (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter path2 (parameter 0 (member _path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member children (return (member children *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter url (member load (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute (parameter node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter str *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member init (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member teardown *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter match *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member offsets *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member globalEval (member jQuery (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member map (parameter key *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (parameter 0 (member trigger *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member equal (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (parameter value (member addClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _iDisplayLength *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter value (member rangelength (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member merge *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter elements *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member movies *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member _path2curve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member $tip (instance (member Constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filter (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member replace (member url *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member triggered (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (parameter n *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member constructor *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member moveEnd *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member maxWidth (member style (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (parameter s (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member html *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member find (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (parameter event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (parameter n *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter collection *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (parameter types (member add *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member exports (member module (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member width (member style (parameter elem *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlSerialize (member support (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member trigger (member blur *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member isLocal (member ajaxSettings *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member trim (parameter $ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member marginRight (member cssHooks *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member jQuery (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member sTitle *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter check (member ATTR (member filter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter checkSet *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member parseTransformString (return (member ninja *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member handle (member change (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member $ (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getClientRects (parameter elem (member visible *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member minWidth (member style (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member aoColumns (parameter oSettings *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter 0 (member _path2curve (member raphael *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (parameter selector (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter match (member compile (member find (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (parameter n *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member button *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter a *))" and t = "san" and result = 0.5085858585858585
      or
      repr = "(parameter 4 (return (member access (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member cloneNode *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member init (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member rangelength *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member init (member prototype (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getClientRects *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (instance (member Bar *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter 1 (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member extend (parameter $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member jQuery (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member teardown (member submit *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (return (member Event (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member onreadystatechange (return (member xhr (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member processHTML *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member minlength *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter url *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member triggered (member event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (member innerHTML *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member propCount *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member parseTransformString (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member teardown (member submit (member special *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member html5Clone (member support (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member create (member dom *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result *))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member access (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (return (member parseFromString *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member options *))" and t = "san" and result = 1.0
      or
      repr = "(return (member encode (member DOM (member tinymce (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter a (parameter 0 (return (member createPseudo *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member errorList (return (member validate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member get (member default (root https://www.npmjs.com/package/axios))))" and
      t = "san" and
      result = 0.7172222222222222
      or
      repr = "(member length (return (member coords (member attrHandle *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member tokenize (member Sizzle (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member height *)" and t = "san" and result = 1.0
      or
      repr = "(member pos *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member marginRight *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (member $ (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (parameter src *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member exports (member module (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (parameter n (parameter 1 (return (member ajaxPrefilter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member extend (member $ (parameter A *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member get (member $http *)))" and t = "san" and result = 0.2222222222222222
      or
      repr = "(member length (parameter match (member compile (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member plugins *))" and t = "san" and result = 1.0
      or
      repr = "(member seller *)" and t = "san" and result = 0.7537878787878788
      or
      repr = "(member length (return (member attr (parameter $this *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member _columnsVisiblity (member prototype (member Responsive *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member now *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter url (member load *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member run *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter selector (member tokenize (member find (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member access (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter group (member compile *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member parseXML *)))" and t = "san" and result = 1.0
      or
      repr = "(member desc *)" and t = "san" and result = 0.5
      or
      repr = "(return (member replace (parameter 1 (member matchesSelector *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (member triggered (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member timers (member $ (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member _pathToAbsolute (member raphael (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html5Clone (member support (member jQuery (parameter window *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 1 (return (member setHTML *))))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member extend (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter fn *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter url (member load (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pos (parameter nc *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member deps *))" and t = "san" and result = 1.0
      or
      repr = "(member html5Clone (member support (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member _encode *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (member result (parameter event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member getAttribute (parameter context *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member options (parameter elem (member get (member select *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member queue (member $ (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter 0 (member addClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pos (parameter 0 (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member content *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member aoColumns *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member ID *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (member jQuery (parameter window *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member breakpoints (member c (instance (member Responsive *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member create (member dom (member editor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member url *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (member path2curve (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Event (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member idx *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementsByName (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member access (member jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter stripAndCollapse *))" and t = "san" and result = 0.4175
      or
      repr = "(parameter 0 (return (parameter stripAndCollapse *)))" and
      t = "san" and
      result = 0.25000000000000006
      or
      repr = "(return (member then *))" and t = "san" and result = 0.25
      or
      repr = "(member length (return (member keys (member placeholder *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member unput (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member entries (parameter result *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter key *))" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member info (member logger *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (parameter __webpack_require__ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member toHTML (parameter 0 (member escapeExpression *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter 1 (return (member if (member helpers *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter s *))" and t = "san" and result = 1.0
      or
      repr = "(return (member slice (parameter a *)))" and
      t = "san" and
      result = 0.07977514921323925
      or
      repr = "(member blockParams (return (member Program (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (parameter 1 (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member literal *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member generateList (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (parameter data *)))" and
      t = "san" and
      result = 0.18312804123466941
      or
      repr = "(member length (return (member getAll *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getOwnPropertyNames *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (member source *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (instance (member Socket *)))" and t = "san" and result = 1.0
      or
      repr = "(member bytesRead (parameter self *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member _prev *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter base64 *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter exprList (member toAssignableList (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _streams *))" and t = "san" and result = 1.0
      or
      repr = "(return (member cast *))" and t = "san" and result = 0.6996239817206438
      or
      repr = "(return (member callExpression *))" and t = "san" and result = 0.05882352941176472
      or
      repr = "(member length (parameter ch (member unput (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member deepEqual (member assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member resolvePath *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member byteLength (member SlowBuffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member environments *))" and t = "san" and result = 1.0
      or
      repr = "(member path (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (root https://www.npmjs.com/package/stream-splicer)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member flatten *)))" and t = "san" and result = 1.0
      or
      repr = "(member 0 (instance (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter str (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member blockParams (parameter program (member Program *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter url *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member LocalStorage *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member copy (parameter data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member _cache (parameter self *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter 0 (return (member restParam *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter string *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member size *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member shallowClone *))" and t = "san" and result = 0.09413694967961772
      or
      repr = "(member length (return (member dump (instance (member LRUCache *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member upgrades (instance (member Socket (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toAssignableList (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter keysFunc *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 1 (return (member on *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter connections (member auth (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isFinite (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member BN (member BN *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter c *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member max (member Math (member Object *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter 0 (member helperExpression (member helpers *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member toAssignableList *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _array *))" and t = "san" and result = 1.0
      or
      repr = "(member index (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member position (instance (member Binary (parameter exports (member binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member errors *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pairs (parameter 0 (member Hash (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter stack *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member params (parameter 0 (member helperExpression (member helpers *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member avail_in *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Buf8 (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member BerReader *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member blockParams (member options *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member length *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member binstring2buf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter values *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter exprList *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member arraylike *)))" and t = "san" and result = 1.0
      or
      repr = "(member toHTML (parameter string *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member entries *))" and t = "san" and result = 1.0
      or
      repr = "(return (member write (member prototype (member SlowBuffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member buffer (instance (member Binary *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member blockParams (parameter program *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member Buffer (parameter m *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (member url *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter partials *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member BN (member BN (member BN *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member Socket *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter packets (member write (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member spawn *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter statements *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter doc *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member state (parameter 0 (member inflate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter string (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member toAssignableList (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member blockParams (parameter 0 (member Program *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member add (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter docs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (parameter environment *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member sliceEnd *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member prerelease (instance (member SemVer (member SemVer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member size (parameter encoded *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter 2 (return (member call *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member variableDeclarator *))" and t = "san" and result = 0.5833333333333334
      or
      repr = "(return (member identifier *))" and t = "san" and result = 0.05882352941176473
      or
      repr = "(parameter 0 (return (member Array (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member decode (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter array (member acceptArray (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member LocalStorage (member internals *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter iterable (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice *))" and t = "san" and result = 0.5
      or
      repr = "(member length (parameter ch *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (return (member require (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getValue *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member writeUInt32BE *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter connections (member auth *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templates (parameter opts *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member children (parameter environment (member compileChildren (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member byteLength (parameter encoded *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _items (instance (member Topo *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member log *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member strictEqual *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member encryptBlock (member _cipher *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member blockStatement *))" and t = "san" and result = 0.7696078608149964
      or
      repr =
        "(member length (member state (parameter strm (member inflate (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevBufferLen *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (parameter partial (member PartialStatement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getFieldNames *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member slice (instance (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 1 (return (member genSalt *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member expect (root https://www.npmjs.com/package/code)))" and
      t = "san" and
      result = 0.061988300377492034
      or
      repr = "(member templates (parameter opts (member cli (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member call *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(member type (return (member readToken *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member Uint8Array (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member write (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member pattern *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member skips *))" and t = "san" and result = 1.0
      or
      repr = "(member yyleng *)" and t = "san" and result = 1.0
      or
      repr = "(return (member substring *))" and t = "san" and result = 0.34955655404030417
      or
      repr = "(member length (return (member LocalStorage *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member kind (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Buf8 *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member copy *)))" and t = "san" and result = 1.0
      or
      repr = "(member depth (return (member wrapProgram (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member indexOf (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member info (return (member plugin *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter upgrades (member filterUpgrades *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member value (member source *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter a (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/assume)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member BN (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (return (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member decode *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (instance (member compiler *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member key (parameter 0 (return (member get *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter openRawBlock *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member pow *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 0 (return (member filter *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter buffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member stringify *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member utf8border (parameter exports *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter parts (member resolvePath (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member min (member Math (member Object *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member pow (member Math (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member stubBuffer (parameter self *)))" and t = "san" and result = 1.0
      or
      repr = "(member yyleng (return (member unput (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body (member body (parameter node *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 0 (parameter 0 (member domManip *)))" and
      t = "san" and
      result = 0.06334428623585248
      or
      repr =
        "(member prevBufferLen (instance (member Socket (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (parameter arr (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter entries (member generateList (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter p *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member body (parameter program (member Program *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member listing (instance (member Emitter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member ___eio *))" and t = "san" and result = 1.0
      or
      repr = "(member 1 (instance (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member equal (member t *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member call (parameter context *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Content-Length (parameter 1 (return (member writeHead (parameter res *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member blockStatement (member builders *)))" and
      t = "san" and
      result = 0.313725472518337
      or
      repr = "(return (member ObjectId (member Types *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member Buffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member triggered (member event (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state (parameter strm (member inflate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member log *)))" and t = "san" and result = 1.0
      or
      repr = "(member size (return (member create *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter value (member checkRequired (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member dirname (root https://www.npmjs.com/package/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member toHTML (return (member escapeExpression (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parseJSON *))" and t = "san" and result = 0.04086598839033016
      or
      repr = "(member upsertedCount (parameter r *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter openBlock *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter 2 (return (member call (member if *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member utf8border *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member skips (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member results *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member directories (parameter res *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member blockParams (parameter 0 (member Program (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member prerelease (instance (member SemVer *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member keys (root https://www.npmjs.com/package/underscore))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (parameter 1 (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pattern *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter params (member pushParams (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member params (parameter node (member helperExpression (member helpers *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member strictEqual *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member url (parameter req *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (instance (member init (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member indexOf (member stack *)))" and t = "san" and result = 1.0
      or
      repr = "(member offset *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter node (member helperExpression (member helpers *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member params (parameter 0 (member helperExpression *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member Socket (member Socket (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (return (member map *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member byteLength (member buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member byteLength (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toString *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member byteLength (member Buffer (parameter m *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Buffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member matches *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member dirname (return (member path *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (return (member split (parameter path *)))))" and
      t = "san" and
      result = 0.19810835629017476
      or
      repr = "(member length (parameter parts (member resolvePath *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yyleng (return (member unput (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member global (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member charReceived (instance (member StringDecoder (root https://www.npmjs.com/package/string_decoder))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member type (return (member readToken (member prototype (member LooseParser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (return (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (member authStore *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member was *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (return (member split *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _items (instance (root https://www.npmjs.com/package/topo))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member same *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member body (parameter 0 (member Program (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toLowerCase (parameter 1 (member attr *))))" and
      t = "san" and
      result = 0.23211743752610914
      or
      repr = "(parameter 3 (return (member pbkdf2Sync *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter string *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member checkRequired (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member before *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member lookup *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member yytext *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member listing *))" and t = "san" and result = 1.0
      or
      repr = "(return (member utf8border *))" and t = "san" and result = 1.0
      or
      repr = "(member templates (parameter opts (member cli *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter exprList (member toAssignableList *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member opcodes (member environment *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member auth *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter peers *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member strictEqual (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member value *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (parameter st *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (member environment (instance (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member names (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member Object *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member values (member _ *))))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (parameter 1 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member module (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter self *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (parameter c *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter str (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member utf8border (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter upgrades (member filterUpgrades (member prototype (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (parameter 0 (member compileChildren *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member deepEqual *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (parameter node (member Function *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (parameter 0 (member compile *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter 0 (member enable *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (parameter 0 (return (member equal (parameter t *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member specifiers (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member filterUpgrades (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member teardown *)" and t = "san" and result = 0.007575757575757569
      or
      repr = "(return (member flatten *))" and t = "san" and result = 0.5800000000000001
      or
      repr = "(return (member byteLength (member SlowBuffer (parameter m *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member map (return (member concat *))))" and
      t = "san" and
      result = 0.25749999999999995
      or
      repr = "(return (member join (root https://www.npmjs.com/package/path)))" and
      t = "san" and
      result = 0.323704073117425
      or
      repr = "(member length (member state (parameter strm *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member buffer (return (member BerWriter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (parameter 0 (member load *))))" and
      t = "san" and
      result = 0.12011242539338028
      or
      repr = "(member hash (parameter open *))" and t = "san" and result = 1.0
      or
      repr = "(return (member toString (parameter err *)))" and
      t = "san" and
      result = 0.9848484848484849
      or
      repr = "(parameter 1 (return (member info (return (member Logger *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (parameter mongoObject *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member write *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member slice (member bin *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member children (instance (member SourceNode (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member parseInt (global))))" and t = "san" and result = 1.0
      or
      repr = "(member path *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member writeBuffer *))" and t = "san" and result = 1.0
      or
      repr = "(member port (instance (member Socket (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter fn *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter aSourceMap *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member same (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member checkRequired *)))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member LRUCache *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member LocalStorage (member internals *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member compiler (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member plan (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter depths (member wrapProgram (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member indentTail *)))" and t = "san" and result = 1.0
      or
      repr = "(member col (return (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(member num *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member substr *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member jQuery (global)))))" and
      t = "san" and
      result = 0.22249999999999978
      or
      repr = "(parameter 0 (return (member push (member hashes *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member names *))" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member _input *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member write (instance (member Binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _cache *)" and t = "san" and result = 1.0
      or
      repr = "(member stack *)" and t = "san" and result = 0.6504434459596958
      or
      repr = "(member length (parameter buf (member utf8border (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parseAuthorizationHeader *))" and t = "san" and result = 0.5
      or
      repr = "(member length (parameter body *))" and t = "san" and result = 1.0
      or
      repr = "(member blockParams (instance (member compiler (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member strictEqual (return (parameter _dereq_ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pending *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toString (member length (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter keys *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member onreadystatechange (return (member xhr (parameter c *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (return (parameter _dereq_ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter fn (member exports *)))" and
      t = "san" and
      result = 0.04999999999999977
      or
      repr = "(member length (parameter 0 (member pushParams *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encryptBlock (member _cipher (parameter self *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter context *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member listeners (instance (root https://www.npmjs.com/package/eventemitter3)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (member BN *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (parameter namespaces (member enable *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (return (member toString *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member exports (root https://www.npmjs.com/package/module))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (return (member call *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (instance (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter iterable *))" and t = "san" and result = 1.0
      or
      repr = "(return (member join (return (member split (member triggered *)))))" and
      t = "san" and
      result = 0.3916126638756444
      or
      repr = "(member length (parameter 0 (member write *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member inlineStack (instance (member compiler (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter params (member pushParams (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member attachments *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member parse (member JSON (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HTMLCollection (return (parameter __webpack_require__ *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (parameter 1 (return (member equal (root https://www.npmjs.com/package/assert)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member Socket (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member key *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (return (member clone *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body (member body *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _bufs (instance (root https://www.npmjs.com/package/bl))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member slice (parameter data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member write (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member programs (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (parameter node (member helperExpression *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member root (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member size (return (member create (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Content-Length *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equals (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member upserted *))" and t = "san" and result = 1.0
      or
      repr = "(return (member literal *))" and t = "san" and result = 0.05882352941176472
      or
      repr = "(member length (return (member toJSON *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member names (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member map (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pairs (parameter hash (member Hash *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member assert *)))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member Socket (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 0 (instance (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member join (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member root *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member BerReader *)))" and t = "san" and result = 1.0
      or
      repr = "(member toHTML (parameter string (member escapeExpression (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeInt (member BinaryParser *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (member types *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member clone *))" and t = "san" and result = 1.0
      or
      repr = "(member type (return (member readToken (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member values (root https://www.npmjs.com/package/underscore))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _prev *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter salt (parameter 1 (return (member genSalt *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (parameter 0 (member compile (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter data *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member blockParams (instance (member compiler (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member skips (parameter exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member unshift *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter 0 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setHeader *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member final *))" and t = "san" and result = 1.0
      or
      repr = "(return (member cast (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 0.6640123819157197
      or
      repr = "(member hash (parameter node (member helperExpression *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member Socket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member writeUInt32BE *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member _update *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member readTmplToken (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member extend (return (member default *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member data *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toString (member byteLength *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member index (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member defaults (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member line (return (member lastPos *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member auth (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Array (return (member defaults *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member buffers (return (member deconstructPacket (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member _cache *))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member blockStatement (member builders (member types (root https://www.npmjs.com/package/recast)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 0 (member acceptArray (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member of *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member bin *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member programs *))" and t = "san" and result = 1.0
      or
      repr = "(member expected *)" and t = "san" and result = 0.1775757575757575
      or
      repr =
        "(member length (return (member keys (member _ (root https://www.npmjs.com/package/underscore)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (return (parameter __webpack_require__ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body *))" and t = "san" and result = 1.0
      or
      repr = "(member yyleng (return (member unput *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member _isIgnored *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter 0 (member write *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member min (member Math *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getOwnPropertyNames (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member charAt *))" and t = "san" and result = 0.007575757575757569
      or
      repr = "(member col (parameter 0 (member context *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter string (member write (member prototype (member Binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member stringify (member JSON (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member body (parameter program (member Program (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member words *))" and t = "san" and result = 1.0
      or
      repr = "(return (member shift (return (member slice (member prototype *)))))" and
      t = "san" and
      result = 0.4952714444980692
      or
      repr = "(member length (parameter chunk *))" and t = "san" and result = 1.0
      or
      repr = "(member position *)" and t = "san" and result = 1.0
      or
      repr = "(return (member join (return (member slice *))))" and t = "san" and result = 0.3
      or
      repr = "(member length (parameter 0 (parameter 0 (return (member map *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member reEnd *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member dirname *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Buf8 *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member listing (instance (member Emitter (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member toHTML (return (member escapeExpression *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member body (return (member Program (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket (member Socket (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member value (parameter loc *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member min (member Math *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (member type (parameter c (member trigger *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member logger (member s *))" and t = "san" and result = 0.04856799667648071
      or
      repr = "(member has_stree *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member trim (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member plan *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (instance (member compiler (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member avail_in (member strm *))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member BN (member BN (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member hosts *))" and t = "san" and result = 1.0
      or
      repr = "(member position (instance (member Binary (parameter 1 (member binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member params (parameter 0 (member PartialStatement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member line *)" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 0 (parameter 1 (return (member apply *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (member type (member type (parameter c *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter params *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (return (parameter _dereq_ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member deepEqual *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 1 (instance (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter depths (member wrapProgram *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member result (parameter c (member trigger (member event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter context (parameter 1 (return (member registerHelper *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter object (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member split *))))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member strictEqual (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Array *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member get (return (member get *))))" and
      t = "san" and
      result = 0.3009574782195088
      or
      repr =
        "(parameter 0 (return (member spawn (root https://www.npmjs.com/package/child_process))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member querySelectorAll *))" and t = "san" and result = 0.0675150967789169
      or
      repr = "(parameter 1 (return (member setHeader (parameter res *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _cache (parameter self *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter 0 (member checkRequired (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member deepClone *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member Object (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter p (parameter 0 (return (member reduce *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member compileStack (instance (member compiler (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter partial *))" and t = "san" and result = 1.0
      or
      repr = "(return (member shift *))" and t = "san" and result = 0.4166666666666667
      or
      repr = "(member length (member compileStack *))" and t = "san" and result = 1.0
      or
      repr = "(member encoder *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter options *))" and t = "san" and result = 1.0
      or
      repr = "(member limit *)" and t = "san" and result = 1.0
      or
      repr = "(return (member objectExpression *))" and t = "san" and result = 0.05882352941176472
      or
      repr = "(member length (member _items *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member Socket (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter id *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter array (member acceptArray (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter params (member pushParams *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member _ (member imports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member buffer (instance (member BerWriter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (instance (member init *))))" and t = "san" and result = 1.0
      or
      repr = "(member position (instance (member Binary *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member dirname *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter chunks (member flattenChunks *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member checkRequired (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member split (return (member replace *))))" and
      t = "san" and
      result = 0.4887851039141664
      or
      repr = "(parameter 0 (return (member basename (root https://www.npmjs.com/package/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter namespaces *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getKeys (return (parameter _dereq_ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member RegExp (global)))" and t = "san" and result = 0.2638636363636363
      or
      repr =
        "(member length (parameter 0 (member pushParams (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (member location (member global (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member pushParams (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unauthorized *))" and t = "san" and result = 0.5
      or
      repr = "(return (member split (parameter path *)))" and
      t = "san" and
      result = 0.06333661906568143
      or
      repr =
        "(member length (member params (parameter partial (member PartialStatement (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (return (member parseAuthorizationHeader *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter num *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member body (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member port *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member params (parameter 0 (member PartialStatement (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member unput (member lexer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member index (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (instance (member Buffer (root https://www.npmjs.com/package/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member params (parameter 0 (member Function *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter chunks *))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter encoder *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member error *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member end *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member unescape *))" and t = "san" and result = 1.0
      or
      repr = "(member limit (parameter 1 (return (member create *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member _hash *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member listeners (return (root https://www.npmjs.com/package/eventemitter3)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (parameter arr *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toAssignableList *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter encoded *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member BN (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member equals (parameter t *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member values (member _ (root https://www.npmjs.com/package/json3)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member col (parameter nc *))" and t = "san" and result = 1.0
      or
      repr = "(return (member _update (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (parameter 1 (return (member registerHelper *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member copy (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member resolve *))" and t = "san" and result = 0.25
      or
      repr = "(parameter 0 (return (member expect (root https://www.npmjs.com/package/code))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter str (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member body (member body (parameter 0 (member parseFunctionBody *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter iterable (member exports *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member _input *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member source *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member strictEqual (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member key (parameter 0 (return (member get (member default *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member ArrayBuffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter c (parameter 1 (return (member on *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member blockParams *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member basename *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member set (member button *)))" and
      t = "san" and
      result = 0.037878787878787845
      or
      repr = "(return (parameter 0 (return (member map (parameter pattern *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member toString (return (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter value (member checkRequired *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member prototype (member Object (global)))))" and
      t = "san" and
      result = 0.08815426997245172
      or
      repr = "(return (member toLowerCase *))" and t = "san" and result = 0.2668911890148754
      or
      repr = "(parameter 0 (parameter 0 (instance (member Uint8Array (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getOwnPropertyDescriptor (member Object (global))))" and
      t = "san" and
      result = 0.34999999999999964
      or
      repr = "(member length (return (member getKeys (return (parameter __webpack_require__ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter entries (member generateList *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Buffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (parameter loc *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member filterUpgrades *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 0 (return (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member join *))))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (parameter 0 (return (member join (root https://www.npmjs.com/package/path)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member prerelease (instance (member SemVer (root https://www.npmjs.com/package/semver)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member byteLength *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member upserted (member result *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member greaterThan *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member memberExpression *))" and t = "san" and result = 0.05882352941176472
      or
      repr = "(member toHTML (parameter 0 (member escapeExpression (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (parameter 1 (return (member deepEqual (root https://www.npmjs.com/package/assert)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member curLine *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (instance (member compiler (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member children (parameter 0 (member compileChildren (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member dump *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member join *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member jQuery (parameter a *)))))" and
      t = "san" and
      result = 0.552943445959697
      or
      repr = "(member length (member buffers (return (member deconstructPacket *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member opcodes (parameter environment (member compile (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member input *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter ch (member unput *)))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (parameter loc (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getLineLength *)))" and t = "san" and result = 1.0
      or
      repr = "(member port (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member environments (member context (instance (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member equal (root https://www.npmjs.com/package/minimalistic-assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member triggered (member event (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter object (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter args *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter connections *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member extend (parameter _utils *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Content-Length (parameter 1 (return (member writeHead *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member ArrayBuffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter file *))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (return (member runInContext *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter pattern *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter string (member write (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member inlineStack (instance (member compiler *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member toByteArray *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member max (member Math *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member copy (parameter buffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member upgrades (instance (member Socket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member merge *))" and t = "san" and result = 0.7203030303030297
      or
      repr = "(parameter 0 (return (member ok *)))" and t = "san" and result = 1.0
      or
      repr = "(member sliceStart *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter codePoints *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member unput *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (parameter environment *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeInt *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (parameter _dereq_ *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member defaults *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member input (member strm *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member blockParams (parameter program (member Program (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter parts (member resolvePath (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tasks *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pairs (parameter 0 (member Hash *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member $in *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member stack *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (instance (member Buffer (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (return (parameter _dereq_ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member generateList *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member concat (return (member getKeys *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter entries *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member skips (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member listeners *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member result (parameter c (member trigger *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parse (return (parameter _dereq_ *))))" and
      t = "san" and
      result = 0.14797932330827068
      or
      repr = "(member length (parameter match (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (root https://www.npmjs.com/package/lru-cache)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fromNumber *))" and t = "san" and result = 1.0
      or
      repr = "(member blockParams (return (member Program *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member post *))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (member literal (member builders *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter base64 (member decode *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (return (member toFunction *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member relative (root https://www.npmjs.com/package/path)))" and
      t = "san" and
      result = 0.16000000000000014
      or
      repr = "(return (member keys *))" and t = "san" and result = 0.5075757575757576
      or
      repr =
        "(member length (return (member keys (member _ (root https://www.npmjs.com/package/json3)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member toString *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member add *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equals (parameter assert (member test_salt_length *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toJSON *))" and t = "san" and result = 0.13499999999999995
      or
      repr = "(member protocol (return (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member log (member console (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member size (member _ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter string (member write (instance (member Binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member names (parameter exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member toHTML *)" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (parameter 0 (return (member reduce *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (return (member invokePartial *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member acceptArray (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member resolve (root https://www.npmjs.com/package/path)))" and
      t = "san" and
      result = 0.13628271954229715
      or
      repr = "(member length (return (member slice (member _buf *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (return (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter iv *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter 0 (member checkRequired (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member size *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member ok (parameter t *))))" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket (member Socket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member opcodes (instance (member compiler *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member accept (instance (member PrintVisitor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member artifacts *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Array (member self (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter str *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (member BN (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 1 (return (member Buffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member Buffer (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member reduce *))" and t = "san" and result = 0.40454509428662494
      or
      repr = "(member length (parameter props *))" and t = "san" and result = 1.0
      or
      repr = "(member position (instance (member Binary (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member Socket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member buffers *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member match (member _input *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member serialize *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member toString (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templates (parameter 0 (member cli *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter packets (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equals (parameter 0 (member test_salt_length *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter str *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member upserted (member result (parameter r *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 6 (member wrapProgram (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket (member Socket (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member equals (parameter assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member environments (member context *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member acceptArray *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter ch (member unput (member lexer (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter buf (member utf8border *)))" and t = "san" and result = 1.0
      or
      repr = "(member blockParams (return (member Program (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter buffer (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (parameter 0 (return (member equal *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member BN *))))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (parameter loc (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (parameter 1 (return (member equal *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member filterUpgrades (instance (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter depths *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (parameter qs *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member literal (member builders (member types *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member dump (instance (root https://www.npmjs.com/package/lru-cache)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member arrayExpression *))" and t = "san" and result = 0.05882352941176472
      or
      repr = "(member length (member buffer (instance (member Binary (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member error (member console (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member write *)))" and t = "san" and result = 1.0
      or
      repr = "(member type *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter 1 (return (member if *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (member value *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member parent *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member values (member exports (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pairs (parameter hash *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member files (parameter res *)))" and t = "san" and result = 1.0
      or
      repr = "(member limit (parameter 1 (instance (member Err *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter parts *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member mappings *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member flattenChunks *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member deepEqual (member assert (root https://www.npmjs.com/package/chai)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member copy (parameter 0 (member write *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member Socket (member Socket (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter node *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (parameter 0 (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pairs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pairs (parameter hash (member Hash (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getKeys *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter format *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member opcodes (parameter environment (member compile *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (member 0 (member matches *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member position (parameter self *))" and t = "san" and result = 1.0
      or
      repr = "(return (member _encodeValue *))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (return (member parse (root https://www.npmjs.com/package/url))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templates (parameter 0 (member cli (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member prerelease *))" and t = "san" and result = 1.0
      or
      repr = "(member line (member end *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter 2 (member invokePartial *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (root https://www.npmjs.com/package/debug))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _bufs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter p (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (return (parameter _dereq_ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member blockParams (member options (instance (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 1 (member resolvePath (member prototype (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member seq *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter array (member indexOf (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member SlowBuffer (parameter m *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member log (member console (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member inlineStack *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member elements *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter packets *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member readTmplToken *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member programs (member context (instance (member compiler *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (return (parameter _dereq_ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body (return (member Program *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter ary *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter val *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (instance (member SourceNode (parameter h *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (instance (member SourceNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member pastInput *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (return (member reduce *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter 0 (member helperExpression *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member Socket (member Socket *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (return (parameter require *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member kind *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member exec (member lineBreakG *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member serialize (member bson *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter upgrades (member filterUpgrades (instance (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member bytesRead *)" and t = "san" and result = 1.0
      or
      repr = "(member protocol (member location (member self (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member equals *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member directories *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member toByteArray (member base64js *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member files *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Buf8 (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member attr (parameter 0 (return (member extend *)))))" and
      t = "san" and
      result = 0.25757575757575746
      or
      repr = "(member length (return (member toString (member byteLength (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (return (member define *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member write (member prototype (member Binary *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member accept *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member Socket (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member size (member buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member assert (root https://www.npmjs.com/package/hoek))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 0 (member filterUpgrades (member prototype (member Socket *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter other *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member readTmplToken (member prototype (member Parser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member map *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (parameter 0 (return (member resolve (root https://www.npmjs.com/package/path)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member body (member body (parameter node (member parseFunctionBody *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body (parameter 0 (member Program *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter holders *))" and t = "san" and result = 1.0
      or
      repr = "(member col (parameter nc (member context *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member toHexString *))" and t = "san" and result = 0.22630500000000003
      or
      repr = "(member length (parameter stackA *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter array (member indexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member min (member Math (member Object *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter upgrades *))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (parameter 1 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeInt (member BinaryParser (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member depth (return (member wrapProgram *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member finishToken *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member _encodeValue (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member specifiers *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member stubBuffer *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member fixedTimeComparison (root https://www.npmjs.com/package/cryptiles))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member listeners (return (member EventEmitter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member strictEqual (return (parameter _dereq_ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member concat (member body *)))" and
      t = "san" and
      result = 0.8414000000000001
      or
      repr = "(member length (parameter array (member acceptArray *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member constructor (return (member _augment *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member clone (root https://www.npmjs.com/package/hoek)))" and
      t = "san" and
      result = 0.08999999999999986
      or
      repr = "(parameter 1 (return (member substr (parameter str *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state (parameter 0 (member inflate (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter upgrades (member filterUpgrades (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member regexEscape *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member words (instance (member BN (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member BN (member BN (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member _id *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member toString (instance (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter a (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (return (member clone (root https://www.npmjs.com/package/hoek))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member toString (member length *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member cwd (member process (global))))" and
      t = "san" and
      result = 0.022956841138659298
      or
      repr = "(member length (return (member toString (return (member BN *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member compileStack (instance (member compiler *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member Socket (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (parameter environment (member compileChildren *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member add *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (parameter partial *)))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (member location (parameter global *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toDate *))" and t = "san" and result = 0.02492668621700879
      or
      repr = "(member length (return (member listeners (instance (member EventEmitter *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member body (parameter program *)))" and t = "san" and result = 1.0
      or
      repr = "(member HTMLCollection *)" and t = "san" and result = 1.0
      or
      repr = "(member hash (parameter options (member invokePartial *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member resolve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parseStackAndMessage *))" and
      t = "san" and
      result = 0.4070374064507583
      or
      repr =
        "(member length (return (member values (member _ (root https://www.npmjs.com/package/underscore)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member opcodes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member ___eio (global)))" and t = "san" and result = 1.0
      or
      repr = "(member upsertedCount *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member deepEqual (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _prev (parameter self *))" and t = "san" and result = 1.0
      or
      repr = "(member blockParams (instance (member compiler *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member fixedTimeComparison *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member _streams (instance (root https://www.npmjs.com/package/stream-splicer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member reStart *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter message *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member slice (return (member trim *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member SlowBuffer *)))" and t = "san" and result = 1.0
      or
      repr = "(member toHTML (parameter string (member escapeExpression *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter value (member checkRequired (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member upgrades *))" and t = "san" and result = 1.0
      or
      repr = "(return (member _extend (root https://www.npmjs.com/package/util)))" and
      t = "san" and
      result = 0.19090981142675012
      or
      repr = "(parameter 1 (member length *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toString (instance (member BN *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter depths (member lookup *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member concat *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equals *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 6 (member wrapProgram *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (root https://www.npmjs.com/package/bl)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter salt *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member prerelease (instance (root https://www.npmjs.com/package/semver))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member Buffer (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member resolvePath (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member src *))" and t = "san" and result = 1.0
      or
      repr = "(member content (parameter 0 (return (member push *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member scopes *)" and t = "san" and result = 1.0
      or
      repr = "(member email (parameter 0 (return (member insert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member pathname *))" and t = "san" and result = 1.0
      or
      repr = "(instance (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(member _id (parameter 0 (return (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member tokens (parameter e (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter e (member compare *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (return (parameter r *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member json (parameter res *))))" and t = "san" and result = 1.0
      or
      repr = "(instance (return (member extend (member Util *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getRules *)))" and t = "san" and result = 1.0
      or
      repr = "(member _dispatchListeners *)" and t = "san" and result = 0.4807408972118963
      or
      repr = "(return (member replace (parameter 0 (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member json *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member text *))" and t = "san" and result = 0.2837823780297508
      or
      repr = "(member length (return (member fill *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getComponent (member props *)))" and
      t = "san" and
      result = 0.6522754231273785
      or
      repr = "(member length (member tokens (parameter e (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter e *))" and t = "san" and result = 0.4198783083919103
      or
      repr = "(return (member render (parameter 0 (return (member createClass *)))))" and
      t = "san" and
      result = 0.5967746151045463
      or
      repr = "(member length (member tokens (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member call *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getHostProps (return (parameter n *))))" and
      t = "san" and
      result = 0.33140390245352336
      or
      repr = "(member creator_id *)" and t = "san" and result = 0.9456092134658927
      or
      repr = "(member length (member content *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getComponent *))" and t = "san" and result = 0.33348580359770363
      or
      repr = "(member length (member tokens (parameter 0 (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getPooled (return (parameter n *))))" and t = "san" and result = 0.25
      or
      repr = "(parameter 0 (instance (parameter 1 (return (member call *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member charAt (parameter e *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member slice (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (return (member extend (member Util (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member Buffer (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter e (member resolve (parameter 1 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member fill (instance (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (parameter 0 (return (member push (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlParser (member constructor *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member construct (parameter 1 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member then (return (member resolve *))))" and t = "san" and result = 0.5
      or
      repr = "(return (member replace (parameter e (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (return (parameter r *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member implicitTypes (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member exports (parameter 1 (return (member call *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(instance (member constructor (member prototype (member constructor (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member type (return (member lexJSX (member prototype (member JSXParser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member mountComponent (return (parameter n *))))" and
      t = "san" and
      result = 0.11260792676583586
      or
      repr = "(member length (parameter 2 (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Buffer (return (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Buffer (return (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member end *))" and t = "san" and result = 0.19206393670773217
      or
      repr = "(member length (parameter 0 (member compare *)))" and t = "san" and result = 1.0
      or
      repr = "(return (return (member default *)))" and t = "san" and result = 0.8153367835223131
      or
      repr = "(parameter 2 (return (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member Symbol *))" and t = "san" and result = 0.023169656789169435
      or
      repr = "(member length (member value (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member type (return (member lexJSX (instance (member JSXParser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member type (return (member lexJSX *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter r *))" and t = "san" and result = 0.40060240963855426
      or
      repr = "(member length (member tokens (parameter 0 (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(member _dispatchInstances *)" and t = "san" and result = 0.48074089721189633
      or
      repr = "(member req *)" and t = "san" and result = 0.06322244887948186
      or
      repr = "(member posMax *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member match (parameter e *)))" and t = "san" and result = 0.2331088109851246
      or
      repr = "(parameter 0 (return (return (parameter r *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member call (parameter e *)))" and t = "san" and result = 0.7945178573117304
      or
      repr = "(member length (member src (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter e (member resolve *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getNodeFromInstance *))" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (parameter 1 (return (member call *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member src (instance (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member fill (member prototype (member Buffer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member exports *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getRules (member ruler *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlParser (member constructor (member prototype (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (member prototype (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (member prototype (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getPooled *))" and t = "san" and result = 0.25
      or
      repr = "(parameter 0 (return (member default (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlParser (member constructor (member prototype *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member implicitTypes *))" and t = "san" and result = 1.0
      or
      repr = "(member tagName *)" and t = "san" and result = 0.35956981617155315
      or
      repr = "(instance (member constructor *))" and t = "san" and result = 1.0
      or
      repr = "(return (member escapeHtml *))" and t = "san" and result = 1.0
      or
      repr =
        "(instance (member constructor (member prototype (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member picture_id (parameter 0 (return (member find *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter e (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getNodeFromInstance (return (parameter n *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member render (parameter 0 (member default *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member type (return (member lexJSX (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(member email (parameter 0 (return (member findOne *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member htmlParser (member constructor (member prototype (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member trim *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member min (member Math (global))))" and
      t = "san" and
      result = 0.9887392073234166
      or
      repr = "(member download *)" and t = "san" and result = 0.22009805500023216
      or
      repr = "(member length (parameter t (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extractEvents *))" and t = "san" and result = 0.25
      or
      repr = "(return (member replace (return (member exports (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member map (parameter e *)))" and t = "san" and result = 0.5800000000000001
      or
      repr = "(member length (parameter t (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter r *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member fill (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlParser *)" and t = "san" and result = 1.0
      or
      repr = "(return (member render *))" and t = "san" and result = 0.5946483856700573
      or
      repr = "(member innerHtml *)" and t = "san" and result = 1.0
      or
      repr = "(return (member styles *))" and t = "san" and result = 0.08000000000000002
      or
      repr = "(return (member construct_object *))" and t = "san" and result = 0.6818911890148754
      or
      repr = "(member length (parameter 2 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member slice (member input *)))" and
      t = "san" and
      result = 0.6648639862685942
      or
      repr = "(parameter 0 (return (member charAt *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Boolean (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeSlug *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member originalUrl *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member textContent (return (member createElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member path *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member stringify *)))" and t = "san" and result = 1.0
      or
      repr = "(member tooltip *)" and t = "san" and result = 1.0
      or
      repr = "(return (member getFile *))" and t = "san" and result = 0.5047050000000001
      or
      repr = "(parameter 0 (return (member match *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member Template (parameter mTemplates *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member numFound *)" and t = "san" and result = 1.0
      or
      repr = "(member UniqueId *)" and t = "san" and result = 0.348
      or
      repr =
        "(parameter 1 (return (member equal (member assert (root https://www.npmjs.com/package/chai/chai)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member formatMessage (root https://www.npmjs.com/package/orion/i18nUtil))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member template (root https://www.npmjs.com/package/util/domtemplate))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length2 (return (member patch_obj (member diff_match_patch (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member flag (parameter _ *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (member body *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (instance (member TextEncoder (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member firstChild *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member makeRelative *)))" and t = "san" and result = 1.0
      or
      repr = "(member _htmlDeps (parameter 0 (parameter 1 (return (member registerPlugin *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getSettings (instance (parameter SettingsRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter 0 (parameter 2 (return (member makeRenderFunction *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member encode (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member formatMessage (parameter i18nUtil *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length2 (return (member patch_obj (member diff_match_patch *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (return (member URL (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length2 (instance (member patch_obj *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter string (member encode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member host (member location (member parent (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member name (parameter 0 (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member _findMatch *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (root https://www.npmjs.com/package/orion/URITemplate)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member uriTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member Parents (member 1 (member Children (parameter log *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (member result *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member positions (parameter proposal *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member replace *)))" and t = "san" and result = 1.0
      or
      repr = "(member Slug *)" and t = "san" and result = 1.0
      or
      repr = "(member tooltip (parameter 2 (return (member registerServiceProvider *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter string *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member confirm (parameter commandService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter children *))" and t = "san" and result = 1.0
      or
      repr = "(member categoryLabel *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member fileRoot (parameter options *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter 0 (return (member map *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member uri *)" and t = "san" and result = 1.0
      or
      repr = "(member length1 (return (member patch_obj (member diff_match_patch *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substring (member 0 (member params (parameter req *)))))" and
      t = "san" and
      result = 0.07575757575757548
      or
      repr = "(parameter 0 (return (member substring (member originalUrl (parameter req *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member includeTemplates *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member problems (parameter computed *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (return (member createElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (instance (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member docs (member response *)))" and t = "san" and result = 1.0
      or
      repr = "(member _htmlDeps (parameter server *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member appendChild *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member join *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter actual *))" and t = "san" and result = 1.0
      or
      repr = "(member description (parameter 0 (return (member push *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member logPageLoadTiming *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member catch (return (member then *))))" and
      t = "san" and
      result = 0.3355704697986577
      or
      repr = "(member length (member host *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter options (member router *)))" and t = "san" and result = 1.0
      or
      repr = "(member host (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member Parents *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member slice (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member startIndex *)" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter 0 (parameter 3 (return (parameter gather *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (return (member Template (root https://www.npmjs.com/package/orion/editor/templates))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member URL (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member typed (parameter input *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member strictEqual (member assert (parameter chai *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member proposal *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member substring (parameter buffer (member computeProposals *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member value (return (member findDirective (root https://www.npmjs.com/package/javascript/finder)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member order *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member matchResourceParameters *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member substring (parameter text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member _binarySearch (member prototype *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member toDom (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member importantChange *))" and t = "san" and result = 0.27272727272727193
      or
      repr = "(member length (return (member getText (return (member getModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter expected *))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (instance (member exports (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length1 (instance (member patch_obj (member diff_match_patch (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member equal (member assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member text (root https://www.npmjs.com/package/i18n!orion/nls/messages)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html (parameter 0 (return (member createView *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member setSafeAttribute *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member prefix (parameter options (member computeContentAssist *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter results *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter files (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member typed (parameter 0 (member _processCaretChange (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text (parameter computed *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getServiceReferences *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 1 (member positions *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member host (member location *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member arguments *))" and t = "san" and result = 1.0
      or
      repr = "(return (member match (return (member substring *))))" and
      t = "san" and
      result = 0.2603200000000005
      or
      repr = "(member pathname (parameter 0 (return (member format *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member typed (parameter input (member _processCaretChange (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member formatMessage (root https://www.npmjs.com/package/orion/util))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member modified *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter resp (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member added *))" and t = "san" and result = 1.0
      or
      repr = "(member Password *)" and t = "san" and result = 1.0
      or
      repr = "(member content (return (member create *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (member positions *)))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member installPlugin (instance (member PluginRegistry (root https://www.npmjs.com/package/orion/pluginregistry)))))" and
      t = "san" and
      result = 0.013312685185186182
      or
      repr =
        "(member length (member parents (return (member findNode (root https://www.npmjs.com/package/javascript/finder)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member resolveRelativeFiles (return (member ScriptResolver *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member resolveRelativeFiles (instance (member ScriptResolver *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getPlugins (return (member PluginRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member delimiter *))" and t = "san" and result = 1.0
      or
      repr = "(member addedCharCount *)" and t = "san" and result = 1.0
      or
      repr = "(member origin *)" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member strictEqual (member assert (root https://www.npmjs.com/package/chai/chai)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Children (member body *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member formatMessage (parameter util *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (member hover (return (member getProposal (parameter template *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getSettings (instance (root https://www.npmjs.com/package/orion/settings/settingsRegistry)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Children (parameter body *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member url (root https://www.npmjs.com/package/i18n!orion/nls/messages)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setSelection *)))" and t = "san" and result = 1.0
      or
      repr = "(member Name (parameter 0 (return (member send *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member indexOf (parameter text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member confirm (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substring (member pathname *)))" and
      t = "san" and
      result = 0.0030133333333332013
      or
      repr = "(member length (member workspaceDir (member file *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (member proposal *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member includeTemplates (parameter 1 (member completion *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member css *))" and t = "san" and result = 0.45
      or
      repr = "(parameter 1 (return (member substring (parameter name *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (return (member Command *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member typed *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member setSelection (member editor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member prefix (parameter 1 (member computeContentAssist *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member baseUrl (parameter req *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter template *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member substring (parameter moduleNamePlusExt (member toUrl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member get (instance (parameter LRU *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member confirm *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pos (instance (member StringStream *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member then (return (member getRepo *))))" and
      t = "san" and
      result = 0.18382550335570474
      or
      repr = "(parameter 1 (return (member substring (parameter text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (parameter 1 (return (member __define *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member replace *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member query *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member description *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getAttribute (parameter e *)))" and
      t = "san" and
      result = 0.43000000000000016
      or
      repr = "(return (member reject (root https://www.npmjs.com/package/bluebird)))" and
      t = "san" and
      result = 0.5151515151515149
      or
      repr = "(member length (member contextPath *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member Template *)))" and t = "san" and result = 1.0
      or
      repr = "(member selectionEnd (member element (instance (member Inputter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (return (return (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member basename (root https://www.npmjs.com/package/path)))" and
      t = "san" and
      result = 0.5
      or
      repr = "(parameter 0 (return (member strictEqual (member assert (parameter chai *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member getFile *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (parameter template (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member value *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (member assert (parameter chai *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member assign (member Object (global))))" and t = "san" and result = 1.0
      or
      repr = "(member selectionStart *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member path *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member setSafeInnerHTML (root https://www.npmjs.com/package/orion/webui/littlelib))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member changed (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (member start *))" and t = "san" and result = 1.0
      or
      repr = "(member name (parameter 0 (instance (member Command (parameter mCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member selectionStart (member element (instance (member Inputter (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member router *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member setSelection *)))" and t = "san" and result = 1.0
      or
      repr = "(member length1 *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member workspaceDir *))" and t = "san" and result = 1.0
      or
      repr = "(return (member _expr *))" and t = "san" and result = 0.1466666666666668
      or
      repr =
        "(member length (return (member getAttributesForNode (return (member HTMLContentAssistProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getAttributesForNode *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (return (member template (root https://www.npmjs.com/package/util/domtemplate))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member pathname (parameter 0 (return (member format (root https://www.npmjs.com/package/url)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member substring (parameter 0 (member computeProposals *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member parents *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member logPageLoadTiming (root https://www.npmjs.com/package/orion/metrics))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member getAnnotations *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member strictEqual (member assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Id *)" and t = "san" and result = 0.5833333333333334
      or
      repr = "(member length2 *)" and t = "san" and result = 1.0
      or
      repr = "(member 0 (parameter 2 (return (member report (parameter context *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member prefix (parameter 1 (member computeContentAssist (member contentAssistImpl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _contentsPath *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member q *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member $$ (root https://www.npmjs.com/package/orion/webui/littlelib))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter location *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _contentsPath (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member createTextNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (instance (member URL (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setSelection (member editor *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (instance (member Template (root https://www.npmjs.com/package/orion/editor/templates))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (return (member floor *))))" and
      t = "san" and
      result = 0.1552760051880675
      or
      repr = "(member host (member location (member self (global))))" and t = "san" and result = 1.0
      or
      repr = "(member templateString *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member name *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member flag *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member prefix (parameter options *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member open *)))" and t = "san" and result = 1.0
      or
      repr = "(member TEMPLATE (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member findScriptBlocks (root https://www.npmjs.com/package/javascript/finder))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member resolveRelativeFiles *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member get (return (root https://www.npmjs.com/package/javascript/lru))))" and
      t = "san" and
      result = 0.18497950300478128
      or
      repr = "(member templates (parameter options *))" and t = "san" and result = 1.0
      or
      repr = "(member instances *)" and t = "san" and result = 0.8399999999999999
      or
      repr =
        "(member innerHTML (parameter results (parameter 2 (return (member makeRenderFunction *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Children (member body (parameter res *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (return (member Command (parameter mCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member name *))" and t = "san" and result = 0.6287878787878787
      or
      repr = "(parameter 0 (return (member template *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member get (instance (root https://www.npmjs.com/package/javascript/lru))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substring (member 0 (member params *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter tags *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (instance (member URL (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member localStorage (global)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member setSafeInnerHTML (parameter lib *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getAttributeDefinitions *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member annotations *))" and t = "san" and result = 1.0
      or
      repr = "(member includeTemplates (parameter query *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter c *))" and t = "san" and result = 1.0
      or
      repr = "(member name (parameter 0 (return (member Command *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parse (root https://www.npmjs.com/package/url)))" and
      t = "san" and
      result = 0.6756528888888889
      or
      repr = "(parameter 0 (return (member createContextualFragment *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member template (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member findScriptBlocks (parameter Finder *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member matchResourceParameters (root https://www.npmjs.com/package/orion/PageUtil))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Parents (member 0 (member Children (parameter log *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter prefix *))" and t = "san" and result = 1.0
      or
      repr = "(member Name *)" and t = "san" and result = 1.0
      or
      repr = "(member content (member hover (return (member getProposal *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member setRequestHeader *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (return (member getBlockCommentDelimiters *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (member textContent *))" and t = "san" and result = 1.0
      or
      repr = "(return (member decodeURIComponent *))" and t = "san" and result = 0.4407382550335571
      or
      repr = "(member _htmlDeps *)" and t = "san" and result = 1.0
      or
      repr = "(return (member substring (member originalUrl (parameter req *))))" and
      t = "san" and
      result = 0.5075757575757575
      or
      repr = "(parameter 1 (return (member logPageLoadTiming (parameter mMetrics *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member href *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member textContent *))" and t = "san" and result = 1.0
      or
      repr = "(member name (parameter 0 (return (member Command (parameter mCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length2 (instance (member patch_obj (member diff_match_patch (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member _contentsPath (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getfileDir *))" and t = "san" and result = 0.037500000000000006
      or
      repr = "(member length (member selection *))" and t = "san" and result = 1.0
      or
      repr = "(member selectionStart (member element (instance (member Inputter *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member includeTemplates (parameter query (member completion *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member mapOffset *))" and t = "san" and result = 0.13738844444444487
      or
      repr = "(member length (member childNodes (member 0 (member childNodes *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 1 (member range *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member appendChild (member element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member strictEqual (member assert (root https://www.npmjs.com/package/chai/chai)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (member content *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member encode (member he *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter moduleNamePlusExt *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getServiceReferences (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member handle *))" and t = "san" and result = 0.12027600518806754
      or
      repr = "(member length (member 0 (member positions (parameter proposal *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter tags (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member template (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member name (parameter 2 (return (member registerService *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member exports (parameter t *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter callback *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member template *)))" and t = "san" and result = 1.0
      or
      repr = "(member content (member hover *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member 0 (return (member exec *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (parameter URITemplate *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member parents (return (member findNode (parameter Finder *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member startIndex (parameter 0 (return (member push *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member indexOf (member href (member location *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member positions *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter string (member encode (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getPlugins *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getSettings (return (root https://www.npmjs.com/package/orion/settings/settingsRegistry)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter string (member encode (member he *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (member target *)))" and t = "san" and result = 1.0
      or
      repr = "(member length1 (return (member patch_obj *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member Parents (member 0 (member Children *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member params (return (member parse (root https://www.npmjs.com/package/javascript/contentAssist/sigparser)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length1 (return (member patch_obj (member diff_match_patch (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member findStyleBlocks (root https://www.npmjs.com/package/webtools/util))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length2 (return (member patch_obj *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member docs *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equal *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 0 (return (member some *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member $$ *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter proposals *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member fileRoot *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member indexOf (member href *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encode (return (member TextEncoder (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member ops *))" and t = "san" and result = 1.0
      or
      repr = "(member selectionEnd (member element *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter actual (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter URITemplate *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member defs *))" and t = "san" and result = 1.0
      or
      repr = "(member Length *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getSettings (return (parameter SettingsRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member added (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLineDelimiter (return (member getModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (member configParams *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (instance (member Template (parameter mTemplates *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member includeTemplates (parameter 1 (member completion (member passes *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member join (root https://www.npmjs.com/package/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member searchStrLength *)" and t = "san" and result = 1.0
      or
      repr = "(member callbacks *)" and t = "san" and result = 0.25
      or
      repr = "(parameter 1 (return (member getAnnotations (return (member AnnotationModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substring (member path *)))" and
      t = "san" and
      result = 0.1455899999999999
      or
      repr = "(member length (member delimiter (parameter context *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member Task *))" and t = "san" and result = 1.0
      or
      repr = "(member name (parameter 2 (return (member registerService (parameter provider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member textContent *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member open (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member delimiter (parameter context (member computeProposals *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member encode *)))" and t = "san" and result = 1.0
      or
      repr = "(member escapePosition *)" and t = "san" and result = 1.0
      or
      repr = "(member start (member cursor *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member toURLPath *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member progress (member progressService *)))" and
      t = "san" and
      result = 0.020202020202019888
      or
      repr = "(parameter 0 (return (member substring (member proposal *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member setSafeAttribute (parameter lib *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/orion/URITemplate)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter actualProposals *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member open (global))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getAttributesForNode (instance (member HTMLContentAssistProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member max *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member findStyleBlocks *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member _binarySearch *))" and t = "san" and result = 1.0
      or
      repr = "(member host (member location *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member typed (parameter input (member _processCaretChange *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member params (return (member parse *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member indexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member flag (parameter 1 (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 1 (member 0 (member diffs *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member _listTemplate (instance (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member toDom (root https://www.npmjs.com/package/util/util))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member selectionStart (member element *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member createElement *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member setSafeInnerHTML *)))" and t = "san" and result = 1.0
      or
      repr = "(member onclick *)" and t = "san" and result = 0.3552231111111109
      or
      repr = "(parameter 0 (return (member slice (parameter text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Children (parameter log *)))" and t = "san" and result = 1.0
      or
      repr = "(member name (parameter 0 (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeSlug (root https://www.npmjs.com/package/orion/form)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member position *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member equal (member assert (root https://www.npmjs.com/package/chai/chai)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member problems (parameter problems *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member host (member location (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (instance (member Command *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member typed (parameter 0 (member _processCaretChange *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter 0 (member encode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodeURIComponent *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (parameter name *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (return (member findDirective *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member importantChange (parameter jsProject *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _listTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter c (parameter 3 (return (parameter gather *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLineDelimiter *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member equal (member assert (parameter chai *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member getAnnotations (instance (member AnnotationModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member parseInt (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (parameter 0 (return (member appendChild *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member parents (return (member findNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member equal (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Slug (member headers *))" and t = "san" and result = 1.0
      or
      repr = "(member numFound (member response *))" and t = "san" and result = 1.0
      or
      repr = "(member index (parameter item *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 2 (return (member setSafeAttribute (root https://www.npmjs.com/package/orion/webui/littlelib))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member delimiter (parameter 2 (member computeProposals *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member prefix (parameter options (member computeContentAssist (member contentAssistImpl *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getProtectedThemes *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getOffsetAtLocation (instance (member TextView *))))" and
      t = "san" and
      result = 0.5
      or
      repr = "(member onclick (return (member createElement (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member _htmlDeps (parameter server (parameter 1 (return (member registerPlugin *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter expectedProposals *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member params (return (member parse (parameter sigparser *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (member content *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member value (return (member findDirective (parameter Finder *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getText *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 1 (member positions (parameter proposal *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member annotations (parameter 1 (return (member execute *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member prefix *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter proposals (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member name (parameter 0 (instance (member Command *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member _nodeLength *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member comments *))" and t = "san" and result = 1.0
      or
      repr = "(member 0 (parameter 2 (return (member report *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member removed (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member url (parameter messages *)))" and t = "san" and result = 1.0
      or
      repr = "(member length1 (instance (member patch_obj (member diff_match_patch *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member async *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member substring (return (member getLine *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member join *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (member configParams (parameter options *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlAstManager *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member equal (member assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length1 (instance (member patch_obj *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member substring (parameter buffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member changed *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member resolvePos (parameter exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member Message *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getServiceReferences (instance (member ServiceRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member host (member location (member self (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member includeTemplates (parameter query (member completion (member passes *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member computeBlocks *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member selection (parameter computed *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member name (parameter 0 (return (member Command (root https://www.npmjs.com/package/orion/commands)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member doc *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter files *))" and t = "san" and result = 1.0
      or
      repr =
        "(member tooltip (parameter 0 (instance (member Command (root https://www.npmjs.com/package/orion/commands)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member tooltip (parameter 0 (instance (member Command (parameter mCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member resolvePos *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member findStyleBlocks (parameter Util *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member resolve (root https://www.npmjs.com/package/bluebird)))" and
      t = "san" and
      result = 0.07242424242424272
      or
      repr = "(member length (return (member $$ (parameter lib *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodeSlug (parameter form *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member then *))))" and t = "san" and result = 0.5
      or
      repr =
        "(member name (parameter 0 (instance (member Command (root https://www.npmjs.com/package/orion/commands)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member removed *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member tooltip (parameter 0 (return (member Command (root https://www.npmjs.com/package/orion/commands)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member createContextualFragment (return (member createRange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member name (parameter 2 (return (member registerServiceProvider *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length2 (instance (member patch_obj (member diff_match_patch *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (return (member substring *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member Parents (member 1 (member Children *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member revision *)" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 2 (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getLocationAtOffset (instance (member TextView *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member selectionEnd (member element (instance (member Inputter (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member set (return (member proxy *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member params *))" and t = "san" and result = 1.0
      or
      repr = "(member end (member cursor *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter resp *))" and t = "san" and result = 1.0
      or
      repr = "(member Message (parameter 0 (return (member reject *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member Location *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member flag (parameter _ (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member findScriptBlocks *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member text (parameter messages *)))" and t = "san" and result = 1.0
      or
      repr = "(member selectionEnd *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member encodeURIComponent *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member Children *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getSettings *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (instance (member Template *)))" and t = "san" and result = 1.0
      or
      repr = "(member pos (instance (member StringStream (instance (member Mirror *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getPlugins (instance (member PluginRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text (parameter 0 (return (member push *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member baseUrl *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member problems *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member pathname *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member _listTemplate (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _listTemplate (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member Source *)" and t = "san" and result = 0.75
      or
      repr = "(return (member _createParents *))" and t = "san" and result = 0.3333333333333333
      or
      repr = "(parameter 1 (return (member join (root https://www.npmjs.com/package/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (member text *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member formatMessage *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member mapOffset (member editor *)))" and
      t = "san" and
      result = 0.6358355992714021
      or
      repr =
        "(member length (return (member getServiceReferences (return (member ServiceRegistry *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (parameter 0 (return (member push *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member getLocationAtOffset *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member toDom *)))" and t = "san" and result = 1.0
      or
      repr = "(member content (return (member create (member Object (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (member match *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member contextPath (parameter req *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter computed *))" and t = "san" and result = 1.0
      or
      repr = "(member name *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member workspaceDir (parameter file *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _listTemplate (instance (member constructor *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member matchResourceParameters (parameter PageUtil *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member strictEqual (member assert *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member mediaElement *)" and t = "san" and result = 0.42
      or
      repr = "(parameter 1 (return (member defaultCompare (member array *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member convo *)" and t = "san" and result = 0.25
      or
      repr = "(member mediaElement (parameter 0 (member onstreamended *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member count (parameter 0 (return (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(member audio *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member replace (parameter str *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member emit *)))" and t = "san" and result = 1.0
      or
      repr = "(member video *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getVideoTracks *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member defaultCompare (member array *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member defaultCompare *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member stringify *)))" and t = "san" and result = 1.0
      or
      repr = "(member mediaElement (parameter 0 (member onstreamended (member connection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getAudioTracks *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member defaultCompare *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member call *)))" and t = "san" and result = 1.0
      or
      repr = "(member mediaElement (parameter 0 (member onstreamended (parameter connection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member mediaElement (parameter e (member onstreamended (member connection *))))" and
      t = "san" and
      result = 0.7466666666666666
      or
      repr = "(member length (member async (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tests (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter fn (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member escapeRegexp (member utils *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member message (member err *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member reduce *)))" and t = "san" and result = 1.0
      or
      repr = "(member prevGlobalsLength (instance (member exports *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter s *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter oldString (member extractCommon (member prototype (member Diff *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter fn (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (parameter element *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter cur *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter oldString (member extractCommon *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tests (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 1 (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tests (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member prevGlobalsLength (instance (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member map (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member extractCommon (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member grep *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter cur (parameter 0 (return (member reduce *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 2 (member extractCommon (member prototype (member Diff *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member grep (member options *)))" and t = "san" and result = 1.0
      or
      repr = "(member async (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member tokenize (instance (member Diff *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member async (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member async (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter newString (member extractCommon (member prototype (member Diff *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (member trajectories *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member assert (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member message (member err (parameter test *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter oldString *))" and t = "san" and result = 1.0
      or
      repr = "(member async (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member async (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member globalProps (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeRegexp (return (parameter require *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member extractCommon *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member grep (member options (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevGlobalsLength (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member reduce *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member indexOf (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeRegexp (member utils (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter parsed *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member createElementNS (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member globalProps (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevGlobalsLength (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member async (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member async (instance (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member reduce (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member fn (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 1 (member extractCommon (member prototype (member Diff *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tests *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member filter (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter newString (member extractCommon *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeRegexp *))" and t = "san" and result = 1.0
      or
      repr = "(member prevGlobalsLength (instance (member constructor *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeRegexp (member utils (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member map *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter fn *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter newString *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter fn (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member async (instance (member constructor (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member files (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter fn (member constructor (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member prevGlobalsLength (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tests (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter fn (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member grep (member options (return (member grep *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member fn (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter oldString (member extractCommon (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member fn (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter obj (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member reduce (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member files (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member filter *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member async *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member constructor *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter 1 (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member message *))" and t = "san" and result = 1.0
      or
      repr = "(member prevGlobalsLength *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member indexOf *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member filter *)))" and t = "san" and result = 1.0
      or
      repr = "(member HTMLElement (global))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter newString (member extractCommon (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member async (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member prevGlobalsLength (instance (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member forEach (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member async (instance (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member filter (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (member trajectories (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 2 (member extractCommon (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 2 (member extractCommon *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter obj (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter fn (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member fn *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (member trajectories (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter js *)))" and t = "san" and result = 1.0
      or
      repr = "(member async (instance (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HTMLElement (member window (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member forEach *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (parameter fn (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member globalProps *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member prevGlobalsLength (instance (member constructor (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member fn (instance (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member createElementNS *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter fn (member constructor (instance (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member constructor (instance (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member map (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member forEach (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member src *)" and t = "san" and result = 1.0
      or
      repr = "(return (member toObject *))" and t = "san" and result = 0.5
      or
      repr = "(return (member split (member host *)))" and t = "san" and result = 0.443030303030303
      or
      repr = "(parameter 0 (return (member send *)))" and t = "san" and result = 1.0
      or
      repr = "(member Html5 (member vjs (global)))" and t = "san" and result = 1.0
      or
      repr = "(member Html5 *)" and t = "san" and result = 1.0
      or
      repr = "(return (member union (root https://www.npmjs.com/package/underscore)))" and
      t = "san" and
      result = 0.515151515151515
      or
      repr = "(return (root https://www.npmjs.com/package/speakingurl))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member map (return (member toArray *))))" and
      t = "san" and
      result = 0.24999999999999972
      or
      repr = "(member length (return (member textTracks *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member toArray (root https://www.npmjs.com/package/underscore)))" and
      t = "san" and
      result = 0.18333333333333374
      or
      repr = "(member room *)" and t = "san" and result = 0.43290043290043284
      or
      repr = "(member $in (member _id (parameter 0 (return (member find *)))))" and
      t = "san" and
      result = 0.9166666666666667
      or
      repr = "(member length (member cues_ *))" and t = "san" and result = 1.0
      or
      repr = "(return (member union *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member send (parameter res *))))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member firstChild (return (member createElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member body (return (member getTestDocument (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (return (member renderIntoDocument (root https://www.npmjs.com/package/react-dom/test-utils))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member from *)))" and t = "san" and result = 1.0
      or
      repr = "(member html_element *)" and t = "san" and result = 1.0
      or
      repr = "(member numElements *)" and t = "san" and result = 1.0
      or
      repr = "(member route *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member attributes (member firstChild *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member expect (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member toBe *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member log (member console (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member toEqual *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member findAllByType (member root *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member pendingUrl *)" and t = "san" and result = 1.0
      or
      repr = "(member events *)" and t = "san" and result = 0.515151515151515
      or
      repr =
        "(parameter 0 (return (member isDOMComponent (root https://www.npmjs.com/package/react-dom/test-utils))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member children (member 0 (member children *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filter (return (member keys *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member isDOMComponent *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member findAllByType *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member childNodes (return (parameter render *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member calls (member mock *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member attributes (member firstChild (return (member createElement *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getElementsByTagName (return (member createElement *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member scryRenderedDOMComponentsWithClass *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member itemCount *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member querySelectorAll (return (member createElement *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member container (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member from (member Array (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member calls *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member renderIntoDocument *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member idCallOrder (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member calls (member mock (return (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member parse (root https://www.npmjs.com/package/query-string)))" and
      t = "san" and
      result = 0.8333333333333334
      or
      repr = "(member _dispatchListeners (parameter event *))" and
      t = "san" and
      result = 0.47117826307770444
      or
      repr = "(parameter 0 (return (return (member useCallback *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member scryRenderedDOMComponentsWithClass (root https://www.npmjs.com/package/react-dom/test-utils))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (return (member useCallback (root https://www.npmjs.com/package/react)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (member firstChild (return (parameter render *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (parameter 0 (member success *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (return (member toString *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter result (member success *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member warn *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member name *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toString (parameter string *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (parameter result *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member warn (member console (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (parameter result (member success *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member success *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member substring (return (member trim *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter result *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (return (member trim *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member escape (return (member sanitize (root https://www.npmjs.com/package/validator)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escape (return (member sanitize *))))" and
      t = "san" and
      result = 0.27272727272727226
      or
      repr = "(member length (parameter items *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter items (parameter 0 (return (member toArray *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escape *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 0 (return (member toArray *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateUrl (parameter options *))" and t = "san" and result = 1.0
      or
      repr = "(return (member camelCase (parameter jQuery *)))" and
      t = "san" and
      result = 0.42674311926605496
      or
      repr = "(parameter 1 (return (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (parameter jQuery *))))" and
      t = "san" and
      result = 0.06971087016958565
      or
      repr = "(parameter 1 (return (member attr (return (member element *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member vgTemplate (parameter attrs *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member $watch (parameter scope *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member trim (parameter jQuery *)))" and
      t = "san" and
      result = 0.20871559633027492
      or
      repr = "(member vgTemplate (parameter attrs (member templateUrl *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member $watch *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member attr (instance (member element *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member url (return (member ajaxSetup (parameter jQuery *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member vgTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member vgTemplate (parameter 1 (member templateUrl *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member $template *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member resolveExternalReference *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member runtime *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member val *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member parent (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member visitAll *)))" and t = "san" and result = 1.0
      or
      repr = "(member _compiledHostTemplateCache *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member segments *))" and t = "san" and result = 1.0
      or
      repr = "(member _compiledTemplateCache *)" and t = "san" and result = 1.0
      or
      repr = "(member _cache (instance (member useClass *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter 0 (return (member create *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter __webpack_require__ (member ../../../../core-js/modules/_for-of.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member nodes *))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member componentProvider (member element (member componentProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member build *))" and t = "san" and result = 0.05749999999999999
      or
      repr = "(parameter 0 (return (member createLiteralArrayConverter *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member _createPipe *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter component *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member btoa (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member TemplateRef *)" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter _a *))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member parent (member renderParent (member lastRenderRootNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member slice (member segments *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member contexts *))" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member renderParent *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member componentProvider (member element *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (parameter component (member tryParseHtml (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member parent (member parent *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member parent *)))" and t = "san" and result = 1.0
      or
      repr = "(member url (parameter 0 (return (member ajax *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 0 (parameter match *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (parameter component (member tryParseHtml (instance (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter __webpack_require__ (member ../../../../core-js/modules/_array-methods.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (return (parameter 2 (return (member d *)))))" and t = "san" and result = 1.0
      or
      repr = "(member active *)" and t = "san" and result = 1.0000000000000002
      or
      repr = "(member template (parameter 1 (member tryParseHtml (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member resolveExternalReference (member reflector (member viewContext *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member compMeta (parameter template *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member bindings (parameter def *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member compMeta (parameter 0 (member _compileTemplate (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member TemplateRef (return (parameter __webpack_require__ (member ../../../common/@angular/common.es5.js *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member resolveExternalReference (member reflector *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _embeddedViews *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member observers *))" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member lastRenderRootNode *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member parent (member lastRenderRootNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member parent (member lastRenderRootNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member createEmbeddedView *)))" and t = "san" and result = 1.0
      or
      repr = "(member lastChild *)" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member renderParent (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _stripComments *)))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter component (member tryParseHtml *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member nodes (member def *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member compMeta (parameter template (member _compileTemplate (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member renderParent (member renderParent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _compiledHostTemplateCache (instance (member useExisting *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _templateParser *)" and t = "san" and result = 1.0
      or
      repr = "(member TemplateRef (return (parameter __webpack_require__ *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member observers (instance (member Subject *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member createEmbeddedView (member _viewContainer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member createComponent *)))" and t = "san" and result = 1.0
      or
      repr = "(instance (member useClass *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter 1 (member tryParseHtml *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member TemplateRef (return (parameter 2 (member ../../../common/@angular/common.es5.js *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member renderParent (member componentProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member useExisting *))" and t = "san" and result = 1.0
      or
      repr = "(member template (parameter compMeta *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member createEmbeddedView *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member path *))" and t = "san" and result = 1.0
      or
      repr = "(member _template *)" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter 2 (member ../../../../core-js/modules/_array-includes.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member template *))" and t = "san" and result = 1.0
      or
      repr = "(member template (member compMeta (parameter template (member _compileTemplate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member msgIdToHtml *)" and t = "san" and result = 1.0
      or
      repr = "(member isTemplateElement *)" and t = "san" and result = 1.0
      or
      repr = "(member template (member compMeta *))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member parent (member componentProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member parent (member componentProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member renderParent (member renderParent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member observables *))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (parameter component (member tryParseHtml (member prototype (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member componentProvider (member element (member renderParent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member el (global)))" and t = "san" and result = 1.0
      or
      repr = "(member _compiledTemplateCache (instance (member useExisting *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substr *))" and t = "san" and result = 0.29091651130218565
      or
      repr = "(member template (member element (member renderParent (member componentProvider *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member parent (member renderParent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member msgIdToHtml (return (member parse (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member renderParent (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member _stripComments (instance (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member parent (member lastRenderRootNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member componentProvider (member element (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member compMeta (parameter 0 (member _compileTemplate *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member componentProvider (member element (member lastRenderRootNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member type *))" and t = "san" and result = 1.0
      or
      repr = "(member enableLegacyTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member _segmentIndexShift (member segmentGroup *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member observers (instance (member Subject (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member bindings *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter 2 (member ../../../../core-js/modules/_array-methods.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter __webpack_require__ (member ../../../../core-js/modules/_array-includes.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member len *)" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member renderParent (member componentProvider *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _htmlParser (instance (member type *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member lastRenderRootNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (parameter 1 (member tryParseHtml (instance (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter consumedSegments *))" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member componentProvider *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter 0 (return (member map (parameter optionsArr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member parent (member parent (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member renderParent (member parent *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member msgIdToHtml (return (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(member nodeIndex *)" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member componentProvider (member element *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member push (member buf *))))" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member componentProvider (member element *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (member element (member parent (member parent (member renderParent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nodeIndex (parameter 0 (return (member _preprocessUpdateExpression *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member template (parameter 1 (member tryParseHtml (member prototype (member type *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _msgIdToHtml *)" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member element (member renderParent (member renderParent (member lastRenderRootNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (return (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(instance (member provide *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (return (parameter 2 (member ../../../../core-js/modules/_for-of.js *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member template (member element (member parent (member renderParent *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member substring (member input *)))" and t = "san" and result = 0.75
      or
      repr = "(member _segmentIndexShift *)" and t = "san" and result = 1.0
      or
      repr = "(member _thenTemplateRef *)" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member renderParent (member renderParent *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _htmlParser *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter deps *))" and t = "san" and result = 1.0
      or
      repr = "(member toRespond *)" and t = "san" and result = 1.0
      or
      repr = "(member template (member element (member parent (member componentProvider *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member total (return (member loadFilter *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member findWhere (root https://www.npmjs.com/package/underscore)))" and
      t = "san" and
      result = 0.16666666666666674
      or
      repr = "(member length (return (member loadFilter *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member datagrid *))" and t = "san" and result = 0.5
      or
      repr = "(member length (member selectedRows *))" and t = "san" and result = 1.0
      or
      repr = "(return (member datagrid (return (member $ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member period *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member selectedRows (return (member data (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member frozenColumns (member options (return (member data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member total (member data (return (member data *))))" and t = "san" and result = 1.0
      or
      repr = "(member index (parameter _359 *))" and t = "san" and result = 1.0
      or
      repr = "(return (member eval (global)))" and t = "san" and result = 1.0
      or
      repr = "(member total (return (member loadFilter (member options *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member total (member rows *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (parameter 1 (member hasCheckbox *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parent *)))" and t = "san" and result = 1.0
      or
      repr = "(member total (member children *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (parameter row *)))" and t = "san" and result = 1.0
      or
      repr = "(member total (parameter data *))" and t = "san" and result = 1.0
      or
      repr = "(member index (parameter options *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (member selectedRows *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member period (member body (parameter req *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member datagrid (return (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member top *)" and t = "san" and result = 1.0
      or
      repr = "(return (member toString (member period (member query *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member total (member treeNodes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member checkedRows (return (member data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member top (parameter 0 (return (member css *))))" and t = "san" and result = 1.0
      or
      repr = "(member total (member data (return (member data (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member loadFilter (member options *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member jq *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member html (return (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member start (return (member getSelectionRange (member methods (member textbox *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member oldHtml *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (member checkedRows *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member frozenColumns (member options *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member checkedRows *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member rows (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extend (root https://www.npmjs.com/package/underscore)))" and
      t = "san" and
      result = 0.8333333333333334
      or
      repr = "(member start (return (member getSelectionRange (member methods *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member datagrid *)))" and t = "san" and result = 1.0
      or
      repr = "(member end (parameter 1 (return (member textbox *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member checkedRows (return (member data (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member panel (member tab *)))" and t = "san" and result = 0.7796610169491525
      or
      repr = "(member length (member selectedRows (return (member data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter $ *))" and t = "san" and result = 0.75
      or
      repr = "(member total (member data *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (return (member treegrid *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member period (member body *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member rows (member data (return (member data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member start (return (member getSelectionRange *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (member children *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (member period (member query (parameter req *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member frozenColumns *))" and t = "san" and result = 1.0
      or
      repr = "(member dataValue *)" and t = "san" and result = 1.0
      or
      repr = "(member to *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter events *))" and t = "san" and result = 1.0
      or
      repr = "(member bookmarks (instance (member Image (member undo *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getColumns (member _picker *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (parameter type *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member jQuery *))" and t = "san" and result = 0.7154573934837092
      or
      repr = "(member useHTML (member options *))" and t = "san" and result = 1.0
      or
      repr = "(member rangeCount *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (member orders *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member escapeHTML *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getAttribute (parameter a *)))" and
      t = "san" and
      result = 0.05840000000000006
      or
      repr = "(return (member renderFgSegEls *))" and t = "san" and result = 0.41666666666666663
      or
      repr = "(return (member next *))" and t = "san" and result = 0.3125
      or
      repr = "(return (member replace (instance (member element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member trim (root https://www.npmjs.com/package/jquery))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getFirst *))" and t = "san" and result = 0.75
      or
      repr = "(parameter 0 (return (member scrollTo *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member replace (member text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member tickPositions *))" and t = "san" and result = 1.0
      or
      repr = "(member d (member shapeArgs *))" and t = "san" and result = 0.5179457366063657
      or
      repr = "(return (member getParent (return (member getParent *))))" and
      t = "san" and
      result = 0.1853144044710312
      or
      repr = "(return (root https://www.npmjs.com/package/jquery))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member length (member files (member dataTransfer *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member DomPosition *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member element *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member toolbarGroups *))" and t = "san" and result = 1.0
      or
      repr = "(return (member html (member utils (member UE (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member contains (return (member elementPath *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unhtml *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (return (member slice *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (root https://www.npmjs.com/package/jquery)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member constructor (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getSetting *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member tickPositions (instance (member Axis *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getHtml *))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter state *))" and t = "san" and result = 1.0
      or
      repr = "(return (member load *))" and t = "san" and result = 0.3866120218579235
      or
      repr = "(member length (member shadows *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member DomPosition (member dom *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getFiles *)))" and t = "san" and result = 1.0
      or
      repr = "(member state *)" and t = "san" and result = 0.4562878787878788
      or
      repr = "(member dataLabel *)" and t = "san" and result = 0.5882850854917894
      or
      repr = "(member length (member aoColumns (member dt *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member min *))" and t = "san" and result = 0.6229603729603729
      or
      repr = "(member style (member attributes (parameter a *)))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member length (return (member clone *)))" and t = "san" and result = 1.0
      or
      repr = "(member pos (instance (member StringStream (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fromCharCode (member String (global))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member htmlState (parameter state (member token (parameter state (member token *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fire *))" and t = "san" and result = 0.22386046511627916
      or
      repr = "(member length (member accept (parameter opts *)))" and t = "san" and result = 1.0
      or
      repr = "(member hints *)" and t = "san" and result = 0.7250587333900663
      or
      repr = "(return (member getParent (member container *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getUrl *))" and t = "san" and result = 0.5
      or
      repr = "(member overflow (member style *))" and t = "san" and result = 1.0
      or
      repr = "(return (member token *))" and t = "san" and result = 0.2430201127819549
      or
      repr = "(return (member getParent *))" and t = "san" and result = 0.6875
      or
      repr = "(parameter 0 (member width *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member extend (member $ (parameter Base *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member add (return (member attr *))))" and
      t = "san" and
      result = 0.3317758172012108
      or
      repr = "(return (member match (member value *)))" and
      t = "san" and
      result = 0.18342690225563907
      or
      repr = "(member attributes *)" and t = "san" and result = 1.0
      or
      repr = "(instance (return (member createClass *)))" and t = "san" and result = 1.0
      or
      repr = "(member collapsed *)" and t = "san" and result = 0.9150757575757575
      or
      repr = "(return (member attr (return (member button *))))" and
      t = "san" and
      result = 0.21737259764767497
      or
      repr = "(member length (return (member getFiles (member queue *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member text (member renderer (member chart *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member setTimeout (global)))" and t = "san" and result = 0.44501005612847155
      or
      repr = "(return (parameter f *))" and t = "san" and result = 0.375
      or
      repr = "(return (member pInt *))" and t = "san" and result = 0.0625
      or
      repr = "(member length (return (member getCheckedNodes *)))" and t = "san" and result = 1.0
      or
      repr = "(member easing (parameter 1 (return (member animate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parse *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlFilter (member dataProcessor *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member accept (parameter 0 (member init *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member text *)" and t = "san" and result = 1.0
      or
      repr = "(return (member L *))" and t = "san" and result = 1.0
      or
      repr = "(member overflow *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member remove (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member Api *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter 1 (member token (parameter state *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member forEach *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member abs *))" and t = "san" and result = 0.13792897998093423
      or
      repr = "(return (member clone *))" and t = "san" and result = 0.5025
      or
      repr = "(parameter 0 (return (member indent *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member shapeseq *))" and t = "san" and result = 1.0
      or
      repr = "(member href *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getNodesByParam *)))" and t = "san" and result = 1.0
      or
      repr = "(member gutters (parameter a *))" and t = "san" and result = 0.37326498128147867
      or
      repr = "(member remaning (member file *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member filterChildren *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member push (parameter 0 (member makeDOMNodeNameBefore *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member global *)" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member options *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member DomPosition (member dom (parameter api *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member indexOf (parameter a *)))" and t = "san" and result = 0.5
      or
      repr = "(member length (return (member split (parameter 1 (member on *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member xData (instance (member Series *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member valuesCount *)" and t = "san" and result = 1.0
      or
      repr = "(return (member attachListener *))" and t = "san" and result = 0.33534301346801354
      or
      repr = "(return (member replace (member dataValue *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getColumns *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member concat (return (member concat *))))" and
      t = "san" and
      result = 0.015303137870393786
      or
      repr = "(parameter 0 (return (member replaceWith *)))" and t = "san" and result = 1.0
      or
      repr = "(member address *)" and t = "san" and result = 0.5075757575757575
      or
      repr = "(return (member getComputedStyle (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlState (parameter state (member token (parameter 1 (member token *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member override (member tools (member CKEDITOR (global)))))" and
      t = "san" and
      result = 0.0751201923076923
      or
      repr = "(member length (return (member split (parameter type (member on *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fromCharCode *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getIconStyle (member skin *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member contains (return (member array *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member series (instance (member Chart *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (parameter 1 (member transformTozTreeFormat (member data (member _z *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member editable (member editor *)))" and t = "san" and result = 0.25
      or
      repr = "(member createDocumentFragment (parameter a *))" and
      t = "san" and
      result = 0.23705408515535067
      or
      repr = "(member length (return (member querySelectorAll (member container *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementsByTagName (member root *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member list *))" and t = "san" and result = 1.0
      or
      repr = "(member address (member email *))" and t = "san" and result = 1.0
      or
      repr = "(return (member formatUrl *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _events (member ctx2 *)))" and t = "san" and result = 1.0
      or
      repr = "(member selectees *)" and t = "san" and result = 0.4674653511862814
      or
      repr = "(return (member fix (member event *)))" and t = "san" and result = 0.5
      or
      repr = "(member easing (parameter 1 (return (member animate (parameter toHide *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member stringify *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getCheckedNodes (return (member getZTreeObj *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member accept *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member xData *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member width (parameter 1 (return (member extend *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member values (member options *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter state (member token (return (member startState *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member from *)" and t = "san" and result = 1.0
      or
      repr = "(member duration (parameter 1 (return (member animate (parameter toHide *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toLowerCase (member nodeName *)))" and
      t = "san" and
      result = 0.6826923076923077
      or
      repr = "(return (member getComputedStyle (global)))" and
      t = "san" and
      result = 0.33149678604224037
      or
      repr = "(parameter 0 (return (member indent (return (member getMode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter node *))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter 1 (member token (parameter state (member token *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member html (member button *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member escapeHTML (return (member string (member lang *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member dataValue (member data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member createDocumentFragment *))" and t = "san" and result = 0.5
      or
      repr = "(return (member escapeHTML (return (member string *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member rangeCount (instance (member Selection *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member html (parameter 1 (member link *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _selectHistory *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member DomPosition *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member text (parameter line *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (member dataValue (member data (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getChild *))" and t = "san" and result = 0.6575000000000002
      or
      repr = "(member power *)" and t = "san" and result = 1.0
      or
      repr = "(member linkedObject *)" and t = "san" and result = 0.32966202185792337
      or
      repr = "(return (member translate *))" and t = "san" and result = 0.25
      or
      repr = "(return (member Event (root https://www.npmjs.com/package/jquery)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member map (parameter $ *)))" and t = "san" and result = 0.46585659270648666
      or
      repr = "(return (member escapeRegex *))" and t = "san" and result = 1.0
      or
      repr = "(member useHTML *)" and t = "san" and result = 1.0
      or
      repr = "(member wysihtml5 *)" and t = "san" and result = 1.0
      or
      repr = "(member data (parameter 0 (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(member val *)" and t = "san" and result = 1.0
      or
      repr = "(member code *)" and t = "san" and result = 1.0
      or
      repr = "(return (member normalize *))" and t = "san" and result = 0.75
      or
      repr = "(return (member getText *))" and t = "san" and result = 0.05555555555555558
      or
      repr = "(return (member showBlock *))" and t = "san" and result = 0.75
      or
      repr = "(member className (parameter a *))" and t = "san" and result = 0.75
      or
      repr = "(member length (member curDragNodes (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getPosition *))" and t = "san" and result = 0.16998181818181818
      or
      repr = "(member frontier (member doc (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isDefined (member angular *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member setHtml *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member stringifyJSONintoOneLine *))" and t = "san" and result = 1.0
      or
      repr = "(return (member html (member utils *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member transformTozTreeFormat (member data *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getColumns (instance (member Picker *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member elementFromPoint *))" and t = "san" and result = 0.6414097744360903
      or
      repr = "(member label (parameter 0 (return (member push (member items *)))))" and
      t = "san" and
      result = 0.9638418964076856
      or
      repr = "(member length (member shadows (return (member attr *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member attr *))" and t = "san" and result = 0.5
      or
      repr = "(return (member Event (parameter $ *)))" and t = "san" and result = 0.5151515151515149
      or
      repr = "(member rangeCount (instance (member Selection (parameter api *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getOwnRanges *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter elementsToRemove *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (return (member createElement (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parseTemplate *)))" and t = "san" and result = 1.0
      or
      repr = "(instance (member constructor (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member classes *)" and t = "san" and result = 0.375
      or
      repr = "(member element *)" and t = "san" and result = 0.12015041329105175
      or
      repr = "(member pos (return (member StringStream *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member canvg (global))))" and t = "san" and result = 1.0
      or
      repr = "(instance (return (member createClass (member tools *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member split *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member apply (member tools (member _z (member zTree *)))))" and
      t = "san" and
      result = 0.03378378378378374
      or
      repr = "(return (member removeCustomData *))" and t = "san" and result = 0.125
      or
      repr = "(member duration *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member points (instance (member Series *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getKey *))" and t = "san" and result = 0.25
      or
      repr = "(return (member createBookmark *))" and t = "san" and result = 0.75
      or
      repr = "(member length (member series *))" and t = "san" and result = 1.0
      or
      repr = "(return (member createText *))" and t = "san" and result = 0.25
      or
      repr = "(member undo *)" and t = "san" and result = 1.0
      or
      repr = "(member duration (parameter 1 (return (member animate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getNextRange (return (member createIterator *))))" and
      t = "san" and
      result = 0.5888461538461539
      or
      repr = "(parameter 0 (return (member split *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member insert *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member execCommand (member editor (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member next (parameter 0 (member tokenize *))))" and
      t = "san" and
      result = 0.19499999999999995
      or
      repr = "(member length (member cells *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getCursor *))" and t = "san" and result = 0.8626373626373627
      or
      repr = "(member length (return (member closest *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getNodes *)))" and t = "san" and result = 1.0
      or
      repr = "(instance (member constructor (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member camelCase (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member e *))" and t = "san" and result = 0.25
      or
      repr = "(member htmlParser (member CKEDITOR (global)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extend (member tools (member CKEDITOR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getNodesByParam (return (member getZTreeObj *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member transformTozTreeFormat *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getNodeCache (member data *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member isDefined (parameter angular *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getSetting (member data (member _z (member zTree *)))))" and
      t = "san" and
      result = 0.5
      or
      repr = "(member length (member childNodes (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(member label (parameter 0 (return (member push *))))" and t = "san" and result = 0.25
      or
      repr = "(member pos (return (member StringStream (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member applyOptions *))" and t = "san" and result = 0.2647058823529412
      or
      repr = "(member length (member aoColumns (member dt (member s *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member jQuery (global)))" and t = "san" and result = 0.625
      or
      repr = "(member length (instance (member Api (member dataTable (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (member prototype (return (member createClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member yearshtml *)" and t = "san" and result = 1.0
      or
      repr = "(return (member getBogus *))" and t = "san" and result = 0.5
      or
      repr = "(return (member getIconStyle *))" and t = "san" and result = 1.0
      or
      repr = "(instance (member constructor (member prototype (member $ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member save *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member applyOptions (member prototype *)))" and t = "san" and result = 0.25
      or
      repr = "(member length (return (member toString (parameter input *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member wysihtml5 (member fn (parameter $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member remaning (member file (parameter 0 (return (member push *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member text *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter f *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getColumns (return (member Picker *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member toHtml *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member transformToArrayFormat (member data (member _z (member zTree *)))))" and
      t = "san" and
      result = 0.7264681761330842
      or
      repr = "(return (member getNext *))" and t = "san" and result = 0.2255281177156177
      or
      repr = "(parameter 1 (parameter 0 (return (member append *))))" and t = "san" and result = 1.0
      or
      repr = "(return (return (member Function (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter moment *))" and t = "san" and result = 0.05877403846153845
      or
      repr = "(member htmlState *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member XRegExp (global)))" and t = "san" and result = 0.0006250000000000006
      or
      repr = "(member overflow (member style (member documentElement *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlState (parameter state (member token *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _selectHistory (return (member mockTabs *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member split (member className *)))" and t = "san" and result = 0.25
      or
      repr = "(parameter 1 (return (member push (parameter html *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member wysihtml5 (member fn *))" and t = "san" and result = 1.0
      or
      repr = "(member 0 (parameter match (member PSEUDO (member preFilter (member selectors *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member map *))" and t = "san" and result = 1.0
      or
      repr = "(member html (parameter 1 (parameter 1 (return (member addListener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _queue *))" and t = "san" and result = 1.0
      or
      repr = "(instance (member element (member htmlParser (member CKEDITOR (global)))))" and
      t = "san" and
      result = 0.47058823529411736
      or
      repr = "(parameter 0 (return (member camelCase (member jQuery *))))" and
      t = "san" and
      result = 0.1250000000000004
      or
      repr = "(member length (return (member transformToArray *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member transformToArray (return (member getZTreeObj *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member htmlEncode *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member text (member renderer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (parameter line *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member parent (return (member parent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member angleTo *))" and t = "san" and result = 0.25
      or
      repr = "(return (parameter g *))" and t = "san" and result = 0.5
      or
      repr = "(member hostname (member location (member document (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member accept (member options *)))" and t = "san" and result = 1.0
      or
      repr = "(member colCnt *)" and t = "san" and result = 1.0
      or
      repr = "(return (member unhtml (member utils (member UE (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member wysihtml5 (global))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member closest (return (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member split (member value *)))" and
      t = "san" and
      result = 0.7699999999999999
      or
      repr = "(return (member data *))" and t = "san" and result = 0.4375
      or
      repr = "(return (member replace (return (member replace (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member html (member button (member options *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member d (parameter 0 (return (member animate *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member groups (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member ch (return (member clipPos (member CodeMirror *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member pos (instance (member StringStream (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member $process *))" and t = "san" and result = 0.4652471838938004
      or
      repr = "(return (member replace (member 1 (return (member match *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member trim (member tools (member CKEDITOR (global)))))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member length (member data (instance (member Series *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member call (member slice (member prototype *))))" and
      t = "san" and
      result = 0.5075757575757575
      or
      repr = "(parameter 1 (return (member push (parameter b *))))" and t = "san" and result = 1.0
      or
      repr = "(member html (parameter html *))" and t = "san" and result = 1.0
      or
      repr = "(member added *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member token *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member x *))" and t = "san" and result = 1.0
      or
      repr = "(return (member match (parameter a *)))" and t = "san" and result = 0.6680324883449884
      or
      repr = "(return (member pick (parameter g *)))" and t = "san" and result = 0.37878787878787873
      or
      repr = "(member length (return (member decodeURIComponent (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member contents *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member pointArrayMap *))" and t = "san" and result = 1.0
      or
      repr = "(member remaning *)" and t = "san" and result = 1.0
      or
      repr = "(member templateRef *)" and t = "san" and result = 1.0
      or
      repr = "(member href (return (member createElement (member document *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getDocumentElement (member document *)))" and
      t = "san" and
      result = 0.25
      or
      repr = "(member length (member selectedTds *))" and t = "san" and result = 1.0
      or
      repr = "(return (member extend (member jQuery (global))))" and t = "san" and result = 1.0
      or
      repr = "(return (return (parameter $interpolate *)))" and
      t = "san" and
      result = 0.5597014925373135
      or
      repr = "(member length (member files (member dataTransfer (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member fix (member event (member jQuery (global)))))" and
      t = "san" and
      result = 0.25757575757575746
      or
      repr = "(member lang *)" and t = "san" and result = 1.0
      or
      repr = "(return (member n *))" and t = "san" and result = 0.12878787878787876
      or
      repr = "(parameter 8 (return (member populateNodeData *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member contains *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member w *))" and t = "san" and result = 0.25
      or
      repr = "(member htmlFilter *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter treeNodes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (member orders (parameter Runtime *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member remove *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member curSelectedList (return (member getRoot *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member css *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member html (parameter $element (member link *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getCustomData *))" and t = "san" and result = 0.5
      or
      repr = "(member a *)" and t = "san" and result = 0.25
      or
      repr = "(return (member stringifyJSONintoOneLine (member AbstractToolbarModifier *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member groups *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setValue *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member DomPosition (member dom (parameter api *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member equals (member startContainer *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member slice (parameter input *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getDialog *))" and t = "san" and result = 0.625
      or
      repr = "(member length (return (member split (member runtimeOrder *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member text *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getParentNode (parameter 1 (member removeNode (member view *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member map (root https://www.npmjs.com/package/jquery)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member extend (member tools *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member indent *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getIconStyle (member skin (member CKEDITOR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter childNodes *))" and t = "san" and result = 1.0
      or
      repr = "(member frontier *)" and t = "san" and result = 1.0
      or
      repr = "(member loopedSlides *)" and t = "san" and result = 1.0
      or
      repr = "(return (member unhtmlForUrl *))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter state (member token (parameter state *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member $G (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member split (member runtimeOrder (member options *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (return (member createElement *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member text *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member accept (parameter opts (member init *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getValue *))" and t = "san" and result = 0.5
      or
      repr = "(member length (member _events (return (member on *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member href (return (member createElement *)))" and t = "san" and result = 1.0
      or
      repr = "(member pos (return (member StringStream (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toLowerCase (member userAgent (member navigator (global)))))" and
      t = "san" and
      result = 0.17720437457279578
      or
      repr = "(return (member fontMetrics (member renderer (member chart *))))" and
      t = "san" and
      result = 0.37878787878787873
      or
      repr = "(return (member getPlotLinePath *))" and t = "san" and result = 0.046263636363636375
      or
      repr = "(member currentLang *)" and t = "san" and result = 0.08000000000000007
      or
      repr = "(parameter 0 (return (member load *)))" and t = "san" and result = 1.0
      or
      repr = "(member bookmarks (instance (member Image (member undo (member plugins *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member data (member options *)))" and t = "san" and result = 1.0
      or
      repr = "(member htmlState (parameter 1 (member token (return (member startState *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member formatter *))" and t = "san" and result = 0.18535353535353538
      or
      repr = "(member length (return (member querySelectorAll (parameter element *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member copy (member tools (member CKEDITOR (global)))))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member bookmarks *)" and t = "san" and result = 1.0
      or
      repr = "(return (member shadow *))" and t = "san" and result = 0.18825122735737243
      or
      repr = "(member remaning (parameter file *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member pending (member $$state (member promise *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlState (parameter 1 (member token (parameter 1 (member token *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member clone (member range *)))" and t = "san" and result = 1.0
      or
      repr = "(member ch *)" and t = "san" and result = 1.0
      or
      repr = "(return (member htmlEncode (member tools (member CKEDITOR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member curSelectedList *))" and t = "san" and result = 1.0
      or
      repr = "(return (member substring (parameter text *)))" and t = "san" and result = 0.5
      or
      repr = "(member length (member attributes (parameter node *)))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member stringifyJSONintoOneLine (member AbstractToolbarModifier (member ToolbarConfigurator (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (instance (return (member createClass *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member distance *)" and t = "san" and result = 0.75
      or
      repr = "(member length (parameter src *))" and t = "san" and result = 1.0
      or
      repr = "(member fontFamily *)" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (parameter name *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 5 (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member toLowerCase *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member loopedSlides (parameter s *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member unshift (member items *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getSelectedNodes (return (member getZTreeObj *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _widgets *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getRanges *))" and t = "san" and result = 0.5
      or
      repr = "(member ch (return (member clipPos *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (instance (member uNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member bookmarks (instance (member Image *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member contains (return (member elementPath (member dom *)))))" and
      t = "san" and
      result = 0.2500000000000002
      or
      repr = "(member length (member selectedTds (instance (member UETable *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member dataValue (member data (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getParentNode (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member htmlEncode (member tools *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member DomPosition (member dom *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member slice (member prototype (member Array (global)))))" and
      t = "san" and
      result = 0.38257575757575757
      or
      repr = "(member html (parameter html (parameter 1 (return (member addListener *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(instance (member constructor (member prototype (member $ (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter b *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member template (member nonTlbTranscludeDirective (parameter 4 (member transclude *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (parameter d *))" and t = "san" and result = 0.001090225563909808
      or
      repr = "(member dataValue (member data *))" and t = "san" and result = 1.0
      or
      repr = "(return (member slice (return (member slice *))))" and t = "san" and result = 0.125
      or
      repr = "(parameter 1 (return (member execCommand *)))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member extend (member $ *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (instance (member Api (member dataTable *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unhtml (member utils *)))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 1 (return (member replace *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member clone (instance (member range (member dom *)))))" and
      t = "san" and
      result = 0.49750000000000005
      or
      repr = "(return (member replace (return (member html *))))" and t = "san" and result = 1.0
      or
      repr = "(member yearshtml (parameter inst *))" and t = "san" and result = 1.0
      or
      repr = "(member easing *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member _events *))" and t = "san" and result = 1.0
      or
      repr = "(member overflow (member style (member documentElement (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member htmlState (parameter 1 (member token *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member curSelectedList (return (member getRoot (member data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (parameter url *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member split (parameter a *)))" and t = "san" and result = 0.7916666666666665
      or
      repr = "(member 0 (parameter match (member PSEUDO *)))" and
      t = "san" and
      result = 0.06249999999999867
      or
      repr = "(member frontier (member doc *))" and t = "san" and result = 0.3333333333333337
      or
      repr = "(member length (return (member getSelectedNodes *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (parameter 0 (return (member exec *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter callback *)))" and t = "san" and result = 1.0
      or
      repr = "(member emailHTML *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member cells (member document (member activeNotebookEditor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member parentNode (member parentNode (member parentNode *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 0 (return (member Uint32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member disposeTemplate *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member RequestQueue *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member items (member completionModel (parameter event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getFolderBackupPaths *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member lastLine *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member textContent *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member toWorkspaceFolders *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member right (member right *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member origin (member location (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getAllThreads *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member splice (member list *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getDataBreakpoints *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member validActions (return (member getCodeActions (root https://www.npmjs.com/package/vs/editor/contrib/codeAction/codeAction)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member format *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (return (member substr *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member 0 (instance (member Uint32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getEditors (member activeGroup *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getEnablementState *))" and t = "san" and result = 0.17499999999999993
      or
      repr = "(member length (member folderQueries (parameter query *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member bodyTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member _lines *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member right (member left (member node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toString (return (member random *))))" and
      t = "san" and
      result = 0.025073804376192
      or
      repr = "(member length (return (member provideWorkspaceSymbols (return (member default *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (member end *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member right (member root *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (member value (parameter query *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member Selection *)))" and t = "san" and result = 1.0
      or
      repr = "(member endColumn *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member workingTreeChanges (member state (member 0 (member repositories *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member executeCommand (return (member ExtHostCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member node *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member hash (member parentNode (member parentNode (member parentNode (member parentNode *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member html (return (member renderViewLine2 (root https://www.npmjs.com/package/vs/editor/common/viewLayout/viewLineRenderer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (return (member loadMessageBundle *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getCallStack (return (member getThread *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (parameter 0 (return (member getText (parameter document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member markers *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member editors (instance (member EditorsObserver (root https://www.npmjs.com/package/vs/workbench/browser/parts/editor/editorsObserver)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member path *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member renderMarkdown *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member right (member right *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member value (member filePickBox *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member Range *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (instance (member Range *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member previousNode *))" and t = "san" and result = 1.0
      or
      repr = "(member originalEndLineNumber *)" and t = "san" and result = 1.0
      or
      repr = "(member firstLineLength *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member node *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getCallStack *)))" and t = "san" and result = 1.0
      or
      repr = "(member origin (member location (global)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member descriptionMatch *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member validActions (return (member getCodeActions *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getReplElements (instance (member ReplModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member translate (member start *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member customize (parameter external *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _lines *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member changes (member model *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member parse (member Uri (root https://www.npmjs.com/package/vscode)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 4 (return (member findPrevBracketInRange (member BracketsUtils (root https://www.npmjs.com/package/vs/editor/common/modes/supports/richEditBrackets)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member dispose *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member trace *)))" and t = "san" and result = 1.0
      or
      repr = "(member pageSize *)" and t = "san" and result = 1.0
      or
      repr = "(member templateData *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member Uint16Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _diffs *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter lineText *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member Position *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member conflictsSettings (return (member merge *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (parameter 0 (return (member equalsIgnoreCase (root https://www.npmjs.com/package/vs/base/common/strings)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member models (return (member createInstance *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member visibleViewDescriptors (return (member getViewContainerModel *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member textContent (return (member $ (root https://www.npmjs.com/package/vs/base/browser/dom))))" and
      t = "san" and
      result = 0.6060606060606061
      or
      repr = "(return (member localize *))" and t = "san" and result = 0.1697317017248225
      or
      repr = "(member message (parameter 0 (return (member push *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member lineNumber *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member findPrevBracketInRange *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _modelSelections *)" and t = "san" and result = 0.7272727272727274
      or
      repr = "(member length (return (member read *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLineContent *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member items (instance (member CompletionModel (root https://www.npmjs.com/package/vs/editor/contrib/suggest/completionModel)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (member element (return (member HighlightedLabel (root https://www.npmjs.com/package/vs/base/browser/ui/highlightedlabel/highlightedLabel)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter edits *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getValueInRange *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member text (parameter 0 (return (member push *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member toResource (root https://www.npmjs.com/package/vs/workbench/common/editor)))" and
      t = "san" and
      result = 0.5000604836828637
      or
      repr = "(member ariaLabel *)" and t = "san" and result = 0.0015984496124031022
      or
      repr = "(member pageSize (parameter 1 (return (member assign *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member left (member right *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member translate *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member localize *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member join *))))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (member element (instance (member HighlightedLabel *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member disposeTemplate (member renderer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member filters *)" and t = "san" and result = 1.0
      or
      repr = "(member _selectionStart *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter res (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member previousNode (return (member getLocation *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (return (member DiagnosticCollection *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member prepareQuery (root https://www.npmjs.com/package/vs/base/common/fuzzyScorer)))" and
      t = "san" and
      result = 0.8000000000000003
      or
      repr = "(parameter 2 (return (member splice *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getReplElements *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member requestsWithAllHeaders (return (member UserDataSyncTestServer (root https://www.npmjs.com/package/vs/platform/userDataSync/test/common/userDataSyncClient)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member folders (member workspace *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLineDecorations *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getBreakpoints (return (member createMockDebugModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member parentNode (member target (parameter event *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member element (return (member HighlightedLabel *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toResource *))" and t = "san" and result = 0.3124395163171363
      or
      repr = "(member length (member suggestions *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getWorkspaceBackups *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getSelections *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter selections *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member text *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (parameter value *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (member element (instance (member HighlightedLabel (root https://www.npmjs.com/package/vs/base/browser/ui/highlightedlabel/highlightedLabel)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (instance (member Range *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member hash (member parentNode (member parentNode (member parentNode (member target *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member selected *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter value (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerText *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member decorations *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member models *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (parameter 0 (return (member equal (root https://www.npmjs.com/package/assert)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member equal *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member text *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member store (member storageService *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member range *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getUntitledWorkspacesSync *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member convertToEndOffset (member LineTokens (root https://www.npmjs.com/package/vs/editor/common/core/lineTokens)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member localize *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member editors *))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (parameter 0 (instance (member Range (root https://www.npmjs.com/package/vscode)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member searchPaths *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member replace (member text *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member modifiedEndLineNumber *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member positionAt *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member fire (member _onDidChange *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member Range (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member basename (root https://www.npmjs.com/package/vs/base/common/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member clamp *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getOption (member _editor *)))" and
      t = "san" and
      result = 0.21127341159786828
      or
      repr = "(member length (member visibleViewDescriptors *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member left (member left *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter lineChanges *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getLeadingWhitespace *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member left (member root *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toUpperCase *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getUriLabel *))" and t = "san" and result = 0.17799877563513927
      or
      repr = "(parameter 0 (return (member substr (return (member trim *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (parameter 1 (instance (member Range (root https://www.npmjs.com/package/vscode)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getSelections (parameter editor *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (return (member dispose (root https://www.npmjs.com/package/vs/base/common/lifecycle))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html (parameter settings *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member root *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member requestsWithAllHeaders (instance (member UserDataSyncTestServer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter startToken *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member items (instance (member CompletionModel *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member key (return (member _readModifiers *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member message (parameter 0 (return (member confirm *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member contents (parameter 0 (instance (root https://www.npmjs.com/package/vinyl))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member right (member right *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member join (root https://www.npmjs.com/package/vs/base/common/path)))" and
      t = "san" and
      result = 0.5016526252579212
      or
      repr = "(return (parameter event *))" and t = "san" and result = 0.9
      or
      repr = "(member length (member children (return (member linkify *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member contents (parameter 0 (return (root https://www.npmjs.com/package/vinyl))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (parameter 2 (return (member $ *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member registerCommand (member CommandsRegistry *)))" and
      t = "san" and
      result = 0.22039129644104982
      or
      repr = "(parameter 0 (parameter 0 (return (member _register *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member range (root https://www.npmjs.com/package/vs/base/common/arrays))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member from *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member left (member root *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (parameter filter *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extname (root https://www.npmjs.com/package/vs/base/common/path)))" and
      t = "san" and
      result = 0.22807443609931266
      or
      repr = "(return (member join (member posix (root https://www.npmjs.com/package/path))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (member buffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member left (member left *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (member setSelection *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member editors (return (member EditorsObserver (root https://www.npmjs.com/package/vs/workbench/browser/parts/editor/editorsObserver)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member value *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (return (member renderFormattedText (root https://www.npmjs.com/package/vs/base/browser/formattedTextRenderer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member customize *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member append *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member trace (member logService *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text (return (member lineAt (parameter document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member editors (instance (member EditorsObserver *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLineContent (member _buffer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member alloc *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member Position *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member localize *)))" and t = "san" and result = 1.0
      or
      repr = "(member index (member focusedMenu *))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member renderFormattedText *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _selectionEnd *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (member localize (root https://www.npmjs.com/package/vs/nls))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member count (member marker *))" and t = "san" and result = 1.0
      or
      repr = "(return (parameter 0 (member children *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getElementById (member document (global))))" and
      t = "san" and
      result = 0.75
      or
      repr = "(parameter 0 (parameter 2 (return (member localize *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member cells (member document *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 2 (parameter 0 (return (member alert (root https://www.npmjs.com/package/vs/base/browser/ui/aria/aria)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member parentNode *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member names (parameter 1 (return (member assign *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (return (member renderMarkdown (root https://www.npmjs.com/package/vs/base/browser/markdownRenderer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member label *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member elements (return (member add *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter values (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member text (return (member lineAt *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member message *))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member toErrorMessage (root https://www.npmjs.com/package/vs/base/common/errorMessage)))" and
      t = "san" and
      result = 0.8333333333333334
      or
      repr = "(parameter 0 (parameter 1 (return (member create (member Range *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member folders (return (member parse *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (instance (member Selection (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (parameter node *)))" and t = "san" and result = 1.0
      or
      repr = "(member resultCount *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member dependencies *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member key *))))" and t = "san" and result = 1.0
      or
      repr = "(member lastLineLength *)" and t = "san" and result = 1.0
      or
      repr = "(member _selections *)" and t = "san" and result = 0.7272727272727273
      or
      repr = "(parameter 0 (return (member notEqual *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member changes (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member from (member Buffer (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member localizations (member contributes *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (member callstack *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member provideWorkspaceSymbols *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member next *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member hash (member parentNode (member parentNode (member target (parameter event *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter path *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getReplElements (return (member ReplModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member validActions *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member subarray *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member openExternal *)))" and t = "san" and result = 1.0
      or
      repr = "(member shadowContent *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member localizations *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Uint32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getBreakpoints *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member executeCommand (member commands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member count (parameter 1 (return (member publicLog2 (member telemetryService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member classList *))" and t = "san" and result = 1.0
      or
      repr = "(return (member findAll (member model *)))" and t = "san" and result = 0.5
      or
      repr = "(return (member _createMouseTarget *))" and t = "san" and result = 0.40625
      or
      repr =
        "(member length (member items (return (member CompletionModel (root https://www.npmjs.com/package/vs/editor/contrib/suggest/completionModel)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 4 (return (member findPrevBracketInRange (member BracketsUtils *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member readdirSync (root https://www.npmjs.com/package/fs))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member contents (parameter source (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member tooltip *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member detectLinks (member OutputLinkComputer *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member executeCommand (instance (member ExtHostCommands *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member firstPage *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getLineDecorations (return (member createTextModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member html (member settings (parameter change *)))" and t = "san" and result = 1.0
      or
      repr = "(member index (parameter 0 (return (member fire *))))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 3 (return (member Range (root https://www.npmjs.com/package/vs/workbench/api/common/extHostTypes))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member deltaDecorations *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member text (return (member lineAt (member document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 3 (return (return (member loadMessageBundle (root https://www.npmjs.com/package/vscode-nls)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member files (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member get (member renderers *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (parameter 1 (return (member Range (root https://www.npmjs.com/package/vscode)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (return (member Selection *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member references *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (return (member parse *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member items (return (member provideSuggestionItems *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member detectLinks *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member suggestions (parameter result *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getValueInRange (parameter 1 (member fromEditorSelection *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member convertToEndOffset *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member right (member node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member toWorkspaceFolders (root https://www.npmjs.com/package/vs/platform/workspace/common/workspace))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member left *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (member range *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member node *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member conflictsSettings (return (member merge (root https://www.npmjs.com/package/vs/platform/userDataSync/common/settingsMerge)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member pad (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member editors (return (member EditorsObserver *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member create *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (return (member loadMessageBundle *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member createInstance *))" and t = "san" and result = 0.625
      or
      repr = "(member length (member children (return (member resolve *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member Selection (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (parameter 0 (return (member alert *))))" and t = "san" and result = 1.0
      or
      repr = "(member html (member settings *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getEmptyWindowBackupPaths *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Uint16Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getBreakpoints (return (member getModel *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member TEMPLATE_ID *)" and t = "san" and result = 1.0
      or
      repr = "(member cursorState *)" and t = "san" and result = 0.5454545454545452
      or
      repr = "(member length (return (member RequestQueue *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter children (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member openCodeEditor *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member text *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Int32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member cells (member 0 (member notebookDocuments *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member withFilter *))" and t = "san" and result = 0.12912520159519703
      or
      repr = "(member innerHTML (return (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (instance (member Selection *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member viewItems *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (instance (member Range (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member split (member callstack (member data *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member folders *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member right (member left *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getReplElements (member parentSession *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member pathFromUri *)))" and t = "san" and result = 1.0
      or
      repr = "(member _process *)" and t = "san" and result = 0.7697455454958863
      or
      repr = "(parameter 3 (instance (member Selection *)))" and t = "san" and result = 1.0
      or
      repr = "(member commentStrLength *)" and t = "san" and result = 1.0
      or
      repr = "(return (member executeCommand (member _commandService *)))" and
      t = "san" and
      result = 0.5
      or
      repr = "(member extension *)" and t = "san" and result = 0.625
      or
      repr = "(parameter 1 (member data *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getFunctionBreakpoints *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member all *)))" and t = "san" and result = 1.0
      or
      repr = "(member hash (member target (parameter event *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 0 (return (member range (root https://www.npmjs.com/package/vs/base/common/arrays))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member placeholders *))" and t = "san" and result = 1.0
      or
      repr =
        "(member innerHTML (return (member append (root https://www.npmjs.com/package/vs/base/browser/dom))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member right (member node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member workingTreeChanges *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getGroups *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (return (return (member loadMessageBundle (root https://www.npmjs.com/package/vscode-nls)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member cells (member document (member activeNotebookEditor (member notebook *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member end (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member activeViewDescriptors *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member writeUInt8 *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member workingTreeChanges (member state *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getHistory *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member task *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member coalesce *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member pageSize (parameter 1 (return (member assign (root https://www.npmjs.com/package/vs/base/common/objects)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member commentStrOffset *)" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member provideWorkspaceSymbols (instance (member default *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (return (member clamp (root https://www.npmjs.com/package/vs/base/common/numbers))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member format *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member read (instance (member MarkerService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member next (parameter searcher *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member items (return (member CompletionModel *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getUntitledWorkspacesSync (instance (member WorkspacesMainService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member folders (return (member getWorkspace *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member Int32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member executeCommand *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter result (parameter 0 (return (member then *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member contents (parameter 0 (parameter 0 (return (member forEach *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getValueInRange (parameter model (member fromEditorSelection *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member localize (root https://www.npmjs.com/package/vs/nls))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member signatures *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member expr *))" and t = "san" and result = 1.0
      or
      repr = "(member contents (parameter source *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member coalesce (root https://www.npmjs.com/package/vs/base/common/arrays))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member left (member left *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member activeViewDescriptors (return (member getViewContainerModel *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getLeadingWhitespace (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _shouldRender *)" and t = "san" and result = 0.75
      or
      repr =
        "(return (member joinPath (root https://www.npmjs.com/package/vs/base/common/resources)))" and
      t = "san" and
      result = 0.5
      or
      repr = "(member origin (member location *))" and t = "san" and result = 1.0
      or
      repr = "(return (member openCodeEditor (member _editorService *)))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (return (member $ (root https://www.npmjs.com/package/vs/base/browser/dom))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter content *))" and t = "san" and result = 1.0
      or
      repr = "(member cursor *)" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 3 (instance (member Range (root https://www.npmjs.com/package/vs/workbench/api/common/extHostTypes))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member outputs (member 0 (member cells (member document *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter lineContent *))" and t = "san" and result = 1.0
      or
      repr = "(member pageSize (parameter 1 (parameter 0 (return (member queryGallery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member findRange *))" and t = "san" and result = 0.25
      or
      repr = "(return (member t *))" and t = "san" and result = 1.0
      or
      repr = "(return (member join (member posix *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member items (member completionModel *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member resourceStates *))" and t = "san" and result = 1.0
      or
      repr = "(member className (parameter e *))" and t = "san" and result = 0.7575757575757572
      or
      repr = "(parameter 0 (return (member substring (return (member getLineContent *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substr (member value *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member left (member left (member right *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (return (member format (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member delta *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (return (member toString *))))" and
      t = "san" and
      result = 0.9436847479807673
      or
      repr = "(return (member newGestureEvent *))" and t = "san" and result = 0.5833333333333333
      or
      repr = "(member length (member secondaryCursors *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member left (member node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (parameter 2 (parameter 2 (return (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 2 (parameter 2 (return (member $ (root https://www.npmjs.com/package/vs/base/browser/dom)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nlpResult *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member convertToEndOffset (member LineTokens *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member edits *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member updateWorkspaceFolders *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Uint32Array (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member label *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member key *))" and t = "san" and result = 1.0
      or
      repr = "(member hash (member parentNode (member parentNode (member target *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter cursors *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter distinctElements *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member localizations (member contributes (member manifest *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 3 (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member getReplElements (return (member createMockSession *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member templateData (parameter templateData *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member right (member right (member left *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member store *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member serialize *))" and t = "san" and result = 0.1875
      or
      repr = "(parameter 0 (member args *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (return (member detectLinks (member OutputLinkComputer (root https://www.npmjs.com/package/vs/workbench/contrib/output/common/outputLinkComputer)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member indexOf (parameter haystack *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member requestsWithAllHeaders (return (member UserDataSyncTestServer *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member format (root https://www.npmjs.com/package/vs/base/common/strings))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member outerHTML (return (member linkify (return (member createInstance *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member getHtmlNode *)" and t = "san" and result = 1.0
      or
      repr = "(return (member $ *))" and t = "san" and result = 0.39999999999999997
      or
      repr = "(parameter 1 (member pos *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getDirty *)))" and t = "san" and result = 1.0
      or
      repr =
        "(return (member createAndFillInContextMenuActions (root https://www.npmjs.com/package/vs/platform/actions/browser/menuEntryActionViewItem)))" and
      t = "san" and
      result = 0.16000000000000014
      or
      repr = "(parameter 3 (parameter 0 (return (member getText *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member target *))" and t = "san" and result = 1.0
      or
      repr = "(return (member toString (member resource *)))" and
      t = "san" and
      result = 0.35454174434890695
      or
      repr = "(member length (member changes *))" and t = "san" and result = 1.0
      or
      repr = "(return (member getTokenOffset *))" and t = "san" and result = 1.0
      or
      repr = "(member x *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (member disposeElement *)))" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (return (member writeUInt32BE (root https://www.npmjs.com/package/vs/base/common/buffer))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member then (return (member then *))))" and
      t = "san" and
      result = 0.10667199999999949
      or
      repr = "(member html (return (member renderViewLine2 *)))" and t = "san" and result = 1.0
      or
      repr = "(member outerHTML (return (member linkify *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member items (return (member provideSuggestionItems (root https://www.npmjs.com/package/vs/editor/contrib/suggest/suggest)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member updateFolders *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (return (member executeCommand *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (parameter 1 (member path *)))" and t = "san" and result = 1.0
      or
      repr = "(member cancelId *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getDirty (member workingCopyFileService *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member matches *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 1 (return (member Range *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (parameter 0 (return (member equalsIgnoreCase *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 1 (parameter 0 (return (member Range (root https://www.npmjs.com/package/vscode)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (return (member getUntitledWorkspacesSync (return (member WorkspacesMainService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member read (return (member MarkerService *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member linesLength *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member get (instance (member DiagnosticCollection *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member coalesce (root https://www.npmjs.com/package/vs/base/common/arrays)))" and
      t = "san" and
      result = 0.1875
      or
      repr = "(member commitTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member root *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member parse (member Uri *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (member localize (root https://www.npmjs.com/package/vs/nls))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (member parentNode (member target *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member fromGitUri *))" and t = "san" and result = 0.08499999999999996
      or
      repr = "(member templateId *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (return (parameter 0 (return (member map *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member match (return (member getText *))))" and
      t = "san" and
      result = 0.04173146839740971
      or
      repr = "(member length (member _secondaryCursors *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member left (member right *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member pad *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getEditors *)))" and t = "san" and result = 1.0
      or
      repr = "(member stack (parameter err *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getActions *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member classList (parameter child *)))" and t = "san" and result = 1.0
      or
      repr = "(member html (member settings (parameter workspace *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member textContent *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (return (member fetch *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member dirname (root https://www.npmjs.com/package/vs/base/common/resources)))" and
      t = "san" and
      result = 0.5
      or
      repr = "(member length (parameter objects *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (parameter 0 (return (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member renderElement *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter ranges *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member notEqual (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (parameter 0 (return (member onDidChange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLineContent (return (member getLineTokens *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member placeholders (return (member parse *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member toDisposeTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member module *)" and t = "san" and result = 1.0
      or
      repr =
        "(parameter 1 (instance (member Position (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member suggestions (return (member provideCompletionItems *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member root *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member outputs *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member setCount *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member previousNode (return (member getLocation (root https://www.npmjs.com/package/jsonc-parser)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (member value *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member set *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member path *))))" and t = "san" and result = 1.0
      or
      repr = "(member endColumn (member range *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member outputs (member 0 (member cells *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member endColumn (parameter 0 (return (member push *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getLineContent (parameter model *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member requestsWithAllHeaders (instance (member UserDataSyncTestServer (root https://www.npmjs.com/package/vs/platform/userDataSync/test/common/userDataSyncClient)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member local *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member piece (member right (member left *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getCellIndex (member viewModel *)))" and t = "san" and result = 0.5
      or
      repr = "(member deleteCount *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member conflictsSettings *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member querySelector *)))" and t = "san" and result = 1.0
      or
      repr = "(member x (member end *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter indentation *))" and t = "san" and result = 1.0
      or
      repr = "(member count (parameter 1 (return (member publicLog2 *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(parameter 0 (parameter 2 (return (member localize (root https://www.npmjs.com/package/vs/nls)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (member ariaLabel *))" and t = "san" and result = 1.0
      or
      repr = "(return (member parent *))" and t = "san" and result = 0.26533170172482257
      or
      repr = "(member hash (member parentNode (member parentNode *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member folderQueries *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 4 (parameter 0 (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(member message *)" and t = "san" and result = 1.0
      or
      repr = "(return (member mixin *))" and t = "san" and result = 0.4600000000000003
      or
      repr = "(parameter 0 (return (member deepEqual (root https://www.npmjs.com/package/assert))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getValueInRange (parameter model *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member requestsWithAllHeaders *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter res *))" and t = "san" and result = 1.0
      or
      repr = "(member callstack *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member children (member 1 (member children *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member readdirSync *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member selections *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member moved *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member fire *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member Position (root https://www.npmjs.com/package/vscode))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member substring (member customize (member element *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member piece (member right (member right (member root *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (member renderElement (member renderer *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter needle *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member labelMatch *))" and t = "san" and result = 1.0
      or
      repr = "(return (member assign (root https://www.npmjs.com/package/vs/base/common/objects)))" and
      t = "san" and
      result = 0.09841422474784932
      or
      repr = "(member clientToken *)" and t = "san" and result = 0.7100000000000002
      or
      repr = "(member visibility *)" and t = "san" and result = 0.25
      or
      repr = "(member id (parameter 0 (return (member findOne (member Skin *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member id (parameter 0 (return (member findOne *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member _groups (instance (member selection (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member Event *))" and t = "san" and result = 0.7676767676767676
      or
      repr = "(return (member trim (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member test *))" and t = "san" and result = 0.2833333333333337
      or
      repr = "(parameter 1 (return (member apply *)))" and t = "san" and result = 0.66
      or
      repr = "(member length (return (member nodes (member graph *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member max *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member nodes *)))" and t = "san" and result = 1.0
      or
      repr = "(member templates (member options (instance (member constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member changedTouches (member event (parameter exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member templates (member options (instance (member Constructor (member multiselect *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member edges *)))" and t = "san" and result = 1.0
      or
      repr = "(member requestAnimationFrame (global))" and t = "san" and result = 0.7575757575757576
      or
      repr = "(member cancelAnimationFrame *)" and t = "san" and result = 0.7575757575757576
      or
      repr = "(member templates (member options (instance (member Constructor *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member Event (member jQuery (global))))" and t = "san" and result = 1.0
      or
      repr =
        "(member templates (member options (instance (member constructor (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member trim (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 0.13967948717948248
      or
      repr = "(member template (parameter step *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member edges (member graph *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member _groups (instance (member transition (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member call (member slice *)))" and t = "san" and result = 0.7575757575757576
      or
      repr = "(member templates (member options *))" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member appHtml *)" and t = "san" and result = 1.0
      or
      repr = "(member sumRequest *)" and t = "san" and result = 1.0
      or
      repr = "(return (member sortTree *))" and t = "san" and result = 0.25
      or
      repr = "(member value (member 0 (member children *)))" and t = "san" and result = 1.0
      or
      repr = "(member sumRequest (parameter 0 (return (member json *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getElementById *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member endorsement *))" and t = "san" and result = 1.0
      or
      repr = "(member sumRequest (parameter 0 (return (member json (parameter res *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member showTree (global))))" and t = "san" and result = 1.0
      or
      repr = "(member length (member beginnerRequests *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getElementById (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member size (return (member getElementById (member document (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member findSkillByName (global)))" and t = "san" and result = 0.5
      or
      repr = "(member value (member 0 (member children (member 5 (member cells *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member intermediateRequests *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member advancedRequests *))" and t = "san" and result = 1.0
      or
      repr = "(member size (return (member getElementById *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (return (member push (member trainings *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member findSkillByName *))" and t = "san" and result = 0.375
      or
      repr = "(member length (member children (parameter displayObject *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 2 (return (return (parameter _dereq_ *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 3 (return (return (parameter _dereq_ *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member log (return (parameter require *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (return (parameter _dereq_ *)))" and t = "san" and result = 0.5
      or
      repr = "(member _dispatchInstances (parameter event *))" and
      t = "san" and
      result = 0.04112554112554102
      or
      repr = "(member reference *)" and t = "san" and result = 0.5
      or
      repr = "(return (member trim (member constructor (member fn *))))" and
      t = "san" and
      result = 0.9915981639344226
      or
      repr = "(return (member find (member element *)))" and
      t = "san" and
      result = 0.6026694045174539
      or
      repr = "(member textTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member array (member lang *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member $ (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member target *)" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (member $ (parameter window *))))" and t = "san" and result = 1.0
      or
      repr = "(member footTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member hostname (parameter 1 (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter result (parameter 3 (return (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member fix *))" and t = "san" and result = 0.5
      or
      repr = "(return (member slice (member prototype *)))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(parameter 1 (return (member collapseToPoint *)))" and t = "san" and result = 1.0
      or
      repr =
        "(member footTemplate (member DPGlobal (member datepicker (member fn (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member cookie (member jQuery (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member uNode *)))" and t = "san" and result = 1.0
      or
      repr = "(member host (parameter 1 (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member then (instance (member Promise (global)))))" and
      t = "san" and
      result = 0.5567725447243519
      or
      repr = "(member host (parameter 1 (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 0 (member array *)))" and t = "san" and result = 1.0
      or
      repr = "(member port (instance (member Socket (member exports (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (parameter node (member selectNode *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member headTemplate (member DPGlobal (member datepicker (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member filter (return (member jQuery (global)))))" and
      t = "san" and
      result = 0.7857142857142856
      or
      repr = "(member chosenLabel *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (instance (member exports *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member footTemplate (member DPGlobal (member datepicker (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member headTemplate (member DPGlobal (member datepicker (member fn (parameter $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member substring (parameter a (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member createRange *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (member constructor (member fn (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member text (instance (member EJS (global))))" and t = "san" and result = 1.0
      or
      repr = "(member innerHTML (return (member deblockAndSurround (member selection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member drawLine *))" and t = "san" and result = 0.39999999999999997
      or
      repr = "(return (member drawCircle *))" and t = "san" and result = 0.39999999999999997
      or
      repr = "(return (member camelCase (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (parameter b *))" and t = "san" and result = 1.0
      or
      repr = "(return (member replace (return (member replace (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hasInput *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (instance (member uNode (member UM (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member camelCase (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 0.24999999999999467
      or
      repr = "(return (member getCorrectInnerHTML (member quirks *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member I *))" and t = "san" and result = 1.0
      or
      repr = "(return (member camelCase (member constructor (member fn (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member data (return (member extend (member jQuery (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member load (member g *))))" and t = "san" and result = 1.0
      or
      repr = "(member chosenLabel (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (member location (member document (global))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(return (member camelCase (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member toLowerCase (parameter name *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member c2p *))" and t = "san" and result = 0.25
      or
      repr = "(member _wysihtml5ScrollIntoViewElement *)" and t = "san" and result = 1.0
      or
      repr = "(member headTemplate (member DPGlobal *))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member extend (member jQuery (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member encodePayload (parameter c *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member getOwnUneditables *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member c2p (parameter axis *)))" and
      t = "san" and
      result = 0.5833333333333333
      or
      repr = "(return (member getCorrectInnerHTML (member quirks (parameter wysihtml5 *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (instance (member Socket (member exports (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member host (parameter b *))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (parameter b (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member childNodes (return (member parentElement *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member headTemplate (member DPGlobal (member datepicker (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (instance (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member height (return (member extend *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member exec (instance (member RegExp (global)))))" and
      t = "san" and
      result = 0.8201425472159124
      or
      repr = "(member content (parameter 1 (return (member get *))))" and t = "san" and result = 1.0
      or
      repr = "(member url (return (member extend (member $ (parameter p *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member camelCase (member constructor (member fn *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getCorrectInnerHTML *))" and t = "san" and result = 1.0
      or
      repr = "(return (member encodePayload *))" and t = "san" and result = 1.0
      or
      repr = "(return (member append *))" and t = "san" and result = 0.5
      or
      repr = "(member index (instance (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member extend (member $ *)))" and t = "san" and result = 1.0
      or
      repr = "(member hostname (parameter b (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member hasInput (instance (member constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getById (member document *)))" and t = "san" and result = 0.15625
      or
      repr = "(member hasInput (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member innerHTML (return (member deblockAndSurround (member selection (parameter composer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nodeIndex (member nodeInfo *))" and t = "san" and result = 1.0
      or
      repr = "(return (root https://www.npmjs.com/package/jquery.qtip))" and
      t = "san" and
      result = 0.84
      or
      repr = "(return (member item *))" and t = "san" and result = 0.043715846994535526
      or
      repr = "(return (member extend (member constructor (member fn *))))" and
      t = "san" and
      result = 0.16588296760710122
      or
      repr = "(return (member htmlencode (global)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member htmlparser (member UM (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member create (return (member use *))))" and t = "san" and result = 0.1875
      or
      repr =
        "(member chosenLabel (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member textNodes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter a (member array *)))" and t = "san" and result = 1.0
      or
      repr = "(member headTemplate (member DPGlobal (member datepicker *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member createElement (parameter context *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member el *))" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (member jQuery (global))))" and
      t = "san" and
      result = 0.7584018360655738
      or
      repr = "(parameter 1 (return (parameter done *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (member setEnd (return (member cloneRange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member _wysihtml5ScrollIntoViewElement (member doc *))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getCorrectInnerHTML (member quirks (member wysihtml5 (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter arr (member array *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (instance (member exports (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member set *))" and t = "san" and result = 0.12626262626262633
      or
      repr = "(member length (member state (parameter 0 (member inflate (parameter c *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member collapseToPoint (return (member createRange *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member extend (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member footTemplate (member DPGlobal (member datepicker (member fn (member jQuery *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (member location (parameter c *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member utf8border (parameter c *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member option *))" and t = "san" and result = 0.25
      or
      repr =
        "(member hasInput (instance (member constructor (member prototype (member Constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member localStorage (member window (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member Zepto (global)))" and t = "san" and result = 0.3273809523809523
      or
      repr = "(member length (parameter node *))" and t = "san" and result = 1.0
      or
      repr = "(member protocol (parameter b (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hostname (member location (member self (global))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member extend (return (member noConflict *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member port (instance (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (parameter b (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member hasInput (instance (member Constructor *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter arr (member array (member lang *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter range *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 3 (return (member find *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member headTemplate *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member writeBuffer (instance (member exports (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member htmlparser *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member extend (member $ (member window (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member createRange (member docSelection *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hasInput (instance (member Constructor (member datepicker (member fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member camelCase (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member host (parameter 1 (member exports (parameter module *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (member $ *))" and t = "san" and result = 1.0
      or
      repr = "(member hasInput (instance (member Constructor (member datepicker *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member trim (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member innerHTML (return (member deblockAndSurround *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member source *)" and t = "san" and result = 1.0
      or
      repr = "(member prevBufferLen (instance (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state (parameter a (member inflate (parameter c *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member Zepto *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member Buf8 (return (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member load (member g (member localStorage *)))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member target (member cache (instance (member qtip (root https://www.npmjs.com/package/jquery.qtip)))))" and
      t = "san" and
      result = 0.4327272727272729
      or
      repr = "(parameter 0 (parameter 1 (return (member array *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 1 (return (return (return (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member compressedSize *)" and t = "san" and result = 1.0
      or
      repr = "(member $trigger (instance (member Constructor (member collapse *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member rangeCount (parameter sel *))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (return (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member trim (member constructor (member prototype (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 0 (member cookie *)))" and t = "san" and result = 1.0
      or
      repr = "(instance (member constructor (member prototype (member $ (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member host (parameter b (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (parameter 1 (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member localStorage (parameter window *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prevBufferLen (instance (member Socket (member exports (parameter b *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member get (return (member array *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member footTemplate (member DPGlobal *))" and t = "san" and result = 1.0
      or
      repr = "(member uncompressedSize *)" and t = "san" and result = 1.0
      or
      repr = "(member length (instance (member exports (parameter b *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member hasInput (instance (member constructor (member prototype (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member camelCase (member constructor (member fn (member constructor *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member protocol (parameter b *))" and t = "san" and result = 1.0
      or
      repr = "(member recordsTotal *)" and t = "san" and result = 1.0
      or
      repr = "(return (member extend (member constructor (member prototype (member $ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member chosenLabel (instance (member constructor (member prototype *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member footTemplate (member DPGlobal (member datepicker *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (instance (member Buf8 (return (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member state (parameter a (member inflate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member apply *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member encode (return (parameter a *))))" and t = "san" and result = 1.0
      or
      repr = "(member host (parameter b (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter vals *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member replace (member nodeValue *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member vpn_addr *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member registerLangHandler *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter d (member createSimpleLexer *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member account_addr *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter d (member registerLangHandler (member PR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member c (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member concat (parameter a (member createSimpleLexer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member client_addr (parameter connection *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (member a *))))" and t = "san" and result = 1.0
      or
      repr = "(member ip *)" and t = "san" and result = 0.4285714285714285
      or
      repr = "(member account_addr (parameter 0 (return (member stringify *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter d (member registerLangHandler *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member getToken *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member nodeValue (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member createSimpleLexer (member PR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member account_addr (parameter 0 (return (member findOne *))))" and
      t = "san" and
      result = 0.75
      or
      repr = "(member length (parameter 1 (member createSimpleLexer (member PR *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member txHash *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter d (member createSimpleLexer (member PR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member price_per_gb *)" and t = "san" and result = 0.7575757575757576
      or
      repr = "(member length (return (member match (member source (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member concat (parameter 0 (member createSimpleLexer *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member up *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member g *))" and t = "san" and result = 1.0
      or
      repr = "(member tx *)" and t = "san" and result = 1.0
      or
      repr = "(member client_addr *)" and t = "san" and result = 0.26515151515151447
      or
      repr = "(member average (parameter 0 (return (member send *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter result (parameter 1 (return (member aggregate *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member account_addr (parameter 0 (member body *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member match (member source *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member vpn_addr (parameter connection *))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (member createSimpleLexer *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member registerLangHandler (member PR *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter d (member createSimpleLexer (member PR *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member a *))" and t = "san" and result = 1.0
      or
      repr = "(member average *)" and t = "san" and result = 1.0
      or
      repr = "(member vpn_addr (parameter connection (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 1 (return (member aggregate *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member a (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter d (member registerLangHandler (member PR *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member match (return (member substring *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member match (member nodeValue *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member g (parameter a *)))" and t = "san" and result = 1.0
      or
      repr = "(member stats *)" and t = "san" and result = 1.0
      or
      repr = "(member title (parameter 2 (return (member splice *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member vpn_addr (parameter 0 (parameter 1 (return (member each *)))))" and
      t = "san" and
      result = 0.5000000000000007
      or
      repr = "(return (member getToken (member tokens *)))" and t = "san" and result = 0.5
      or
      repr = "(member length (return (member match (member a (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member from_addr *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member replace (member nodeValue (parameter a *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member stats (parameter 0 (return (member send *))))" and t = "san" and result = 1.0
      or
      repr = "(member account_addr (parameter 0 (return (member findOne (member Node *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter 1 (member registerLangHandler (member PR (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 1 (return (member match (member className *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member concat (parameter a *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member match (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (return (parameter r *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member slice (member pathname *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 1 (return (member match (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member replace (return (member slice (member src *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member classes *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member attrs *))" and t = "san" and result = 1.0
      or
      repr =
        "(member length (member delimiters (parameter 0 (member postProcess (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unescapeMd (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(member posMax (instance (member exports *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (member delimiters (parameter 0 (member postProcess *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (return (member push (member eMarks *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member pathname (return (member parse (member prototype *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member skipSpacesBack *))" and t = "san" and result = 0.03235294117647061
      or
      repr = "(member length (member delimiters *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member scanDelims (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member validate *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member list (member footnotes *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member scanDelims (parameter e (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HTML_OPEN_CLOSE_TAG_RE *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member list (member footnotes (member env *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member HTML_TAG_RE *)" and t = "san" and result = 1.0
      or
      repr = "(member posMax (instance (member exports (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member delimiters (parameter e (member postProcess *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member getRules (member ruler (member block *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member content (return (member push (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr =
        "(member length (member delimiters (parameter e (member postProcess (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 0 (member pathname *))" and t = "san" and result = 1.0
      or
      repr = "(member content (return (member push *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member push (member bMarks *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (parameter e *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member hash (parameter e *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member 1 (return (member match (member __text_cache__ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter e (member decode *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (member pathname (return (member parse *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (return (parameter n *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member scanDelims (parameter 0 (member tokenize *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member 0 (return (member match (member __text_cache__ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (parameter n (parameter 1 (return (member replace *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member unescapeMd *))" and t = "san" and result = 1.0
      or
      repr = "(member length (member list (member footnotes (member env (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member id (member meta *))" and t = "san" and result = 1.0
      or
      repr = "(member id (member meta (return (member push (parameter e *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member id (member meta (return (member push *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member escapeHtml (parameter t *)))" and t = "san" and result = 1.0
      or
      repr = "(member xhtmlOut *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member delimiters (parameter e *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member scanDelims *)))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member substr (member pathname *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member email (member user (member session (parameter req *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member _ (member exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nestingSuffix *)" and t = "san" and result = 1.0
      or
      repr =
        "(member __html (member dangerouslySetInnerHTML (parameter 1 (return (member createElement *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member _ (global)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nestingPrefix *)" and t = "san" and result = 1.0
      or
      repr = "(member text (parameter 0 (instance (member model *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member exports (parameter module *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member text (member message (member props *)))" and t = "san" and result = 1.0
      or
      repr = "(member email (parameter 0 (return (member find (member model *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member placeholder (member partial *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member _ (parameter exports *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member suffix *)" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member fetchItems *)))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys (member _ (parameter global *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member prefix *)" and t = "san" and result = 1.0
      or
      repr = "(member email (member user (member session *)))" and t = "san" and result = 1.0
      or
      repr = "(member className *)" and t = "san" and result = 0.6775757575757575
      or
      repr = "(return (member replace (return (member interpolate *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (return (parameter require *)))" and
      t = "san" and
      result = 0.08000000000000002
      or
      repr = "(member name (member user *))" and t = "san" and result = 0.060606060606060656
      or
      repr = "(parameter 3 (return (return (parameter require *))))" and t = "san" and result = 1.0
      or
      repr = "(member email *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member keys (member _ (member placeholder *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (member _ (member _ *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member keys (return (member mixin *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(parameter 1 (return (member read *)))" and t = "san" and result = 1.0
      or
      repr = "(member email (parameter 0 (return (member find *))))" and t = "san" and result = 1.0
      or
      repr = "(member text (member message *))" and t = "san" and result = 1.0
      or
      repr = "(member text (parameter 0 (return (member model *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 2 (return (return (parameter require *))))" and t = "san" and result = 1.0
      or
      repr = "(parameter 0 (return (member read *)))" and t = "san" and result = 1.0
      or
      repr = "(member email (member user *))" and t = "san" and result = 1.0
      or
      repr = "(return (member equals *))" and t = "san" and result = 1.0
      or
      repr = "(return (member clone (instance (member range *))))" and
      t = "san" and
      result = 0.35538461538461547
      or
      repr = "(return (member trim (member tools *)))" and t = "san" and result = 0.6271742777424595
      or
      repr = "(return (member inArray *))" and t = "san" and result = 0.5151515151515149
      or
      repr = "(return (member toHtml *))" and t = "san" and result = 0.015353535353535397
      or
      repr = "(member href (member attributes (parameter a *)))" and
      t = "san" and
      result = 0.13793103448275848
      or
      repr = "(member toolbarGroups *)" and t = "san" and result = 0.5
      or
      repr = "(parameter 0 (return (member extend *)))" and t = "san" and result = 0.917950336700337
      or
      repr = "(return (member fire (member CKEDITOR (global))))" and
      t = "san" and
      result = 0.5098722104768616
      or
      repr = "(return (member getComputedStyle *))" and t = "san" and result = 0.4166666666666667
      or
      repr = "(instance (member element (member htmlParser *)))" and t = "san" and result = 1.0
      or
      repr = "(return (member getTokenAt *))" and t = "san" and result = 0.2309
      or
      repr = "(return (member override (member tools *)))" and
      t = "san" and
      result = 0.16728983688833124
      or
      repr = "(parameter 1 (return (member extend (root https://www.npmjs.com/package/jquery))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member splitLines *))" and t = "san" and result = 0.2777777777777778
      or
      repr = "(return (member inArray (root https://www.npmjs.com/package/jquery)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(return (member insertAfter *))" and t = "san" and result = 0.11624579124579126
      or
      repr = "(member label *)" and t = "san" and result = 0.37070707070707076
      or
      repr = "(return (member contains (instance (member elementPath (member dom *)))))" and
      t = "san" and
      result = 0.2500000000000002
      or
      repr = "(return (member replace (member className *)))" and
      t = "san" and
      result = 0.6151515151515151
      or
      repr = "(member id (parameter 0 (return (member index *))))" and t = "san" and result = 1.0
      or
      repr = "(return (member toLowerCase (member recipient *)))" and
      t = "san" and
      result = 0.7575757575757576
      or
      repr = "(member count (parameter 1 (return (parameter fn *))))" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter docs (parameter 0 (return (member toArray *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member secret *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter docs (parameter 2 (return (member distinct *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member msg_id (parameter 0 (return (member findOne *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member unique_opens *)" and t = "san" and result = 1.0
      or
      repr = "(member length (parameter 1 (parameter 2 (return (member distinct *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member unique_clicks *)" and t = "san" and result = 1.0
      or
      repr = "(member length (member data (parameter 1 (return (parameter fn *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member msg_id *)" and t = "san" and result = 1.0
      or
      repr = "(member msg_id (parameter 0 (return (member updateOne *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member user (parameter req (parameter 2 (return (member post *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member available_agents *))" and t = "san" and result = 1.0
      or
      repr = "(return (member signup *))" and t = "san" and result = 0.27000000000000013
      or
      repr = "(return (member createIfNotExistsWithLeadId *))" and t = "san" and result = 0.25
      or
      repr = "(member length (member tags *))" and t = "san" and result = 1.0
      or
      repr = "(member user (member req *))" and t = "san" and result = 0.5882352941176477
      or
      repr = "(return (member closeRequestByRequestId *))" and t = "san" and result = 0.625
      or
      repr = "(return (member sign *))" and t = "san" and result = 0.7575757575757576
      or
      repr = "(return (member createWithIdAndRequester *))" and
      t = "san" and
      result = 0.48000000000000004
      or
      repr = "(return (member removeParticipantByRequestId *))" and t = "san" and result = 0.5
      or
      repr = "(return (member json (parameter res *)))" and
      t = "san" and
      result = 0.2575757575757575
      or
      repr = "(member length (member agents *))" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filterTasks (return (member $new *)))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (return (member filterTasks (member $scope *))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member nodeTemplateMap *)" and t = "san" and result = 1.0
      or
      repr = "(member nodeTemplateMap (return (member make *)))" and t = "san" and result = 1.0
      or
      repr = "(member tasks *)" and t = "san" and result = 0.5661700879765394
      or
      repr = "(return (member displayLastYearKPI *))" and t = "san" and result = 0.3099706744868038
      or
      repr = "(member length (member activity (parameter perimeter *)))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member number *)" and t = "san" and result = 1.0
      or
      repr = "(member length (return (member filter (root https://www.npmjs.com/package/lodash))))" and
      t = "san" and
      result = 1.0
      or
      repr = "(member length (member activity *))" and t = "san" and result = 1.0
      or
      repr = "(return (member filter (member _ (global))))" and
      t = "san" and
      result = 0.11363636363636331
      or
      repr = "(member length (return (member filterTasks *)))" and t = "san" and result = 1.0
      or
      repr = "(member nodeTemplateMap (return (member make (member GraphObject *))))" and
      t = "san" and
      result = 1.0
    }
  }
}
